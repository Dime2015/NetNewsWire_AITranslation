# 归档:「到头再拽,翻上/下一篇」手势(2026-08-11 封存)

> 这份文档是 T55 那条线(共二十轮 + 一轮封存)的最终归宿。
> 详细的排查过程仍然全部留在 `NOTES-todo.md` 的 T55 里,这里只整理**现状 + 怎么捡回来**。

## 现状

**用户决定**:这个功能不常用,先关掉,以后有空再回来解决抖动问题。

**怎么关的**:`iOS/Article/NNWArticlePaging.swift` 的 `nnwInstallReadingGestures()` 里,
驱动这整条功能的竖向 `UIPanGestureRecognizer`(触发 `nnwHandleVerticalPan(_:)`)
**不再被装到 view 上**——代码原样留着,只是从来不会被调用。

```swift
// let vertical = UIPanGestureRecognizer(target: self, action: #selector(nnwHandleVerticalPan(_:)))
// vertical.delegate = nnwVerticalSwipeDelegate
// vertical.cancelsTouchesInView = false
// vertical.delaysTouchesBegan = false
// view.addGestureRecognizer(vertical)
```

**要捡回来**:把这 5 行取消注释即可,状态机、指示器箭头、翻页动画全部原样都在,
不需要重写任何东西。

⚠️ **没受影响的功能**(同一个文件里,但入口不同,继续正常工作):
- **右滑回列表 / 左滑开原文**(`nnwHandleHorizontalPan`,横向那条 pan)——没动,照常用
- 底部 dock 上的「上一篇 / 下一篇未读」箭头按钮 —— 走的是协调器直接选中,和这条手势无关

## 这个功能是什么

在文章正文**顶部**再往下拽 → 翻到上一篇;在**底部**再拽 → 下一篇未读(没有就露出彩蛋页)。
拽的过程有一个箭头指示器(`—` 变 `^`/`v`),松手播一段整页竖向推移的翻页动画。

## ✅ 已经做对、且用户验收过的部分(翻页功能本身,不是抖动)

| 东西 | 状态 |
|---|---|
| **门 = 路**:守门条件和 `nnwGoToPreviousArticle` 的四条路一一对应 | 实测救回过真实翻页 |
| **进门锚**:换文章那一刻记下「列表里它前面那一篇」+ 按日期兜底 | 真机实测救回过「翻不动」 |
| **「到头」和「失联」分开画线** | 已落地 |
| **翻页收尾双路 + 代际令牌**:系统回调实测会漏,0.9 秒兜底救回过多次 | 已验收 |
| **重入守卫**:动画没放完时拒绝新的翻页请求 | 已验收 |

这部分**没有已知 bug**,如果以后重新启用,不需要重新验证这几条。

## ❌ 卡住的地方:标题/正文抖动,二十轮未解决

**症状**:翻页成功时标题和正文会抖一下(用户的强约束:**抖 ⟺ 翻页成功**,双向全称命题)。

**已排除的四个候选**(每条都有实测证据,详见 `NOTES-lessons.md` L134/L135/L138):

1. `syncInset()` 反复改内边距 —— KVO 抓栈:一次翻页只写一次,排除
2. 解冻那一帧标题突变 —— 12 次翻页跳变=0,排除
3. 宿主安全区 62→116 摆 54pt —— **频率对不上**:20 次里只 5 次,而症状每次都有,排除
4. **滚动视图安全区 0→116 晚到,正文中途整体掉 116pt** —— 实测存在、频率完全吻合(11/12 次)、
   **修掉之后用户说"一点没变"**。它是真实位移,但不是用户看见的那个抖。

**核心教训**(L138):「必须每次都发生」是必要条件,不是充分条件 —— 一次成功翻页里
「每次都发生」的东西很多(新建 WebView、装载网页、`setViewControllers` 整页动画、
安全区到货、协调器换选中……),频率筛只能把候选缩到一类,缩不到具体那一个。
**从类到个,唯一的一步是「改掉它 → 用户看一眼、症状变没变」,没有近路。**

## 🎯 还没查过的方向(都是猜测,一个都没验证过)

按 `NEXT-PROMPT.md`(2026-08-09 版)整理:

- **A(最值得先探)**:翻页动画本身有两层同时在动 ——
  系统 `setViewControllers(direction:animated:)` 的整页竖向推移,
  加上我们自己叠的 `nnwSetContentPushBack()`(拽的时候给翻页容器加 `transform` 推开正文让位)。
  ⚠️ 用户当时没同意"砍掉推开正文那一层"(这是他的设计决定),但**临时关掉做探路**是便宜的。
- **B**:头区高度在动画期间会变(`measuredHeight` 要等标题渲染完才稳定),
  即使飞行进度已冻住,头区容器本身的高度变化仍会带动标题块移动。
- **C**:一次翻页排版 27~129 次,`applyGeometry` 每次都在重设几何,
  如果输入里有任何一个还在变,就会体现出来。
- **D(最容易被忽略)**:二十轮全在找"哪个元素在动",**从没确认过"抖"到底是位移还是掉帧/卡顿**。
  重新捡起这条线时,第一步应该是当面问用户这两个问题,再动手。

## 相关文件

- `iOS/Article/NNWArticlePaging.swift` —— 手势、状态机、指示器、翻页动画,全部在这
- `iOS/Article/ArticleHeaderBar.swift` —— 标题飞行动画,`nnwIsPullingToTurnPage` /
  `nnwIsTurningPage` 是这两个文件之间的接口;⚠️ 文件头有硬约束
  (只许改 `transform`/`alpha`,绝不在滚动回调里碰 `contentInset`/`safeArea`),
  改核心函数前务必重读(`NOTES-lessons.md` L137 就是在这栽的,弄出过白屏假死)
- `NOTES-todo.md` T55 —— 完整的二十轮排查记录,细节都在这
- `NOTES-lessons.md` L134~L138 —— 这条线上摔出来的教训
