# Babel Shell 阶段一

## 版本边界

- genesis-v2 是独立前端重构之前的稳定基线。
- codex/babel-shell-phase-1 是新界面的施工分支。
- 旧界面仍是默认入口；阶段一不会改变正式版本的启动结果。
- 新界面只在 Debug 构建中开放，Release 构建强制使用创世版本 2。

## 阶段一目标

建立一个与 NetNewsWire 旧视觉层分离的 Babel 根界面，让后续首页、文章时间线和文章阅读页能够逐步迁移，不必一次性改写账户、订阅、同步和文章数据库。

当前阶段只完成：

1. 新旧界面的双入口。
2. 新 Babel Shell 的最小根页面。
3. 随时返回创世版本 2 的退路。
4. 外链、通知、快捷操作触发时自动回到旧界面处理，避免功能中断。
5. 对开关优先级的自动测试。

## 开启方式

在 Debug Scheme 的启动参数中加入 -BabelShell。

强制使用旧界面时加入 -GenesisV2。

也可以设置环境变量 BABEL_SHELL=1。未设置任何开关时，仍进入创世版本 2。

## 下一步

阶段一后续按这个顺序推进：

1. 定义 Babel 自己的信息架构和导航状态。
2. 接入真实账户与未读数量，只读不改。
3. 建立首页骨架。
4. 把文章时间线迁入新 Shell。
5. 接入文章阅读页。
6. 三张母版通过真机视觉验收后，再决定是否让新 Shell 成为默认入口。

## 当前进度：真实资料库预览

阶段一的第二个提交已经把占位 Shell 扩展成可浏览的只读阅读流：

- 首页显示真实账户数、订阅源数、今天文章数、未读数和收藏数。
- “接下来读”从本地未读文章中选择最新一篇。
- 今天、未读和已收藏都能进入 Babel 自己的文章时间线。
- 时间线显示真实来源、日期、标题、摘要和未读状态。
- 文章可进入 Babel 自己的沉浸阅读页，并可打开原始网页。
- 已缓存的标题译文会直接显示；不会为了预览额外发起 AI 请求。

当前仍然刻意保持只读：

- 打开文章不会自动标记已读。
- 新阅读页尚未接入正文翻译按钮、星标、分享和阅读进度。
- 外链、通知和快捷操作仍然交给创世版本 2。
- 新 Shell 仍然只在 Debug 开关开启时出现。

等上述交互迁移完成并经过真机验收后，再讨论把新 Shell 设为默认入口。
