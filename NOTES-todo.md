# NOTES-todo.md — 已知问题与待办

> 这里记录**已经发现、但决定暂时不修**的问题。
> 目的:防止"先放一放"变成"永远忘了"。
> 每条都写清楚:现象、原因、什么时候修、为什么现在不修。

---

## T1 · 翻译按钮与相邻按钮视觉粘连,工具栏被撑宽

**状态**:✅ 已解决(2026-07-21)

**解决方式**:把工具栏从三组改为两组。

iOS 26 的 Liquid Glass 工具栏按 flexibleSpace 把按钮切成若干玻璃胶囊,
剩余宽度由弹性间隔平分。上游原本三组
`[已读 星标] | [下一篇未读] | [阅读视图 分享]`,我们加入第 6 个按钮后,
两个间隔各自只剩 40pt 出头,相邻胶囊边缘几乎相接 —— Liquid Glass 会把
靠近的玻璃融合,视觉上就是粘成水滴。

改为两组后剩余宽度全部给中间一个间隔,分隔清晰:
`[已读 星标 下一篇未读] ⟷ [阅读视图 分享 翻译]`
(左=这篇文章的状态与去向,右=拿这篇文章做点什么)

实现位于本 fork 的 `installTranslationButton()`,只在 iOS 26 分支重排,
旧系统分支保持上游的均匀分布布局。已截图确认两个胶囊分离。

---

### 原始记录(保留)

**状态**:待修复
**发现时间**:2026-07-19,Phase 2 验收时由用户发现
**严重程度**:纯视觉,不影响功能

### 现象

底部工具栏加入翻译按钮后:
1. 整条工具栏变宽了
2. 翻译按钮和左边那个"下一篇未读"(圆圈里的下箭头)看起来像水滴一样粘连在一起

### 原因

iOS 26 的 Liquid Glass 工具栏会把相邻的 bar button item 自动吸附成一个胶囊分组。
当前实现是把翻译按钮**追加到 toolbarItems 数组末尾**
(`iOS/Article/ArticleViewController.swift` 末尾的 `installTranslationButton()`),
系统就把它和前一个按钮归进了同一组。

### 为什么现在不修

Phase 3 接入真实后端后,按钮可能需要更多状态展示(进度、错误提示等),
届时按钮在工具栏里的位置和形态**很可能要重新设计一次**。
现在修等于修两遍。

### 修的时候该怎么做

需要调整按钮的插入位置而不是简单 append。参考上游自己的做法:
`iOS/Article/ArticleViewController.swift` 里 iOS 26 分支用的是
`toolbarItems?.insert(articleExtractorBarButtonItem, at: 5)`,即插入到特定索引。
但索引 5 的实际含义**尚未实测确认**(见 NOTES-architecture.md 🔴 第 1 条),
修之前需要先跑起来打印 `toolbarItems` 看清楚真实顺序。

---

## T4 · 离开文章再返回,译文丢失,需要重新翻译

**状态**:✅ 已解决(2026-07-19,本地缓存落地)
**发现时间**:2026-07-19,Phase 2 验收后由用户提出

**解决方式**:新增 `Shared/Translation/TranslationCache.swift`。
译文按「文章 ID + 模型 + 原文哈希」缓存在内存 + 磁盘(系统 Caches 目录,上限 50 篇,删最旧)。
再次打开已翻译过的文章并点翻译按钮 → 命中缓存,秒开、零请求。

设计要点:
- 只缓存**全部成功**的翻译;中途取消或有失败组的不缓存,避免固化残缺版本
- 换模型会重新翻译(键里含模型名,有意的 —— 便于对比模型效果)
- 文章内容更新后键会变,不会拿旧译文冒充新内容
- 未触碰 Modules/ArticlesDatabase(C 级禁区),缓存文件与上游数据库零交集

### 现象

翻译文章 A → 跳到上一篇/下一篇 → 再返回 A → A 显示原文,按钮为"未翻译"。

### 这不是 bug

译文存在于**网页的 DOM 里**(路线 B 的做法)。返回文章时 NetNewsWire 会重新渲染页面,
新页面是全新的 JS 环境,译文自然不存在。按钮显示"未翻译"与页面实际内容**一致**,
没有状态错乱。

但这也不是刻意的产品设计 —— 是路线 B 的固有后果,选型时未预先说明。

### 为什么现在不处理

用 mock 时重翻只要 1 秒,无感。接入真实后端后,每次返回 = 一次真实翻译请求,
才会变成实际问题(耗时、可能产生费用)。

**选哪个方案取决于后端的真实速度与成本,而这些数字目前不存在。**

### 三个候选方案

- **A(默认)**:什么都不做,每次重新请求。若后端自带缓存则第二次很快,
  完全符合 CLAUDE.md 第 5 节「缓存逻辑全部在后端」。**零代码改动。**
- **B**:Swift 侧内存缓存译文,返回时直接重新应用。体验最好,
  但在 Swift 侧存译文与第 5 节有擦边,需要用户确认。
- **C**:只记住"这篇翻译过"的标记,返回时自动重新请求一次。
  体验上像"记住了",但仍有网络开销。

**Phase 3 后端跑通、拿到真实耗时数据后再决定。**

---

## T5 · 个别组的翻译耗时呈数倍波动(尾部延迟)

**状态**:方案 1(provider throughput)+ 方案 2(对冲请求)均已上线,**观察效果中**(2026-07-22)
**严重程度**:体验问题 —— 一个慢组会卡住顺序阅读

### 现象(用户实测日志,同一篇文章、同一模型)

```
组4 第1次:完成,耗时 22.2s,原文 4326 字符
组5 第1次:完成,耗时 81.6s,原文 4374 字符   ← 尺寸相同,耗时 4 倍
```

### 数据能排除什么

- 不是切分问题:渐进分组已生效(组1=1243字符/4.1s 最先回,顺序基本正确)
- 不是模型本身性能:组4 和组5 尺寸几乎一样,同模型,耗时差 4 倍 → 是**服务端方差**
  (OpenRouter 会把请求路由到不同 provider,各家速度差异巨大,还有排队)

### 候选方案(按性价比排序)

1. ✅ **已实施(2026-07-19)**:请求体里加 provider 偏好 `"provider": {"sort": "throughput"}` ——
   让 OpenRouter 优先路由到吞吐量高的服务商(只对 openrouter 域名发送,其他服务商不认识此字段)。
   **观察中:用户日志里 80s 级异常值是否消失。**
2. ✅ **已实施(2026-07-22,用户仍报长文开头等 >30s 后上线)**:对冲请求(hedging)。
   先发一份,某请求超过「按大小估的健康耗时 × 2」还没成功,就并发补发一份,
   谁先成功用谁、另一份取消(`translate()` 用支持取消的 `URLSession.data(for:)`,
   输的那份能真掐掉,不白烧 token)。实现在 `TranslationController.hedgedTranslate`。
   **先导块用固定 4s 阈值(它在关键路径上,不回来后面都不开始);
   正文各组按大小估阈值(200 字符/秒 × 2 倍,下限 6s)。**
   只有真正偏慢的请求会被对冲,健康的不会平白多花钱。
   日志里 `对冲触发` 可看它多久触发一次(= 成本)。**观察中:开头是否稳定快进几秒内。**
3. 缩小 maxGroupChars(4000→3000):治标,收益有限,先不动。

### 相关缓解(2026-07-19 同轮实施)

断点续翻上线后,慢组卡住时用户翻页离开的代价大幅降低 ——
已翻好的组存成"未完成缓存",回来点一下接着翻,不重复花钱。

---

## T6 · 装到真机 —— ✅ 已成功(2026-07-21)

**状态**:✅ **已解决,真机安装成功**
**记录时间**:2026-07-21

### 结论:免费 Personal Team 可以装,而且 App Groups 没有报错

这条一直标着 🔴「免费 Apple ID 能否签 App Groups 权限」——
**实测答案是可以**,预想的那个报错没有出现,退路(去掉 App Groups)没用上。
按 L3 的纪律「先跑一次让 Xcode 报错说话」,这次是推测偏悲观、实测更好。

### 实际用的配置(已生效并核实)

`/Users/wenbopan/Downloads/SharedXcodeSettings/DeveloperSettings.xcconfig`
(**在仓库外**,工程用 `#include?` 可选包含,不进 git):

```
DEVELOPMENT_TEAM = 7RDUVMLBSJ          ← Wenbo Pan (Personal Team)
ORGANIZATION_IDENTIFIER = com.wenbopan
CODE_SIGN_STYLE = Automatic
DEVELOPER_ENTITLEMENTS = -dev
PROVISIONING_PROFILE_SPECIFIER =
```

核实方式:
```bash
xcodebuild -project NetNewsWire.xcodeproj -scheme NetNewsWire-iOS \
  -destination 'generic/platform=iOS' -showBuildSettings | grep -E "DEVELOPMENT_TEAM|BUNDLE_IDENTIFIER"
# → DEVELOPMENT_TEAM = 7RDUVMLBSJ
# → PRODUCT_BUNDLE_IDENTIFIER = com.wenbopan.NetNewsWire.iOS-DEBUG
```

> 💡 **Team ID 在新版 Xcode 的账号界面里不显示**。不用让人去翻,直接读:
> ```bash
> defaults read com.apple.dt.Xcode | grep -iE "teamID|teamName"
> ```

### 遗留:7 天过期

免费账号签出的 app **7 天后过期**,到期重新用 Xcode ⌘R 装一次即可
(bundle id 没变 → **原地覆盖**,订阅源/已读状态/Keychain 里的 API Key 全部保留)。

⚠️ **不要把「7 天过期」和「App ID 额度」搞混** —— 我犯过这个错(见下方 T18)。

长期方案的讨论见 **T18**。

---

### 原始记录(保留)

**状态**:待办,**排在界面改造之后**(2026-07-21 用户明确选择先改界面)
**记录时间**:2026-07-21

> ⏳ **可以提前做掉的一件事(需要用户本人操作,AI 做不了)**:
> 打开 Xcode → 菜单 `Xcode → Settings → Accounts` → 左下角 `+` → 用 Apple ID 登录。
> 登录后才能拿到 Team ID,才能填下面那个 `DeveloperSettings.xcconfig`。
> 界面改完就能直接进真机环节,不用再等。

模拟器不需要签名,真机需要 —— 这是本项目至今一直绕开的东西。

### 已查证的事实

工程默认写死了上游作者的身份,**必须覆盖**
(`xcconfig/common/NetNewsWire_codesigning_common.xcconfig`):

```
ORGANIZATION_IDENTIFIER = com.ranchero
DEVELOPMENT_TEAM = M8L2WTLA8W        ← Ranchero 自己的 team,不是我们的
```

覆盖方式:在**仓库外面**新建(工程用 `#include?` 可选包含,不存在也不报错):

```
/Users/wenbopan/Downloads/SharedXcodeSettings/DeveloperSettings.xcconfig
```

内容:

```
DEVELOPMENT_TEAM = <你的 Team ID>
ORGANIZATION_IDENTIFIER = <自己的反向域名,例如 com.wenbopan>
CODE_SIGN_STYLE = Automatic
DEVELOPER_ENTITLEMENTS = -dev
PROVISIONING_PROFILE_SPECIFIER =
```

bundle id = `$(ORGANIZATION_IDENTIFIER).NetNewsWire.iOS$(BUNDLE_ID_SUFFIX)`,
改 ORGANIZATION_IDENTIFIER 即可避开与上游/App Store 版冲突。

`DEVELOPER_ENTITLEMENTS = -dev` 切到精简权限文件,差异已核对:

| 文件 | 包含的权限 |
|---|---|
| `NetNewsWire.entitlements`(默认) | iCloud/CloudKit、推送、App Groups、钥匙串组 |
| `NetNewsWire-dev.entitlements` | **只有** App Groups、钥匙串组 |

即真机 dev 版没有 iCloud 同步和推送。用户用本地账户(我的 iPhone),不受影响。

### 🔴 尚未验证的关键点

- **免费 Apple ID(个人团队)能否签 App Groups 权限?** 若不能,需进一步精简 entitlements。
- 免费账号签出的 app **7 天过期**,需每周用 Xcode 重装;付费($99/年)为 1 年。

按 L3 的纪律:**先真机跑一次让 Xcode 报错说话,不要凭推测下结论。**

### 顺带注意

翻译用的 API key 存在 Keychain 里,**不会跟着代码走** ——
装到手机后需要在设置里重新填一次。

---

## T8 · app 图标:三种外观全部到位

**状态**:✅ 已解决(2026-07-21,第三版素材)
**经过**:一共换了三版,前两版各暴露一个问题,都已修掉

| 版本 | 问题 | 结果 |
|---|---|---|
| v1 `app icon.png` | 素材**自带圆角边框**,iOS 再套一层 → 框里套框,图案内缩一圈 | 废弃(教训见 L26) |
| v2 `New app Icon.png` | 满铺了,但右下角 wifi 图形被圆角切断;「色调」外观下明暗与别的图标相反 | 废弃 |
| **v3** `app icon v2_{light,dark,mono}.png` | 无 | ✅ 采用 |

### v3 做对了什么

1. **满铺、无自带圆角** —— 不再框里套框
2. **重要元素离边缘留了余量** —— wifi 图形的下边距从 5.1% 提到 8.2%,
   放大 6 倍确认三道弧全部完整,和圆角之间还有空隙
3. **用户提供了三张独立素材**,不再让 iOS 自动生成:
   - light:浅报纸底 + 深色 R
   - dark:黑底 + 米色 R
   - mono:黑底 + 白色 R 的灰度图 → 「色调」外观下正确变成深底浅字,与系统图标一致

编译产物已用 `xcrun assetutil --info Assets.car` 核实,三种外观都在:
`(默认)` / `UIAppearanceDark` / `ISAppearanceTintable`。

### 验证时踩的一个坑(排查用得上)

改完深色素材后,`xcrun simctl ui booted appearance dark` 切到深色模式,
主屏图标**还是浅色的** —— 一度以为深色素材没生效。

实际原因:**主屏图标外观是一个独立设置**,之前的验证把它钉死在「默认」上了,
系统深色模式管不着它。要测深色图标必须在
主屏长按 → 编辑 → 自定 → **深色** 里显式切换。

**教训**:验证图标外观时,`simctl ui appearance` 只改系统明暗,
不等于改主屏图标外观。两者是两回事。

---

## T9 · 图标跟随深浅色切换 —— ✅ 没有问题,是我一开始漏看了一个开关

**状态**:✅ 已澄清(2026-07-21),**功能完全正常,无需任何改动**
**经过**:我先给出过一个**错误结论**,由用户指出后纠正

### 真相

主屏「自定」面板里,选中「深色」之后**下面还有一排子选项:「始终 / 自动」**。

- **始终** = 永远用深色图标,不管系统明暗
- **自动** = 跟随系统明暗切换 ← **这才是"图标会跟着主题实时变"的开关**

选「自动」后实测:系统切深色 → NetNewsWire 立刻变成黑底米色 R;
切回浅色 → 立刻变回浅色版。Apple 自己的「通讯录」「文件」同步切换。
**我们的三套素材工作完全正常。**

### 我一开始错在哪(值得记住)

我的错误结论是"模拟器不支持自动切换"。当时的推理是:
"Apple 自己的图标在深色模式下也没变 → 别人家也不跟随 → 不是我们的问题 → 是模拟器的问题"。

**前半段没错,后半段是错的。** 那条证据只能排除"我们写坏了",
**排除不了"有个全局开关没打开"** —— 因为那个开关(当时是「默认」)
本来就作用于**所有** app,包括 Apple 自己的。
两种假设对这条证据的预测**完全相同**,所以它根本没有区分能力。

方法上的教训已记入 L27。

### 顺带记下的正确操作

主屏长按空白处 → 左上角「编辑」→「自定」→ 选「深色」→ 再选下面那排的「自动」。
**只选「深色」不够,默认可能落在「始终」上。**

---

## T11 · 阅读视图:哪些源能抽出全文,哪些不能

**状态**:已实测一轮,列在下面;遇到新的失败源就往表里加
**记录时间**:2026-07-21

### 实测结果(2026-07-21,模拟器)

| 源 | 结果 | 备注 |
|---|---|---|
| 纽约时报中文网 | ✅ | 摘要 → 全文,约 3 秒。**最典型的受益场景** |
| BBC Chinese | ✅ | 成功,会带一点页面元信息(作者/发布时间/阅读时长) |
| 少数派 | ✅ | 成功,小标题层级都保留了 |
| Daring Fireball | ✅ | 链接博客,抽的是它指向的原文(Stratechery)—— 正是想要的行为 |
| **YouTube 类的源** | ❌ | **抽不出来,而且永远抽不出来** |

### YouTube 源为什么必然失败(不是 bug)

用户订阅里有好几个 YouTube 源(老孫聊遊戲、林亦LYi、石投记、Terry Chen 泰瑞…)。
查过数据库确认:这些文章的 `url` 是 `https://www.youtube.com/watch?v=...`,
`contentHTML` 长度为 **0**。

Readability 是从网页里找**文章正文**的。YouTube 播放页里没有文章,只有一个播放器 ——
**没有东西可抽**。点了会变成错误状态,这是诚实的结果,不是坏了。

原来那个 Feedbin/Mercury 服务同样抽不出来。

### 还没遇到但可能出现的失败模式

**前端渲染的站点**:如果页面在 `didFinish` 之后才用 JS 把正文渲染出来,
我们可能抓到一个空壳。对策是"抽到空就等 1~2 秒再试一次"。
**目前没有实测到这种情况,所以没有加这段重试逻辑**(不给没见过的问题写代码)。
用户如果报告某个源明明是文章却抽不出来,先怀疑这个,再加重试。

### 怎么排查

失败会记进日志(error 级别,会落盘):

```bash
xcrun simctl spawn booted log show --last 30m --predicate 'process == "NetNewsWire"' --style compact | grep 阅读视图
```

会打印失败原因和文章链接。

---

## T12 · Substack 的图片:多余按钮与被劫持的点击

**状态**:✅ 已解决(2026-07-21)
**用户报告**:"很多文章的图片下面都会出现这两个按钮,这是什么?"

### 那两个按钮是什么(查证过程值得复用)

**不是 iOS 的,也不是我们的代码** —— 是 **Substack 塞在 RSS 内容里的**。
直接把那篇文章的 `contentHTML` 从数据库拉出来就看到了:

```html
<div class="image-link-expand">
  <button class="… restack-image">   ← Substack 的「Restack(转发)」
  <button class="… view-image">      ← Substack 的「放大看图」
```

- 灰色空壳的那个是 Restack。它的图标用了 `stroke="var(--color-fg-primary)"` ——
  **这个 CSS 变量只存在于 Substack 网站**,在我们这儿没有,所以描边没颜色,渲染成空壳
- 蓝色箭头是放大按钮,用 `currentColor`,继承链接色所以显蓝
- **两个都是死的**:Substack 的 JS 不在我们这儿,点了没反应

### 顺带查出的更要紧的问题

Substack 把图片包在 `<a href="图片地址">` 里。
实测点图片 → **打开内置浏览器跳到 substackcdn.com**。

而 app 本来就有全屏图片查看器(`main_ios.js` 监听图片点击 → 原生查看器),
只是**被这个链接抢走了点击**,在 Substack 文章里永远触发不到。

### 解决方式(全在 `Shared/Appearance/nnw_appearance.js`,**上游零改动**)

1. CSS 一条 `.image-link-expand { display: none }` 藏掉两个按钮
2. 捕获阶段拦一道点击:点的是图片就 `preventDefault()`,掐掉跳转。
   **不改 DOM**,只取消默认行为 —— `main_ios.js` 照常收到事件,全屏查看器正常打开

### 长按:一度做了,又撤了

原计划自己做一个长按菜单(拷贝地址/浏览器打开/存相册),**代码写完并跑通了**。
但用户验收时发现 **WebKit 自带的长按菜单本来就有**
(打开链接 / 添加到阅读列表 / 拷贝链接 / 共享 / 保存到"照片" / 显示文本),
明确表示够用,于是**整段撤掉**。

⚠️ 注意其中的因果:那个系统菜单**正是因为图片被 `<a>` 包着才有的**。
我们只取消了跳转、**没有拆掉那个链接**,所以系统菜单照常工作。
以后如果有人想"顺手把这些多余的 `<a>` 拆了",会把这个菜单一起弄没 —— 别拆。

**最终的交互分工**:点一下 = 全屏看图;长按 = 系统菜单。

---

## T10 · 缩略图偶尔会选到"不像配图"的图

**状态**:待观察 —— 需要用户在实际使用中发现具体案例
**记录时间**:2026-07-21,列表改 Reeder 式布局时

缩略图取的是正文里**第一张** `<img>`。有些源的第一张图并不是配图:
站点 logo、作者头像、订阅按钮、追踪像素、表情。

**已经做的过滤**(`iOS/MainTimeline/ArticleThumbnail.swift`):
- 非 http(s) 的跳过(`data:` 内联图等)
- **显式写了** width/height 且小于 64 的跳过(追踪像素、小图标)
- 没写尺寸的一律放行 —— 宁可放过不可错杀,大多数正常配图不写尺寸

**没做的**:下载后按真实像素尺寸过滤。那样最准,但要等图下完才知道,
而且会白白下载一批不用的图。**除非实际很碍眼,否则不做。**

**怎么修**:用户看到不对的截图给我,按具体源的具体特征加规则
(例如某个域名的图一律跳过、或某个 class 的 img 跳过)。

---

## T14 · Reddit 限流:一次只订一个,别连着点

**状态**:已知限制,**不是 bug**,无需修复
**记录时间**:2026-07-21

Reddit 按 IP 限流,而且很紧。现在的设计已经把请求压到最少
(搜索零请求,订阅一次一个),但如果**连着把四种排序全订一遍**,
仍可能中途挨 429。

表现:订阅失败,弹出我们自己写的中文提示(两种可能:限流 / 版块名错)。
处理:等一两分钟再试。**app 挨过 429 后会按域名停发一段时间
(`DownloadSession` 的退避表,存在内存里),所以立刻重试必然还是失败** ——
重启 app 可以清掉 app 这侧的退避,但 Reddit 服务端的额度还得等。

⚠️ 排查时注意:**开发机用 curl 测 Reddit 会和模拟器共用出口 IP**,
测多了会把用户的额度打光,造成"代码坏了"的假象。详见 L33。

---

## T13 · ACX 连续多图之间空隙偏大 —— 未诊断,待观察

**状态**:待观察
**记录时间**:2026-07-21,正文页第一轮验收时

用户截图里 Astral Codex Ten 一篇的三张连续图片(Substack 的
`captioned-image-container` 结构)之间空隙明显偏大。
查过数据库:两个 figure 之间**零内容**,空隙纯排版产生,
但**没有定位到具体是哪条 CSS 撑出来的**。改居中后用户验收说"好像没问题",未再提。

**若用户再报**:按 L30 的路子 —— 拉那篇 contentHTML,数清楚每层容器,
先定位再动手,不要猜。

---

## T7 · 界面改造:大字号(无障碍)布局是另一套代码,改默认布局时容易漏

**状态**:✅ 2026-07-21 已实际处理并实测(改 Reeder 式布局时两套都改了)
**记录时间**:2026-07-21

**本轮的处理方式**:大字号档位下**竖着堆**(favicon → 源名 → 标题 → 正文 → 时间),
并且**不显示缩略图**,把整个宽度让给文字 ——
横排的源名+时间+缩略图在那个字号下会挤成一团。

**实测方法(以后照做)**:
```bash
xcrun simctl ui booted content_size accessibility-extra-extra-extra-large
# 看完恢复
xcrun simctl ui booted content_size medium
```
比在设置里点快得多。实测:布局正常、无截断、无约束冲突。

下面是原始记录,规则仍然有效 —— **以后再动列表布局,两套还是都要改。**

`MainTimelineCellLayout.swift` 里有**两套**布局:

| 结构 | 何时生效 |
|---|---|
| `MainTimelineDefaultCellLayout` | 平时 |
| `MainTimelineAccessibilityCellLayout` | 系统字号调到「辅助功能」档位时 |

切换点在 `MainTimelineCell.updatedLayout()`:
`traitCollection.preferredContentSizeCategory.isAccessibilityCategory`。

两套的**元素顺序都不一样**(无障碍版把日期换行放到订阅源名下面,不是右对齐)。

**风险**:以后调列表布局时,如果只改了默认那套,用户一旦把系统字号调大,
就会看到一个完全没改过的旧样子,而且**我们平时的验证根本不会触发那条分支**。

**怎么办**:
- 已铺的 `TimelineStyle.swift` 里的常量**两套都在用**,所以纯改数值(字号、颜色、间距)
  两套会同时生效,不用担心。
- 但如果将来要改的是**结构**(挪位置、增删元素),必须两套都改,并且实测:
  模拟器里 `设置 → 辅助功能 → 显示与文字大小 → 更大文字` 拉到最大,再看列表。

---

## T2 · 旧版 iOS 的工具栏分支无法验证

**状态**:已知限制,可能永远不修
**严重程度**:低(用户自己只用 iOS 26)

`installTranslationButton()` 里有一个 `if #unavailable(iOS 26)` 分支,
用于在更早的系统上手动补 flexibleSpace 间隔。

用户的模拟器是 iOS 26.5,**只能验证 iOS 26 那条分支**。
旧系统分支是按同样逻辑写的,但从未实际运行过。

若将来需要支持旧系统,必须先在旧版模拟器上实测。

---

## T3 · macOS 版无法用常规方式编译验证

**状态**:环境限制,非代码问题
**严重程度**:低(macOS 不在项目范围内)

macOS target 需要 Apple 开发者签名才能编译,用户没有配置:

```
error: No profiles for 'com.ranchero.NetNewsWire-Evergreen-DEBUG' were found
```

**绕过方法**(已验证有效,2026-07-19 实测 BUILD SUCCEEDED):

```bash
xcodebuild -project NetNewsWire.xcodeproj -scheme NetNewsWire \
  -configuration Debug -destination 'platform=macOS' \
  CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" \
  ENTITLEMENTS_REQUIRED=NO build
```

**每次改动 `Shared/` 下的文件后,都应该用这条命令验证一次**,
因为 `Shared/` 会被 macOS 版一起编译,写错了 iOS 这边发现不了。

---

## T15 · 播客语音条的三个固有限制

**状态**:已知限制,**不是 bug**。除非用户明确要求,否则不做
**记录时间**:2026-07-21

| 限制 | 原因 | 要修的话得做什么 |
|---|---|---|
| **时长不显示** | 上游 `RSSParser` 构造 `ParsedAttachment` 时 `durationInSeconds: nil` 写死,`<itunes:duration>` 根本没被解析 | 自己解析 XML 取 itunes:duration。**不值得** —— 为一行显示文字去碰 XML 解析 |
| **切后台/锁屏音频停,无锁屏控件** | WKWebView 里的 `<audio>` 不是原生播放器,拿不到后台音频权限 | 改用 `AVAudioSession` + 原生播放器 + Now Playing 信息。**工作量大**,而且语音条的定位本来就是"试听几分钟决定要不要认真听",认真听跳 Podcasts 更好 |
| **首次打开某播客的文章慢 1~2 秒** | 要重拉一次 feed 才知道音频地址(数据库里没有,见 L35)。Exponent 的 feed 有 767 KB | 已按 feed 缓存(含负缓存),同会话内只慢一次。**可接受** |

另:私人/付费 feed 不在苹果目录里,「在播客中打开」会静静地不显示 —— 这是有意的降级,不报错。

---

## T16 · 「全部」以外的四个分类 tab,可能是多余的

**状态**:待观察,**不要现在动**
**记录时间**:2026-07-21

订阅发现页顶部有五个 tab:全部 / 播客 / Reddit / YouTube / 网站。
「全部」会自己判断输入类型(`FeedQueryRouter`,18/18 测试通过),
所以另外四个的作用只是**缩小范围**。

如果日常使用中发现自动识别足够准,那四个 tab 可以撤掉,页面会更干净。
**但要用几天再说** —— 用户当时的原话是「A/B 用完再说」,
现在就撤等于用推测代替使用数据。

---

## T17 · 网站类订阅:探测路径清单可能要加

**状态**:够用,遇到失败再加
**记录时间**:2026-07-21

`WebsiteFeedResolver.commonFeedPaths` 现在有 7 个:
`/feed/` `/rss` `/feed.xml` `/index.xml` `/rss.xml` `/atom.xml` `/feed/atom/`

实测覆盖情况:
| 站点 | 命中方式 |
|---|---|
| stratechery.com | 探测 `/feed/`(它的 `<head>` 里**一个 RSS 声明都没有**) |
| jvns.ca | 探测 `/atom.xml` |
| daringfireball.net | 读网页声明 `/feeds/main`(常见路径全不中) |

**两步缺一不可** —— 上面三个站正好各证明一半。
用户报某个站订不上时:先 `curl` 那个站的首页看有没有 `<link rel="alternate">`,
再挨个试常见路径,确认是「真没有 RSS」还是「路径没覆盖到」。

---

## T18 · 长期安装与分享给朋友:选项与一处我说错的地方

**状态**:已讨论,用户决定暂不处理
**记录时间**:2026-07-21

### ⚠️ 先纠正我说错的一条(重要,别再重复)

我曾告诉用户「这个 app 一次占 4 个 App ID,免费账号 7 天限 10 个,
所以**一周最多重装两次**」,并据此建议删掉三个扩展。

**这是错的。** 查实际描述文件后确认,那 4 个 App ID **早已注册好**:

```bash
for f in ~/Library/Developer/Xcode/UserData/Provisioning\ Profiles/*.mobileprovision; do
  security cms -D -i "$f" | plutil -extract Entitlements.application-identifier raw -
done
# → 7RDUVMLBSJ.com.wenbopan.NetNewsWire.iOS-DEBUG(以及 .Share-Extension / .SpringboardWidgets / .IntentsExtension)
```

那个「7 天 10 个」的额度管的是**新建** App ID。重装的是同样这 4 个,**不消耗额度**。
→ **重装次数不受限制**,删扩展解决的是一个不存在的问题。

### 因此:删三个扩展 = 不划算,已决定不做

三个扩展是通过 app target 的「嵌入扩展」构建阶段挂进去的,
**scheme 里根本没列它们**(只有 `NetNewsWire-iOS` 和其测试),
所以要删必须改 `project.pbxproj` —— 第 8 节禁区,且该文件里
`NetNewsWire` 出现 143 处,扩展条目是结构性的,删了严重影响 merge。
收益只剩「编译快一点、包小一点」,不值得。

### 长期安装(自己用)

| 办法 | 说明 |
|---|---|
| 手动重装 | 每周连线 ⌘R,一分钟 |
| AltStore / SideStore 类工具 | 用**用户自己的 Apple ID** 后台自动重签,免费 |
| 开发者账号 $99/年 | 描述文件 1 年有效 |

### 分享给朋友:没有免费的成品分发

签名与设备绑定,**给不了朋友一个装上就能用的包**。只有两条路:
- **朋友自己签**(免费):他用自己的 Apple ID,Xcode 编或侧载工具签,同样 7 天一续
- **$99/年 + TestFlight**:唯一「点链接就能装、90 天不用管」的方式

不要碰的:企业账号($299/年)只允许给本公司员工装,拿来分发违规会被吊销证书;
Ad Hoc(100 台设备)**仍然需要 $99 账号**,不是免费方案。

⚠️ 这部分知识截止到 2026 年 1 月。原理(签名、7 天、额度)不会变,
但侧载工具能否使用、欧盟替代分发规则会变,**以工具官网当前说明为准**。

---

## T19 · 同一篇文章「订阅源版」与「阅读模式版」的译文共用一个缓存键,会互相顶掉

**状态**:已知小限制,**不是 bug**,暂不处理(2026-07-22,item③ 落地时记录)

翻译缓存的键是「文章ID + 模型 + 提示词版本」,**不含「是不是阅读模式」**。
而阅读模式抽出来的正文和订阅源自带正文文字不同 → 存进条目里的 `bodyHash` 不同。
所以:先翻订阅源版、再翻阅读模式版,同一个键只留得下一份 ——
**两版会互相顶掉**,来回切时另一版要重翻一次(花一次钱)。

- **正确性没问题**:`bodyHash` 会把对不上的老译文挡掉,不会张冠李戴,只是白重翻一次。
- item③ 自动恢复不受影响:因为自动恢复只在「本地有匹配缓存」时才应用,
  对不上就停在原文等用户点(用户选的「不悄悄花钱」)。
- **要修的话**:缓存键里加一个「变体」维度(feed / reader),让两版能并存。
  改动都在我们自己的 `TranslationCache.swift` / `TranslationController.swift`,不碰禁区。
  但收益只是「来回切阅读模式时少重翻一次」,**除非用户实际觉得烦,否则不值得**。

## T20 · item③ 自动恢复:阅读模式每次打开都重新抓一次原网页

**状态**:固有代价,**由用户选择时已知**(2026-07-22)

阅读模式(Reader View)的提取结果没有本地缓存(`extractedArticle` 只活在
当前 WebViewController 实例里,离开就没了)。所以 item③「记住并自动进阅读模式」
= 每次打开这篇都重新抓一次原网页(网络 + 电)。翻译那半边多数命中缓存则免费即时。

- 用户提这个需求时已被告知这条代价,接受。
- 若某源提取总失败(如 YouTube,见 T11),item③ 会在失败那次把该文记成
  「不开阅读模式」,避免下次反复做无用提取 —— 这是有意的。
- 要省这次网络:得把提取结果也落一层缓存。**暂不做**,除非用户觉得慢。

## T21 · ★ 档下打开大文件夹可能偏慢(取全部再筛星标)

**状态**:已知代价,**暂不处理**(2026-07-23,底部三档 Phase 2 落地时记录)

★ 档的文章列表是这么取的:`fetchArticlesAsync()` 拿全部,再在内存里筛
`status.starred`。原因是上游**没有"只取星标"的按源接口** ——
`ArticleFetcher` 只有「全部」和「未读」两个口子。

- **对单个源没问题**:一个源的全部文章本来就不多。
- **对大文件夹可能慢**:比如「科技」有一万多篇,取全部要走一次大查询。
  ⚠️ 但这不是新增开销 —— **「全部」档打开同一个文件夹本来就要走这一趟**。
- **要优化的话**:改成「先 `account.fetchArticles(.starred)` 拿全账户星标(量很小),
  再按 feedID 挑出属于这个文件夹的」。星标索引 `NNWStarredIndex` 已经有现成的分组数据,
  接上去不难。**除非用户实际觉得卡,否则不值得现在做。**

## T22 · 模拟器上改 app 偏好设置不可靠(排查方法备忘,不是 bug)

**状态**:环境问题,记下来省得下次再花时间(2026-07-23)

想在不点界面的前提下验证「换档之后长什么样」,试过四条路,**都不稳定**:
`simctl spawn defaults write`、直接 `plutil` 改容器里的 plist、
`killall cfprefsd` 之后再改、先杀 app 再改。
现象是**写进去了、`defaults read` 也读得到,但 app 起来还是旧值** ——
cfprefsd 会拿它自己缓存的那份回写覆盖。

- 偶尔会成功(所以别看到成功一次就以为找到方法了)。
- **本项目的分工本来就是"界面上的点按交给用户"**,所以结论是:
  **别在这条路上花时间**,把要点的那一下写进验收清单给用户。
- 命令行能稳定验证的仍然有:编译、装机、`assetutil` 查资源进没进包、
  查数据库、看日志、离线跑纯逻辑决策表。这些一个都不要省。

---

## T21 · 将来上架 App Store / 商业化前的功课清单(2026-07-24 用户问起,先记下)

**状态**:远期,未排期。用户问「不支持 Feedly/Inoreader 能不能上架卖」,结论:**能,那不是门槛**。

**Feedly/Inoreader 的判断**(把握程度:大方向有把握,细节未联网核实):
- Feedly 在大陆长期被墙,对中文目标用户几乎无意义,放弃损失很小;
  且其客户端密钥基本不对个人/小开发者开放(详见 2026-07-24 对话)
- Inoreader 有损失但**可后补**:密钥自助注册,走 `updateSecrets.sh` 注入即可,零代码改动
- v1 的同步:上游自带 iCloud(CloudKit)+ 本地 + Feedbin/FreshRSS 等直连,足够

**真正的拦路虎(按严重程度)**:
1. **头图素材商标**:Feed 页深色版有 PLAYBOY 兔子标与字样(2026-07-23 已记过)、
   浅色版有 THE NEW YORKER —— 上架**必被卡**(商标 + 内容分级),须换图或去标
2. **浮世绘素材版权归属 —— 已理清(2026-07-24)**:全部是 ChatGPT 生成。
   结论(非法律意见):AI 生成本身可商用(OpenAI 条款把输出权利给用户)、
   浮世绘风格不受版权保护;**风险只在画中出现的真实商标**(见上一条),
   与 AI 无关 —— AI 画的商标照样是商标。解法:上架前用 ChatGPT 重生成,
   把 PLAYBOY / THE NEW YORKER 换成虚构刊名(可以直接用 BABEL 当刊名,品牌自用)。
   顺带:纯 AI 图在美国不受版权保护 → 别人可复制这几张头图,无关痛痒,知道即可
3. **「Babel」查重名/商标**(App Store 里同名产品不少)
4. MIT 义务:保留上游版权声明;NetNewsWire 名字/图标是上游商标,全局盘一遍残留
5. **翻译的商业模式**:现在是用户自带 OpenRouter key;卖订阅要走苹果内购,
   多半得转"代理 API + 订阅含额度"模式(= 要做后端)。苹果审核细则常变,届时再核
6. 小件:付费开发者账号($99/年,顺带解决 T18 真机 7 天过期)、CloudKit 上架配置、隐私政策

---

## T22 · 长按分享 → 文章长图 —— ✅ 已实现(2026-07-24 当天拍板当天做),待验收后关账

**需求**:长按分享按钮 → 把当前文章生成一张滚动式长图(所见即所得:翻译状态、
阅读模式、图片、排版都照当前显示),末尾加浅色 app icon + 「分享自 <品牌名>」页脚。

**结论:可行,中等工作量。** 技术路线:WKWebView.createPDF(整页导出,天然所见即所得)
→ 按页栅格化拼长图 → 画页脚 → 系统分享单。长按手势挂在现有分享按钮上,短按行为不变。

**四个已想清楚的坑**:
1. 标题不在网页里(被阅读栏 CSS 藏了)→ 截图前 JS 临时解除隐藏,截完还原
2. 懒加载图片没滚到过就是空白 → 截图前强制加载全部图片、等完成(超时 ~5s)
3. 超长文章位图撞内存/分享上限 → 高度上限 ~2.5 万像素,超了整体降分辨率
4. 视频/播客内嵌截出来是黑块 → 第一版接受现状
页脚 icon 要把浅色图标复制一份进普通 imageset(app 图标资源不能按名字加载);
「分享自 X」的品牌名走 NNWBrand.displayName,不写死。

---

## T23 · 把系统弹窗逐步换成自绘品牌选单(NNWMenu)—— 全 app 盘点清单

**状态**:**A 档已全部换完**(2026-07-24 深夜,共 7 处,含追加的 confirmDelete),待用户验收;B 档等用户逐条拍板

**组件**:`iOS/DesignKit/NNWMenu.swift`(自绘圆角卡片选单,样式参考 Reeder)。
**替换原则**:选单类(actionSheet,几行选项)优先换,收益最大;
「好」一个按钮的纯提示 alert 换了收益低,先放;带文本输入框的 NNWMenu 不适用,不动。

**A. 我们自己文件里的(零 merge 风险,想换就换)**
- [x] 订阅列表 `+` 选单 —— 2026-07-24 换成品牌选单;**2026-07-25 选单整个退役(方案乙)**:
  「文件夹管理」提为导航栏铅笔按钮「编辑订阅」,`+` 直达搜索订阅源,中间不再有选单
- [x] 订阅发现页「订阅到」文件夹动作单 —— 2026-07-24 深夜已换(组件同时加了内部滚动)
- [x] 文件夹管理页「在哪个账户下新建?」—— 2026-07-24 深夜已换(从右上角按钮下方弹出)
- [x] 文件夹管理页「移动 N 项到…」—— 2026-07-24 深夜已换(超高内部滚动)
- [x] 文件夹管理页「删除文件夹」双选项确认 —— 2026-07-24 深夜已换(危险项红色+保留「取消」行)
- [x] **追加**:文件夹管理页「删除源/空文件夹」确认(confirmDelete)—— 和删文件夹同一条删除流程,
  只换一半会一半卡片一半系统框,故一并换了(同为我们自己的文件)
- [x] 文章页「重新翻译整篇」确认 —— 2026-07-24 深夜已换(从翻译按钮头顶弹出)
- 不动:新建/重命名文件夹(带文本框)、各处纯提示 alert(保存失败/已切换语言等)

**B. 上游文件里的 —— 2026-07-25 全部换完(用户拍板「B 全做」),各走最小通道**
- [x] 文章列表右滑「更多」—— 上游 30 行拼装收敛成 1 行调用;判断与标题**复用上游工厂**,
  动作体住我们的 `MainTimelineModernViewController+NNWMoreMenu.swift`
- [x] 「标记已读」确认 —— `confirm` 里换 1 行调用,实现在 `NNWMenu+Bridges.swift`;
  上游原 alert(...) 私有方法**原样保留不删**(减小合并冲突)
- [x] 设置页 OPML 导入/导出两个选账户动作单 —— 原地换写法,带账户图标(共用 accountIcon)
- [x] 全局错误弹窗 —— **同签名遮蔽**:`UIViewController+NNWError.swift` 定义与 RSCore
  一模一样的 `presentError(title:message:dismiss:)`,app 内 15 个文件的调用点自动改走品牌卡片,
  **上游零改动**。已核实 RSCore 模块内部无人调它。账户错误/可恢复错误两个弹窗
  (`UIViewController+Extras`)也换了
- Inspector / 主题导入的 alert:全是 presentError 系 → **被遮蔽自动覆盖,白拿**
- **追加(用户点名):长图链路** —— 「正在生成长图…」换自绘进度卡片 `NNWProgressCard`
  (转圈+文字,不可点外面关);「生成长图失败」「保存失败」走遮蔽后的品牌卡片

**C. 换不了外观的(默认不动,除非用户强烈想要)**
- 长按菜单(UIMenu):系统渲染,外观不可定制;要换=整个重做长按交互,动上游,代价大
  ~~⚠️ 长按分享按钮弹的「分享长图」小气泡也属于这类~~ → **2026-07-25 已不存在**:
  文章页底部整体换成自绘控件板(见 NOTES-progress),长图有了自己的键,长按菜单整个拿掉了
- 系统分享单、邮件/相册权限弹窗:系统的,不可定制

---

## T24 · 真机:阅读栏在重 JS 文章上偶发排版错乱(标题压住导航按钮、头像飘进状态栏)

**状态**:已定位并修复,**待真机复验**(2026-07-25;修复未经真机验证前不关账)

**现象**:Liberty Street Economics 某篇(大头图 + JS 交互图表,加载慢)进入后,
大标题第一行被返回键/上下篇胶囊压住,源头像缩到状态栏位置 —— 像飞行动画停在了半路。
用户反馈"大部分时候没问题",只这类重文章偶发。

**定位(用户四条复现答案收口)**:偶发 / 翻页后立刻快速滑动触发 / 滚动不恢复 / 换文章恢复
→ 排除进度公式(它自洽,滚到顶必归零),锁定 **safeAreaInsets 未传播**:
错乱截图里头像 y≈36、标题 y≈74,正是按"安全区=0"排出的版;每帧都读到同一个 0,
所以滚动不愈;换文章=换视图,新视图正常。pageVC 预载页在"装载中+藏栏折腾"时
安全区传播被卡,是 L59(pageVC 子树 trait 回调不触发)的亲戚。

**修法(两道自愈,都不碰每帧写入)**:①排版时 safeTop 取 max(宿主值, 窗口安全区)兜底
(正常时宿主值更大,max 无副作用);②检测到差值就 async 请求父级重排一次(带防重发标记),
并打了 [外观] 日志(host=/window= 两个值),复发时可从日志确认诊断。
容器新增 safeAreaInsetsDidChange → 重排(系统补灌安全区那一刻自动归位)。

**真机复验法**:同一篇重 JS 文章(Liberty Street),翻页进入后立刻快速上下滑,
反复几次 —— 修复后即使触发,下一拍应自动归位;若仍长时间错乱,截图 + 告诉我,
日志里搜「安全区疑似未传播」可确认走没走到兜底路。

**顺带记录(不是 bug,固有限制)**:该源图表是 JS 现画的,RSS 版和阅读模式版都只剩
图表的文字标签 —— 源头就没给静态图,救不回来。看图表的正路:点阅读栏标题开原网页。

---

## T25 · Xcode 真机运行每次报「not installed」(装成功、启动步骤失败)—— ✅ 已修(2026-07-25)

**现象**:⌘R 到真机,装上了、能手动打开,但 Xcode 报
`The requested application com.ranchero.NetNewsWire.iOS-DEBUG is not installed`。

**根**:bundle id 有两套来源 —— 真值 com.wenbopan 来自仓库外 `Downloads/SharedXcodeSettings/
DeveloperSettings.xcconfig`(#include? 可选包含),兜底 com.ranchero 写在
`xcconfig/common/NetNewsWire_codesigning_common.xcconfig`。编译读得到真值(装的是 wenbopan),
Xcode 的启动工人进程读不到那个 Downloads 里的文件(隐私保护),静默回落 ranchero →
拿旧 id 找 app → not installed。**模拟器双 Babel 分身、install 脚本的"跳过 install 改覆盖"
补丁,都是同一道裂缝。**

**修**:把兜底值改成 com.wenbopan(一行,[品牌] 标记)——读不读得到可选文件,身份都一致。
iOS/macOS 编译均过。**验证**:下次 ⌘R 真机,错误应消失;若仍报错,说明诊断不全,回来找我。

**遗留可选项**:把仓库挪出 ~/Downloads(隐私保护常年埋雷的根;挪了要同步改 CLAUDE.md
里的路径),用户想做再做。上游合并时这一行会冲突,按 [品牌] 标记保留我们的值即可。

---

## T26 · 拖放跨容器的"原生丝滑"动线(乐观更新)—— 备选,等用户对甲的口感反馈

**状态**:✅ **方案乙已实现并验收**(2026-07-25 深夜,用户确认丝滑达标)。实现要点:
- **复用同一个 section 构建器**:抽出 sectionSnapshot(for:previewing:),预演(MovePreview
  覆盖成员关系)和终局重画走同一段代码 → 预演画面与模型到位后的重画**结构上必然一致**,
  没有第二套实现可发散(L78/L89 药方)。
- 放手瞬间:排序同步落库(与 performMove 的 plannedOrder 一字不差,其重算幂等)→
  预演快照无动画应用 → 落下动画 drop(toItemAt:) 直飞预演行(收起文件夹时飞文件夹行)。
- 冻结配合:乐观路径跳过动画完成时的补画(模型未到位,补画会打回原形),
  解冻交给 didEnd+0.6s 兜底,届时重画=无差异。
- 已知取舍:同步账户搬家慢于 0.8s 时兜底重画可能闪回一次(本地账户无此问题);
  多项拖拽走老路(本页拖拽本就单项)。

(下面是当初的方案记录,留档)
拖放功能已完好(L87/L88/L89 三连修),只剩观感:
跨容器放下时"让开的行回归→(闪/跳)→新行出现",根因是搬家异步+行身份含文件夹,
做不了"放手瞬间数据源同步更新"的原生动线。

**已上的方案甲**:放手后的补画改为无动画瞬时就位(nextReloadAnimated 一次性标志),
把"闪烁"压成"啪一下"。让开的行仍会先回归。

**方案乙(真解,待拍板)**:乐观更新 —— 放手瞬间在快照层预演(旧身份行删除、新身份行
插到目标位),落下动画直接飞进预演行;模型随后到位,重画与界面一致=零闪烁。
要做:排序序号→快照行位的映射、身份替换、失败回滚;全在 L65/L89 雷区,要配离线验证。
**方案丙(否决)**:Apple 的异步拖放占位 API,与 diffable 数据源是知名雷组合,不碰。

---

## T27 · Reeder 观感对标 —— ✅ **已结(2026-07-28 当天开当天结)**

**结局**:提了 10 条改进,**只有 3 条落地**。第一梯队 4 条里 1 条被用户否决(favicon 去色);
第二梯队 3 条用户看过效果图后判「都不值得改」;第三梯队 2 条**我自己考古后推翻了**(见 L91)。
剩下的账不再挂着,谁也别再翻出来重做 —— 结论和理由都写在下面。

**背景**:用户提供了 5 张 Reeder 截图,问「为什么 Reeder 看起来精美,我的显得粗糙」。

> ⚠️ **那 5 张截图不在仓库里**。它们放在用户本地的 `Reference/` 目录,
> 而该目录已被写进 `.gitignore`(用户的选择,不想让 840K 截图进仓库)。
> **clone 本仓库的人看不到它们** —— 本条早期的措辞和一条 commit message 里
> 写着「存入 `Reference/`」,那是不准确的,以此处为准。
> 下面的量化结论都是从那些截图里取样得出的,数字本身已经记在这里,不依赖原图。

量化对比得出的核心结论:**Reeder 全 app 的中性色是同一个色相(39°)、同一个饱和度(0.01),
只在明度轴上取三档(0.98 / 0.94 / 0.90);高饱和的红只出现在开关上。**
精美感来自克制与一致,不是高级技巧 —— **这一条以后调外观仍然适用,是本轮最值钱的产出。**

**✅ 本轮已做(第一梯队,用户 2026-07-28 批准)**
1. **深色底暖化**:`paperDark` 从 `#1E1E1E`(色相 0、饱和 0,纯中性灰,深色一族里唯一的孤例)
   改为 `#1E1C19`(色相 36°,与 menuCardDark 一致,**明度分毫不差**)。
2. **三档控件降饱和**:当前档从「强调色字 + 强调色药丸」(饱和 0.49 / 0.26,**全屏最跳**)
   改为「暖墨字 + 统一选中高亮底」,视觉重心还给内容。
3. **透明背景图标补底**:新增 `iOS/Appearance/NNWFeedIconStyle.swift`,两个 cell 各一行调用。
   半透明中性灰底,深浅色自动适配。只命中「没有方块边界」的那几个图标(星形、书签之类),
   满铺型图标会把底完全盖住、看不出变化。

**❌ 本轮做了又按用户要求关掉的:订阅源图标灰度化**

做出来实测有效(三个采样点饱和度 1.00 / 0.80 / 0.18 → 全部 0.00),
但**用户看过实物后明确表示「希望 RSS 订阅源的 favicon 保留颜色」**,
于是改为 `isGrayscaleEnabled` 默认 **false**。恢复后取样确认 favicon 回到原色、
与改动前一字不差(`#FF8300` / `#164A69`)。

⚠️ **代码保留着,别删也别改回默认开启**:将来要做成设置页开关,
把 UserDefaults 的 `NNWFeedIconGrayscale` 置 true 即可生效,一行代码都不用改
(记得切换后调 `invalidateCache()`)。

**我从这件事该学到的(可复用)**:我是看到 Reeder 有「Grayscale favicons」
才提议并**默认打开**它的。但 Reeder 把它做成**开关**而不是直接去色,
这件事本身就说明**那是个口味问题,不是最佳实践**。
→ **参考物提供了开关的地方,默认值该跟用户走,不该跟我的审美走。**

**⬜ 待拍板**

- ~~**T27-a「装饰线」不做,建议换成「统一 symbol 图标的颜色」**~~
  → ❌ **2026-07-28 用户决定不做,已销账。**
  原清单第 5 条是「学 Reeder 在标题下加一条居中短装饰线」,
  实地看过首页后判定不适用(Reeder 那页没有头图;我们顶上是浮世绘 + Babel 字标,
  分隔与仪式感已由头图承担)。当时提的替代是「把智能组三个彩色 SF Symbol
  (橙太阳/红圈/黄星)和橙色文件夹图标统一成一色」——
  但 favicon 保留彩色之后,这几个 symbol 和彩色 favicon 并存反而是协调的,
  紧迫性消失。**用户明确不做,别再提。**
- ~~**T27-b 灰度开关进设置页**~~
  → ❌ **2026-07-28 用户决定不做,已销账。**
  用户明确要 favicon 保留颜色,不需要一个「哪天想清爽一下」的入口。
  灰度实现仍留在 `NNWFeedIconStyle` 里、默认关闭(`NNWFeedIconGrayscale` 置 true 可复活),
  但**不为它做设置页 UI**(那要改上游 SettingsViewController 4 处,见 L47,不值)。
- ~~**T27-c 第二梯队(3 条)**~~
  → ❌ **2026-07-28 用户看过效果图后决定「都不值得改」,已销账。**
  出过一张可交互效果图(三个开关逐条预览)给用户拍板,他看完判定三条都不做。
  三条各自的现状实测值留档如下,**将来若重新提起,不必再量一遍**:
  1. **「全部」档右侧留白**:拿掉数字后每行右侧约 60% 是空的。
     ⚠️ 要改就得**推翻用户 2026-07-23 自己定的「全部档不显示任何数字」**。
  2. **层级缩进**:现状顶层源图标 32pt、文件夹内源 48pt(缩进 16pt),左侧无锚点。
     Reeder 对照:三角 32pt → 源图标 54pt,缩进 22pt 且三角在最左当锚。
  3. **顶部停靠带**:实测 y=93pt 处明度 0.23,到 y=167pt 才降到列表底色 0.16,中间 73pt 渐变。
     ⚠️ **别再把这条当缺陷** —— 它是 **L62 精心调过的停靠态毛玻璃**
     (`headerDockedScrimStrength`),是有意设计。而且当初拿来对比的
     「Reeder 顶栏与列表同色无缝」是**不对等的对照**(Reeder 未滚动 vs 我们滚动后),
     那个论据不成立(L91)。真要调,是调浓度,不是抹平。
- ~~**T27-d 第三梯队(2 条)**~~
  → ❌ **2026-07-28 销账。这两条是我自己考古后推翻的,用户同意。**
  1. **按钮形状语言统一**:提这条时说「同屏三种形状并存:圆形描边 / 药丸 / 药丸容器」。
     **查代码后发现没有一个是我们画的** —— 顶部是
     `navigationItem.rightBarButtonItems = [search, edit]`、底部是 storyboard 的标准
     `toolbarItems`,**两个普通 bar button item,零自绘**。
     那些形状全是 **iOS 26 Liquid Glass 自动套的玻璃胶囊**(单按钮→圆胶囊、
     相邻按钮→合并长胶囊、自定义视图→按尺寸包一层),系统里所有 app 都这样。
     要"统一"就得全改自绘、绕开系统 bar button item,代价是失去系统适配
     (深浅色/动态字体/无障碍/将来的系统改版),而且效果图里的裸图标版**更散、更不像原生**。
  2. **设置页 Reeder 化**:能改的比想象中少得多,三个原因 ——
     ① Reeder 那招「分区小标题全大写 + 加宽字距」是**英文排版特有**,
     中文没有大小写,只能加字距模拟,而汉字加字距显松散(效果图里验证过);
     ② 「卡片低对比」我们**早就做过头了**(`applyPaperStyle` 已是暖纸底 + cell 同色 + 无分隔线,
     连卡片边界都没有,比 Reeder 走得远);
     ③ 去 `›` 只能去三四行(Reeder 主页全是入口所以能全去,我们这页混着入口/开关/选值三种)。
     另有一个"重构成 9 个入口 + 二级页"的版本,**明确不做**:我们这页 8 分区 20 多行,
     大部分是上游 storyboard 的静态 cell,那是动上游最深的一次改动,merge 风险远超收益。

**📌 这一整条的元结论**:第一梯队之后就是边际递减 —— 后面每一条要么收益极小,
要么要拿系统兼容性 / merge 安全去换。**外观对标到此为止。**
若还想提升观感,方向应换成**动效与手感**(截图看不出来,须真机对比),而不是继续抠静态外观。

---

## T29 · 首页原地编辑模式 —— ✅ **两步都已验收**(2026-07-28)

铅笔就地进编辑:行抖动 + 勾选圈 + 多选批量删除/移动 + 拖放排序 + 行尾「⋯」操作。
详见两个 commit 的说明与 **L94–L96**。

**还没做的(以后想做再说)**:
- 编辑模式里**新建文件夹**。做完这条,那个 2080 行的「编辑订阅」页就完全冗余了,可以整个删掉。
- 跨账户移动源(现在明确挡掉并提示,上游对跨账户是另一条 API)。

⚠️ **别删的守卫**:编辑期间禁左右滑切档;**拖动进行中**禁展开/折叠(不是"编辑期间");
智能组整节排除;勾选账本按**节点**记不按行号记。理由都写在代码注释里。

---

## T31 · 发现页交互 Phase A:订阅不跳走 + 绿勾取消订阅 —— ✅ **已验收**(2026-07-29)

**验收后追加一改(用户要求)**:点绿勾**直接**取消订阅,去掉了初版的 NNWMenu 二次确认。
理由:误触的挽回成本很低 —— 行还在原地,绿勾变回加号,再点一下就订回来了。

**背景**:用户提了「发现页试读」大需求(不订阅先看文章,阅读模式/翻译/长图都能用),
拆成三个 Phase。**Phase A 就是本条**;B/C 见下面 T32 的规划部分。

**改了什么(只动 fork 自己的 `FeedDiscoveryViewController.swift`,上游零改动)**:
1. **订阅成功后留在本页**:刻意不再发 `.UserDidAddFeed`。
   考古结论:那个通知被 `SceneCoordinator.userDidAddFeed`(SceneCoordinator.swift:584)接住后
   `discloseFeed(feed, animations: [.scroll, .navigation])` —— `.navigation` 就是"自动跳进新源"的元凶。
   首页列表的刷新**不靠它**:账户加/删源自己发 `.ChildrenDidChange`,
   协调器听到就 `rebuildBackingStores`(先例:文件夹管理页增删源从不发 UserDidAddFeed,首页照样刷新)。
2. **点绿勾 = 取消订阅**:绿勾从 UIImageView 换成 UIButton;点了直接跨账户找到源的所有落点
   (`existingFeed(withURL:)` + `existingContainers(withFeed:)`,Account.swift:595),
   逐个调公开接口 `removeFeed`。期间行尾转圈(新增 `unsubscribingURLs` 集合)。
   (初版点绿勾会先弹 NNWMenu 红色确认,验收后按用户要求去掉了。)
3. 转圈判断挪到"已订阅"之前 —— 取消订阅进行中账户里还查得到源,顺序反了转圈永远不显示。

**验收点**:① 订阅一个新源 → 转圈变绿勾,**页面不跳走**;返回首页能看到新源。
② 点绿勾 → 弹红色确认 → 确认后绿勾变回加号;首页里该源消失。
③ 点选单外面 → 什么都不发生。

---

## T32 · 两个已评估未动手的需求(2026-07-29 方案已谈妥,留档)

**① 列表标题批量翻译(优先级高于②)**:
- 展示通路:列表行统一在 `MainTimelineModernViewController.swift:835` 组装 cellData;
  `Article` 是纯内存对象、公开构造器 → 造"标题换成中文"的副本喂列表,**上游只改一行**。
- 批量:标题编号打包、一次请求翻几十条(省的是每条重复付的系统提示词),
  编号校验对不上就单条补翻;缓存按「文章 ID+标题哈希+模型」存磁盘(照抄 TranslationCache 思路)。
- 策略三层:默认**进列表现翻**(先显示原文,翻完原地刷新);
  按源开关**预翻译**(放进将做的「源设置页」,刷新后后台翻新文章);总开关。
- 用户未拍板:标题用哪个模型;中文标题下要不要保留原文小字;预翻默认关。

**② 发现页试读 Phase B/C**:
- B(中):✅ 已验收(T33)。
- C(大):✅ 已验收(T35)。**试读三阶段全部完成。**
- 已否决的省力路:「点预览先真订阅、退出再删」—— 首页闪源、未读数变、iCloud 会同步出去。

---

## T49 · 装 iOS 27 模拟器(Xcode 27)—— ✅ **已装好,而且立刻回本**(2026-08-05 晚)

**结果**:`/Applications/Xcode-beta.app` 与 Xcode 26.6 并存,
runtime **iOS 27.0 (24A5390f)** —— **build 号和用户手机完全一致**。
新建了模拟器 `iPhone 17 (iOS 27)`(UDID `87C9AE05-88C0-44E6-A871-242FF782D949`)。

**⚠️ 一个重要的用法决定**:**仍然用 Xcode 26.6 编译**(`xcode-select` 没动),
只是把产物装到 iOS 27 的模拟器上跑 —— 这样**和用户手机上的情况一模一样**
(app 用 iOS 26 SDK 编译、运行在 iOS 27 上)。
换成 Xcode 27 编译会改变系统控件的外观(样式按**编译时的 SDK**走),那是另一件事,
要单独评估。**别顺手把 `xcode-select` 切过去。**

**装机命令**(简单,不用改脚本):
```bash
SIM27=87C9AE05-88C0-44E6-A871-242FF782D949
BUILT=$(find ~/Library/Developer/Xcode/DerivedData/NetNewsWire-*/Build/Products/Debug-iphonesimulator -maxdepth 1 -name "NetNewsWire.app" -type d | head -1)
xcrun simctl install $SIM27 "$BUILT"
xcrun simctl launch $SIM27 com.wenbopan.NetNewsWire.iOS-DEBUG   # ⚠️ 是 com.wenbopan,不是 com.ranchero
```
⚠️ 这台是**空模拟器**(只有 NetNewsWire 自带的默认源),不是用户的数据 —— 随便点。
⚠️ 用户已给这台授权("Let Claude use it"),截图/点按都能用。

**立刻回本**:装完 20 分钟就推翻了 T42/L116 那条僵化的结论,顺手治好用户报的两处丑
(见 **T50** / **L122**)—— 而且**省掉了原计划里"搬四颗控件"的全部风险**。

---

## T49-旧 · (已完成,留档)装 iOS 27 模拟器的前期评估

**用户问**:「能不能在电脑上安装 iOS 27 的模拟器,直接调试?」——**能,但要装 Xcode 27。**

**实测当前环境**:Xcode **26.6** / macOS **26.6** / SDK 最高 **iOS 26.5** /
模拟器 runtime 只有 iOS **18.3** 和 **26.5**。
模拟器 runtime 跟 Xcode 大版本绑死,**Xcode 26 不会提供 iOS 27 的模拟器**。
用户手机是 27.0(build 24A5390f,beta),所以 Xcode 27 至少有 beta。

**为什么值得做**:今晚有一半时间花在"我复现不了,只能靠用户截图来回"上
(T45 修错一次、T47 搬家三轮)。装上之后我能**看到用户看到的东西**,
这一整类问题直接消失。**这是目前性价比最高的一件事。**

⚠️ **一个必须先说的副作用**:**用 iOS 27 SDK 编译,app 自己的系统外观也会变** ——
很多系统控件的样式是按**编译时的 SDK**决定的,不是按运行的系统。
换 SDK 之后要整体重看一遍,可能有些地方自动变好、也可能冒出新的不一致。

**⚠️ 卡点:磁盘空间不够**(实测)。可用 **29 GB**,而 Xcode 27 + iOS 27 runtime 大约要 30–40 GB。

可回收的(**都要用户点头再删**):

| 东西 | 大小 | 能不能删 |
|---|---|---|
| `/Library/Developer/CoreSimulator/Volumes/iOS_22D8075`(**iOS 18.3 模拟器**) | **18 GB** | ✅ 用不到(本项目只在 26 上测),可重新下载 |
| `~/Library/Developer/Xcode/DerivedData` | 4.9 GB | ✅ 编译缓存,删了重编即可 |
| `~/Library/Developer/Xcode/iOS DeviceSupport/iPhone17,1 26.5.2` | 5.7 GB | ✅ 另一台设备的调试符号,连上时会重建 |
| `~/Library/Developer/CoreSimulator/Devices` | 6.2 GB | ❌ **别删** —— 模拟器上那个 app 的全部数据(87 个源、已读状态、Keychain 里的 API Key)都在这里面 |

前三项加起来能腾出 **≈28 GB** → 总可用 ≈57 GB,够了。

---

## T48 · 「今天」的太阳保留原色 —— ✅ **已做**(2026-08-05)

**用户原话**:「能否让"今天"的这个太阳,走原来的配色,而不是受主题色控制
(我希望在全部统一的主题色里加一抹亮色,但是用克制的方式)」。

**做法**:`MainFeedCollectionViewCell.nnwIconTint(for:)` ——
默认仍走调色板,**唯一例外是「今天」**,用它**上游自带的 `preferredColor`
(`UIColor.systemOrange`)**,不另立色号。
好处:天然跟随深浅色;以后上游改了这颗图标的颜色我们自动跟上。
⚠️ 判据用 `===` 比 `Assets.Images.todayFeed`(IconImage 是 final class,那是唯一实例)——
**别改成比标题字符串**(会被本地化)**或比颜色**(循环论证)。
已在模拟器上截图确认:太阳橙、其余(全部未读圆圈 / 星标 / 文件夹三角)仍跟主题色。

---

## T51 · 用户 2026-08-08 提的 9+1 件事 —— ✅ **全部完成**(第二批 7 件于 2026-08-08 做完并逐条实测)

用户一次提了 9 件。约定**分两批**:先做"坏没坏"的三件(都动翻译链路或阅读体验,一起验最省事),
验收后再做其余六件(新功能/改交互,量大)。

### ✅ #1 某些源只翻译超链接 —— 已修,端到端验证过

**根因(不是用户猜的"翻译标题"设置,和设置无关)**:分块函数 `collect()` 只遍历
`container.children`(**元素**子节点),**直接挂在容器上的裸文本节点从头到尾没被看见**。
MacRumors 这类源的正文就是**裸文本 + `<br/>`**、全篇没有一个 `<p>`(抓它的 RSS 原文确认过),
于是 `<br>`/`<img>` 无文字被跳过、**只有 `<a>` 因为有文字被收走翻译** ——
屏幕上就是"整篇英文,只有超链接是中文"。

**修法**:新增 `wrapLooseText()`,分块前把裸文本连成的一段(以 `<br>` 和块级元素为界)
包进 **行内 `<span>`**,它就成了正常的翻译单元。
⚠️ **必须是 `<span>` 不能是 `<p>`** —— `<p>` 是块级的,会让这类文章凭空多出段间距。
⚠️ 天然幂等(包完之后容器直接子节点里不再有裸文本),重复调用不会套娃。
⚠️ 在 `originalHTML` 存档**之后**调,所以"切回原文"不受影响。

**顺带的改善**:句子里的链接从此**跟着句子一起翻**,不再被单独拆成
"Bloomberg 报道 短信摘录Axios报道"那种碎片。

**已验**:模拟器订阅 MacRumors 真跑翻译 → 整篇正文都翻了、链接留在句中 ✅;
切回原文正常、排版无多余段间距 ✅;回归 Stratechery(标准 `<p>`)翻译正常 ✅。

### ✅ #8 关掉"思考模式" —— 本项目**原来没防**,已加

用户在**另一个项目**踩过这个坑后问起。查证:本项目请求体里
**没有 `reasoning` 参数**,解析只读 `content` —— 也就是说:
默认开思考的模型会白烧 token 和时间;而**推理文字放在单独的 `reasoning` 字段**里、
`content` 可能留空 → 我们会拿到**一段空译文且看不出原因**。

**修法(查 OpenRouter 文档确认的写法,不是凭记忆)**:三处请求都带
`reasoning: {effort: "none", exclude: true}` ——
`effort:"none"` 彻底不思考;`exclude:true` 是关不掉时的兜底(至少别回传)。
⚠️ 只对 OpenRouter 发(和 `provider` 同一个理由)。
**第二道保险**:`content` 为空但 `reasoning` 非空时,给一条能照着做的错误
(「这个模型把正文写进了思考区…请换一个模型」),而不是笼统的"响应格式不对"。

### ✅ #2 标题飘上去时毛玻璃/大小/位置不对 —— 已修

⚠️ **这里有两个不同的现象,别混为一谈**:

1. **停靠了但看不到毛玻璃**:埋探针实测 `flight=1.000 / strength=0.720 / 回读=0.720 /
   状态=active / effect=有` —— **数字全对**(含回读的反向确认),说明毛玻璃机制**没坏**。
   真相是那一刻**窄条背后没有内容可透**(正文顶部恰好齐平窄条下沿),再滑一点就正常。
2. **大标题压在正文上**(用户截图 2 的那个)—— **这个是真 bug,已修**。
   原代码在 `contentSettled == false`(网页装载中)时把 `flight` **无条件钉死为 0**,
   理由是"WebKit 装载中会自己重置滚动位置,偏移不可信"。那条防护本身没错,
   但**用户此时已经能滚了** —— 一滚,内容跟手上移、头区纹丝不动 → 大标题叠在正文上;
   等 `didFinish` 一到,下一帧突然算出真值 → 用户说的"**突然**变成正常的毛玻璃和大小,还有位置"。
   **修法**:装载期间改为"偏移**明显**超过静止位才认"(`Style.settleGrace = 8`)——
   WebKit 的重置总在静止位附近,用户真滑时一下就远超。两种情形各得其所。

**已验**:进文章立刻滑 → 标题正常收起、毛玻璃正常、大小位置都对 ✅。


### 🔴 追加 #10(用户 2026-08-08 深夜补报)· 文章列表页齿轮挡住标题

**现象**:文章列表页顶部,源名较长时(如 "Conversable Economist"),
右上角的**齿轮压在标题上**,很难看。
**用户的要求**:把**设置(齿轮)和搜索(放大镜)放进同一个控件里**。

⚠️ **这一条和今天刚做的改动方向正好相反,动手前必须先读懂**:
今天为了修「右上角两颗变连体药丸」,在 `nnwHideSystemGlassCapsule()` 里加了
`sharesBackground = false`(**让相邻 bar item 不要被系统合并成一颗**,见 T50/L121)。
而 #10 要的恰恰是**把这两颗合成一个控件** —— 但那是**首页**(编辑/搜索)和
**文章列表页**(齿轮/搜索)两个不同的页面,**范围不同,不冲突**:
- **首页**保持现在这样:两颗**独立**圆钮(用户已验收)
- **文章列表页**改成:**一个控件里放两个图标**

**建议做法(别退回让系统合并)**:系统合并出来的是**系统材质**,
在 iOS 26/27 上又会两副样子(今天绕了一整轮才统一)。
应当**自己画一颗"双图标胶囊"** —— 复用 `NNWSoftGlassButton` 的材质,
做成一个 customView:一个胶囊里横排两个图标,中间一条极淡分隔线,
两半各自可点(命中测试按左右半区分)。这样 26/27 一致,也和全 app 的材质同源。

⚠️ 顺带确认一下**标题被挡**是不是还有别的原因:导航栏标题是居中的,
右侧控件变窄能缓解但未必根治(源名可以任意长)。
**量一下**标题的可用宽度,必要时给标题加省略号或缩小字号 —— 别只凭观感。

### ✅ 第二批(2026-08-08 全部做完并在模拟器上逐条验过):#3 #4 #5 #6 #7 #9 #10

**验收环境**:iOS 26 模拟器(用户那台 87 个源、有真 API Key)浅色 + 深色各走一遍;
iOS 27 模拟器复验了双图标胶囊和「外文」。**一件例外见 #3 的下半条。**

#### ✅ #3 每个源各自记住滚到哪(主体成了;「第二次点顶栏回原位」没做成)

**做法**:按侧栏项存**文章 ID**(不是像素),UserDefaults,上限 300 条,跨启动有效。
新文件 `iOS/MainTimeline/NNWTimelineScrollMemory.swift` +
`…+NNWTimelineExtras.swift`;上游只加了 1 行(`reinitializeArticles` 里)。

**两个坑,都是埋日志才看清的(别再踩)**:
1. **只能记"用户自己滑出来的"位置**。换源时列表清空再填上、`contentOffset` 归零,
   `scrollViewDidScroll` 照样触发 —— 那一下会把"停在最顶上"记下去,
   **刚要恢复的那条记录当场被自己抹掉**(实测抹除发生在恢复入口的前 0.14 秒)。
   判据:`isDragging || isTracking || isDecelerating`。
2. **恢复不能只试一次**。上游是「先 `replaceArticles`(`dataSource.apply` 是异步的)
   → 再叫我们」,调到我们这儿时 `numberOfItems` 往往还是上一个源的 —— 那一行还不存在。
   改成**每帧重试最多约 1 秒**。

🔴 **「点顶栏第二次回原位」没做成**,三条路全部实测失败,原因与证据见 **L125**。
现在的行为:点状态栏 = 回顶(系统给的),而且原位**已经记下来了**;
将来那一半接上就能直接用。

#### ✅ #4 右滑回列表、左滑下一篇未读 + 彩蛋页

前一页恒 `nil`;后一页 = **本列表里的下一篇未读**(会跳过已读的,实测验过);
没有了就给彩蛋页「没有下一篇啦!」(`NNWNoMoreArticlesViewController`,浅深两色都看过)。
彩蛋页上 dock 自动收起。

⚠️ **右滑必须自己做一个手势**:系统的返回手势是**屏幕左边缘**那一条,
从屏幕中间往右滑它根本不参与 ——「前一页返回 nil」只做到"不再翻页",不等于"会返回"。
⚠️ **出栈要对父导航控制器**:文章页住在一个**只有它一个**的导航控制器里,
对自己那个栈出栈什么都不会发生(埋日志才查出来:手势明明识别了 `dx=220`,就是没反应)。

#### ✅ #5 智能 Feed 加「外文」

`Shared/SmartFeeds/`(A 级禁区)**一行没改**:在 `iOS/ForeignFeed/` 里实现
`SmartFeedDelegate`,启动时 `smartFeeds.append(...)`。判语言 = `NLLanguageRecognizer`
认最近 15 条标题 + 汉字占比兜底,结果缓存 30 天;源设置页多一行「这是外文源」手动压过自动。

⚠️ **一个不得不碰的地方**:`SmartFeedsController.find(by:)` 在禁区里、认不出我们的标识,
于是侧栏那一行**没有图标**。在 `Shared/IconImageCache.swift` 里补了 4 行认领(`#if os(iOS)`)。
**还剩一个已知小缺**:冷启动时"上次选中的是外文"这个状态恢复不了(同一个 `find(by:)`),
会退回默认页。不致命,记档。

#### ✅ #6 长按文章 →「订阅源设置」

上游长按菜单里加 3 行,复用 `coordinator.showFeedInspector(for:)`。智能源页里也有效。

#### ✅ #7 模型菜单重做

`OpenRouterCatalog.swift`(全部模型 + 价格 + 磁盘缓存)、`OpenRouterBalance.swift`(余额)、
picker 整页重写。**用真 key 实测**:余额显示「余额 $11.06 / 共 $55.00」——
`/api/v1/credits` 解析成功。目录实测 **400 个模型 / 58 个厂商 / 14 个 `:free`**。

⚠️ **精选除了排 `:free`,还必须排掉另外两类(拉真数据才发现的)**:
- **价格为负**:`openrouter/auto` 等 5 个路由模型给的是 `-1` → 算出来 **−¥57600/篇**,
  不排就霸占精选前 5;
- **价格为 0**:`google/lyria-3-*`(音频)、`openrouter/free` —— 挤掉真正便宜的文本模型。

##### ✅ 精选改用一套完整标准 + 字号收紧(2026-08-08 晚,用户两次追加)

**用户给的筛选配方**(全部实现在 `OpenRouterCatalog.featured(from:)`,常数都提到文件上方了):

| 步骤 | 规则 | 实测砍掉 |
|---|---|---|
| 排除 | `~` 前缀(别名) | 11 |
| 排除 | 含 `:` 的变体(`:free` / `:batch`…) | 75 |
| 排除 | 名字带 `3b/8b/nano/mini/gemma/ministral` | 37 |
| 排除 | 名字带 `image/vision/audio/coder` | 18 |
| 保留 | 文本进、文本出 | — |
| 保留 | 上下文 ≥ 128K | 31 |
| 保留 | **发布于近 12 个月内** | **74 ← 最狠** |
| 保留 | 每篇估算 $0.005–$0.30 | 51 |

→ 剩 103 → 每家厂商取「最便宜 + 上限内最贵」各一档(27 家 → 42) → **取 15** → 按价格升序摆。

⚠️ **「名字带某个词」必须按词比,不能按子串比** —— 子串比会把 `google/gemini-…`
当成带 `mini` 给排掉。已抽成 `nameTokens`(按 `-._ ` 切词)。

🔴 **「按流行度选前 15」这条只做到了一半,而且是数据本身的限制**:
OpenRouter **不公开调用次数**,唯一能拿到的是网站自用的
`/api/frontend/v1/rankings/task-spend` —— 近 30 天、按**花费占比**、**分任务**、
**每个任务只给 10 条**。实测:29 个任务去重后**全站只有 29 个模型上榜**,
其中**只有 4 个**落在我们的精选候选池里。
所以实现成:**上榜的优先(按分数),其余按价格补齐到 15**,
并且给上榜的那几个在界面上标「热门」—— 让"真按流行度选的"和"按价格补的"**看得见区别**,
而不是让人以为 15 个都很流行。⚠️ 那个接口是内部接口,拉失败不算错,精选自动退回纯按价格。

⚠️ **加字段要跟着换缓存文件名**(现在是 `nnw-openrouter-models-v3.json`):
`OpenRouterCatalogModel` 是 `Codable`,加了字段之后旧缓存**整份解不出来** →
目录变空 → 页面自动去拉一次 → **自愈**。忘了换的话用户会看到一个空目录。

**字号**:模型行 body(17)→ subheadline(15)、副行 caption1(12),
上下内边距 11 → 7、两行间距 3 → 1。一屏从 5 个模型变成 8 个。
⚠️ 仍然走**动态字体**,没写死 pt —— 无障碍放大照样跟。

⚠️ **精选那一段不要写脚注**(用户看了截图当场说「不需要加这段」)。
我原来在那儿写了一大段说明:筛选规则、「热门」是什么、价格怎么估、没选中的去哪找 ——
**占了大半屏,把刚刚靠缩字号省出来的空间又全吃回去了**,而且用户本来就是嫌这页空、要更密。
判据:**这些解释属于代码注释,不属于用户界面。**
他要的是一份能挑的清单,不是一篇说明书 —— 每一行自己已经写着「≈¥X/篇(估算)」和「热门」,
够他做决定了。规则本身仍完整记在 `OpenRouterCatalog.featured` 和 `OpenRouterRankings` 的注释里。

##### ✅ 厂商行加字母徽标 + 品牌名(2026-08-08 晚,用户:「能不能把厂商 logo 加在前面,替代那个 ~」)

🔴 **真 logo 查过了,不划算 —— 别再试同一条路**:

| 想拿 logo | 实测 |
|---|---|
| `/api/v1/models` 里有图片地址吗 | ❌ 一个都没有(字段全查过) |
| OpenRouter 自己的图标 | ✅ 有,但**只有 SVG**(`/images/icons/Anthropic.svg`) |
| 有 PNG / WebP 吗 | ❌ 404;`_next/image` 转换接口 400 |
| 文件名能从 slug 推出来吗 | ❌ `deepseek` → `DeepSeek.svg`、`google` → `GoogleGemini.svg`,58 家得手工维护 |

而 **iOS 运行时加载不了 SVG**(只有编译进资源目录的能用)。要显示就得自己写 SVG 路径解析器,
或者拿 WKWebView 截 58 张图 —— 为装饰做这个不值,而且那个图标路径**没有文档**,随时会变。

**改成两件自己能掌控的**(`Shared/Translation/OpenRouterVendorStyle.swift`):
1. **厂商名写成品牌名**:`z-ai` → 智谱 Z.AI、`x-ai` → xAI、`bytedance-seed` → 字节跳动 Seed;
   没列进对照表的走通用美化(首字母大写 + 连字符换空格),漏一个也不难看。
2. **自绘字母徽标**:主题色圆角方块 + 白色首字母,22pt。不下载、不依赖第三方素材、不会 404。

⚠️ **那个 `~` 不是噪音,是 OpenRouter 的别名命名空间**
(`~anthropic/claude-opus-latest` 永远指向最新的 Opus)。所以不是删掉了事,
而是翻译成人话:**「Anthropic · 始终最新」**。
⚠️ 徽标是**画成图片**的,不会自己跟深浅色/主题色走 —— 已挂 `registerForTraitChanges` +
`nnwObserveAccentChanges` 各重画一次,缓存键也带上了颜色(L105 第 2 条 / L119 那一族的老账)。
⚠️ 首字母取自 **slug**(一定是拉丁字母),不取显示名 —— 显示名可能是中文,画在方块里认不出。

###### ✅ 追加:25 家换成真 logo(用户看过对照表后选方案 A)

**关键更正**:我之前说「iOS 加载不了 SVG」**只说对了一半** ——
运行时确实不行,但**放进资源目录(asset catalog)是支持的**,Xcode 会编译成矢量。
用户一句「拿了存在本地,放资源目录」就把这条路点开了。

**素材来源**(清单 + SHA-256 见 `Shared/Translation/README-vendor-icons.md`):
OpenRouter 自己的 9 个 + Simple Icons(CC0)16 个 = **25 家**,合计 **23.8 KB**。
剩下 27 家两边都没有,继续用字母徽标 —— 用户接受混排。

⚠️ **矢量图没有"分辨率"** —— 用户提出"不用存高清大图,清晰就行",
而 SVG 天然满足:任何尺寸都清晰,25 个加起来才 24KB。**别再想着存位图。**

⚠️ **单色 logo 必须标 `template-rendering-intent: template` 并染色**:
`openai / ibm-granite / inception / nex-agi / moonshotai / x-ai` 六个原色是**纯黑**,
深色模式下会直接消失(L112)。染成**主题色**一举两得:深浅色都读得出来,
也和这个 app「单一强调色」的调子一路。彩色的标 `original`,品牌色原样保留。

⚠️ **Xcode 的 SVG 支持范围比传说中宽**:`google`(径向渐变)和 `amazon`(线性渐变)
都正常渲染了 —— 事先担心的"渐变不支持"没有发生。**下次别凭传闻先自我设限。**

⚠️ `imageProperties.reservedLayoutSize` 要给死(22×22):真 logo 宽窄不一,
不给的话厂商名会参差不齐。

**已验**:iOS 26 浅色 + 深色都逐屏看过;真 logo、字母徽标、`· 始终最新` 三种混排都正常。

###### ✅ 再追加:精选那 15 行的**模型名前面**也放 logo(用户同晚要求)

判据写在 `Row.model(_:showsVendorIcon:)` 上:**只有"脱离了厂商分组"的行才画** ——
精选 / 当前选择 / 搜索结果那三处光看短名(`kimi-k3`、`glm-5.2`)根本看不出是谁家的;
**厂商分组里面的行不画**,上面几厘米就是那家的分组头,再重复一遍是噪音。

⚠️ 行里的 logo 用 **18pt**(分组头是 22pt)—— 行的字号已经收紧过一档,
22pt 会压过模型名。同样要给 `reservedLayoutSize`,否则模型名参差不齐。

**已验**:浅色 + 深色各一遍。真 logo(小米/美团/MiniMax/Google/DeepSeek)、
染成主题色的单色 logo(NexAGI/月之暗面/xAI)、字母徽标(StepFun/ThinkingMachines/智谱)
三种混在一列里,对齐和辨识度都正常。

###### ✅ 再追加:精选口径重定 + 每家最多 5 个 + 别名并组(用户 2026-08-08 连问三个问题)

用户问了三件事,**其中一件是我的 bug**:

**Q「gpt-5.6-luna 这么便宜又出名,为什么不在精选?」**
两个独立原因,都拉真数据验过:
1. **被「每篇 $0.005 下限」筛掉了**,连候选池都没进(luna $0.0028)。
   同样被误杀的还有 **`deepseek-v4-flash-0731`($0.0011)—— 用户当时正在用的那个**。
2. 就算过了,**「取 15」是按价格补齐的**,而中国厂商便宜整整一个数量级
   (各家最便宜档:deepseek/minimax/小米/qwen $0.005 vs anthropic $0.024、openai $0.028),
   按价格排 OpenAI 是 27 家里的**第 24 名** —— 大厂全军覆没是这套规则的**必然结果**,不是 bug。

**Q「OpenAI 分类下为什么只有 gpt-latest?」→ 🔴 我的 bug。**
OpenAI 真正的 95 个在列表更下面;用户看到的是**别名分组**(只有 2 个),
而我让 6 个 `~` 别名组**排到了厂商列表最前面**,长得和真分组一模一样。
**修法:别名并进对应厂商,一个品牌只有一节**,别名行标「始终最新」。
⚠️ OpenRouter **不告诉我们别名当前指向哪个具体模型**(`canonical_slug` 就是别名自己),
所以标不出"= 谁",只能标"是个会自动跟最新的别名"。

**用户拍板的两条改动**:
- **去掉价格下限**。判据:**价格不是能力的代理指标**;"排掉小模型"已有名字规则和
  上下文 ≥128K 两道更直接的闸门在管,再拿价格兜一遍是重复的,而且专杀"又强又便宜"那一类。
- **知名厂商保底**(`featuredMajorVendors`,手工有序名单)。精选改成三步占席位:
  ①榜上有名的 → ②知名厂商各 1 个(取该家最便宜的合格档)→ ③剩余按价格补,
  **全程每家最多 1 个**。结果:15 个里 OpenAI(luna)/Anthropic(haiku)/Google/Meta/
  NVIDIA/Mistral/Amazon/Perplexity/xAI/DeepSeek/Qwen/MiniMax/Moonshot/Z.AI 都在。
  ⚠️ 这份名单是**人工判断**,新厂商冒出来要手动加 —— 诚实承认,别假装是算出来的。

**顺带补的两条噪音规则**(不补的话 OpenAI 那一档会选中 `gpt-oss-safeguard-20b` 这个审核模型):
- 参数量标记 `<100B` 算小参数线(`20b`/`70b` 出局,`120b`/`550b`/`671b` 这些旗舰留下)
- `safeguard`/`guard`/`moderation`/`embed`/`rerank` 归入专用线
⚠️ **试过往小参数线里加 `lite`/`small`,会误伤 `gemini-3.1-flash-lite`、`mistral-small-2603`
—— 恰恰是"又便宜又好用"该推荐的那一类。别加。**

**Q「每个厂商最多显示 5 个可以吗?保证最便宜的、最强的、最平衡的都在」→ 已做**
`grouped` 现在每家最多 5 个,并返回 `total`,标题写成「OpenAI · 精选 5 / 共 97」——
**不许静默截断**(用户刚被"只有 gpt-latest"骗过一次)。挑法:
最便宜 / 最贵(以价格代理"最强")/ 价格中位("最平衡")+ 补最新发布的。

⚠️ 三个踩过才知道的细节:
1. **必须先按系列去重**(`family`:扔掉版本号那类词)。不去重的话 Anthropic 那一节
   实测是**三行 opus 变体**,sonnet 根本不出现。去重后是 haiku/sonnet/opus/fable。
2. **没超上限就别去重**。第一版一上来就去重,「Aion Labs」的 aion-2.0 / aion-3.0
   被合成一个 —— 明明一屏放得下却把"最强那档"藏了。
   判据:**删减是为了治"多得没意义",不是为了整齐;不多就别删。**
3. **兜底要分两层**。有些厂商(Cohere、字节跳动、Cognitive Computations)名下全是老模型,
   严格过滤后是空的;退回时**仍然不要 `:free`/`:batch` 变体**,
   否则 Cohere 那一节头一行是「north-mini-code:free · 未标价」。

⚠️ 又加了字段 → 缓存文件名 **v3**(`nnw-openrouter-models-v3.json`)。

###### ⚠️ 追加更正:精选的「上榜优先」原来挑错了型号(用户第三次追问)

用户接着问:「为什么 deepseek v4 flash 0731 **还是**不在精选?」——
根因:那一步挑的是**上榜的那个模型本身**,于是 DeepSeek 的唯一席位被
`deepseek-v4-pro`(¥0.038,流行度 0.78/命中 15 个任务)占掉,
而 `deepseek-v4-flash-0731`(¥0.008)因为太新还没进那份 30 天花费榜,流行度是 0。

**改法:流行度只决定"哪家先占位",型号一律取该家最便宜的合格档。**
判据:**精选是"每家的性价比之选",不是"每家最多人花钱的那个"。**
流行度数据只覆盖 4 个候选,拿它决定"选哪个型号"太弱;决定"哪家先占位"刚好 ——
那是它唯一撑得住的用途。「热门」标记照旧按每一行自己的数据显示,不受影响。

改后 15 个(实数据预演):ling-2.6-flash / qwen3.7-flash / **deepseek-v4-flash-0731** /
glm-4.7-flash / nemotron-3-super / **gpt-5.6-luna** / mistral-small-2603 / minimax-m2.5 /
gemini-3.1-flash-lite / nova-2-lite / grok-build-0.1 / kimi-k2.6 / muse-spark-1.1 /
claude-haiku-4.5 / sonar-pro-search。

⚠️ **已知的一个别扭**:x-ai 那一席变成了 `grok-build-0.1`(¥0.086,比 grok-4.5 便宜),
名字看着像编程/agent 专用。没有数据能证实,**没有擅自加规则去排它** ——
要排的话往 `featuredSpecialistTokens` 里加 `build` 即可,等用户发话。

⚠️ 顺带改了 `TranslationConfigStore.selectedModel` 的读取:**不再要求它出现在
`availableModels` 里**。老守卫的前提是"列表 = 全部可选项",现在从 400 个里挑,
再留着它会把用户选的模型**静默换掉**。
⚠️ 搜索框要显式 `preferredSearchBarPlacement = .stacked` —— iOS 26 默认把它**吊在屏幕底部**
浮在列表上(装机截图看到的)。这一页可以放心用 `.stacked`(它是普通分组表,
没有 L94/L95 那页的头图/会飞的标题,**范围不同,结论不能照搬**)。

⚠️ 旧的 `OpenRouterModelCatalog.swift`(翻译榜,内部接口)**从此没人调用**,文件留着没删。

#### ✅ #9 开关/控件接入主题色

`NNWAccentTint.install(in:)`:`window.tintColor` + `UISwitch.appearance().onTintColor`
都走 `NNWAccentPalette.live`,并订阅换色通知**重设一遍现有窗口里的开关**
(appearance 只管以后新建的,**两件事各管一半,少一件就有一半不跟**)。
storyboard 里写死指向 `primaryAccentColor` 的 `onTintColor`/`thumbTintColor`/`tintColor`
共 **14 处已删**(Settings 9 + Inspector 5)。iOS 侧 `Assets.Colors.primaryAccent` /
`secondaryAccent` 的引用 **20 处**改走调色板。

⚠️ **`Shared/Assets.swift` 一行没动,而且不能动**:它被 iOS Share Extension **单独编译**
(pbxproj 的 membershipExceptions 里点名了),那个目标看不见 `iOS/` 下的 `NNWAccentPalette`。
这正是 L103 的那条边界。

#### ✅ #10 齿轮压住标题

两手一起:① 右上角两颗独立圆钮(≈96pt)换成**一颗自绘双图标胶囊** 68pt
(`NNWSoftGlassDualButton`,26/27 实测同宽同样式,**没有退回让系统合并**,守住 L121);
② 把**会飞的标题**的可用宽度**跟着飞行进度收紧**到控件之间的净空(改在 `TimelineFeedHeader.swift`)。

⚠️ **第二手一开始加错了地方**,而且"看起来是好的"差点骗过我 —— 详见 **L124**。
判据一句话:**本 fork 已经把上游的标题机制整个换掉了,凡是和标题有关的活,
第一站是 `TimelineFeedHeader.swift`,不是 `navigationItem`。**

#### ✅ 追加:**首页那两颗也合了**(2026-08-08 晚,用户看截图后要求)

⚠️ **这一条推翻了上面 #10 里「首页保持两颗独立圆钮」那句。**
当初那么写的理由是「两个页面范围不同,别顺手一起改」;
用户看过文章列表页的成品之后主动要求首页也统一 —— 理由消失了,照做。

首页右上角的**编辑 + 搜索**换成同一个 `NNWSoftGlassDualButton`
(左 = 编辑订阅 `square.and.pencil`,右 = 搜索全部订阅源 `magnifyingglass`,
和原来「搜索在最右」的位置一致)。改在
`iOS/ReadingMode/MainFeedCollectionViewController+ReadingMode.swift`。

⚠️ **改完必须跟着换那个幂等判据**:原来是
`rightBarButtonItems?.first?.customView is NNWSoftGlassButton`,
不改类型就等于把 2026-08-04 那个 bug 原样放回来(每次 `viewWillAppear` 重造一对按钮)。
⚠️ 只剩单颗圆钮的地方(智能源页 / 文件夹页的放大镜)照旧用 `NNWSoftGlassButton`;
原来那个 `nnwGlassButton(icon:action:label:)` 私有工厂没人用了,**已删**。

**已验**:iOS 26 浅色 + 深色、iOS 27 浅色都看过;左半点开「编辑订阅」(多选圈 + 完成 +
底部三个操作都在)、右半点开「搜索全部文章」—— 两半各自可点。

## T50 · 首页右上角两颗变连体 + 底部中间不对称(iOS 27)—— ✅ **已修,而且没有搬那四颗控件**(2026-08-05 晚)

**⚠️ 修法和原计划不同,而且便宜得多。装上 iOS 27 模拟器之后第一件事是复验旧结论,结果旧结论被推翻。**

**原计划**:把首页剩下四颗(齿轮/加号/搜索/编辑)也搬出栏。
**实际修法**:**删掉 `systemDrawsBarCapsule` 那处版本分叉**(改成恒 `false`)+
`nnwHideSystemGlassCapsule()` 去掉 iOS 27 早退 —— 一共两处,零结构改动。

**凭什么敢删(在真的 iOS 27 模拟器上逐行采样)**:

| 页面 | 拆掉系统胶囊后,栏那一带 | 判定 |
|---|---|---|
| 首页(`UICollectionView`) | 203–235 **随内容起伏**,到 870pt | ✅ 内容照旧穿过去 |
| 文章列表页 | 212–235 随内容起伏 | ✅ |
| 文章页(当初出问题那页) | **不适用** —— dock 已搬出工具栏、`toolbarItems = []`,没有 bar item 会去设那个属性 | ✅ |

也就是说 **T42/L116 那处分叉的前提,在 dock 浮层化那一刻就已经消失了**,
而我们让它多活了一轮,代价就是用户报的这两处。完整教训见 **L122**。

**改后实测(两个系统逐项一致)**:

| | iOS 26 | iOS 27 改前 | iOS 27 改后 |
|---|---|---|---|
| 三档控件高 | 40.0pt | 40.0pt | 40.0pt |
| 齿轮 / 加号 | 40.4pt(我们画) | **48.0pt(系统画)** | **40.4pt(我们画)** |
| 右上角两颗 | 两颗独立圆钮 | **一颗连体药丸** | **两颗独立圆钮** |

**iOS 27 上已验**:右上角两颗独立 ✅ 三个控件同高 ✅ 三档能点、切档正常 ✅
编辑模式进出 + 浮层收放 ✅ 文章页 dock 单层 ✅ 内容穿过栏底 ✅。
**iOS 26 上已验**:改动前后逐项不变(它本来就走"我们画"这条路)✅。

### 追加(同晚,用户复看后报「中间还是比两边高」)—— ✅ **已修,用户已在真机确认**

⚠️ **上面那句"2pt 记档不修"是错的判断,已推翻。** 用户一眼就看出来了,而且他还多看出两条:
「三个控件没对齐」「左右两个可能不一样大」——**三条全部成立**。
(我上一轮量齿轮用的是**弦**再反推直径,正是 L109 那个坑,所以报了"没问题"。)

**按规矩重量外接框(阈值扫亮边,不靠任何假设的圆心),修完之后**:

| | 上缘 | 下缘 | 高 | 中心 |
|---|---|---|---|---|
| 齿轮 / 三档 / 加号(iOS **26**) | 802.00 | 841.67 | 39.67 | **821.83** |
| 齿轮 / 三档 / 加号(iOS **27**) | 802.00 | 841.67 | 39.67 | **821.83** |

**两个系统、三个控件,四项全部一致。**

**① 对齐(+2pt)的依据 —— 不是凑数**:实测 bar item 的圆盘下缘 **841.67 > 安全区上沿 840**,
即**工具栏允许 bar item 探入 Home 条那一段 1.67pt**,而浮层老实贴着安全区,所以恒定高 2pt。
这个量 **26/27 完全相同**,所以敢对齐。常量叫 `nnwBarItemOvershoot`,
注释里写明"实测值、非推导值、改之前先重量"。

**② 右上角连体药丸的真正原因(以前根本查不到,靠 iOS 27 SDK 头文件才看到)**:
> `hidesSharedBackground` … **This property is ignored if the item is in a
> `UIBarButtonItemGroup` with more than one item.**

iOS 27 会把**相邻的 bar item 自动归进隐式组** → 我们那句"拆系统胶囊"**被系统忽略** → 连体。
正解是同一个头文件里的 `sharesBackground = false`(先脱组)。
⚠️ **两句的顺序不能换**,已写进 `nnwHideSystemGlassCapsule()` 的注释。

### 🔴 仍未解决:齿轮左边那块圆角矩形残留(iOS 27 独有)

齿轮左上角露出一小块圆角矩形,**加号完全干净**(同一段代码、同一个函数)。
iOS 26 上完全没有。

⚠️ **我误判过一次,别重蹈**:加了 `paddingRemoved` 后外接框宽度 43.67 → 39.67,
我据此宣布"修好了"—— **放大截图一看那块东西还在**,只是整项外移 4pt、被圆盘盖住更多,
落到了阈值扫不到的地方。**宽度是代理指标,回答不了"那块东西还在不在"**(教训见 **L123**)。

**已排除**:`sharesBackground = false`(加了没用)、`paddingRemoved`(没用)、
上游进度条(`configureToolbarWithProgressView` 在 iOS 26+ 直接 return)、
`NNWProgressCard`(只用在文章页)。

**下一步(直接做,别再猜)**:把 `toolbarItems` 整个打出来 ——
数量、每一项的 `action` / `image` / `customView` / 是否被 `nnwRestyleToolbarIcons` 上色。
**怀疑那边多了一项我们没上色的**(`nnwRestyleToolbarIcons` 只认 `add(_:)` 和 `settings(_:)`,
别的项会保留系统玻璃)。

⚠️ **`paddingRemoved` 那段 KVC 暂时留着**(用户已在真机验收当前观感),
但它**没达成目的**,而且让 iOS 27 的控件比 26 往外挪了 4pt(=**新引入的 26/27 分歧**,
正是 L121/L122 反对的)。**查清残留物之后,大概率应该连它一起删掉。**

---

## T50-旧 · (原计划,已不需要)把首页四颗控件也搬出栏

用户 2026-08-05 在 iOS 27 真机上报的两处,**是同一个病的两个症状**(判据见 **L121**):
控件**一半归系统画、一半归我们画**,必然对不齐。

| 症状 | 谁画的 |
|---|---|
| 右上角搜索/编辑变成一颗连体药丸 | 27 上 `NNWSoftGlassButton` 跳过自己的圆盘 → 只剩系统给两项合起来的那颗 |
| 底部中间三档比两边窄 / 不对称 | 三档是**我们**画的(直径 = `controlDiameter`),齿轮/加号是**系统**画的 |

**用户拍板的方向**:把"半分"补齐 —— **首页剩下那四颗(齿轮 / 加号 / 搜索 / 编辑)也搬出栏**,
全部自己画。路子已经跑通(T47),复制即可。

**⚠️ 但用户选择先装 Xcode 27 再动手**(T49),理由:搬的时候能当场逐像素对齐,
不用再靠截图来回。**别在没有 iOS 27 模拟器之前动手。**

**动手时的已知风险**:
- `addNewItemButton`(`+`)是 storyboard 的 **IBOutlet**,上游还往它身上**挂 menu**、写 `isEnabled` ——
  搬出来要自己接管那棵选单(好消息:`NNWMenu+SystemBridge` 本来就能把任意 `UIMenu` 转成我们的卡片,
  接过来反而更统一)。
- 底部工具栏 `expectedItemCount == 3` 的守卫:四颗全搬走之后数组会变空,
  **要确认那条守卫在空数组下的行为**(现在是 3 项,正好满足)。
- 导航栏那两颗压在**头图**上,位置随导航栏高度变 —— 比底部那条难,别照抄常数。

---

## T47 · 首页三档控件搬出系统栏(浮层化)—— ✅ **已做,模拟器全套回归通过;真机已装待用户验**(2026-08-05)

**用户拍板走这条**(相对于"就地凑一个补偿值"):一次解决三件事 ——
① 修「iOS 27 真机内圈比外圈小很多」(两把尺子问题,见 T45 下面的 B);
② 27/26 渲染不再分叉(外圈内圈都归我们画);
③ **T43 陀螺仪的前置条件达成**(那圈亮边归我们了)。

**新文件** `iOS/ReadingMode/NNWFloatingModeBar.swift`;
`NNWReadingModeBar` 加了 `drawsOwnTrack` 开关(浮层版两个系统版本都画外圈)。
装配点 `nnwInstallModeBarIfNeeded` 从「插 toolbarItems」改为「装浮层」——
**顺带把 `toolbarItems` 还原成故事板原样 3 项**,`expectedItemCount == 3` 那条守卫不用再绕。

**⚠️ 三个来之不易的实现判据(改这块前必读)**:
1. **浮层必须挂在 `splitViewController.view` 上** —— 挂本页或导航控制器的 view 都拿不到触摸,
   因为 `UILayoutContainerView` 的命中测试不按 z-order 走(三轮探针的结论,见 **L120**)。
2. **位置用 `window.safeAreaInsets.bottom` 算**,别量工具栏(它的 frame 是整屏,L120)。
   实测与加号图标中心差 1.8pt。
3. 宿主铺满整屏,`hitTest` 只收控件矩形内的触摸,其余放行。

**收放的三个钩子**:`viewWillDisappear`(离页收)、编辑模式进/出(`nnwUpdateEditingChrome`)、
`nnwInstallModeBarIfNeeded`(回页放)。

**模拟器已回归**:点三档切换 ✅ 左右滑 ✅ 列表滚动放行 ✅ 齿轮/加号 ✅
模态弹层压在浮层上 ✅ 进文章列表页收起/返回恢复 ✅ 编辑模式收起/恢复 ✅ 深色 ✅。

**已知限制(记档)**:
- iPad 未适配(split 真分栏时按整窗居中,不按列居中)。用户只用 iPhone。
- **文章列表页那条三档还住在工具栏里**,iOS 27 上它的内外圈差还在 ——
  用户的范围决定是「首页抓眼球、之后的页面低调」,那页要不要也搬等用户发话。

---

## T45 · 切回 app 时选中胶囊变成深色 —— 🟡 **已按机制修,等用户在 iOS 27 上确认**(2026-08-05)

**用户原话**:「从别的软件切过来的时候,底栏中间、被选中的地方,不是主题色,而是红色,
而且是深色模式下的」(附 iOS 27 真机截图:整个 app 是浅色,唯独那颗选中胶囊是黑底)。

**机制**:`NNWSoftPanel.layout(in:)` 把语义色 `resolvedColor(with:)` 之后写进
`CAGradientLayer.colors` —— 那是 **`CGColor`,不跟随深浅色**,只有下一次
`layoutSubviews` 才会重新解析。**只要曾经有一帧是用深色排的版、之后没人再叫它排一次,
就永久停在深色。** iOS 退到后台时会为 app 切换器额外用另一种深浅色渲染一遍,
那是最可能造出这一帧的地方。

⚠️ **为什么只有这一颗露馅**:iOS 27 上外层轨道由**系统**画(`systemDrawsBarCapsule`),
系统的东西自己跟深浅色;而这颗选中胶囊是**我们画的**,所以只有它停在深色 —— 与截图完全一致。

**⚠️ 我在 iOS 26 上复现不出来(四条路径全试过)**:冷启动 / 切后台再回来 /
切系统深浅色 / **系统深色 + app 配色设成「浅色」**(最贴近用户当时的处境)——
全部正常。探针还确认:**26 上退到后台根本没有发生任何一次烘焙**,
这正好解释了为什么 26 上没有 —— 那一帧在 26 上不存在。

**修法(自愈,不是堵路)**:`NNWSoftPanel.install(in:)` 里订阅 `didBecomeActive`,
回前台时重画一次。不管那一帧怎么来的,回前台必重画。
⚠️ **挂在 `NNWSoftPanel` 这一层**,不是挂在某个宿主上 —— dock / 三档 / 选单 / 圆钮
共用同一个烘焙机制,病是共用的(L112)。

**⚠️ 第一版修法无效,用户复验后改过一次(2026-08-05 晚)**:
第一版写的是 `hostView?.setNeedsLayout()` —— 但真正调用 `capsuleMaterial.layout()` 的
是**外层三档控件**的 `layoutSubviews`,不是胶囊自己的;叫子视图重排不会触发父视图重排。
iOS 26 上碰巧看不出来(26 上外层轨道也是软面板,宿主就是外层控件,顺带把内层一起重画),
**27 上外层归系统画,那条链不存在** —— 我在 26 上"验过链路是活的",验的恰是 27 上没有的那条路。
改成 `layout(in:cornerRadius:)` **自己直接重画**(圆角记在 `lastCornerRadius`),
不再依赖"谁会调 layoutSubviews"。**仍要用户在 27 上验收:切出去再切回来,胶囊还变不变色。**

**验了什么(说清楚差别)**:复现不了就验不了"好了"。所以验的是**链路是活的** ——
埋探针,从主屏点图标回 app,日志里每一块面板(含那颗胶囊)都收到
`becomeActive → setNeedsLayout`,紧接着 `bake style=light`。
**"回前台会重新解析颜色"是量出来的。至于 27 上那个症状有没有消失,只能用户确认(L114)。**

⚠️ **验收话术(L117 的教训)**:要问用户「**原来那个症状还出不出现**」,
别等他报下一个症状。

完整教训见 **L119**。

---

## T46 · 深色下 dock 底板"有点过于透明" —— 🔵 **用户 2026-08-05 观察,未动**

用户原话:「在深色模式下,好像底部 docker 的底板,好像有点过于透明了,
但这只是我的观察,我美学素养不高,所以不一定对」。

**别急着调**:这一档的浓度是 `NNWSoftPanel.layout(in:)` 里那层白 ——
走原生玻璃时深色是 `0.03 / 0.06`。⚠️ **那个数字是唯一的旋钮**:
调高 = 更白更实但更不透,调低 = 更透但会跟着背景变灰(T40 第六轮记过)。

**和材质四版对比一起定案**(用户说材质"不着急",临时开关 `NNW_DOCK=v1/v2/v3` 还留着)。
⚠️ 动之前先在 **iOS 27 真机**上量一次:27 上 dock 的底是不是我们画的都要先确认
—— 文章页 dock 已搬出工具栏(归我们),但首页那条还在栏里(归系统)。

---

## T44 · 浮动 dock 的三项功能验证 —— ✅ **验完了,其中一项是真坏的,已修**(2026-08-05)

文章页 dock 搬出工具栏之后(`iOS/Article/NNWFloatingDock.swift`),上一轮**只看过静态截图、
没有实际操作验证**就交了。这一轮在 iOS 26 模拟器上逐项实测,浅色深色都截了图。

### ① 沉浸阅读:下滑藏栏 dock 跟着淡出 / 上滑回来 —— ❌ **本来是坏的,已修**

**症状**:下滑藏栏时 dock **纹丝不动**,一直浮在正文上。

**病根(探针实测,不是推断)—— 两个坑叠在一起**:

1. **「藏工具栏」在安全区上没有留下任何痕迹。**
   我们为了让 dock 浮起来,把工具栏抹成了透明底 + `toolbarItems = []`,
   于是**它对底部安全区的贡献是 0**。实测藏栏前后
   `ArticleViewController.view.safeAreaInsets.bottom` 都是 **96.0**
   (= 34 Home 条 + 62 我们给 dock 让的),**一个字节都没变**。
   钩子挂在 `viewSafeAreaInsetsDidChange` 上,而唯一那次回调是**导航栏**藏起来
   (顶部 inset 变了)带来的 —— 跟工具栏毫无关系。

2. **回调恰好插在上游那两句中间,所以工具栏的值一定是旧的。**
   上游 `hideBars()` / `showBars()` 都是「先 `setNavigationBarHidden`、后 `setToolbarHidden`」
   两句,而安全区回调在第一句里就同步发生了。实测两个方向都读到旧值:

   | | 回调时 navHidden | 回调时 toolbarHidden(旧值) |
   |---|---|---|
   | 下滑藏栏 | true | **false** ← 原代码读的就是它 |
   | 上滑现栏 | false | **true** |

   ⚠️ 我第一版只修了坑一(改成读导航栏 `||` 工具栏),结果**藏得下去、回不来** ——
   上滑那次读到工具栏的旧值 true,`||` 之下仍然判定该藏。**两个坑必须一起修。**

**修法**:`nnwSyncFloatingDockVisibility` 里**推迟一轮 runloop 再读**这两个状态,
等它们都落定;判据改成「导航栏藏 **或** 工具栏藏」。
另外「文内查找」(`beginFind`/`endFind`)那条路**只藏工具栏、不藏导航栏**,
安全区完全不动、回调根本不来,所以在那两处各**显式叫一次**本方法(一行)。

**改后实测(浅色 + 深色各走一遍)**:下滑 → `navHidden=true toolbarHidden=true` → dock 淡出 ✅;
上滑 → 两个都 false → dock 淡回 ✅。

### ② 翻页后 dock 还在最上层 —— ✅ **本来就是好的**

用 dock 上的「下一篇未读」和导航栏的「下一篇」各翻了一次,dock 都仍浮在最上层
(截图里能看到磨砂透出新页面的图片颜色)。装它的代码比 `pageViewController.view` 早
(文件头记着的那个坑)已经被 `nnwSyncFloatingDockVisibility` 里那句 `bringSubviewToFront` 兜住了。

### ③ 滚到最底,最后一行能露到 dock 之上 —— ✅ **够,而且有 14pt 余量**

⚠️ **不是目测,是让 app 自己把几何报出来的**(截图工具的图底部有裁切,量不得,L104 的老问题):

| 量 | 实测 |
|---|---|
| dock 实际高 | **44.0pt**(⚠️ 不是 `intrinsicContentSize.height` 的 54) |
| dock 底边距屏幕底 | **38.0pt**(= 34 Home 条 + 4 留白,与代码一致) |
| dock 顶边距屏幕底 | **82.0pt** |
| 栏显示时正文底部预留 | **96.0pt**(WKWebView 的 `adjustedContentInset.bottom`,实测) |

96 > 82,**余量 14pt**,最后一行不会被 dock 压住。

⚠️ 顺带查清的一件事,别当成 bug:**沉浸态下正文是通铺到底的**
(实测 WKWebView `adjustedContentInset.bottom = 0`)——
上游 `updateBottomSafeAreaForFullScreen()` 把安全区抵消掉了。
那一刻 dock 本来就藏着,不冲突。栏一回来,预留立刻恢复成 96。

⚠️ **余量为什么是 14 而不是 0**:让位用的是 `board.intrinsicContentSize.height`(54),
而实际布局出来是 44。多留 10pt 无害,**别为了"精确"去改小** —— 改小只会把余量吃掉。

### 顺手记下的一个观察(**没查,别当结论**)

进列表第一篇文章时,导航栏那对「上一篇 / 下一篇」箭头是**灰的(禁用)**,
翻到第二篇之后就正常了。判据来自上游 `coordinator.isPrevArticleAvailable` /
`isNextArticleAvailable`,**这一轮没有碰过那段代码**。要查的话从那两个属性入手。

---

## T40 · 视觉换血试验(软面板 / 橙色强调)—— 🟡 **进行中,全部在 `design/soft-dock` 分支**(2026-08-04)

### ⛑ 回滚点:tag `genesis` = `649f85fd5`
用户约定:**任何时候说「回滚到创世版本」,就 `git checkout genesis`**,
那是这一整轮设计试验开始之前的 main。main 至今未被这轮改动污染。

### 走过的三条路(前两条已废弃,别再捡起来)
1. **Soft Shell 全局换肤**(设计师方案,分支 `design/soft-shell`)——
   冷灰画布 + 全局令牌 + 页面外壳卡。用户看后**否决**并要求全部回滚:
   「订阅源抬头、文章抬头、正文卡片没必要」「几乎没什么变化」。已回滚,分支留档。
2. **全自绘 + 玻璃**(我的提案)——用户否:「玻璃感不明显」「控件绘制不够精美」。
3. ✅ **当前方向**:内容通铺、不加卡片;**只有 dock / 三档 / 选单是软面板**;
   图标全部手绘;橙色 `#FF5A1F` 作为强调色铺开。

### 已完成(分支上,已装机自查)
- `NNWSoftMaterial`:软面板材质。**色值相对于 app 自身底色推导**(见 L102),
  0.55pt 上缘 hairline、极淡宽阴影。一处开关 `isEnabled = false` 即整体退回。
- `NNWDockIcons`:13 个手绘图标(dock 7 个 + 首页 6 个),24pt 画布 / 2.1pt 圆头。
- 文章页 dock:软面板 + 手绘图标 + 加高到 54pt;系统工具栏底抹掉(离页还原)。
- 三档控件:整条软面板 + 当前档橙字 + 浮起胶囊。
- 首页:搜索/编辑/齿轮/加号、分组头与文件夹三角、星标 → 全部手绘 + 橙。

### 第二轮(2026-08-04 晚,用户提了两个问题后做的)

**用户的两个问题**:①「底栏中间那三个,一定要后面有个阴影吗?范例里也没有」
②「你新画的几个控件,玻璃的感觉都不强,能否和底栏中间那三个统一起来」

**问题 ① 的答案(逐像素量出来的,不是判断)**:
- 参考图**有**阴影,但极淡:贴边只比背景暗 **7–10 级**、扩散约 5pt、**上方完全没有**。
  那个量级人眼基本读不出,所以用户"看着没有"是对的。
- 我们屏幕上那圈灰**不是阴影参数太大,是套娃**:iOS 26 会给工具栏/导航栏的每一项
  自动垫一层系统玻璃胶囊,我们的软面板画在**它里面** → 两层胶囊 + 中间一道灰缝 + 两道阴影。
  实测(纸底 240):齿轮/加号(只有系统胶囊)下方暗 7 级;**三档(系统胶囊+软面板)暗 13 级**。
- **修法**:`UIBarButtonItem.hidesSharedBackground = true`(iOS 26 正规接口,**逐项属性,
  不动 `toolbarItems` 数组**,守卫和 IBOutlet 都不受影响)。拆完实测暗 **6 级**,回到参考图量级。

**问题 ② 的答案**:病根是 **L104** —— 上一轮采样只扫了上缘,把"亮边只在上缘"当成了事实,
于是下半圈画到 3% = 没有。重扫四条边发现**整圈都是 246–255**。整圈亮边才是玻璃感的来源。

**本轮改了什么**:
- `NNWSoftMaterial`:亮边改**整圈均匀**(0.8pt),阴影浓度/半径收到实测量级,
  新增「圆钮」烘焙(`roundButtonImage`)与半透明档(`translucent`)。
- 拆套娃:三档控件、文章页 dock、首页齿轮/加号/搜索/编辑,全部 `hidesSharedBackground`。
- 首页四个控件统一成**软面板圆钮**,直径 34 = 三档控件的高度,并排一样高。
  - **齿轮 / 加号**(底部工具栏):面板**烘焙进图片** —— 那两项只允许换 image
    (加号是 IBOutlet,上游还往它身上挂 menu,换 customView 会把菜单弄丢)。
  - **搜索 / 编辑**(导航栏,压在**头图**上):新文件 `NNWSoftGlassButton`,
    磨砂玻璃 + 同一圈亮边。⚠️ 先按不透明圆盘做过,截图一看是两张**贴纸** ——
    压在照片上必须真透。参考图没有这个场景,按 app 自身情况推导(L102 的精神)。
- 三个实现坑,见 **L105**(customView 不钉尺寸就点不动 / UIImageAsset 对 bar item 无效 /
  `.current` 的 displayScale 是 1)。

**已自查**(截图 + 采样):浅色底栏、深色底栏、深色 dock、头图上的两颗、
搜索能开、编辑能进编辑模式、加号能进搜索订阅源页。

### 第三轮(2026-08-04 深夜):所有选单 / 长按菜单改成 IMG_2442 那套

**用户拍板的两条**(我把代价讲清楚后他选的最彻底那档):
① 连系统长按菜单一起换成自绘;② 质感 + 结构都照搬 IMG_2442。

**已实现**:
- `NNWMenu` 卡片:底改成**和 dock / 三档同一套软面板材质**(整圈亮边 + 上暗下亮渐变);
  阴影从 0.22/24/8 收到 0.14/14/4;宽 250→268、圆角 22→26。
- **结构照搬**:顶部一排只有图标的快捷键 → 分隔线 → 文字行,且**文字在左、图标在右**。
- 长标题**允许折两行**(原来钉死一行,「将"Marginal Revolution"全部标为已读」会被截断 ——
  这是换掉系统菜单必须补上的,不是美化)。
- 新增 `NNWMenu+SystemBridge.swift`:**把任意 `UIMenu` 树转成我们的卡片**。
  接入的 6 处(订阅源行 / 文件夹行 / 智能源行 / 账户分组头 / 时间线行 / 正文页 / 简介页)
  **每处只改两三行**,落在「一行换一行」通道里。详见 **L106**。

**⚠️ 这套方案的地基(别推翻)**:`UIAction.handler` 是私有的、也没有公开的 `perform()`。
用 `UIControl.addAction(_:for:)` + `sendActions(for:)` 触发(公开 API,
`UIButton(primaryAction:)` 本来就这么工作)。**已实测**:长按订阅源 → 「拷贝 Feed 网址」→
`xcrun simctl pbpaste booted` 读回了正确的网址。

**顶部图标行放几个,由调用点点名**(不是自动的,理由见 L106 末尾):
订阅源 2(简介/主页)、时间线 2(已读/星标)、正文 2(上一篇/下一篇)、其余 0。

**已知代价(用户知情)**:丢掉系统长按的预览抬起动画、触感反馈、菜单自带的无障碍节奏、
与拖放手势的联动。功能项一个不少。
**不在射程内**:WKWebView 自己的**链接 / 选中文字**菜单(打开链接、拷贝、翻译那种)
仍是系统的 —— 那是 web view 的内建菜单,不走上游的 `UIMenu`,也不该由我们接管。

**回滚**:`NNWMenu.replacesSystemContextMenus = false` 一处即可退回系统菜单。

### 第四轮(2026-08-05):选单像素级对齐 IMG_2442 + 强调色做成可换的基础设施

**用户的反馈**:「玻璃质感完全没有、边缘也没有反光、字体大小不太对、黑色不够深不够通透」
+「某些 icon 希望做成橙色,或者把这个变成基础设施和设置项,之后一键换所有橙色的地方」。

**病根是比例尺**(见 **L107**):前两轮的 px→pt 换算用的 6.4 px/pt 是**猜的**。
这次用「菜单正文 = iOS 标准 17pt」当锚点重定为 **4.94 px/pt**,
并用「卡片宽 1255px ÷ 4.94 = 254pt = iOS 系统菜单标准宽度」交叉验证。

**改后逐项复量(参考图 vs 我们)**:

| 量 | 参考图 | 改前 | 改后 |
|---|---|---|---|
| 亮边宽 | 1.21pt | 0.8pt | **1.33pt** |
| 卡片填充 | 232 | 232 | **232** |
| 文字最暗 | 亮度 10 | 亮度 **40** | **13** |
| 卡片宽 | 254pt | 268pt | **252.7pt** |
| 圆角 | 34pt | 26pt | **34pt** |
| 行距 | 41.4pt | ≈46.5pt | **41.4pt** |
| 分隔线 | 1.2pt / 暗 21 级 | 0.33pt 极淡 | **1.2pt / 暗 21 级** |

**仍有差距的一项(记档,别当成做完了)**:行图标笔画。参考图是 **2.0pt**(占图标框 11.5%),
我们用 SF Symbols 顶到 `.semibold` 也只有约 8.5%(实测 1.3–1.7pt)。
要真到 11.5% 得像 dock 那样**整套手绘**(`NNWDockIcons` 就是 2.1pt 圆头)——
但选单图标来自上游 `UIAction.image`,手绘意味着给每个动作单独配一张图。**没做。**

**强调色基础设施**(用户自己提的方案,比手动改色号好):
- 新增 `NNWAccentPalette`:六个预设(橙/陶土红/靛蓝/森绿/梅紫/石墨),存 UserDefaults,
  换色发通知。`NNWSoftMaterial.accent` 只是转发 —— **别再往任何地方写死色号**。
- 设置 → 外观 → **「强调色」**(照「界面语言」那一行的既有做法程序化插入,不动 storyboard)。
- **已实测跟着变的**:首页搜索/编辑/齿轮/加号(烘焙图,靠通知重画)、分组头三角、
  三档控件的当前档、自绘选单顶部那排快捷图标。
- ⚠️ **暂时跟不上的**:`Assets.xcassets` 里的 `starColor` / `primaryAccentColor`
  是**静态资源**,编译后改不了 —— 智能组的星、文件夹行的三角与文件夹图标换色后仍是橙。
  要一起跟上得把 `Assets.Colors.star` 等引用改成走 `NNWAccentPalette`(iOS 侧只有 2 处引用,
  但 `Shared/Assets.swift` 会被扩展目标编译,**那里看不到 iOS 的类型**,见 L103)。**没做。**

### 第五轮(2026-08-05):中性玻璃画布 —— ⚠️ **等用户拍板**

**用户第三次说"玻璃质感不够"之后量出的真因**(完整记录见 **L108**):
参考图三张图里 **app 自身的表面是 #EAEAEA(精确中性 234)**,本 app 是 #F3F0EB(243,偏暖)。
白色是亮度天花板 → 画布越亮,纯白亮边越发不出光 →
我们只有参考图**一半**的边缘对比度(Δ10 vs Δ21)。**"玻璃"全靠那道边。**

**做了什么**:`AppAppearance.isGlassCanvas` 一处开关(现为 `true`),
把整套语义色从"暖纸"换成"中性玻璃";并把面板色差从 −9/−6 收回实测的 **−2/0**
(参考图的面板与画布几乎同色,层次全靠亮边,我们之前用色差硬撑,方向反了)。

**实测结果**:画布 #EAEAEA(与参考图逐像素相同)、面板 232(相同)、亮边对比度 **Δ10 → Δ18**。

✅ **用户 2026-08-05 看图后拍板:「全局换肤吧」。** 这推翻了他 2026-08-04 的否决 ——
理由是量化之后确认中性画布是玻璃质感的唯一入口。`isGlassCanvas = true` 保留。
(开关留着不删:`false` 即整体退回暖纸,其余改动全部不受影响。)

**拍板后收的尾**:
- ✅ **文章列表页控件统一**:右上角齿轮/放大镜 → 磨砂圆钮(和首页同一套,它们也压在头图上);
  底部「标记全部已读」「下一篇未读」→ 烘焙软面板圆钮。
  上游 `MainTimelineModernViewController.swift` 只加了一行调用。
  ⚠️ `markAllAsRead` 有两个重载,`#selector` 必须写类型标注才不歧义。
- ✅ 已自查:首页、文章列表页、正文页、设置页在中性画布下都正常。

### 第六轮(2026-08-05):真磨砂 —— 让内容从面板底下穿过去

**用户第四次说"没什么玻璃的感觉,是不是不够透明"。** 这次他的直觉一半对一半不对:

- ❌ **参考图里的面板是不透明的**(已验证):IMG_2441 的 dock 压在页面卡片圆角上,
  竖扫 dock 内部是一条平滑渐变(231→238),**没有任何内部台阶** —— 卡片那条边没透出来。
- ✅ **但"不够透明"在我们这里是对的**,原因参考图看不出来:
  参考图是静态样张,面板背后是**一片纯色** —— 纯色背后放什么都一样,
  所以设计师用"亮边 + 大圆角"去*暗示*玻璃就够了。
  而我们的 app 里 dock / 三档 / 选单**背后是真有内容的**,不透就等于把唯一能体现玻璃的地方盖死。

**做了什么**:`NNWSoftPanel` 支持真磨砂(`translucent: true`),用在
**文章页 dock、三档控件、自绘选单**上。实测:文章文字确实从三档控件底下穿过去了;
选单能透出头图的绿色和正文。

⚠️ **实现上唯一的坑**:`insertSublayer(at:0)` 加的是**裸图层**,永远排在**子视图**图层下面 ——
磨砂是子视图,会把 fill/rim 整个盖住。解法是半透明档把 fill/rim 挂到**磨砂自己的 contentView**,
顺序就天然正确了。

**追加(同日,用户说「不够白不够亮」)**:磨砂玻璃压在深色内容上会跟着变灰 ——
实测三档面板从不透明版的 233 掉到 **230 且随背景浮动**(参考图是恒定 232)。
修法:**在模糊之上再叠一层白**(苹果自己的 `*Material` 就是这么干的),
浓度 0.52/0.60,模糊改用更透的 `.systemUltraThinMaterial`(亮度既然归白色管,
模糊就可以更透、轮廓透得更清楚)。改后实测 **240–241**,比画布 234 还亮,且仍然透。
⚠️ **白的浓度是这里唯一的旋钮**:调高 = 更白更亮但更不透;调低 = 更透但会跟着背景变灰。

**追加二(同日,用户贴出参考图问「为什么看起来还是差了不少」)**:
又量出三处**尺寸**差(不是质感差),全部改掉:

| | 参考图实测 | 改前 | 改后 |
|---|---|---|---|
| 文字左 / 图标右内边距 | 167px = **33.8pt** | **18pt** | 34pt |
| 分隔线 | 左右各内缩 **33pt** | **通栏** | 内缩 34pt |
| 顶部图标行两端内缩 | ≈**46pt** | 8pt | 46pt |
| 行图标笔画 | 2.0pt(占比 11.6%) | semibold ≈8.5% | **bold ≈10.5%** |

**内边距差了一倍**,是"看着差不少"里最大的一块 —— 卡片一直是挤的。
定数收进 `NNWMenuMetrics`,以后调一处即可。

**追加三(同日,用户报「三个控件大小不一样」)—— 一个真缺陷,四颗圆钮全中**:
实测左圆钮 **21.7pt**、三档 **34pt**、右圆钮 **20.7pt**。
根因:**UIBarButtonItem 会把图片缩放到约 28pt 以内**,而余量也算进画布 ——
44pt 画布配 34pt 圆盘(占 77%),缩放后圆盘只剩 21.7pt。
修法:余量 5 → **1.5pt**(圆盘占 96%),同时把三档高度 34 → **28** 去对齐缩放后的实际直径。
改后实测 28.3 / 28.0 / 27.7pt,差距从 13pt 收到 **0.6pt**。
⚠️ **判据**:凡是走 `UIBarButtonItem.image` 的自绘图,**画布里的留白会被缩放放大成尺寸误差** ——
留白越大,实际画出来的东西越小。要精确控制尺寸只能走 customView(但那条路有 menu/isEnabled 的坑)。

**追加四(2026-08-05,用户要求改文件夹图标)—— 只做了 1/3,考古结论先记下,别重查**:

用户要三件事:① 匹配主题色 ② 有一点玻璃质感 ③ 照他给的参考重绘得漂亮简洁
(参考图是一个粗圆头描边的文件夹轮廓,形状基本就是 SF Symbol `folder`)。

**考古结论(下一个窗口直接用,别再查一遍)**:
- 这个图标**不是** `NNWDockIcons.folder()` —— 那个 2026-08-04 画了但**全仓没有任何调用**,是死代码。
- 真正的来路:上游的 `IconImage` → `NNWFeedIconStyle.styled(...)`,
  装配点在 `iOS/MainFeed/Collection View Cells/MainFeedCollectionViewFolderCell.swift`
  的 `iconImage` didSet 里(两行:设 `faviconView.iconImage`、设 `faviconView.tintColor`)。

**① 已做**:tint 从 `Assets.Colors.secondaryAccent`(xcassets 静态色板,换强调色跟不上)
改成 `NNWSoftMaterial.accent`(走调色板)。一行换一行。
⚠️ **只验了编译+装机,没验换色**(secondaryAccent 本身就是橙,静态截图看不出差别;
要验得进设置换个颜色再回首页看文件夹跟不跟着变)。

**②③ 未做**。建议做法:在 `NNWFeedIconStyle`(本 fork 的文件)里加一个
`folderTile(for traits:)`,画「软面板圆角方块 + 居中的 `folder` 字形(强调色)」——
尺寸对齐旁边订阅源的 favicon 方块,材质复用 `NNWSoftPanel`,这样"玻璃质感"和全 app 同源;
然后把上面那个 didSet 里的 `faviconView.iconImage = NNWFeedIconStyle.styled(iconImage)`
**一行换一行**换成它。⚠️ 动手前先确认 `IconImage` 的构造签名(没读过)。

**②③ 定案(2026-08-05,几轮往返后)**:
- ❌ **玻璃质感那版已回滚**(2f06dd2dd)。做过"软面板圆角方块 + 亮边 + 字形",
  用户否掉:「这个玻璃质感不好」「和上一次区别不大」。**别再往这个方向走。**
- ✅ **定案 = 纯图标重绘**:照用户给的参考图重画 `NNWDockIcons.folder()` ——
  机身宽高比 ≈1:0.83、左上抬高的页签占宽约 1/3、**斜边两端各一个小圆角**
  (参考图那两处是圆的;上一版是折线拼的、转角全尖,所以用户觉得"区别不大")、
  四角大圆角 2.4。装配点 `IconImage(NNWDockIcons.folder(), isSymbol: true)`。
- ✅ **随主题色变色**:输出**模板图**,颜色由宿主 tintColor 决定。
  ⚠️ **别改成烘焙好颜色的位图** —— 那样就跟不了主题色(用户明确要求能变色)。

⚠️ **踩了一次 L74**:`faviconView.tintColor` 在这个 cell 里有**两个写入点** ——
`iconImage` 的 didSet 一处、`updateVisuals` 的 default 分支一处(原写死
`Assets.Colors.primaryAccent`,xcassets 静态色板、偏陶土色)。
后者在前者**之后**跑,所以只改 didSet 时颜色纹丝不动。**两处都要改。**

**追加五(2026-08-05):阅读进度环接入调色板**。
用户:「能否把这个阅读进度条的颜色,也改成受设置里主题色的控制」。
位置:**文章页头像外面那一圈**(`iOS/Article/ArticleHeaderBar.swift` 的 `applyProgressRing`),
原来写死 `Assets.Colors.primaryAccent`(xcassets 静态色板)→ 改走 `NNWSoftMaterial.accent`。
⚠️ `CGColor` 不跟随深浅色,所以要 `resolvedColor(with: iconView.traitCollection)` 再取 —— 
这个方法每次滚动都跑,所以深浅色变了下一帧就带上新值。
⚠️ **只验了编译 + 装机,没有肉眼验证**:这圈环只在滚动"飞行"时可见,
要验得进文章页滚一下、并且换个强调色对比。**下个窗口补验。**

**追加六(2026-08-05):换主题色的三处修复 + 一条性能问题待查**

① **底栏圆钮换色后变纯色圆饼** —— 根因见 **L113**(合成函数读写同一个字段、不幂等,
   我给它加观察者之后就烂了)。改成从 `Assets.Images` 原图现合成。
② **星标 / 分组头三角不跟主题色** —— 两个不同的病:
   - **三角**:tint 写在 **IBOutlet 的 `didSet`** 里,那只在 nib 加载时跑一次,
     而分组头是**复用**的 → 换色后没人再走它。挪到 `disclosureExpanded` 的 didSet(每次配装都跑)。
   - **星标等侧栏图标**:`MainFeedCollectionViewCell` 两处
     `iconImage?.preferredColor ?? Assets.Colors.secondaryAccent` —— 静态色板,跟不上。
     改成直接用 `NNWSoftMaterial.accent`。
   ⚠️ **判据**:"换主题色跟不上"到目前为止只有两种原因 ——
   **(a) 用了 `Assets.Colors.*` 静态色板;(b) 上色写在只跑一次的地方**。查这两条就够。

③ 🔴 **未做:底部控件进/回页面时慢 0.5–1s 才出现**(用户 2026-08-05 报)。
   **没有排查,以下只是怀疑方向,别当结论**:
   - 底栏那条三档控件是在 `viewWillAppear → nnwUpdateReadingMode → nnwInstallModeBarIfNeeded`
     里装的,而不是 `viewDidLoad` —— 每次出现都要走一遍装配判断。
   - `nnwRestyleToolbarIcons` / `nnwRestyleTimelineToolbarIcons` **每次调用都现画位图**
     (`UIGraphicsImageRenderer`,两颗圆钮 × 每次 viewWillAppear),没有缓存。
   - ★ 档还要等 `NNWStarredIndex` 异步查库(L53),开机停在★档时必然有延迟。
   **建议排查方式**:按 L94 的老规矩**埋日志**打时间戳(viewWillAppear / 装配完成 /
   图片画完各一条),先证明时间花在哪一段,别猜。

**追加七(2026-08-05):边缘渐隐 —— 被"拍平工具栏"一起拿掉的东西**

用户:「现在是毛玻璃效果,但是没有边缘的渐变,这个部分坏了」「创世版里这个地方是好的」。

**原因**:iOS 26 起滚动视图与栏的交界处有系统画的渐隐(`UIScrollEdgeEffect`)——
内容滚到栏附近柔和淡出。创世版有,是因为那时工具栏用的是**系统自带的底**。
而我们为了让 dock 浮起来,调 `configureWithTransparentBackground()` 把栏拍平,
**那道渐隐是随着栏的底一起被拍掉的**。

**修法**:不把栏的底装回去(套娃会回来),而是直接向滚动视图要 ——
新增 `UIScrollView.nnwEnableSoftBottomEdgeFade()`,已接三处:
首页(`nnwUpdateReadingMode`)、文章列表页(`TimelineFeedHeader`)、
**正文页(`WebViewController.nnwUseUIKitPaperBackground`,那是唯一够得到那个
`WKWebView` 的地方 —— `webView` 是 WebViewController private,外面拿不到,
第一次就是插错到 ArticleViewController 的**导航栏**那段才编译不过的)。

🔴 **2026-08-05 结论:这个修法不管用,用户实测仍然没有渐隐。**

---

### ✅ **2026-08-05 深夜:查清并修好了。上面那整套因果判断是错的,已证伪。**

**结论先写**:病根是 **`UIBarButtonItem.hidesSharedBackground = true`**
(第二轮为「拆套娃」加的那一句),**和"拍平工具栏"毫无关系**。
而且**症状的描述本身也是错的** —— 不是"渐隐丢了",是
**内容压根没滚到栏底下去**:iOS 27 上拆掉工具栏的共享背景,
系统就不再让内容穿过栏底,于是交界处是硬切断、栏那块是一条不透明黑带。
**栏底下没有内容,自然没有任何东西可以渐隐。**
用户之前报的「底栏变成了遮罩」和这次报的「没有渐变」,是**同一个病的两个症状**。

**怎么查出来的**(方法本身值得复用,见 **L115**):
`xcrun devicectl` 可以给有线连着的真机装 app、**用环境变量启动**、读控制台、
**把文件从 app 容器拉回电脑**。于是让 **app 自己给自己截图、就地采样、把数字打进日志**,
把在 iOS 26 上做的隔离实验**原样搬到用户的 iOS 27 真机上跑**。
⚠️ 仪器先在 26 上校准过(130 个采样点,与 Mac 侧直接截图量的平均差 0.12 级)才敢用。

**真机 iOS 27 实测(同一屏、同一滚动位置,内容→底栏交界处每行平均亮度)**:

| 条件 | 交界处 | 判定 |
|---|---|---|
| 现状 | 214 → **55 → 33**(4 像素掉到底) | ❌ 硬切断 |
| 不拍平底栏(`noflat`) | 与现状**逐字节完全相同** | ❌ 与拍平无关 |
| 只跳过软面板绘制(`nopanel`) | 214 → 55 → 33 | ❌ 不是它 |
| **不设 `hidesSharedBackground`** | 214 → **195 → 225** | ✅ **就是它** |
| 整套软面板停用(`off`) | 214 → 196 → 230 | ✅(用户肉眼也说这次最接近对) |

**已实测排除、别再走的两条**(都有日志确认代码执行了,画面逐行不变):
- `UIScrollView.bottomEdgeEffect`(style / isHidden):**对 WKWebView 完全无效** ——
  反向强制 `isHidden = true` 并回读确认写入,画面**逐像素不变**。
- `UIScrollEdgeElementContainerInteraction`:**无效**。上一轮 NEXT-PROMPT 指定的
  "下一步该试的"就是它,已挂上(有日志),画面**逐行完全不变**。

**修法**:`nnwHideSystemGlassCapsule()` **按系统版本分道** ——
iOS 27+ 直接 return(系统本来就只画一层,不需要拆);iOS 26 照旧拆(不拆就是套娃)。
**两个版本要的正好相反,没有统一写法**,理由与证据见 **L116**。

**已验**:
- iOS 27 真机:交界处 55/33 → **195/211/225**,内容穿过去了 ✅
- iOS 26 模拟器:改前改后**逐像素零差异**(171 行全部相同),浅色深色都截图确认无套娃 ✅

⚠️ **一处我验不了、需要用户看的**:这个函数**同时**被首页(齿轮/加号/搜索/编辑)、
文章列表页、三档控件用。文章页 dock 是一整块 customView,系统胶囊和它重合,不显套娃;
但**首页那几颗圆钮是烘焙进图片的小图标**,iOS 27 上系统胶囊套在外面**可能**露出双层。
我没法自己导航到那些页面截图,**要用户在 27 上看一眼**。

⚠️ (原记录)正文页那处只验了编译 + 装机,没有肉眼确认(截图时没导航到正文页,几次都截到空白,
而当时上下文已耗尽)。**下个窗口第一件事:打开一篇文章滚一下,确认底栏上方有渐隐。**
⚠️ 全部三处在 **iOS 27 上都未验证** —— 开发机 SDK/模拟器都是 26(同 L94/L95 的处境)。

⚠️ **判据(已记 L114 一族)**:**"让某个系统外壳消失"时,先想清楚那层外壳还顺带提供了什么。**
这次拍平工具栏,顺带丢掉的就是这道渐隐。

**遗留的不一致(下一步)**:首页与文章列表页那**四颗圆钮**(齿轮/加号/标记已读/下一篇未读)
仍是**不透明**的 —— 它们的面板是**烘焙进图片**的,而图片里装不下磨砂。
要它们也透,得改成 customView,但加号身上挂着上游的 `menu`、
标记已读是 IBOutlet 且上游在写 `isEnabled` —— 换掉会**静默**丢功能,风险高。
现在屏幕上的表现:中间那条透、两侧圆钮不透,并排能看出来。

**仍未做(按建议顺序)**:
1. **静态色板没接进调色板**:`Assets.Colors.primaryAccent` / `.star` 仍读 xcassets,
   所以换强调色时智能组的星、文件夹三角跟不上。
   ⚠️ **试过一条捷径但失败了,记下来别再走**:想把 `NNWAccentPalette` 搬到
   `Shared/DesignKit/`(好让 `Shared/Assets.swift` 一处接入,替代改 10 个调用点)——
   **新建的 `Shared/DesignKit/` 没有被编译进来**(报 "cannot find NNWAccentPalette in scope"),
   已还原。要做得先查清这个工程的同步文件夹为什么不认这个新目录。
2. **头图与中性底的关系**没调过(浮世绘是暖褐,底是中性灰,交界处略冲)。
3. 深色模式在新画布下**只验过 dock,没逐页看**。

### 已知未做 / 待定
- **文章列表页(时间线)的控件没统一**:那一页右上角的齿轮/放大镜、底部的
  「标记已读」「下一篇未读」还是系统玻璃胶囊 + 系统图标,和首页那四颗对不上
  (2026-08-04 做菜单时截图发现的)。修法和首页完全一样
  (`roundButtonImage` + `hidesSharedBackground`),**没做,等用户点头**。
- **文章列表页(时间线)的行**没动:仍是原版式(favicon + 源名 + 时间 + 标题 + 摘要 + 缩略图)。
  设计稿里那版是「橙色时间 → 粗标题 → 灰摘要」,要不要做没定。
- ~~**深色模式没验过**~~ → 2026-08-04 二轮已验:底栏三件套 + 文章页 dock 深色都对。
  (途中抓到一个真 bug:烘焙图不跟随深浅色,见 L105 第 2 条。)其余页面的深色仍没逐页看过。
- ~~**选单 / 长按菜单的质感**~~ → 2026-08-04 已做,见下面「第三轮」。
- **正文页**除 dock 外没动(标题、正文排版、阅读栏都是原样)。
- 设置页、发现页、试读页等**其余页面的控件没有重绘**,还是系统图标。
- 橙色的**覆盖面**是用户当轮拍板"都变成橙色的";若日后觉得过了,
  收敛的成本很低(图标是模板图,改 tintColor 一处)。

---

## T39 · 三件小修:按文章记忆刷满 / 搜索页中文标题 / 翻译中可取消 —— ✅ **已验收**(2026-07-30)

1. **「按文章记忆」被刷满(交接文档点名的现成 bug)**:源已开「始终使用阅读视图」的
   **不再记单篇**阅读模式(WebViewController.nnwRecordAndAutoRestoreOnDidFinish 加一条 guard)。
   全局开关已决定每次进阅读视图,单篇记忆纯冗余,却每篇占一条 500 上限的名额。
   代价(已记档):这种源里提取失败的文章,下次打开会再试一次提取。存量脏条目不清,LRU 自然淘汰。
2. **全局搜索页显示中文标题**(T34 补账):结果行走 displayArticle 同一替换入口 +
   监听译文通知刷新 —— 至此所有列表(时间线/智能组/试读/搜索)标题翻译全覆盖。
3. **「翻译中再点一下 = 取消」修通**:toggle() 早就支持取消,但按钮 .working 态禁了交互,
   那条路一直点不到。改为转圈期间保持可点;无障碍标签同步改为"正在翻译,点一下取消"。
   主阅读页和试读页共用按钮类,一处修两处生效。

**验收点**:① 给某个源开「始终使用阅读视图」→ 连开几篇它的文章 → 别的源的
"上次开过阅读模式"记忆不再被挤掉(间接验:老文章的阅读模式记忆还在);
② 全局搜索(首页放大镜)→ 搜到已开标题翻译的源的文章 → 标题显示中文;
③ 点翻译 → 转圈期间再点一下 → 应立刻取消并回到原文(不再干等)。

---

## T38 · 翻译按钮图标换代(系统 translate 符号 + 四态角标)—— ✅ **已验收**(2026-07-30)

**多轮用户驱动的选型,过程记在 NNWTranslateIcon.swift 头注释,别推倒重来**:
单气泡「字」→ 自绘双气泡(嫌丑)→ **系统 SF「translate」符号(定案)**,字号 **14.5pt**
(17 显大 → 15 略大 → 14 太小;宽字形同字号显大,字号归 TranslationButton 管,控件板不覆盖)。

**状态表达(设计稿确认后定案)**:图标本体四态不变,**右下角小角标**表达状态 ——
已翻译=小勾、完整缓存=实心小圆、未完成缓存=空心小圆。角标**合成进图标位图**
(NNWTranslateIcon.compose),晕圈用 destinationOut 挖成真透明(毛玻璃上也成立),
整体模板图随 tint/深浅色;按钮不再有任何附件视图(淡色底衬、右上角圆点均退役)。

---

## T37 · 文章页点头像 → 进这个源的**完整**文章列表页 —— ✅ **已验收**(2026-07-30)

**需求演化(两版,拍板过程要记住)**:
- 第一版:push 试读列表页(独立副本,右划可一层层原路返回)。用户看过截图后不满意:
  要的是**那个有头图/搜索/底部三档的真页面**,不要"临时缩减版"。
- 关键取舍(已向用户讲透,用户拍板"真页面"):完整列表页全 app **只有一个实例**,
  内容全挂在协调器身上 —— 往导航栈放两份会共享同一全局状态,回退必然显示错误内容;
  拆成可多开 = 重写核心页面,merge/回归风险不可接受。
  **完整功能=单实例(不可叠);无限回退=独立副本(功能装不齐)。二选一,用户选前者。**

**最终实现**:
- 阅读栏头像点击 → `coordinator.discloseFeed(feed, animations: [.scroll, .navigation])`
  (点通知跳源的同一条路):首页选中该源、时间线切过去、文章页收场。
  右划回的是首页(上下文切换,原阅读上下文不保留 —— 用户知情并接受)。
  点**标题**保持原行为(开原文网页);文章无源的理论边角,头像保持老行为(开源主页)。
- **试读列表页的升级保留**(发现页语境受益):右上角齿轮(已订阅的源,formSheet 弹
  简介/设置页)、订阅按钮只在有处理方时出现(init 的 handler 已可选)、行标题走
  displayArticle(缓存命中显示中文)+ 监听译文通知刷新、viewWillAppear 收工具栏。

**验收点**:① 从智能组(如「今天」)进一篇文章 → 点**头像** → 应跳到该源的**完整**列表页
(头图/搜索/齿轮/底部三档俱全);② 文章页点**标题**照旧开原文网页;
③ 回归:发现页 → 试读 → 订阅,订阅完右上角出现齿轮;齿轮能开设置页。

---

## T36 · 三件收尾:标题预翻译 + 试读↔订阅缓存打通 + 编辑模式新建文件夹(删旧页)—— ✅ **已验收**(2026-07-30,commit 451f64072)

**① 标题预翻译(T34 尾巴)**:监听 `.AccountDidDownloadArticles`(通知自带新文章集合,
零数据库查询),属于已开启源、没缓存、非中文的标题直接入队(既有攒批管线)。
单次上限 50 条、新的优先(首次订阅/重订老源会带回全部历史,别把永远看不到的也翻了)。
⚠️ 单例懒加载,观察者要在启动刷新前就位 —— AppDelegate 启动时摸一下(带 [翻译] 标记的一行)。

**② 试读↔订阅译文缓存打通(T35 尾巴)**:
- 正文译文缓存键**去掉账户前缀**(articleID 本身含 feedID+唯一号哈希)。
  本地和 iCloud 账户 feedID 都是 feed 网址(审查核实 CloudKitAccountDelegate)→
  **试读时翻的,订阅后照样秒开**;Feedbin 这类同步服务的 ID 形式不同,对不上就重翻,不算坏。
- **老键的存量缓存不作废**:读的时候兜底查一次老键(lookupCache 帮手),写入一律新键。
- 试读文章身份映射:已订阅的源用真实 accountID/feedID 造内存文章 →
  标题翻译开关/缓存直接命中、article.feed 能解析出真源(源名图标都对)。
- ArticleReadingStateStore(阅读模式/翻译状态记忆)的键**保持带账户前缀**,没动。

**③ 编辑模式补「新建文件夹」+ 删除旧「编辑订阅」页(T29 尾巴)**:
- 编辑模式工具栏:[新建文件夹 | 移动到… | 删除]。多账户先弹品牌选单选账户,再输名字
  (流程照抄旧页);成功后靠 ChildrenDidChange 自动刷新。
- **删除 FolderManagerViewController.swift(1922 行)**:它 2026-07-28 起已无入口(死代码),
  全部功能(新建/改名/批量删移/拖放/删文件夹释放源)编辑模式都有。DropZoneResolver 保留(拖放在用)。

**独立审查:0 必修 + 3 建议,均已修**:懒加载单例错过启动刷新(AppDelegate 摸一下)、
预翻译加单次上限 + 注释改诚实、iCloud 注释事实错误(其实能对上,对不上的是 Feedbin 类)。

**验收点**:
① 预翻译:给某源开着「标题翻译成中文」→ 下拉刷新全部 → 等半分钟 → 进那个源,
   新文章标题**直接就是中文**(不再有"先原文几秒");
② 缓存打通:发现页试读一个**没订阅**的源 → 试读文章页点翻译 → 翻完 → 退回订阅它 →
   从首页进这个源打开同一篇 → 翻译按钮带实心角标、点了秒开(零请求);
③ 新建文件夹:首页铅笔进编辑模式 → 工具栏左下「新建文件夹」→ 选账户(若多账户)→
   输名字 → 列表里出现新文件夹;老的「编辑订阅」页已删,任何地方都进不去了(本来也没入口);
④ 回归:编辑模式的移动/删除/拖放照旧;主阅读页翻译秒开(老缓存兜底)照旧。

---

## T35 · 试读 Phase C:试读文章页的翻译 / 阅读模式 / 长图 —— ✅ **已验收**(2026-07-30,commit 620f1007b,含主阅读页回归实测)

**核心手法:协议抽取,主阅读页零行为变化(独立审查逐行 diff 确认"搬家原样")**
- 新增 `Shared/Translation/NNWArticlePageHost.swift`:协议(约束 UIViewController,
  三个要求:nnwHostArticle / nnwHostWebView / nnwTranslationTitleDidChange)。
  原 WebViewController.swift 尾部 ~200 行翻译 JS 桥接 + 长图快照钩子**原样搬**成协议扩展。
- WebViewController.swift 删 ~200 行、只剩 6 行 conformance(**比开工前更接近上游**);
  TranslationController / ArticleLongImageExporter 的入参放宽成协议。
- 试读文章页 conform 协议后,三件功能全是现成组件:TranslationController(按钮+整套
  流程原实例)、ArticleExtractorButton + ReaderViewExtractor(状态机照抄主页)、
  ArticleLongImageExporter + NNWProgressCard + LongImagePreviewViewController。
  底部工具栏:翻译 ‖ 阅读模式 ‖ 长图(进页亮出、离页收起)。

**独立审查:1 必修 + 3 建议,均已修**:
1.【必修】阅读模式按钮初始无图标(buttonState 默认 .off,didSet 有"没变不动"守卫)
   → 显式 setImage + tintColor(.label,主页同色)。
2. 转圈中取消不可达(.animated 会禁点)→ 试读页单独恢复交互,"再点=取消"走得通。
3. pop 页面不掐在飞的翻译 → willMove(toParent: nil) 里 resetForNewArticle
   (取消并存断点缓存)。
4. 切阅读模式重载页面时先掐翻译(rerender() 统一入口),别让译文往新 DOM 上白贴。

**已知事实/限制(有意,记档)**:
- 试读页与主阅读页**互不共享**正文译文缓存(缓存键含 accountID,试读固定 "nnw-preview")
  —— 同一篇文章订阅前后各翻一次。标题翻译的缓存同理不命中(render 里有诚实注释)。
- 不做「长按翻译键强制重翻」「按源自动进阅读视图」(试读的源没有 Feed 对象)。
- 翻译按钮 .working 态禁点导致"翻译中再点=取消"两页都走不到 —— **上游同款既有问题**,
  不属本次;要修应两页一起(TranslationButton.applyDisplayState 的 .working 分支)。

**验收点**:① 发现页 → 点行试读 → 点一篇文章 → 底部有工具栏(翻译/阅读模式/长图);
② 点翻译 → 正文分块变中文(和主阅读页同一套观感);退出再进同一篇 → 翻译按钮带角标,
  点了秒开(断点/完整缓存都认);
③ 点阅读模式 → 转圈 → 换成提取的全文;再点切回;
④ 点长图 → 转圈卡片 → 全屏预览,能保存/分享;
⑤ 回归(重要):**主阅读页**的翻译、长图、阅读模式全部照旧 —— 这轮把它们的地基
  搬了家,虽然审查确认零回归,还是请把主阅读页三件功能各点一遍。

---

## T34 · 标题 AI 翻译(按源开关)+ 简介页翻译修缮 + 齿轮入口 —— ✅ **已验收**(2026-07-30,commit 9068e3ad6)

**用户拍板的三条**:模型和正文同款;v1 不保留原文小字;时间线入口=头图小按钮。

**① 简介页的英文修缮**:那页 storyboard 文字其实早翻好了,英文来自上游代码在运行时
用 `feed.notificationDisplayName`(Account 模块的 NSLocalizedString,键不在任何翻译目录)
覆盖「新文章通知」那行。修法:把 "Notify about new articles"/"posts" 两个键手工补进
`Shared/Localizable.xcstrings`(照 inject.py 的字节级格式,已验证 round-trip)+ 登记进
`i18n/zh-Hans.json`。**代码零改动**。审查确认:SPM 模块里不带 bundle 的 NSLocalizedString
查的是 Bundle.main,补主目录恰好生效。

**② 标题翻译链路(全部 fork 文件 + 上游 4 处带 [翻译] 标记的小改)**:
- `NNWTitleTranslationStore.swift`:按源开关(UserDefaults,键=**账户ID|源ID**,吸取
  FeedOrderStore 没带前缀的教训)+ 译文磁盘缓存(Caches,键=代号|文章ID|模型|标题哈希,上限 4000)
- `NNWTitleTranslationController.swift`:时间线每行装配时 `displayArticle(for:)` ——
  缓存命中给换标题的内存副本;没命中记入待翻名单,0.4s 攒批(≤40 条)一次请求;
  翻完发 `.nnwTitleTranslationDidUpdate`,时间线刷可见行。智能源天然覆盖(按文章所属源判断)。
  假名判日文不翻;空译文不入库;整批失败→剩余全部记入失败名单并停发(防断网连环超时);
  失败名单在**切源/回前台/改配置**时清空重试。
- 简介页开关行:**不动 storyboard**,程序化插在第 0 区末尾(行号写死 3,上游加行会在
  merge 时撞标记行,不会静默错位)。上游 VC 的 numberOfRows/cellForRow +1 行拦截,
  **新增 heightForRowAt/indentationLevelForRowAt 两个 override**(静态表拿不存在的行号
  问 super 会崩,四处必须拦全 —— 审查逐方法核过,didSelect/contextMenu 等都安全)。
- 时间线上游改动仅 2 处:configure 里一行换 displayArticle;viewDidLoad 加一个观察者
  (处理方法在 +TitleTranslation 扩展文件里,走上游 queueReloadAvailableCells 同一套刷新)。

**③ 头图「源信息」按钮**:标题浮层(TimelineHeaderOverlayView,fork 文件)加 info.circle
按钮,守左下角(标题停右下),随滚动淡出,颜色跟标题走;浮层从"完全不可点"改为
"hitTest 只认按钮、其余全穿透"(审查确认穿透性与原来等价)。只有真订阅源的页面显示;
点它 → `coordinator.showFeedInspector()`(上游现成无参版,自己从 timelineFeed 取源)。

**独立审查:1 必修 + 5 建议,全部修掉**:失败名单永不清空(必修)、配好 key 后屏上不自动重翻、
enqueue 热路径同步读 Keychain、断网连环批请求、日文汉字标题误判、空译文入缓存。

**已知限制(接受,记档)**:纯汉字的日文标题(无假名)仍会被当成中文不翻;
全局搜索页(NNWGlobalSearchViewController)的结果行 v1 不换中文标题;
「刷新完成后后台预翻」没做(现在是进列表现翻,首次看到原文 2~5 秒后原地变中文)。

**首轮验收(2026-07-29 深夜)已过**,用户追加四条,均已实现(待复验):
1. **开关行左端对齐**:初版用 textLabel(边距+0),storyboard 的标签是**边距+4pt** ——
   重写成自建标签逐项照抄 storyboard 度量(边距+4、body 动态字体、开关尾部 20、行高≈51)。
2. **简介页套暖纸风**:viewDidLoad 一行 applyPaperStyle(to: tableView) + 新增 willDisplay
   override 逐 cell 刷(设置页同款配方)。开关颜色两页本就都是系统默认绿,统一后无差别。
3. **入口从头图 info 圆钮改为导航栏齿轮**:头图按钮整套代码已撤干净(浮层还原为
   isUserInteractionEnabled=false 纯装饰);resetUI 那行改为 rightBarButtonItems =
   [放大镜, 齿轮](齿轮只在 timelineFeed is Feed 时出现,点它 showFeedInspector())。
4. **文章内容页显示已翻译的标题 + 正文翻译跳过标题**:
   - 控制器新增 `cachedTranslatedTitle(for:)` / `cachedDisplayArticle(for:)`(只查缓存不入队);
   - `ArticleHeaderBar.applyContent` 的 baseTitle 优先用缓存译文(正文翻译的 titleOverride
     仍然能盖它,互不打架);`WebViewController.renderPage` 开头一行局部遮蔽 article
     (沉浸模式下网页里的标题也是中文);
   - `TranslationController` 标题分支插一档:列表已翻过 → 转义后直接 apply,**零请求**
     (顺序:正文翻译自己的缓存 > 列表标题缓存 > 现翻)。

**复验点**:① 简介页:开关行左端和上面几行对齐、整页暖纸底色(和设置页一个风格);
② 单源列表右上角 = 放大镜 + 齿轮,齿轮打开简介页;智能组/文件夹只有放大镜;头图上不再有圆钮;
③ 开了标题翻译的源,从任何列表点进一篇已翻标题的文章 → 顶部阅读栏直接是中文标题;
④ 在这篇文章里点「翻译」→ 标题瞬间就位(不再发标题请求),正文照常分块翻。

---

## T33 · 发现页试读 Phase B —— ✅ **已验收**(2026-07-29 晚,commit ec28e50c2)

**做了什么**:点结果行不再是订阅,而是 push 进「试读」页 —— 当场抓 feed →
上游解析器(RSParser)→ **凭空构造内存 Article(不落库、不碰账户,accountID="nnw-preview")**
→ 列表展示(标题/日期/摘要走上游 ArticleStringFormatter,和正式时间线同一套截断规则);
右上角「订阅」按钮(订到发现页选中的文件夹);点行进基础版文章页
(上游 ArticleRenderer 渲染 + 主阅读页同款 WKWebView 配置,链接点击一律外开)。

**新增 2 文件**:`Shared/Discovery/FeedPreviewViewController.swift`(试读列表 +
FeedPreviewSubscriptionHandling 协议)、`FeedPreviewArticleViewController.swift`(基础文章页 +
源头像 scheme handler)。发现页改动:点行行为、subscribe 加 completion(带 Error?)、
viewWillAppear 刷新、已订阅行恢复可点(点行=试读了,"已订阅"只由绿勾和淡字表达)。

**交付前独立审查抓到 2 必修 + 5 建议,已修 5 条**:
1. 【必修】completion 在 subscribingURLs 清理**之前**调 → 试读页订阅按钮永远转圈。
   修法:清理挪到 completion 前面。回调时序假设,L66/L93 同形状。
2. 【必修】注入的 nnw_appearance.js 无条件给 <html> 打 `nnw-reading-bar` 类,
   把模板的大标题/日期/源头衔全 display:none(主阅读页有原生阅读栏画回来,试读页没有)。
   修法:试读页追加一个 user script,在它的**两个打类点之后**各摘一次类
   (document start 靠注入顺序、DOMContentLoaded 靠监听器注册顺序,都有保证)。
3. 【建议→已修】试读页发起的订阅失败时,错误改由试读页自己弹(发现页被盖着,弹了看不见)。
4. 【建议→已修】试读列表 cell 走复用(上限 100 行)。
5. 【建议→已修】下拉刷新失败且已有内容时,不再往表格背景垫提示字。

**2 条已知限制(接受)**:发现页在飞的订阅完成时,已打开的试读页按钮要等 viewWillAppear 才刷新;
源头像若内存缓存被清(退后台/低内存)后进文章页,那一次会缺头像(不重取)。

**验收中的一个插曲(已结案,非代码问题)**:用户首测报"试读页里订阅全部失败(-1005)",
日志钉死是 **Mac 本机代理对新建连接回 503**(模拟器继承系统代理),同一时段全量刷新也在全灭;
用户关掉代理后一切正常。完整教训见 **L99**。

**验收点**:① 搜到源 → 点行 → 进试读列表(标题/日期/摘要),下拉可刷新;
② 点一篇 → 文章页**有大标题/日期/源名**、正文正常、点链接外开、右上角 safari 按钮好使;
③ 试读页右上角「订阅」→ 转圈 → 变「已订阅」;返回结果页该行是绿勾;首页出现新源;
④ 结果页点行尾 ⊕ 订阅、点绿勾取消订阅 —— 行为和 Phase A 验收时一致(回归);
⑤ 搜索框激活状态下点行进试读、退回来,搜索词和结果还在(L97 家族的回归检查)。

---

## T30 · 发现页(搜索订阅源)的两个问题 —— ✅ **已验收**(2026-07-29 用户实测确认)

**问题**:① 搜出结果之后,「订阅到」的文件夹选不了;② 那个源"订阅不了"。

**根因(两个是同一个)**:`UISearchController` 一激活就是一次真实的 presentation,
于是这一页再想弹任何东西都会被 `NNWMenu.show` 开头那句
`guard host.presentedViewController == nil else { return }` 静默吃掉 ——
选文件夹的卡片弹不出来,**"已经订阅过了"的提示也弹不出来**(所以看着像"订阅不了")。
完整分析见 **L97**。

**那个源本身没问题**(已实测):HTTP 200(4 种 UA 全通)、合法 RSS 20 条、
app 自己的解析器 `canParse: true` —— 它只是**早就订阅过了**。

**改了三处**:
1. `NNWMenu.show` 改为顺着 presented 链走到**最顶上那一层**再弹;
   防连点的守卫收窄成"只挡另一张选单"。**全 app 16 个调用点受益**。
2. 发现页「已订阅」改为**当场问账户**(不再靠只记本次会话的缓存):
   行尾绿勾 + 文字调淡 + **点不动**(用户要求:不要点了才提示)。
3. 搜索框位置:曾换成普通 `UISearchBar` 放表头(为了消掉那一层),
   但**用户要底部那个位置**,而位置和那一层是同一个东西的两面 —— 用户拍板要位置,
   已换回 `UISearchController`。

⚠️ **待验证**:"最顶上那一层再弹"这个修法**没有被实测确认过**。
`NNWMenu.show` 里埋了一行日志(记"从第几层弹的、弹出者是谁"),
若用户实测仍弹不出来,**读那行日志即可定位**,别再猜。
退路两条:自己在底部摆一个搜索框、或接受搜索框放表头(顶部)。

---

## T28 · 全局搜索:退出后回到"点搜索前所在的位置" —— ✅ **已解决(方案 D),用户已验收主流程**

**状态(2026-07-28 晚)**:按用户首选的 modal 方向重做完成(方案 D),用户实测确认修好。
日志实证:「点取消」「点结果」两条路径关页后,导航栈都稳定停在 `1 页:首页`;
点结果那条还确认了"目标文章在当前列表里=true",跳转没有静默失败。

⚠️ **中途撞过一个坑,修法已钉死在代码注释里,别去"简化"它**:
`closePage()` 里必须**先**把 `presentingViewController` 取到局部变量,**再** `isActive = false`,
最后用局部变量发 dismiss。顺序反了会停在一张文章空白页上(完整分析见 **L93** 第二段)。

**顺带解决的老账:范围条(该列表/全部文章)**——用户验收时报"从上一次验收开始就没了"。
考古发现 `a92a012cc` 那次只修好了**全局搜索**那条路,「进到源里点放大镜」这条路一直是坏的。
四轮定案(完整过程见 **L94**),两条硬事实:
1. **手动设 `showsScopeBar` 会把系统的自动档关掉** —— 那句"打开范围条"的代码正是它不显示的原因。
   现在改成 `scopeBarActivation = .onSearchActivation`,一行都不碰 `showsScopeBar`。
2. **iOS 26 的 `.integratedButton` 摆法根本不渲染范围条**(日志实证:状态全对就是不画),
   而摆法**必须在 `willPresent`(排版之前)切成 `.stacked`** —— 之前一直在 `didPresent` 切,来不及。

用户已验收:范围条正常显示。

**⚠️ 但范围条带来了一个真机回归(2026-07-28 晚,已修待验)**:
让搜索期间切摆法 = **让导航栏会变高变矮**,下游两处没跟上,用户**真机 iOS 27 beta 4**
退出搜索后顶栏乱掉(搜索框+放大镜并存、自绘标题跑到右上角压住头图)。模拟器 iOS 26 不复现。
两个病灶都已修(完整分析见 **L95**):
1. 摆法恢复原来挂在 `willDismiss` + `async` 一拍(猜时机),改挂 `didDismissSearchController`
   (系统的"已收完"承诺),并给恢复方向补上和进入方向同款的 `isActive` 守卫。
2. 时间线的头图/自绘标题**没挂 `safeAreaInsetsDidChange`**(文章页的 `ArticleHeaderBar` 早就挂了),
   导航栏高度一变没人重算 → 标题停在"飞到一半"的进度上。已给两个自绘视图补上。

那两处修完**真机仍然乱**,于是按上面写的备选方案改走"不切摆法"那条路(见下)。

**最终方案(2026-07-28 收尾,第五轮):文章列表页不再挂系统的 UISearchController**

- 右上角放大镜改成**我们自己的按钮**,点它打开那个已经好用的 modal 搜索页;
  范围切换在搜索页里用一条**普通的分段控件**(该列表 / 全部文章)实现,完全自己掌控。
- 「该列表」用的是上游系统范围条「Here」档同一个数据源
  (`SearchTimelineFeedDelegate(searchString:articleIDs:)`),当前列表的文章 ID 协调器手上就有。
- **导航栏高度从此恒定** —— 顶栏错乱、标题卡半空那一整族问题连根消失。
- ⌘⌥F 和长按桌面图标的「搜索」(上游 `showSearch()`)也一并接到同一个 modal ——
  它原来会 `selectSidebarItem(nil)` 清空选中,正是当年那个空白页的病根,顺手一起解决。
- **净收益**:上游 `MainTimelineModernViewController.swift` **净减 14 行**,
  三个搜索代理回调全部还原成上游原样 —— **比这件事开始之前更接近上游**。

完整的五轮试错与"该在第二轮就消掉机制"的反省见 **L96**。

**⚠️ 别把系统搜索控制器加回这一页** —— 加回来就等于把"导航栏会变高"这个自由度也加回来。

**方案 D 做了什么**:
- 新增 `iOS/ReadingMode/NNWGlobalSearchViewController.swift`:独立的 modal 搜索页
  (pageSheet,下拉可关)。搜索框挂导航条(`.stacked` 摆法显式钉死),边打字边搜、
  ≥3 字门槛、250ms 防抖、任务取消防竞态(订阅发现页同套路)。
  数据直连上游 `SearchFeedDelegate`(FTS 全库检索),**主时间线从头到尾不知道有搜索这回事**
  ——上游 `searchArticles` 是"把主时间线换成搜索结果"的模式,绝不能复用(会走回老路)。
- `SceneCoordinator` fork 扩展区:`nnwShowGlobalSearch()` 改为弹 modal;
  新增 `nnwOpenSearchResult(_:)` —— 照抄 `handleReadArticle`(点通知跳任意文章)的配方:
  `exceptionArticleFetcher` + `discloseFeed` + 0.5s 后 `selectArticleInCurrentFeed`。
  两处延时是上游原样的节奏,别调小。
- 删掉方案 B 的挂起机制(`nnwRequestGlobalSearch` 那套约 70 行)和上游 viewDidAppear 的钩子行
  (上游文件净还原 5 行)。**保留**了文章列表页自己的放大镜三件套
  (`nnwUseCompactSearchPlacement` / `nnwUseStackedSearchPlacementIfNeeded` /
  `nnwRestoreSearchPlacement`)——那是本页搜索(含范围条)在用的,和全局搜索无关。

**为什么 modal 能同时解决两半诉求**:它盖在当前页面之上,关掉自然露出原页面,
"原来在哪就回哪"是 dismiss 自带的;进搜索也不再需要先垫一页「全部未读」。

**⚠️ 拦路的硬事实(留档,防止后人再撞,7 次试错的完整记录见 L92)**:

> 在 iPhone 的 collapsed 分栏里,`rootSplitViewController.show(.primary)` **调用无效** ——
> 日志实锤:调用执行了、`selectFeed(nil)` 的完成回调也执行了,**界面纹丝不动**。

**2026-07-28 晚补:静态尸检结论(只读代码分析,未重新做实验)**:
- 守卫排除:`RootSplitViewController.show()` 的 iPhone 分支无条件走 `super.show()`;
  `isNavigationDisabled` 全仓库只在 selectPrev/NextUnread 的同步执行期内短暂为 true,
  搜索退出路径碰不到它。
- 代理排除:`UISplitViewControllerDelegate` 只实现了 collapse 瞬间的栈顶选择和
  displayMode 记忆,没有任何能拦截 show() 的方法。
- **最可能根因**:collapsed 下整个界面就是 primary 列的导航栈,UIKit 实现了
  `show(.supplementary/.secondary)→push` 的映射,但**没实现 `show(.primary)→pop` 的逆映射**,
  于是判定"无事可做"。(对 UIKit 内部行为的推断,标不确定,但"原因不在本仓库代码里"是确定的。)
- **真需要"代码回首页"时的替代原语**:
  `mainFeedCollectionViewController.navigationController?.popToViewController(mainFeedCollectionViewController, animated:)`
  —— collapsed 下活跃栈就是 primary 列的导航栈(设置页的推入/收场就在这个栈上,实测可用)。
  当年失败清单里的 "popViewController" 试的是**时间线页自己的导航栈**(深度=1,当然 pop 不动),
  不是这个。⚠️ 未实测,若用需在搜索收起转场结束后再调。modal 方案用不着它。

**留一个已知边界(有意不做)**:外接键盘 ⌘⌥F 和长按桌面图标的搜索走的仍是上游
`showSearch()` 老路(清空选中 → 空白页背景),没有改 —— 用户不用外接键盘,
改它要动上游文件,不值得。哪天要改,把 `showSearch()` 的身体换成调 `nnwShowGlobalSearch()` 即可。

---

**以下为历史记录(方案 D 之前的状态,保留存档)**:

**状态**:2026-07-28 尝试后**回滚**,当前停在方案 B 的行为上。用户已知,准备换更强的模型另开一轮。

**用户的诉求(原话)**:
> 每次点击搜索按钮,都会先跳到全部未读,然后再进入搜索,并且退出搜索后,会留在全部未读,
> 而不是最一开始点击搜索按钮时所在的位置。这不是一个大问题,但我觉得设计上很反直觉,
> 我想能不能改成退出的时候,原来在哪里,就返回哪里。

**当前行为(方案 B,已提交 `e34d26aea`)**:
点首页放大镜 → 先切到「全部未读」→ 在它上面激活搜索 → 退出搜索后**停在「全部未读」**。
好处是背后一直有内容(不再是空白页);代价就是用户说的这条反直觉。

**⚠️ 拦路的硬事实(别再重新验证一遍,7 次试错的完整记录见 L92)**:

> 在 iPhone 的 collapsed 分栏里,`rootSplitViewController.show(.primary)` **调用无效** ——
> 日志实锤:调用执行了、`selectFeed(nil)` 的完成回调也执行了,**界面纹丝不动**。
> 而且这一页**独占一个导航栈**(实测深度=1),`popViewController` 无从下手;
> `viewController(for: .compact)` 拿不到导航控制器。

所以:**只要"回到首页"依赖 `show(.primary)`,这条路就是死的。**
方案 C(记下来处 + 退出时 `selectFeed(来处)`)已试过并回滚 ——
来处是首页时 `selectFeed(nil)` 真的清空了列表,空白页又回来了,**绕一圈踩回同一堵墙**。

**还没试过的方向**:
1. **把全局搜索做成 modal(从下往上弹出的独立页面)** —— 我评估的首选。
   modal 覆盖在当前页上,关掉自然露出原页面,**天然"原来在哪就回哪"**,
   完全绕开分栏导航。代价:要自己搭搜索页(搜索框 + 结果列表 + 点结果进文章),
   几百行,且要复用上游的 `coordinator.searchArticles(_:_:)`。
2. 搞清楚 `show(.primary)` 为什么无效 —— 也许是 `rootSplitViewController` 的
   `preferredDisplayMode` / 某个自定义行为导致。没深挖过。
3. 让搜索直接发生在首页上(首页自己挂 searchController),结果推到文章列表。
   没评估过可行性。

**相关代码**:
- 入口:`SceneCoordinator.nnwShowGlobalSearch()`(注释里有完整的死因记录)
- 激活时序:`MainTimelineModernViewController+ReadingMode.swift` 的
  `nnwRequestGlobalSearch` / `nnwConsumePendingGlobalSearch`(L79 的四版试错都在这)
- 摆法与范围条:`nnwUseCompactSearchPlacement` / `nnwUseStackedSearchPlacementIfNeeded`
  ⚠️ 范围条**只在 `.stacked` 摆法下显示**,改动这块时别再把它弄丢(已修,见 `a92a012cc`)

## T41 · `nnwEnableSoftBottomEdgeFade()` 是死代码,待清理 —— 🟡 **留着,但别再当成"渐隐的修法"**(2026-08-05)

**现状**:`UIScrollView.nnwEnableSoftBottomEdgeFade()` 接在三处
(首页 `nnwUpdateReadingMode`、文章列表页 `TimelineFeedHeader`、正文页 `WebViewController`)。

**已证伪的部分**:在**正文页**它是**彻底的空操作** —— 反过来强制 `isHidden = true`
(日志回读确认真写进去了),画面**逐像素不变**。`bottomEdgeEffect` 对 WKWebView 不起作用。
它当初的立论(「拍平工具栏把渐隐一起拍掉了,所以直接向滚动视图要回来」)**整套是错的**,
真凶是 `hidesSharedBackground`,见 T40 与 L116。

**为什么现在不删**:上面的证伪只在 **WKWebView** 上做过。
首页和文章列表页用的是普通 `UICollectionView`,那上面 `bottomEdgeEffect` **有没有用没验过**。
删一个没验过的调用,风险大于留着一个无害的调用。

**要清理的话怎么做**(下一个窗口可直接执行):
用同样的办法验那两页 —— 加一个临时开关强制 `bottomEdgeEffect.isHidden = true`,
在 iOS 26 模拟器上截图比对。若逐像素不变 → 那两处也是空操作,三处连同扩展一起删掉。

⚠️ **代码里的注释已经改成实话了**(NNWSoftMaterial 与 WebViewController 两处),
不会再误导下一个人。**这条只是"清不清理"的问题,不是"对不对"的问题。**

## T42 · iOS 27 上「栏里的胶囊」改用系统液态玻璃 —— ✅ **已做,用户已验收**(2026-08-05)

**背景**:修好 T40 那个「内容不从栏底下穿过去」之后(不再拆系统的共享背景),
**双层套娃在 iOS 27 上出现了** —— 用户截图指出文章页 dock 和首页所有控件都是两个边框。

**为什么不能两全**(SDK 里查过,`hidesSharedBackground` 是唯一相关开关,
**没有"保留行为、只隐藏外观"的第三种接口**):

| iOS 27 上 | 内容穿过栏底 | 单层 |
|---|---|---|
| 拆系统胶囊 + 画我们的面板 | ❌ 硬切断 + 黑带 | ✅ |
| 保留系统胶囊 + 画我们的面板 | ✅ | ❌ 两层 |
| **保留系统胶囊 + 不画我们的面板** | ✅ | ✅ ← 用户拍板选这条 |

**做法**:新增唯一真源 `NNWSoftMaterial.systemDrawsBarCapsule`(iOS 27+ 为 true),
四处读它 —— 文章页 dock(`NNWControlBoard`)、三档控件(`NNWReadingModeBar` 的**外层**面板,
⚠️ **选中指示胶囊要留着**,那不是栏的背景)、导航栏磨砂圆钮(`NNWSoftGlassButton`)、
底部烘焙圆钮(`roundButtonImage` 改为只出图标)。
⚠️ **自绘选单 `NNWMenu` 不受影响** —— 它不在栏里,系统不给它垫胶囊。

**已验**:iOS 26 底栏区域 602 个采样点、浅色深色**平均差 0.000 / 最大差 0**(完全零退化);
iOS 27 真机用户肉眼确认修好。

**代价(用户知情)**:iOS 27 上 dock / 三档 / 圆钮的**底由系统决定**,
我们那套从参考图逐像素调出来的材质(1.2pt 整圈亮边、上暗下亮渐变、极淡宽阴影、
面板与画布只差 2 级色差)在 27 上**看不见了**。留下的是手绘图标、主题色、
三档的选中胶囊、自绘选单整套。另外代码里多了一处版本分叉,且押在 beta 行为上。

**➡️ 已被 T43 接手**:用户 2026-08-05 决定试"把 dock 从工具栏里整个搬出来自绘",
若成功则这一条连同那处版本分叉一起作废。**本状态已打 tag 作为回滚点。**

## T43 · 陀螺仪驱动的边缘反光(dock 亮边跟着手机倾斜转)—— 🔵 **用户 2026-08-05 提出,待做**

**用户原话**:「利用上 iPhone 的陀螺仪,手机动的时候,按钮的边缘反光也会跟着动」。
基线:在「阴影拉开」那一版(阴影半径 6→18、下移 2→8)之上做。

**做得成的前提(已具备)**:dock 已从工具栏搬出来当浮层(见 NNWFloatingDock.swift),
那圈亮边完全归我们画 —— 在系统胶囊里是做不了的。

**技术路线**:
- `CMMotionManager.startDeviceMotionUpdates` 读 roll/pitch(**不需要任何权限**,
  陀螺仪不像定位那样要授权)
- 亮边从"一圈均匀纯白"改成**锥形渐变**(`CAGradientLayer.type = .conic`,iOS 12+)
  描边,再按 roll 旋转它 → 高光就沿着边缘跑
- 现在的 `rimAlpha` 是恒定值,要改成"沿角度分布":最亮的一段对着"光源",背面衰减

**⚠️ 必须一起做的三件事**:
1. **`UIAccessibility.isReduceMotionEnabled` 为真时整个关掉**(系统级无障碍要求)
2. **只在文章页可见时更新**,`viewWillDisappear` 停掉 —— 否则后台一直转陀螺仪费电
3. 更新频率压到 ~30Hz,别用默认的 60/100Hz

**⚠️ 我自己验不了的部分**:**模拟器没有陀螺仪**,这个效果在模拟器上恒定不动。
验证只能在真机上。可行的办法(这一轮已经打通,见 L115):
把算出来的角度打进日志 + 让用户把手机摆成几个固定倾角、各截一张图比对 ——
这样"管不管用"仍然是可测量的,不用靠"看着好像动了"。

**用户对基线的态度**:四版材质对比里「都不怎么能看出太多区别」,
第三版(阴影拉开)「还不错,但不着急纳入正式版」。
所以**那四版的开关先留着别删**,等这个效果做出来一起定。

### T43 追加(2026-08-05 用户拍板范围)—— ⚠️ **有一个前置障碍,别一头撞上去**

**用户的范围决定**:「**只在订阅源页(首页)用**,那个是进入 app 的首页,能够抓人眼球,
之后的页面可以低调点。」——**别做成全 app 都在闪**,这是分寸,不是遗漏。

**⚠️ 前置障碍 —— ✅ 已于 2026-08-05 完成(见 T47)**:
陀螺仪反光要求**那圈亮边归我们画**。现在:
- ✅ **文章页 dock** 已搬出工具栏(T42 之后),边归我们 —— 但用户说**不要**在这页做
- ✅ **首页三档控件已搬出栏**(T47,浮层化,`drawsOwnTrack: true`)——
  两个系统版本上外圈都由 `NNWSoftPanel` 画,`rimAlpha` / 锥形渐变都够得着了

**下一轮可以直接做陀螺仪本体了**(技术路线见上面;三件配套别忘:
无障碍开关 / 离页停更新 / 30Hz;验证只能真机,办法见 L115)。

**建议只搬「三档控件」这一个**(星标/未读/全部),理由:
- 它本来就是 customView(`NNWReadingModeBar`),搬出来的手术和文章页 dock 同型
- 它在首页最显眼、居中,正是"抓人眼球"该落的地方
- **绕开了首页那两个雷**:`+` 是 storyboard 的 IBOutlet 且上游往它身上挂 menu;
  底部工具栏有 `expectedItemCount == 3` 的守卫(CLAUDE.md 点名过)——
  只要不动 `toolbarItems` 数组的结构,这两个雷都不碰

齿轮/加号/搜索/编辑那四颗**先别动**,让它们继续用系统胶囊(低调,正合用户的意)。

---

## T52 · 全屏控件的玻璃/层次/尺寸统一 —— ✅ **已做,等用户验收**(2026-08-09)

**用户的原话**(给了一张文章列表页的截图):
「上下部分的各个控件,好像玻璃的透明度,层次,质感,圆形控件的大小,都不太一致
(**特别是在深色模式下**),能否修改一下?」

### 查出来的病根:同一屏上混着**三套底**

| 控件 | 改之前的底 | 归谁画 |
|---|---|---|
| 左上 `<` 返回键 | **系统的液态玻璃** | **系统**(我们一行都没碰) |
| 右上 齿轮+放大镜胶囊 | 真玻璃 `UIGlassEffect` + 一层淡白 | 我们 |
| 底部中间 三档轨道 | **同上,完全一样** | 我们 |
| 底部「未读」那颗选中胶囊 | **不透明实心色** | 我们 |
| 底部左右两颗圆钮 | **不透明,而且是画进一张图片里的** | 我们 |

**为什么深色下尤其明显**(数字说话,底色 `#1E1E1E` = 30):
玻璃档在深色下只补 **3–6% 的白**,所以很透、亮度**跟着背后滚动的文章浮动**;
而实心档是恒定的 —— 圆钮 `#272727`–`#2B2B2B`、选中胶囊 `#323232`–`#383838`。
浅色下实心档是 232、纸底 234,几乎同色,**所以浅色一直混得过去**。

### 做了什么(五处)

1. **底部四颗圆钮**(首页齿轮/加号 + 文章列表页标记已读/下一篇未读)
   从「烘焙进图片」换成**真玻璃 customView**(`NNWSoftGlassButton`)。
   新文件 `iOS/DesignKit/NNWSoftGlassBarButton.swift`。
   ⚠️ **对象一个都没换**,只设 `customView` —— 所以 `toolbarItems` 结构没动
   (`expectedItemCount == 3` 的守卫照旧满足)、`addNewItemButton` 这个 IBOutlet 也没动。
   代价是 `isEnabled` 和 `menu` 得自己搬运(UIKit 不会传给 customView),
   挂点是上游两个"刚写完状态"的方法各**加一行**:
   `MainFeedCollectionViewController.updateUI()` / `MainTimelineModernViewController.updateToolbar()`。
   为什么原来那条「只能换 image」的约束不成立,见 **L127**。
2. **选中胶囊**改成**半透明提亮**(`NNWSoftPanel(kind: .capsule, translucent: true)`),
   不再是实心色。⚠️ 半透明的 `.capsule` 档**不会**再垫一块自己的磨砂 ——
   它坐在已经磨砂过的轨道上,再垫一层就是玻璃套玻璃(`needsOwnBlur`)。
3. **左上返回键**换成我们自己的玻璃圆钮(新文件 `NNWSoftGlassBackButton.swift`)。
   ⚠️ **换完必须自己把边缘侧滑返回捡回来** —— UIKit 有条不写在明面上的规矩:
   一旦设了自定义 `leftBarButtonItem`,`interactivePopGestureRecognizer` 就不再工作,
   **而且没有任何报错**。修法是给它一个我们自己的 delegate,规则只有一条:栈里不止一页才允许。
4. **图标字号收进唯一真源** `NNWSoftMaterial.iconPointSize`(= 直径 45% = 18pt)。
   改之前全 app 有四种:三档 13 / 文章页 dock 17 / 顶部 18 / 底部圆钮 20 ——
   同样 40pt 的盘子上图标从 13 到 20,这是"质感不一致"里最实的一条。
   三档是**唯一的例外**,13 → **15**(那一格里图标要和「未读」两个汉字并排,
   拉到 18 会挤,而且 `expandedWidth` 是写死的)。
5. 顺带:`nnwNavSymbol` 里那个自己算的 `* 0.45` 收进真源,别再两处各算一遍。

### ✅ 我自己能验的都验了

**对齐没有跑偏**(这是这次唯一有回归风险的地方,不能靠猜):
把"图片按钮"换成"自绘视图"之后,**UIToolbar 摆它们的规矩可能变** ——
而首页那条**浮起来的**三档是按旧摆法校准的(`nnwBarItemOvershoot = 2`)。
埋探针读真 frame(不是截图反推),iOS 26 实测:

| | 上缘 | 下缘 | 高 | 中心 |
|---|---|---|---|---|
| 首页两颗圆钮(圆盘) | 802.00 | 842.00 | 40.00 | **822.00** |
| 首页三档浮层 | 802.00 | 842.00 | 40.00 | **822.00** |

**四项完全相同**,而且比 T50 那轮的旧数字(39.67 / 821.83,截图扫亮边反推的)更精确。
所以那个 `+2` 的补偿仍然成立,**不用改**。

### 🔴 悬着的三件

1. **文章列表页那三个控件的对齐我量不到** —— 要进那一页才会触发,而进那一页得点界面。
   **探针先留着**(`nnwLogGlassAlignment`,日志频道 `NNWGlassAlign`),
   用户点进去之后读一次日志就能确认,确认完连同探针一起删。
2. **颜色的浓度是按机制定的,没法自己验**(视觉的事归用户看)。
   选中胶囊的提亮:浅色 42%→50% 白、深色 10%→14% 白。
   浅色要压得够浓才看得出"这一格被选中"(轨道自己已经有 10–16% 的白),
   深色下白一浓就变塑料板(L112 那一族)。**不对就调这两组数,在 `NNWSoftMaterial` 里。**
3. **文章页 dock 的 17pt 图标我没动** —— 当初说好要一起拉到 18,查下来**不该动**:
   那里是 54pt 的板 + 44pt 的格子,**没有 40pt 圆盘**,"直径 45%"这条规则对不上它;
   而且 17pt 是 **2026-07-25 用户逐版验收过的**,翻译按钮还专门压到 15pt 去对齐它。
   动它 = 推翻一个签过字的校准,还要连带重调翻译按钮。**留给用户发话。**

### 📌 顺带记档

`NNWSoftMaterial.roundButtonImage` / `renderRoundButton` **全仓已无调用者**,
留着当回退路径(万一玻璃圆钮在某个系统上出事,改回来只要一行)。
⚠️ **别再拿它做新控件** —— 它画的是不透明盘子,和全 app 其余控件不是一种材质。
待遇和 T41 那条死代码一样:留着,但注释顶上写清楚。

---

## T53 · 头图硬边界 + 外文图标 + 文章页手势重排 —— ✅ **已做,等用户验收**(2026-08-09)

用户一次提了三件。

### ✅ 1. 首页头图上那两条硬边界

**用户的原话**:「"智能feed"几个字上方,是有一个和背景暖纸色相同颜色的底层**直角**蒙版的边界处,
所以这个列表滚到这个部分的时候,就很丑」「底下的"今天"上方,也是一个……**圆角**蒙版的边界处」。

**病根不是"哪里画错了",是必然结果**:头图是 `collectionView.backgroundView`,**固定不动**,
内容从它上面滚过去。而滚过去的每样东西各自带着**不透明的暖纸色底**:

| 用户看到的 | 真身 | 在哪 |
|---|---|---|
| **直角**那条 | 分组标题栏自己的 `backgroundColor` | `MainFeedCollectionHeaderReusableView.swift:104` |
| **圆角**那条 | 分组卡片(`insetGrouped`)的 `backgroundConfiguration` | `MainFeedCollectionViewCell.swift:145` |

压在纸色背景上时完全隐形,**一压到图上就现出硬边**。

**修法(用户从三条里选的第一条)**:在**内容的上边缘**加一条「纸色 → 透明」的渐变带
(`TimelineStyle.headerContentFadeHeight = 52`),画在浮层最底层(内容之上、标题之下)。
内容的上边缘被埋进纸色里,那条硬边就永远不成形。

🔴 **这里有一个想当然会栽的地方,已经绕开**:
第一版想把带子**钉在头图底边**。那样只在"内容刚探头"的一瞬有效 ——
再往上滚,内容的上边缘越过了带子,**硬边照样露出来**。
所以带子的位置必须用**未夹过的滚动量**算(`contentTopY = headerHeight - scrolled`),
不能拿 `progress` 反推(那个夹在 0…1,滚过 140pt 之后就不动了)。
判据:**要盖住的是一条会动的边,遮罩就得跟着它动。**

⚠️ 两个实现细节:① `CATransaction.setDisableActions(true)`,否则每帧的 frame 变化被排成
0.25 秒动画,带子会拖在手指后面(毛玻璃那条羽化早就栽过);
② 渐变两端必须是**同一颜色的两个透明度**,不能用 `.clear`(那是透明的黑,中段会掺灰)。

⚠️ **CGColor 不跟深浅色**(L119):记住上次解析用的是哪一套,`registerForTraitChanges` 里清记号。

📌 首页和文章列表页**共用同一套头图**,所以一处修、两处好。

### ✅ 2.「外文」换成彩色地球

素材是用户放在 `external resources/外文.png` 的 512×512 扁平地球,
已缩成 3 倍图进资源目录(`nnwForeignFeedIcon.imageset`,120×120)。
`smallIcon` 不再传 `preferredColor` —— 传了会被染成单色,那这张图就白换了。

**深色调和**(用户:「不知道深色下好不好看,如果过于 standing out,稍微调一调色调,
加一层蒙版,**保留它的彩色**,但让它和深色更和谐」):
新文件 `iOS/ForeignFeed/NNWForeignFeedIcon.swift`,深色那张 = 原图
→ `.saturation` 叠 40% 中灰(**降饱和**,色相不变)
→ `.sourceAtop` 叠 22% 黑(**压亮度**)
→ `.destinationIn` 再画一遍原图(**把 alpha 收回来**)。
⚠️ 最后那步不能省:`.saturation` 会把圆形之外的透明区也涂上东西,不收就多一个灰方块。

⚠️ **深浅色切换走 `UIImageAsset`,不自己监听**:注册浅/深两张,
从 asset 取出的 `UIImage` 放进 `UIImageView` 后由 UIKit 自己重解析 ——
和资源目录里"两张图"的图完全同一个机制。
**这条路只对 `UIImageView` 成立**,2026-08-04 给 `UIBarButtonItem.image` 试过同样办法**实测不生效**。
侧栏图标正是 `UIImageView`(`FeedListMetrics.iconSize = 28`),所以能用。

**两个旋钮**都在 `harmonizedForDark` 里:`desaturation = 0.40`、`darkening = 0.22`。
嫌深色下还太跳就调大,嫌太灰就调小。

### ✅ 3. 文章内容页的手势重排

| 手势 | 改之前 | 现在 |
|---|---|---|
| 右滑 | 回文章列表 | **没变** |
| 左滑 | 下一篇未读 + 彩蛋页 | **打开原文**(app 内浏览器) |
| 已在**底部**,再用力上拽 | — | **下一篇未读**;没有了 → 彩蛋页 |
| 已在**顶部**,再用力下拽 | — | **上一篇** |

⚠️ **`viewControllerAfter` 从此恒为 nil**(和 `viewControllerBefore` 一样)——
左滑既翻页又开链接必然打架,二选一。原来那套"下一篇未读 + 彩蛋页"**一行没丢**,
只是搬到 `nnwGoToNextUnread()`,仍然只在当前列表内找。

⚠️ **横竖分成两条 pan,各自一个 delegate**(`NNWAxisSwipeGestureDelegate(axis:)`)。
不能一条 pan 里判方向:`shouldBegin` 要在手指刚动几个点时就给答案,一条只能给一个。

⚠️ **"到没到头"必须在手势 `.began` 那一刻记下来**,不能等 `.ended` 再问 ——
不然"从中间一路甩到底"也会在结束时量到"现在在底部",白白翻页。
用户要的是"**已经**在底部了,再拽一下"。
📌 到没到头用上游自己的 `canScrollUp()/canScrollDown()`,不自己拿 `contentOffset` 算(L73 那一族)。

⚠️ **竖向的门槛比横向紧得多**(距离 110 **且** 速度 900,两个都要满足;横向是满足一个即可)。
理由:竖滑和"读正文"是同一个动作,门槛一松就会在读到底时莫名其妙跳篇。

### 🔴 悬着的四件

1. **这三件的观感我一件都没法自己验**(界面的事归用户看)。渐隐带 52pt 够不够柔、
   地球在深色下是不是还太跳、竖拽的门槛是不是太紧或太松 —— 全靠用户实测。
2. **左滑可能在"横向可滚的正文元素"上误触**(宽表格、代码块):
   我们的手势允许和别人同时识别,所以在代码块上左滑会**既滚代码块又开浏览器**。
   ⚠️ 这不是新问题(改之前那里是"左滑翻页",一样会误触),但现在后果更重(弹出浏览器)。
   用户报了再收紧左滑的门槛。
3. **彩蛋页现在只能从"底部再拽"进入**,而且进去之后左右滑都不翻回文章
   (前后页都是 nil)—— 页上那个「回到列表」按钮是唯一出口。够用,记档。
4. **文章列表页三个控件的对齐仍未量到**(T52 第 1 条),探针还留着。

### 🔴 T53 补丁(2026-08-09,用户打回一次)

**用户原话:「现在的效果变成这样,更丑了」** + 「地球图标稍微感觉大了点,
能否改成和"全部未读"前面的那个 icon 一样大」。

#### 1. 渐隐带做反了 → 改成**三段**

第一版是**两段**(上纸色、下透明)、整块放在交界线**上方**。
结果:带子顶端不透明、上面又什么都没有 → **在图上多画一条新硬边**;
而要治的那条边在带子下方、那里 alpha 已经是 0 → **根本没盖住**。

现在改成**骑在交界线上的三段**:透明 → 不透明 → 透明。
两个旋钮:`headerContentFadeAbove = 40`(往图那侧化开,要给足)、
`headerContentFadeBelow = 12`(往内容那侧,**要小** —— 内容本来就是纸色,
纸色盖纸色看不出来,它唯一会影响的是那上面的文字)。
判据记在 **L129**:**用遮罩藏边,遮罩自己的每条边界也得是化开的,否则只是把边挪了个地方。**

#### 2. 地球图标偏大 —— **量了再改,不是目测**

扫不透明像素的外接框,占画布的比例:

| | 画布 | 图形 | 占比 |
|---|---|---|---|
| `largecircle.fill.circle`(全部未读) | 75×73 | 65×65 | **0.87** |
| `sun.max.fill`(今天) | 79×77 | 69×69 | 0.87 |
| 地球 PNG | 120×120 | 120×120 | **1.00** |

**SF Symbol 天生自带一圈留白,而这张 PNG 是满铺的。**
28pt 的格子里,圆圈实际画出来 ≈24.3pt,地球是整整 28pt —— 大了 15%。
修法:给地球补上同样比例的透明留白(`paddedToMatchSymbols`,ratio = 0.87)。
📌 判据:**两个东西"看起来一样大"比的是图形,不是画布。**

#### 3. ⚠️ 顺带避开的一个坑:`isSymbol` 要设成 `true`

地球不是 SF Symbol,但**必须标 `isSymbol: true`**(和文件夹图标同一个套路)。
两个理由:
- `NNWFeedIconStyle.styled()` 会**跳过**标了这个的图标。否则它走 favicon 加工流水线,
  而那条会给"背景透明的图标"**补一块底板** —— 地球四角本来就透明,
  补完就是一张卡片糊在圆球后面。⚠️ 补留白之后四边采样点**全部**落在透明区,
  比原来更容易被判成"要补底",所以这一步是跟着上面第 2 条一起必须做的。
- `IconView` 对标了它的图标按**整个格子**摆,不再做二次 aspect-fit ——
  留白已经在图里补好了,不需要它再插一手。

### 🔴 T53 补丁二(2026-08-09,用户第二次打回 —— **前两版的诊断只对了一部分**)

用户放大截图:「仔细看智能 feed 和今天中间,那个边缘的直角和圆角还是在」。

**真正的成因**(前两版都没查到底):
这一页的列表底色是 **`.clear`**(`MainFeedCollectionViewController.swift` 里
`config.backgroundColor = .clear`,2026-07-23 为了让头图透出来才特意设的)——
于是**卡片之间的缝、卡片左右的留白,露出来的全是头图**。
所以边**不是**"内容上边缘压在图上"的那一条,而是
**每一块不透明的纸色元素都被头图从四面描了一圈**。

**正解:垫一层纸,不是盖一块遮罩。**
在**头图之上、列表内容之下**铺一层纸色底(`TimelineFeedHeaderView.paperBackdrop`),
上沿落在内容的上边缘、往上羽化 44pt(`headerContentBackdropFeather`),往下一路铺满。
于是内容底下从头到尾都是纸,卡片和缝隙同色 → **一条边都长不出来**;
内容上边缘以上仍然是图,一点没挡。

⚠️ **三个实现要点**:
1. **必须 `addSubview` 加在 `backgroundImageView` 之后** —— 裸图层(`layer.addSublayer`)
   永远排在子视图的图层下面,那样会被头图盖住,等于没加。
2. **不受 `contentAlpha` 影响**(那个只作用于 `backgroundImageView`)。这是对的:
   头图随滚动渐隐,而底下这层纸要一直在 —— 它是内容的底,不是装饰。
3. **上沿位置用未夹的 `scrolled`**,不能拿 `progress` 反推(L128)。

✅ **已量过的两件**(不靠推断):
- 头图视图(`collectionView.backgroundView`)**是整屏高**:实测 874pt,头图本身 174.8pt。
  它开着 `clipsToBounds`,如果只有头图那么高,垫的纸会被整个裁掉、改动等于没做。
  实测纸板高 743pt,没有被裁。**这个探针已删。**
- `syncInset()` **幂等**(目标没变就 return),所以每帧 `setNeedsLayout` → `onLayout` → `syncInset`
  不会成环(L113 那条"挂钩子前先确认幂等")。

⚠️ **浮层里那条 `contentFadeLayer` 已整个删掉**,别再加回来 ——
浮层在内容**之上**,往里加"盖住内容"的东西一定会把文字一起洗糊
(v2 就把「智能 Feed」几个字洗花了,用户截图里能看见)。

判据记在 **L130**:**"边"是两种颜色相邻才有的东西;与其一条条去糊,不如让相邻的两边同色。**

---

## T54 · 全屏控件统一到 44pt + 返回键真的换掉了 —— ✅ **已做,等用户验收**(2026-08-09)

**用户的要求**:右上「设置+搜索」胶囊、底部两颗圆钮、底部中间三档,
**全部调到和左上返回键一样大、一样高、并且对齐**,而且要**应用到所有出现这些控件的页面**。

### 🔴 先纠正上午的一个错误说法:返回键**从来没换成过**

上午交付时我写了"左上返回键已换成我们的玻璃圆钮"。**那是没验证就说的。**
用户截图里那颗箭头是**深灰**的(我们那颗是橙的)——埋日志一查:

```
nav=UINavigationController   栈深=1   父=UINavigationController
```

文章列表页住在**分栏控制器自己那一列的导航控制器**里,那个栈**只有它一个**;
真正装着「列表 → 文章」的是**父导航控制器**。
而我的守卫写的是 `navigationController.viewControllers.count > 1` → **恒不成立**,
返回键一次都没装上,**而系统那颗顶着位置,看起来像是做好了**(L124 同一个形状)。

⚠️ **文章页早就知道这件事**(`NNWArticlePaging.nnwPopTargetNavigationController`
逐字写着),我没去看。现在三处(装不装、点了对谁出栈、接管谁的侧滑手势)
全部改走 `nnwBackPopTarget`(自己那个栈够深就用自己的,否则用父的)。

### ✅ 尺寸:**40 → 44**,而且是"换根"不是"追赶"

📌 关键认识:我们自己的四个控件**本来就全是 40pt、中心全在 822**(实测,不是推断)——
唯一的异类是**系统那颗返回键**,它归系统画,比我们大一圈。

所以这一轮不是去"量系统那颗有多大再追上它",而是**把它也换成我们的**:
于是"唯一真源" `NNWSoftMaterial.controlDiameter` 第一次真的覆盖整屏,
以后系统版本再变也不会跑偏。顺带把这个数抬到 **44**(苹果的最小点按标准)补上视觉差。

**改一个数,五个控件、两个页面一起变** —— 用户要的"应用到所有页面"是白拿的。

### ✅ 实测确认(SIM27,页面停稳后才采样)

| 控件 | 可见高 | 中心 |
|---|---|---|
| 底部两颗圆钮(圆盘) | **44.00** | 822.00 |
| 中间三档轨道 | **44.00** | 822.00 |

同高、同心。⚠️ 圆钮的**外框**是 48(点按区 = 直径 + 4),画出来的圆盘是 44 —— 别把外框当尺寸。

### ⚠️ 三件要知道的

1. **三档控件的图标仍是写死的 15pt**(用户 2026-08-09 上午定的)。
   轨道从 40 长到 44 之后,这个图标**相对变小了一点**。要不要跟着抬到 16,等用户发话。
2. **`iconPointSize` 跟着变了**:直径 ×0.45,18 → **20**。所有圆钮/胶囊里的图标一起变大。
3. **首页那条三档浮层**的位置是按安全区 + `nnwBarItemOvershoot = 2` 算的,
   直径变了要复验一次对齐(探针 `nnwLogGlassAlignment` 还留着,进一次首页就能读)。

### 📌 工作方式的变更(用户 2026-08-09 明确)

🔴 **以后一律以 SIM27(iOS 27)为主** —— 装机、看日志、复验都对它。
这一轮我一路在往 SIM26 装、在 SIM26 读日志,而**用户全程在 SIM27 上看**,
白白多花了好几个来回(判据见 **L131** 第 ① 条)。

### ⚠️ T54 补丁:首页那条浮层从"对底边"改成"对中心"(改尺寸带出来的真回归)

**2026-08-09,提交前自查发现的**,不是用户报的。

`NNWFloatingModeBarHost` 原来把浮层的**底边**钉在 `屏高 − 安全区底 + 2`
(那个 `+2` 是 2026-08-05 实测校准的)。**但工具栏摆 bar item 用的是"中心对齐"**,
它的中心**不随控件大小变** —— 实测两次都是 822(直径 40 一次、44 一次)。

| 直径 | 工具栏圆钮中心 | 老公式给浮层的中心 |
|---|---|---|
| 40 | 822 | 822 ✅ **碰巧一样** |
| 44 | 822 | **820** ❌ 差 2pt |

也就是说:2026-08-05 那次"对齐"是**两个不同的算法碰巧算出同一个数**,不是真对齐;
**直径一改立刻错位**。已改成直接钉中心:`中心 = 屏高 − 安全区底 − 18`
(18 是实测常量,在 40 和 44 上各验过一次)。

📌 判据(L122 的又一例):**结论要连着适用范围一起记。**
那个 `+2` 当时若写成「**在直径 40 时**两者吻合」,这次改直径就会立刻想到要复查它。
更一般地:**两个东西"数值相等"不等于"用同一个规则算出来的" —— 只有后者才经得起参数变化。**

⚠️ 这一条**我没法自己验**(要进首页才触发探针,而模拟器停在文章列表页)。
数字是从两次实测常量推出来的,但**首页底部三个控件是否齐平请用户看一眼**。
探针 `nnwLogGlassAlignment` 为此**先留着**,用户进一次首页我读一次日志就能确认,确认完就删。

---

## T55 · 文章页「拽过头翻篇」的指示器与动画 —— 🔴 **未完成,下一轮接着修**(2026-08-09)

⚠️ **这一条是 2026-08-09 整个下午的坑,来回七八轮仍未干净。接手前把这一整条读完。**

### 用户想要的最终形态(已经确认过好几轮,别再改设计)

| 手势 | 行为 |
|---|---|
| 右滑 | 回文章列表 ✅ 已验收 |
| 左滑 | 打开原文(app 内浏览器)✅ 已验收(宽表格/代码块上不误触,用户确认) |
| 已在**底部**再拽 | 下一篇未读;没有了 → 彩蛋页 |
| 已在**顶部**再拽 | 上一篇 |

指示器:一条**很宽很扁**的线,`—` 随进度**弯**成 `^`/`v`(不是变深变浅);
拽够时**震一下**;**正文同时被推开**给箭头让位;松手翻页,动画**垂直、稍慢**。

### ✅ 已经查清并修好的(别再重查)

1. **WebView 在底部不回弹**(实测:`峰值=0`,`offset` 精确卡在极限值)。
   所以"超出了多少"这个量在底部恒为 0 —— 必须**同时**量"贴住之后手指还走了多少"(打五折)。
2. **沉浸阅读的栏会在拽的过程中冒出来**,`inset下` 0↔96、`inset上` 237↔291 跳变,
   UIKit 会跟着重新夹紧 `contentOffset` —— **用户原地没动却突然离底 200 多点**。
   所以"贴住哪一头"要**锁住**,只认手指反向解锁。
3. **彩蛋页是单向死胡同**(`currentWebViewController` 是 `as? WebViewController`,彩蛋页不是)。已修。
4. **「全部未读」会把读过的移出列表**,`articles.firstIndex(of: 当前这篇)` 随时失败。
   下一篇未读已加退路;上一篇改成**来路栈**(`nnwPageBackStack`)。
5. 门槛 84、缓动 `1−(1−t)^1.8`、起显 8pt、推开上限 72 —— 都是用户逐条定的。

### 🔴 仍然没解决的两件(下一轮的活)

#### ① 标题和正文一直抖动

**已确认的病根**:我给翻页容器加的 `transform` 平移(推开正文)**会真的改变子视图的安全区**,
于是 `ArticleHeaderBar` 里那条 T24 自愈("host < window 一定是系统没传播")每帧触发一次
`setNeedsLayout` → 重排 → 下一帧再触发。真机日志里刷了 150+ 行。

⚠️ **我修了两次都没修对**:
- 第一次:`host as? ArticleViewController` —— **`host` 其实是 `WebViewController`**,恒为 nil,口子从没打开(L132)。
- 第二次(当前代码):改成沿 `parent` 链往上找 `ArticleViewController`。**没验证,用户说问题还在。**

📌 **下一轮第一件事:埋一行日志确认 `isPullingToTurnPage` 在拽的过程中真的是 `true`。**
别再靠读代码推断 —— 这条路上我已经错了三次(L132)。

#### ② 向下翻篇后再往上翻,只触发动画但翻不动

⚠️ **注意**:我逐条核对了用户最后那份真机日志,**里面每一个 `拽够=top` 后面都跟着 `article didSet`**,
也就是**那份日志里往上翻其实每次都成功了**。所以有两种可能:
- (a) 用户测的是修复前的版本;
- (b) 抖动风暴(每帧强制重排)**本身就在打断翻页动画**,两件事是同一个因。

📌 **下一轮的判据很简单**:修好抖动之后再看 —— 日志里 `[拽] 松手 拽够=top` 后面
**有没有** `ArticleViewController: article didSet`,一眼就能分辨"没触发"还是"触发了没换成"。

### 💡 一个应该认真考虑的选项:**砍掉"推开正文"那一层**

「用 transform 推开正文」是**抖动、安全区风暴、翻页动画可能被打断**这三件事的**共同来源**。
去掉它能一次性消掉一整类问题,代价只是箭头压在正文边缘、而不是在让出来的空隙里。

⚠️ 这是**用户的设计决定,不要擅自砍**。但下一轮如果修抖动又要花两三个来回,
应该把这个选项摆到用户面前:**"留着这个效果要再花 N 轮,砍掉现在就能干净,你选。"**

### 🔧 现场留了什么

- **探针 `[拽] 松手 …`**(`nnwLogOverscrollDiagnostics`)**留着**,category `NNWArticlePaging`。
  它会打出:拽够了哪一头、峰值、滚动视图的全部几何。**这是这条路上最有用的工具,别急着删。**
- **探针 `[对齐] …`**(`nnwLogGlassAlignment`)也留着,category `NNWGlassAlign`。
  文章列表页的对齐已确认(44/44/822);**首页那条三档浮层在 44pt 之后还没复验过**。
- 看日志:`xcrun simctl spawn <UDID> log show --last 3m --info --predicate 'category == "NNWArticlePaging"' --style compact`
  ⚠️ **必须带 `--info`**。
