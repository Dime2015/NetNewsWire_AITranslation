# Babel 当前状态

> 基准日期：2026-08-27
>
> 用途：接手项目时的短版事实入口。功能历史看 `NOTES-progress.md`，未提交文件归属看 `WORKTREE-LEDGER.md`。

## 一句话判断

Babel 是基于 NetNewsWire 的 iOS/macOS RSS 阅读器 fork，产品主线已经从“给现有阅读器加翻译按钮”扩展为四个相互关联的系统：订阅发现与试读、正文和标题翻译、阅读交互、订阅组织与视觉定制。当前主要风险不是缺功能，而是 **2026-08-09 之后的多批功能和修复仍混在一个 44 文件的未提交工作区里**。

## 当前 Git 基线

- 分支：`design/custom-dock`
- HEAD：`e2b8fd153`（2026-08-09，`[文档] T55 收尾:翻页功能已好用,抖动挂账;重写 NEXT-PROMPT 供换方向重来`）
- 与上游分支比较：本地领先 6 个提交，落后 0 个提交
- 接手前工作区：37 个已跟踪文件有修改，7 个新文件未跟踪，共 44 个文件；本阶段另新增本文件和 `WORKTREE-LEDGER.md`
- 已跟踪差异：增加 2635 行、删除 379 行；新文件未计入这个数字
- `NEXT-PROMPT.md` 是历史交接材料，不再代表当前工作区全貌

## 当前产品结构

### 1. 订阅发现与试读

- 输入网址时按来源类型解析；输入关键词时并行搜索网站、播客、YouTube、Reddit。
- 搜索结果按“网站 → 播客 → YouTube → Reddit”分组，网站默认展开，其余默认收起。
- Reddit 和 YouTube 凭据保存在 Keychain，并有独立设置页；网站关键词搜索依赖 Feedly 的非公开搜索端点。
- 搜索结果可直接订阅或取消订阅；点结果进入不落库的试读流程。
- 首次试读加载失败改为页内提示，下拉重试失败才弹出菜单，减少转场中弹模态层的风险。

### 2. 翻译系统

- 正文采用分组翻译：首组流式显示，后续组并发完成；支持缓存、断点、恢复原文和重新翻译。
- 标题翻译有独立批处理队列；内容格式错误会拆批重试，避免一条异常标题拖垮整批。
- HTML 分组把媒体及包裹媒体的元素视为物理边界，修复翻译后图文重排；每组最多 6 个顶层元素。
- 应用译文前检查顶层元素数量，模型少还段落时拒绝替换，保留原文并进入失败/重试路径。
- 翻译中使用骨架色条；首组流式呈现为“译文逐步吃掉剩余占位色条”。
- 点翻译不再改变滚动位置；翻到一半也允许长按从头重翻。

### 3. 阅读与导航

- 文章页右滑返回列表；左滑松手后以普通系统方式打开原文。
- 原文浏览器使用系统自带的左缘右滑关闭。此前的自定义交互式滑入已删除，因为它会覆盖系统返回手势。
- “顶部/底部拽过头翻篇”已封存：实现仍保留，但竖向手势不再安装。
- 切换阅读模式会回到顶部，避免把两套不同排版中的像素位置错误对应；代价是长文中途切换时不保留阅读位置。
- 阅读提取会清理明确隐藏的内容、归一化懒加载图片，并为翻译分组保留媒体结构。

### 4. 订阅组织与界面

- 新订阅源获得负权重并排到当前容器顶部。
- 单源设置页底部增加“取消订阅”；全局 feed 相关中文措辞从“删除”改为“取消订阅”，文件夹仍使用“删除”。
- 列表在图片尚未下载时先预留缩略图位置，减少文本重新排版。
- 外文源图标的深色处理按原 alpha 裁切，避免透明边缘出现灰底。
- 文章头像与阅读进度环改走相同的 UIView 几何提交路径，以减少快速滚动时的错位。
- 浅色/深色画布当前都回到暖纸方案；如果只想让浅色使用暖纸，需要拆分全局开关。

## 证据状态

| 项目 | 当前结论 | 证据边界 |
|---|---|---|
| iOS 完整构建 | 通过 | 2026-08-27 使用 Xcode 27 beta、Swift 6.2、iOS 27 模拟器构建通过 |
| macOS 构建 | 通过 | 2026-08-27 无签名构建通过 |
| JS 语法 | 通过 | 2026-08-27 将 Swift/Clang 模块缓存指向临时目录后，项目检查覆盖 5 个脚本并全部通过 |
| 本地化注释检查 | 通过 | 项目现有检查通过 |
| diff 空白/冲突标记 | 有 1 项待清理 | `Shared/ReaderView/ReaderViewExtractor.swift:320` 文件末尾有新增空行；本阶段不改业务文件 |
| 统一发现与试读流程 | 用户已真机验收 | 2026-08-27 用户确认下一阶段列出的验收项目均无问题，包括当前统一发现与试读版本 |
| 浏览器右滑返回的产品取舍 | 用户已真机验收 | 用户选择系统右滑返回，放弃自定义跟手滑入；2026-08-27 确认当前真机行为无问题 |
| 预览失效源不再锁死 | 用户已真机验收 | 第一版守卫曾被真机日志证伪；当前第二版转场等待和兜底方案于 2026-08-27 确认无问题 |
| 单源设置页取消订阅 | 用户已真机验收 | 第一版静态 section 曾导致崩溃；当前 `tableFooterView` 重写于 2026-08-27 确认无问题 |
| 翻译完整性、骨架动画、进度环、缩略图及相关视觉 | 用户已真机验收 | 2026-08-27 用户确认下一阶段列出的真机验收项目均无问题 |
| Feedly / Reddit / YouTube 搜索可用性 | 未做 2026-08-27 在线复测 | 代码路径和错误降级存在；外部接口、配额和凭据状态可能变化 |

## 当前最高风险

1. **提交边界缺失**：44 个文件混合了发现、翻译、阅读、订阅管理、视觉和文档，直接整体提交会让审查、回滚和定位回归都变困难。
2. **历史记录把中间方案和终局混写**：例如浏览器手势、骨架动画、预览卡死都经历多轮推翻，只读较早段落会得到错误结论。
3. **验收记录仍需保持版本绑定**：2026-08-27 的真机验收覆盖当前工作区；后续拆分或修改这些文件后，需要按受影响功能做定向回归。
4. **外部服务依赖**：网站关键词搜索使用非公开 Feedly 端点；Reddit 和 YouTube 需要用户凭据。失败应保持局部降级，不能拖垮其他搜索组。
5. **上游合并冲突面**：37 个已跟踪文件里包含多处上游核心控制器。拆提交前需要按功能切 hunk，不能只按文件粗暴分组。

## 下一步执行顺序

1. 按 `WORKTREE-LEDGER.md` 的 9 个功能包拆分暂存范围，先处理边界清晰、依赖少的包。
2. 每个包单独执行静态检查和对应构建；翻译包另外跑 JS 检查。
3. 拆分过程中保持当前行为不变，每个功能包完成后只做受影响范围的定向回归。
4. 形成最终提交序列；提交和推送需要单独授权。
