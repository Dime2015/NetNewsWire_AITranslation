# Babel 当前状态

> 基准日期：2026-08-28
>
> 用途：接手项目时的短版事实入口。功能历史看 `NOTES-progress.md`；2026-08-27 的拆分过程看 `WORKTREE-LEDGER.md`。

## 一句话判断

Babel 是基于 NetNewsWire 的 iOS/macOS RSS 阅读器 fork。产品主线包含订阅发现与试读、正文和标题翻译、阅读交互、订阅组织与视觉定制。上一版记录中的“44 个文件尚未提交”已经解决：9 个功能包已拆成 10 个可审查提交，经真机验收和完整测试后通过 PR #1 合入 `main`。

## 当前 Git 基线

- 默认分支：`main`
- 稳定合并提交：`a68656837658ad959807cb4fb4887487e8daf9c5`
- 稳定标签：`babel-stable-2026-08-28`
- 合并记录：PR #1，`design/custom-dock → main`
- 功能提交：从旧基线 `e2b8fd153` 到功能分支末端 `988da4152` 共 10 个提交；合并时保留提交历史
- `design/custom-dock` 仍保留，便于追溯；日常开发应从最新 `main` 新建分支
- 合并并同步后，本地 `main` 与 `origin/main` 一致，工作区无未提交文件

## 当前产品结构

### 1. 订阅发现与试读

- 输入网址时按来源类型解析；输入关键词时并行搜索网站、播客、YouTube、Reddit。
- 搜索结果按“网站 → 播客 → YouTube → Reddit”分组；可直接订阅、取消订阅或进入不落库试读。
- Reddit 和 YouTube 凭据保存在 Keychain；网站关键词搜索依赖 Feedly 的非公开搜索端点。
- 首次试读加载失败使用页内提示；共用菜单会等待页面转场完成，并有防止透明浮层锁死页面的兜底。

### 2. 翻译系统

- 正文首组流式显示，后续组并发；支持缓存、断点、恢复原文和重新翻译。
- HTML 分组保留媒体位置，每组最多 6 个顶层元素；模型少还段落时拒绝覆盖原文并进入失败/重试路径。
- 标题批处理遇到内容格式异常时拆批重试，避免一条异常标题拖垮整批。
- 翻译过程使用骨架反馈；点翻译不改变滚动位置，翻译进行中也能从头重翻。

### 3. 阅读与导航

- 文章页右滑返回列表；左滑松手后用系统方式打开原文。
- 原文浏览器使用系统左缘右滑关闭。
- “顶部/底部拽过头翻篇”已封存，竖向手势不再安装。
- 切换阅读模式回到顶部，避免两套排版之间错误映射像素位置。
- 阅读提取会清理明确隐藏的内容、归一化懒加载图片，并为翻译保留媒体结构。

### 4. 订阅组织与界面

- 新订阅源排到当前容器顶部。
- 单源设置页提供“取消订阅”；feed 与文件夹使用不同的动作措辞。
- 列表预留缩略图位置，减少图片下载完成后的文字重排。
- 外文源图标按原 alpha 裁切；文章头像和阅读进度环走同一套几何提交路径。
- 浅色和深色画布目前都采用暖纸方案。

## 验证基线

| 项目 | 结果 | 证据边界 |
|---|---:|---|
| 用户真机验收 | 通过 | 2026-08-27，用户确认账本所列下一阶段验收项无问题 |
| macOS 完整测试计划 | 590/590 通过 | 2026-08-28，`NetNewsWire` 测试计划，0 失败、0 跳过 |
| iOS 测试计划 | 4/4 通过 | 2026-08-28，`NetNewsWire-iOS` 测试计划 |
| ActivityLog 定向测试 | 31/31 通过 | 2026-08-28，本地化敏感断言修正后复跑 |
| 项目离线逻辑脚本 | 4/4 通过 | 拖放落点、源排序、阅读档位、SSE 解析 |
| Git 状态 | 通过 | PR #1 合并后，本地与远端 `main` 无分叉且工作区干净 |

## 仍存在的风险和边界

1. **外部服务**：网站关键词搜索依赖 Feedly 非公开端点；Reddit 和 YouTube 依赖用户凭据、配额及服务策略。一次验收不能证明长期可用。
2. **自动测试环境**：项目当前使用 Xcode 27。GitHub 的 `xcode-27` runner 仍是公开预览，可能出现镜像变化、工具不稳定或排队；这类失败需要先区分平台问题与代码回归。
3. **完整 macOS 测试的运行环境**：ScriptingTests 会启动 App。此前本地首次运行曾被 Sparkle 首次询问窗口阻塞；自动测试使用跳过 ScriptingTests 的 CI 计划，完整计划仍应在发布前本地运行。
4. **外部接口冒烟**：Feedly、Reddit、YouTube 的在线链路没有在每次提交后自动调用，发布前仍需用真实凭据做一次小范围检查。
5. **视觉验收**：自动测试不能判断动画、深色模式、快速滚动和手势的实际观感；影响这些路径的后续改动仍需真机验收。

## 下一步执行顺序

1. 新 PR 等待 macOS、iOS 和 SwiftLint 三项 GitHub 检查通过后再合并。
2. 如需从机制上阻止未测试代码直接进入 `main`，在 GitHub 仓库设置中启用分支保护并把三项 CI 检查设为必需。
3. 新功能从最新 `main` 建独立分支，继续保持一个改动主题对应一个可回滚提交或 PR。
4. 发布前再跑完整 macOS 计划、iOS 计划和外部服务冒烟检查，并单独做真机视觉验收。
