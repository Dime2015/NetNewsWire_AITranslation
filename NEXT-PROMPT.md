我在继续 NetNewsWire iOS fork（已改名 Babel）的工作。项目在 `/Users/wenbopan/Downloads/RSS ai translation`

**请先按顺序读这几份文件，读完再动手：**

1. `CLAUDE.md` —— 项目规则。**但第 0 节第 7 条已被下面的「工作方式更正」推翻，以更正为准**
2. `NOTES-progress.md` —— 只读最前面的「📍 接手须知：2026-08-04」
3. `NOTES-todo.md` 的 **T40**（当前这一轮设计换血的完整来龙去脉、已完成清单、已知未做）
4. `NOTES-lessons.md` 的 **L100–L103** —— 这四条全是这一轮踩出来的，和接下来的工作直接相关

---

## ⚠️ 第一件事：搞清楚你在哪条分支上

```
main                停在 649f85fd5，这一轮的设计改动一行都没进去
tag genesis         = 649f85fd5。用户说「回滚到创世版本」= git checkout genesis
design/soft-dock    ← 当前工作分支，所有视觉改动都在这里
design/soft-shell   被否决的设计师方案，留档，别捡起来用
```

**开工前先 `git branch --show-current` 确认在 `design/soft-dock` 上。**

---

## ⚠️ 工作方式的一条硬更正（这一轮最贵的教训，L100）

CLAUDE.md 里写着「别用操作电脑去点模拟器验收」——**那条规则的理由是"太慢太费 token"，
针对的是通用 computer-use**。但本环境有**专门的 iOS 模拟器工具**（截图 + 按坐标点按），
又快又便宜。

**所以：任何视觉改动，交付前必须自己先截图看过。**

上一个窗口我连着四轮盲交（改完就装机让用户看），用户连说三次"不行"才点破我有这个工具。
改用自己看之后，**一轮就定位了三个真缺陷**（渐变画成矩形、冷灰色号糊在暖纸上、图标根本没换）。

配套工作流（都在 scratchpad 里做过，可重建）：
- `xcrun simctl io booted screenshot` 取图
- 写个 30 行的 Swift 取色工具按坐标打印色号（**视觉还原不要目测，要采样** —— L101）
- 用模拟器工具点按导航到目标页面，再截图

**其余验收分工不变**：编译、装机、看日志、查数据库仍然归你；
"这个好不好看"仍然归用户。

---

## 当前的设计方向（用户三轮拍板后收敛的）

参考图在 `external resources/references/`（三张，软面板风格）。

**做**：
- 内容**通铺**，不加卡片（用户明确否掉了订阅源抬头卡、文章抬头卡、正文 sheet）
- **只有 dock / 三档 / 选单是软面板**：0.55pt 上缘 hairline + 极淡宽阴影 + 由上向下变亮
- 图标**全部手绘**（`NNWDockIcons`，24pt 画布 / 2.1pt 圆头），不用 SF Symbols
- 橙色 `#FF5A1F` 铺开作为强调色（用户当轮的原话是"都变成橙色的"）

**别做**（都被否过）：
- 全局换肤 / 页面外壳卡 / 正文装进卡片
- 把参考图的**绝对色号**搬进来 —— 必须**相对于 app 自身底色推导**（L102）

---

## 接下来可以做的（按我的建议排序，未与用户确认）

1. **深色模式一次都没验过** —— 材质里写了深色分支，但从没在深色下截图看。
   这是最可能藏问题的地方，建议先做。
2. **文章列表页（时间线）的行**还没动。设计稿那版是「橙色时间 → 粗标题 → 灰摘要」，
   要不要做没定，建议先问用户。
3. 设置页 / 发现页 / 试读页的控件还是系统图标，没重绘。
4. 橙色的覆盖面如果用户日后觉得过了，收敛成本很低（图标是模板图，改 tintColor 一处）。

---

## 硬约束（CLAUDE.md 里都有，这里只列最容易忘的）

- **一律中文回复，代码注释也用中文**（写给读不懂代码的用户看）
- **最高优先级是保持可 merge**：优先新增文件；改上游要最小化并带
  `[翻译]/[外观]/[管理]/[发现]/[链接]/[长图]/[品牌]/[阅读档]/[编辑]` 标记
- **每次改完自己编译**：
  `xcodebuild -project NetNewsWire.xcodeproj -scheme NetNewsWire-iOS -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17' build`
- **装模拟器只用 `./tools/install-to-simulator.sh`**
- ⚠️ **编译和装机分两步跑**，别串成 `xcodebuild ... && 装机`（`&&` 判断的是前一条的退出码，
  编译失败会把旧产物装进去）
- ⚠️ **`Shared/` 目录会被扩展目标编译，那里看不到 `iOS/` 的类型**（L103，这一轮踩了两次）
- ⚠️ **底部工具栏的 `toolbarItems` 结构不能动**：有 `expectedItemCount == 3` 的守卫，
  且 `addNewItemButton` 是 storyboard 的 IBOutlet。只能换 `image` 和 `tintColor`
- **push 我说了才做**

## 这个项目反复验证有效的排查方法

- **改了没生效时，第一件事是证明"跑的确实是新代码"**（L41）
- **别猜，埋日志**；埋的时候把相关状态一次打全（L94）
- **视觉的事别猜，截图 + 采样**（L100 / L101）
- **能抽成纯逻辑的部分自己先离线验**：仓库里有现成的仿真脚本，改了对应规则必须重跑
- **交付前跑一轮独立审查**：前几次分别抓到 5 / 8 / 3 / 2 / 1 个真 bug
- **一个修法开始在下游长出新症状，说明它引入的不是修复，是耦合**（L96）
