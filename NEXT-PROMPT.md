> ⚠️ **2026-08-11 更新：本文件描述的「拽过头翻页」功能已封存**(用户:不常用,先关掉,
> 以后有空再查抖动)。关闭方式、能复用的部分、卡住的地方、还没查的方向,
> 全部整理进了 `NOTES-archive-overscroll-paging.md`——**要接着查抖动,直接读那份,
> 不用读本文件剩下的部分。** 本文件以下内容原样保留,仅作历史记录。

我在继续 NetNewsWire iOS fork（Babel）的工作,项目在
`/Users/wenbopan/Downloads/RSS ai translation`

**这一轮的任务：换个方向重查「文章页翻页时的抖动」。**(已封存,见上方更新)
前一轮（2026-08-09，共二十轮）已经排除了四个候选，全部记在下面。**别重走这四条路。**

---

# 0. 先读这几份（顺序别乱）

1. `CLAUDE.md` —— 项目规则。⚠️ **第 0 节第 7 条必须读**（界面验收交给用户，不许自己点）
2. 本文件剩下的部分（问题、已排除清单、当前结论、猜测）
3. `NOTES-lessons.md` 的 **L135 / L136 / L137 / L138** —— 全是这条抖动线上摔出来的，四条都短，务必读完
4. `NOTES-todo.md` 的 **T55**（完整来龙去脉，二十轮的全部数据都在里面；要挖细节再读）

---

# 1. 🔴 三条硬规矩（这一轮最容易违反的）

## ① 界面上的点按与截图验收 —— **交给用户**

覆盖**一切驱动界面的工具**（iOS 模拟器 MCP 的 screenshot/tap/swipe、computer-use、claude-in-chrome）。**默认一律不用。**

| 事情 | 谁做 |
|---|---|
| 写代码、`xcodebuild` 编译 | **你** |
| `xcrun simctl install/launch` 装机 | **你** |
| 看日志、查数据库、`curl`、埋探针 | **你**，一个都别省 |
| **在界面上点、看效果** | **用户** |

判据：规则的理由是"太贵"，换个工具不改变成本（L126）。

## ② 🔴 小步 + 马上让用户看一眼 —— **这一轮的头号规矩**

**上一轮最贵的教训（L138）**：我攒了一个完整假设 → 做一次大改 → 用户跑真机 → 「一点没变」。
来回八次真机，大半花在错误方向上。

✅ **这一轮反过来做**：先用**最糙的探路改动**（哪怕丑、哪怕临时）让用户一眼判定方向，
方向对了再谈实现。**验证成本落在用户身上时，要优化的是"让他判定的间隔"，不是"假设的完备度"。**

## ③ 加了带条件的钩子 → 同一轮内埋日志、跑一次、回读

L124/L132 的老账，一天犯过三次。`if` / `guard` / `as?` 才生效的东西，
**没有"它被走到了"的正面证据就别说做好了。**

⚠️ 附带（L137）：**`ArticleHeaderBar.swift` 文件头有一段硬约束，改它的核心函数前回去重读一遍** ——
「只允许改 `transform`/`alpha`，**绝不在滚动回调里碰 `contentInset`/`safeArea`**，
内容下推只在绑定文章时做一次」。我违反过一次，**app 白屏假死**。

---

# 2. 环境与 git

## 设备

```
SIM27 = 87C9AE05-88C0-44E6-A871-242FF782D949   # iPhone 17 (iOS 27)，用户在看的就是这台
SIM26 = A7B1AE1F-0391-4C3A-B979-FA35653256FF   # iOS 26，保持同步即可
```

⚠️ 仍用 Xcode 26.6 编译（`xcode-select` 别动），产物装到 27 上跑。
⚠️ 编译和装机**分两步**，别用 `&&` 串 —— 编译失败会把旧产物装进去。

```bash
xcodebuild -project NetNewsWire.xcodeproj -scheme NetNewsWire-iOS \
  -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17' build

BUILT=$(find ~/Library/Developer/Xcode/DerivedData/NetNewsWire-*/Build/Products/Debug-iphonesimulator -maxdepth 1 -name "NetNewsWire.app" -type d | head -1)
xcrun simctl install 87C9AE05-88C0-44E6-A871-242FF782D949 "$BUILT"
```

**真机**：用户自己 ⌘R（会重新编译）。他把 Xcode 日志整段贴回来 —— 那是最有价值的输入。

## 看日志

```bash
xcrun simctl spawn 87C9AE05-88C0-44E6-A871-242FF782D949 log show --last 3m --info --predicate 'category == "NNWArticlePaging"' --style compact
```
⚠️ **必须带 `--info`**，否则 `Logger.info` 不落盘。

## 不点界面也能进文章页（L133，能省很多来回）

```bash
xcrun simctl openurl <UDID> "netnewswire://x?id=<文章ID>#nnw://showunread"
```
文章 ID 从数据库取：
```bash
DB=$(xcrun simctl get_app_container <UDID> com.wenbopan.NetNewsWire.iOS-DEBUG data)/Documents/Accounts/OnMyMac/DB.sqlite3
sqlite3 "$DB" "select articleID, substr(title,1,30) from articles limit 5;"
```
⚠️ **只能打开文章，替代不了"拽过头翻页"的手势** —— 那条路只有手指能走。

## git

```
分支：design/custom-dock
最新：3517660aa（抖动这条线暂停，代码=只有探针、没有垫付）
```

回滚点：
- 「回到抖动这轮之前」→ `5ed408517`（44pt 统一那一版，手势之前）
- 「回到创世版本」→ `git checkout genesis`

---

# 3. 问题是什么

文章页有一套「拽过头翻篇」手势：在正文**顶部**再往下拽 → 翻到上一篇；在**底部**再拽 → 下一篇。
松手后播一段**整页竖向推移**的翻页动画。

**症状**：翻页成功时，**标题和正文抖动**。

🔴 **用户给的最强约束（双向全称命题，这一轮的地基）**：

> 「如果最后真的成功滑到上一篇，那就**肯定会抖**；
> 如果不抖，那就是只触发了动画、但**没成功**滑到上一篇。」

即 **抖 ⟺ 翻页成功**。

---

# 4. ✅ 已经修好且用户验收过的（别回头改）

| 东西 | 状态 |
|---|---|
| **门 = 路**：守门条件和 `nnwGoToPreviousArticle` 的四条路一一对应 | 实测救回过真实翻页 |
| **进门锚**：换文章那一刻记下「列表里它前面那一篇」 | 已验收 |
| **进门锚 · 按日期兜底**：列表里找不到自己时（状态恢复进来的已读文章）按日期补 | **真机实测救回过一次翻不动** |
| **「到头」和「失联」分开画线**：完整平线 vs 中间缺一口的线 | 已落地，`lost` 那条还没被眼睛验过 |
| **翻页收尾双路 + 代际令牌**：系统回调**实测会漏**，0.9 秒兜底救回过多次 | 已验收 |
| **重入守卫**：动画没放完时拒绝新的翻页请求 | 已验收 |

---

# 5. ❌ 已排除的四个候选（**别重走**，每条都有实测证据）

| # | 候选 | 怎么倒的 | 详见 |
|---|---|---|---|
| 1 | `syncInset()` 反复改内边距 | KVO 抓栈：**一次翻页只写一次**，且没有第三方在改 | L134 |
| 2 | 解冻那一帧标题突变（冻结飞行进度的副作用） | 12 次翻页 `跳变=0.000000` | 第十五轮 |
| 3 | 宿主安全区 62→116 摆 54pt | **频率对不上**：20 次里只出现 5 次，而症状是每次都有 | L135 |
| 4 | **滚动视图安全区 0→116 晚到**，正文在动画中途整体掉 116pt | **实测存在**（11/12 次）、**频率完全吻合**、**修掉之后用户说「一点没变」** | L138 |

⚠️ **第 4 条要特别注意**：它**不是猜的**，探针直接量到
`滚动视图安全区 首0→末116 | offset 首-175→末-291`，正文确实掉了 116pt。
**它是真实存在的位移，只是不是用户看见的那个抖。**
→ 一个系统里可以同时存在好几个"真实、同频、但无关"的运动。

### 🔴 由此得到的方法论（L138，这一轮的核心）

「必须每次都发生」是**必要条件，不是充分条件**。
一次成功翻页里"每次都发生"的东西多得很：新建 WebView、装载网页、
`setViewControllers` 的整页动画、写内边距、安全区到货、协调器换选中……**全都通过这个筛子**。

**频率筛只能把候选缩到一个"类"，不能定位到"个"。
从类到个，唯一的一步是"改掉它 → 症状变没变"，而那一步只有用户的眼睛能做。**

---

# 6. 当前结论

- 抖动的真凶**仍未定位**。
- 四个候选已排除，其中一个（安全区晚到）是**真实位移但与症状无关**。
- 代码目前处于 **"只有诊断探针、没有任何抖动修复"** 的状态，功能正常、不卡死。
- 翻页功能本身（第 4 节那些）是这条线上**真正的收获**，已经好用。

---

# 7. 猜测（**都是猜测，没有一个被验证过**）

## 🎯 A. 翻页动画本身 —— 唯一还没查过的大件（最值得先探）

翻页时**有两层位移同时在动**：
1. `setViewControllers(direction:animated: true)` 的**整页竖向推移**（系统在动真视图）
2. 我们自己叠在上面的 `nnwSetContentPushBack()` —— 拽的时候给**翻页容器**加 `transform` 把正文推开给箭头让位

⚠️ 第五轮曾提过「砍掉推开正文那一层」，**用户当时没选**（是他的设计决定，别擅自砍）。
但作为**探路**，临时关掉它让用户看一眼是极便宜的。

## B. 头区高度在动画期间变化

日志里 `头区容器高` 确实变过（如 `首237→末291`）。
`container.frame.height = safeTop + measuredHeight`，而 `measuredHeight` 要等标题渲染完才稳定。
虽然飞行进度已经冻住，但**头区容器本身的高度变了，标题块就会动**。

## C. 高频重排

一次翻页 `排版=27~129 次`。飞行进度冻住后它不再改变标题的"飞行状态"，
但 `applyGeometry` 每次都在重设几何 —— 如果输入里有任何一个还在动，就会体现出来。

## D. 也许"抖"根本不是元素位移，而是掉帧 / 动画不平滑

**这一条最省事也最容易被忽略**：二十轮全在找"哪个元素在动"，
但从没确认过用户说的"抖"到底是**位移**还是**卡顿**。

---

# 8. 🎯 建议的第一步（按第 ② 条硬规矩：小步 + 马上看）

**先问用户两个问题，一句话就能大幅收窄范围**：

1. **抖的是什么？** 标题在动、正文在动、还是整页一起动？
2. **是"位置跳一下"还是"动画卡顿/掉帧"**？抖一下还是连续抖几下？
   （如果能录一段慢动作屏录最好，但别强求）

**然后做一个最糙的探路**（不要做完整实现）：

- 临时把 `nnwSetContentPushBack()` 整个短路成空操作（一行 `return`），装机让用户翻两页
  → 抖动消失/明显变小 = 方向对了，再谈怎么把这个效果做对
  → 一点没变 = 排除猜测 A，接着探 B/C

⚠️ **别在探路阶段追求实现优雅，也别一次探两个变量。**

---

# 9. 现场留下的探针（都还在，`category == "NNWArticlePaging"`）

| 探针 | 打什么 |
|---|---|
| `[拽] 松手 …` | 拽够哪一头、峰值、滚动视图全部几何 |
| `[拽] 上一篇 闸门/来路栈/我这页在第/进门锚/画的线/上游` | 四条路各自的状态 |
| `[拽] 让路探针` | 只在翻转时打；**沉默也有含义** |
| `[拽] 翻页期间 排版=N \| 飞行进度 \| offset \| inset上 \| contentH` | 动画期间每个会变的量的范围 |
| `[拽] 解冻 冻结值→真实值 \| 跳变 \| 安全区 \| 头区容器高` | 解冻那一帧 |
| `[拽] 安全区来源 / 滚动视图安全区 / 夹后总量` | 各安全区来源的首末 |
| `[拽] 改内边距 / inset被改（含调用栈）/ 内边距探针已装` | 谁在改内边距 |
| `[拽] 进门锚 …按日期兜底…` | 按日期兜底有没有被走到 |

⚠️ 这些探针**很啰嗦**。如果这一轮不需要，可以让用户点头后一次性清掉
（它们本来就标着"查完就删"）。⚠️ 但 `[拽] 松手` 和 `[拽] 上一篇` 是这条路上最有用的两个，
**要删也留着这两个**。

---

# 10. 其他挂着的账（与抖动无关）

- **标题批量翻译大批量失败**：同一模型同一时刻，小批成功、大批失败
  （`标题批量翻译失败:11 条 — 翻译服务返回了无法识别的内容`）。
  多半是批量大了模型吐不出合法 JSON。修法方向：先把失败响应原文落日志看一眼。
- 日志里偶发 `[拽] 翻页请求被忽略`、`排版=129 次` —— 不影响功能，先记着。

---

# 11. 硬约束（最容易忘的）

- **一律中文回复，代码注释也用中文**（写给读不懂代码的用户看）
- **最高优先级是保持可 merge**：优先新增文件；改上游最小化并带
  `[翻译]/[外观]/[管理]/[发现]/[链接]/[长图]/[品牌]/[阅读档]/[编辑]/[阅读位置]/[外文]/[阅读]` 标记
- ⚠️ **别用 `NSLocalizedString` 给本 fork 自己的控件加字符串**（会往上游的
  `Localizable.xcstrings` 里自动加条目，平白增加冲突面）。**直接写中文。**
- ⚠️ **`Shared/SmartFeeds/`、`Modules/Account/` 是 A 级禁区**，一行都别改
- ⚠️ **底部工具栏的 `toolbarItems` 结构不能动**（`expectedItemCount == 3` 的守卫）
- ⚠️ **改 `translation.js` 之后跑 `swift tools/check-js-syntax.swift`**
- ⚠️ **改翻译提示词要把 `TranslationCache.promptGeneration` +1**
- **每次改完自己编译**（scheme `NetNewsWire-iOS`）
- **commit / push 我说了才做**；记录文件和代码在**同一个 commit** 里（CLAUDE.md 第 9 节）

---

# 12. 这一轮我想做的

（在这里写。如果就是按第 8 节探路，写「按 NEXT-PROMPT 第 8 节开始探路」即可。）
