# NOTES-lessons.md — 经验教训

> 本项目踩过的坑和吃过的亏,按时间顺序编号。
> **接手的 AI 请先通读一遍** —— 这里每一条都是真实付出过代价的,
> 不读的话大概率会再付一次。
> 格式:现象 → 教训 → 怎么应用。

最后更新:2026-07-23

> 目录尾部新增:**L47**(派生文本没有刷新时机)、**L48**(`URL(string:)` 判不出错地址)、
> **L49**(构建产物换身份,装机脚本装出双胞胎 app;并更正 L41 的"持续卡死"结论)、
> **L50**(本工程 CoreImage 必编译失败,模糊用缩放法)、
> **L51**(上游取图 API 会悄悄退回按网址哈希染色的占位图标)、
> **L52**(我连续两次误判根因,把中间产物导出来看一眼才定案)、
> **L53**(异步到货的数据不能「问一次就算数」;四层原因叠加才让高清图抓不到)、
> **L54**(调深浅要在 HSB 走明度轴,RGB 混黑白会把饱和度一起冲掉;指标全绿≠好看)、
> **L55**(升级逻辑只比一个维度,用更大的坏东西换掉了更小的好东西)、
> **L56**(导航栏画在控制器 view 之上;自绘标题的底必须和标题同层)、
> **L57**(iOS 26 的 navigationBar.bounds 含状态栏,拿它中点对齐会偏高 28pt)、
> **L58**(脚本 str.replace 误伤重复样板,把递归种进函数,app 启动即栈溢出)、
> **L59**(registerForTraitChanges 在 UIPageViewController 子树里不触发;设 nil 让系统自适应最稳)、
> **L60**(透明导航栏露出背后 WKWebView 底,web 深浅色不同步时浅色顶栏变黑)、
> **L61**(普通标题栏 scrollEdgeAppearance 设 nil 会回落成毛玻璃,永远没有透明态 ——
> 顶栏"不够透"折腾多轮的真凶)、
> **L62**(毛玻璃调"薄"要用 animator 的 fractionComplete 调浓度,不是调 alpha;
> 换材质换不出来的东西别一直换材质)、
> **L63**(滚动藏栏 × 顶栏跟踪滚动 = 28000 层递归栈溢出;两个功能共用同一个状态就会成环,
> 滚动回调里改布局必须防重入;**无法亲手操作的交互路径要做离线模拟,别把风险丢给用户**)、
> **L64**(L32 那个反引号坑二次重演,静默毁掉整层正文样式;`.js` 不参与编译,
> **改完必须跑 `swift tools/check-js-syntax.swift`**)、
> **L65**(拖放占位存在时改数据源必崩;另:订阅源顺序在上游模型里是 `Set`,
> 要拖动排序只能自己存一份并接管主列表的排序入口)、
> **L66**(对有内部状态机的系统 API,**调用顺序即语义** ——
> `coordinator.drop` 写在数据计算之前,导致排序整个失效;
> 锦上添花的改动风险不比修 bug 低)。

---

## L1 · 全新 clone 后第一次编译必失败

**现象**:`cannot find 'SecretKey' in scope`,两处报错都在 Feedly 相关文件。
**原因**:`SecretKey.swift` 被 .gitignore 排除,由编译期脚本生成,但生成时机晚于 Secrets 模块编译。先有鸡还是先有蛋,只在第一次出现。
**应用**:再编译一次就好。**绝对不要去"修"那两个报错的文件** —— 那是 A 级禁区,而且根本没坏。

## L2 · 仓库根目录的新文件夹不参与编译

**现象**:原计划把新代码放根目录 `Translation/`,但那里的代码 Xcode 根本看不见。
**原因**:本工程用 Xcode 16 的文件系统同步组机制,只有 `Mac/ iOS/ Shared/ Modules/ Widget/ Tests/ Technotes/ xcconfig/` 这 8 个文件夹里的新文件会自动进编译,根目录不在其中。
**应用**:所有新代码放 `Shared/Translation/`。**新增文件前先确认目标文件夹在同步组清单里**(grep pbxproj 搜 `PBXFileSystemSynchronizedRootGroup`)。

## L3 · 用探针文件实测,不要只靠推理

**现象**:「`Shared/Translation/` 下的 .js/.json 会不会被打进 app 包?」—— 光看工程文件推不出来。
**应用**:放一个一行的探针文件 → 编译 → 直接 `ls` 构建产物的 app 包看它在不在 → 删探针。**一次真实构建胜过十次推理**,本项目用这招验证过 3 次关键假设。

## L4 · NSLocalizedString 会污染上游的共享文件

**现象**:代码里用了 `NSLocalizedString`,编译后发现 `Shared/Localizable.xcstrings`(上游共用大文件)被 Xcode 自动塞进了我们的中文文案。
**教训**:这种"编译器自动改文件"的副作用,肉眼看代码根本发现不了,是 merge 冲突的隐形炸弹。
**应用**:本 fork **禁用 NSLocalizedString**,直接写中文字符串(单用户 app 不需要多语言)。每次编译后跑 `git status` 检查有没有意料之外的文件被改。

## L5 · 上游文件的 private 成员够不着 → 用"纯末尾追加"的扩展

**现象**:`webView`、`currentWebViewController`、`toolbarItems` 都是 private/内部的,`Shared/Translation/` 里的代码访问不到。
**应用**:必须动上游文件时,遵守两条:①能追加在文件末尾的绝不插在中间(上游改中间,我们改末尾,几乎不冲突);②每处改动都带 `[翻译]` 注释标记,方便 ⌘F 盘点。验证手段:`git diff --stat` 应该只见 `+` 几乎不见 `-`。

## L6 · Storyboard 是 XML,能不碰就不碰

**现象**:工具栏按钮实体定义在 `Main.storyboard` 里,直觉上加按钮要改它。
**应用**:上游自己就有"纯代码创建按钮"的先例(ArticleExtractorButton),照抄即可。设置页新行同理 —— 上游在代码里动态增删行(`accounts.count + 1`),跟着这个模式走,932 行的 Settings.storyboard 一个字符都没碰。

## L7 · LLM 的输出永远不能直接信

**现象**(全部真实发生过):把译文包在 ``` 里;把原文和译文一起吐出来(中英对照);整段原样返回不翻译。
**教训**:**提示词是软约束,代码才是硬的。** 提示词里明令禁止的事,模型照样会干。
**应用**:三层防线:①提示词明令禁止;②代码清洗(剥代码围栏、切掉回显的原文);③事后自检(JS 本地判断哪些组还是英文/混了原文,重翻)。每层都要保守设计,宁可漏抓不可误杀。

## L8 · "第一块翻完就点亮按钮"是错误的产品判断

**现象**:我以为提前点亮按钮能传达"已经开始了",实际用户的感受是"按钮说完成了内容却还在变,我不知道该不该等"。
**教训**:**状态指示器的语义必须诚实**:转圈=还在干活,实心=真的干完了。"内容在逐段变中文"本身已经是进行中的信号,不需要按钮抢戏。
**应用**:任何异步流程,完成态只在**全部**结束后设置。

## L9 · 均分的并行块对"顺序阅读"是错的

**现象**:其余内容均分 4 组,每组几千字符 → 用户读完先导块,要干等第 1 组(和最后一组一样大、一样慢),体验断粮。
**教训**:优化目标错了 —— 均分优化的是**总完成时间**,但用户按顺序读,真正该优化的是**第 N 块在被读到之前就绪**。
**应用**:渐进式分组:第 1 组最小(1000 字符),逐组翻倍,4000 封顶。供给节奏跟着阅读节奏走。

## L10 · 标题在正文容器外面

**现象**:只切 `#bodyContainer` 里的内容,标题(`.articleTitle`)漏翻了。
**应用**:改任何"处理正文"的逻辑时,记得问一句:**标题、日期、作者这些容器外的东西要不要一起处理?** 目前只处理标题,日期作者有意不翻。

## L11 · 游离的 Task 不会跟着父任务取消

**现象**:标题翻译用了独立的 `Task {}`,父任务取消时它还活着 —— 用户翻页后,它可能把上一篇的标题译文安到下一篇文章头上(WebViewController 是复用的)。
**应用**:凡是"发射后不管"的 Task:①保存句柄;②`defer { task?.cancel() }`;③任务内部在写回 UI 前检查 `Task.isCancelled`。三样都要。

## L12 · 主题会改 DOM 结构,选择器必须带兜底链

**现象**:8 个内置主题里,Biblioteca 的正文容器是 `body-container` 而不是 `bodyContainer`。只认一个 id 的话,用户换主题后翻译按钮静默失灵。
**应用**:`translation.js` 里所有选择器都是候选链(id → 变体 id → class → 变体 class → 标签名),全落空才报"主题不受支持"。**永远不要静默失败。**

## L13 · Shared/ 的代码会被 macOS 一起编译

**现象**:`TranslationController` 用了 UIKit,iOS 编译过了,macOS 版会挂 —— 而且平时只跑 iOS 根本发现不了。
**应用**:①UIKit 相关文件整个包 `#if os(iOS)`;②**每次改 Shared/ 后必须跑一次 macOS 免签名编译**(命令见 NOTES-progress 第六节)。macOS 常规编译会卡在签名,要加 `CODE_SIGNING_ALLOWED=NO` 系列参数。

## L14 · UIPageViewController 的翻页回调只覆盖手势

**现象**:按钮状态重置挂在 `didFinishAnimating` 上,结果只有手指滑动翻页会触发;右上角箭头、底部"下一篇未读"、列表点选都不走这个回调。
**教训**:修 bug 时别只堵报上来的那个口子 —— 要找**所有路径的必经之地**。
**应用**:挂在 `article` 属性的 `didSet` 上(一切换文章方式最终都经过它)。第二层保险:按钮点击时不信 Swift 记的状态,先问网页当前真实显示的是什么。

## L16 · "替换失败"被静默吞掉,表象是"某组总是最后才翻好"

**现象**:用户报告分块并行时第 1 组总是压轴才变中文,后面的组都好了它还是英文。
排查发现两个叠加机制:
① 组译文替换回页面(applyGroup)失败时,代码写的是 `_ = try? ...` —— 结果被扔掉,
不重试、不计数。这一组一直显示原文,**拖到最后的自检阶段才被救回来**,所以永远最后。
② 引用型博客(Michael Tsai / Daring Fireball,恰是用户的主要订阅)常有一整段
几千字符的 `<blockquote>`,顶层切分切不动它,谁分到它谁就巨慢。
**教训**:异步流水线里**每一步的失败都必须有去向**(重试/计数/上报)。
`_ = try?` 等于把故障扫进地毯下,最后以离奇的表象冒出来。
**应用**:①替换结果并入重试循环;②超大元素下钻一层按子元素切
(同一组绝不跨父节点,否则替换会插错位置);③模型返回裸文本时用原标签包回;
④加了每组"大小/耗时/结果"日志 —— **下次再有性能问题,先看 `[翻译]` 日志,
用数据定位,不猜**。

## L17 · 改提示词必须让旧缓存作废

**现象**:加了"专有名词保留英文"的新规则,但已缓存文章命中缓存直接秒开 ——
旧译文是按旧规则翻的,新规则永远轮不到生效,用户会以为改了没用。
**应用**:缓存键里加了 `promptGeneration` 版本号(TranslationCache.swift),
**每次大改提示词都要把它 +1**,全部旧缓存自动作废重翻。

## L18 · 缓存指纹不能取自"活的"DOM HTML

**现象**:用户报告缓存"时灵时不灵" —— 同一篇翻过的文章,有时秒开,有时明显重新请求。
**原因**:判断"原文变没变"的指纹取的是正文 innerHTML 的哈希。但页面加载后,
NetNewsWire 自带脚本会**异步**改 HTML(图片查看器装饰、懒加载等),
图片加载到哪张取决于用户点按钮的时机 → 同一篇文章两次取到的 HTML 不同 →
哈希不同 → 被误判为"文章更新了" → 重新翻译。
**教训**:**指纹必须取自稳定的东西。** "时灵时不灵"类 bug 十有八九是
输入里混进了时序相关的变量。
**应用**:指纹改为正文**纯文字**(normalizeSpace(textContent))的哈希 ——
文字才是我们翻译的对象,也不受图片装饰影响。同时缓存版本号 +1 清掉坏指纹的旧条目。

## L19 · customView 的工具栏按钮必须有尺寸约束,否则会被压成 0 宽永久消失

**现象**(2026-07-21 用户报告):文章页底部工具栏的"阅读视图"和"翻译"两个按钮同时消失;
一旦出现,之后**所有**文章都没有这两个按钮,重启 app 才恢复。
用户复现方式:反复进文章 → 退回列表 → 再进文章。

**定位过程(值得复用的方法)**:
1. 抓模拟器日志:`xcrun simctl spawn booted log show --predicate 'process == "NetNewsWire"'`
2. 关键证据:`Unable to simultaneously satisfy constraints` 里出现
   `'UIView-Encapsulated-Layout-Width' ButtonWrapper.width == 0` —— 宽度被算成 0
3. 对照消失的是哪些按钮:○☆⌄⬆️(标准 bar button)全在,
   📄阅读视图 和 💬翻译(**两个 customView 按钮**)全没 —— 一个不多一个不少

**原因**:两个按钮都只设了 `frame = 44×44`、没有任何尺寸约束,
宽度靠"图标撑出来的固有尺寸"。而两者在转圈状态都会 `setImage(nil)` ——
图标一没,固有尺寸变 0,iOS 26 的 Liquid Glass 工具栏把它算成 0 宽并**永久塌陷**
(ArticleViewController 是常驻实例,工具栏不重建,所以之后所有文章都受影响)。

⚠️ 阅读视图按钮是**上游自己的代码**,同样的毛病;我照抄它的写法等于复制了隐患。

**修复**:给两个 customView 都加死 44×44 约束
(`translatesAutoresizingMaskIntoConstraints = false` + width/height anchor)。
上游那个按钮的约束加在本 fork 的 `installTranslationButton()` 里,不动上游原有代码行。

**验证(受控对比)**:修复前日志里 `ButtonWrapper ... width == 0` 出现 **40 次**
(其中最早两次在启动过程中、用户尚未交互时);修复后同样的启动路径 **0 次**。

**教训**:
- **把 UIButton 当 `UIBarButtonItem(customView:)` 用时,一定要给显式尺寸约束**,
  永远不要依赖"图标撑出来的固有尺寸" —— 任何会把图标置空的状态都会让它塌掉
- "一旦发生就再也不恢复"这类 bug,几乎都指向**常驻对象上的一次性布局塌陷**,
  找那个不会被重建的东西
- 症状里"哪些坏了、哪些没坏"的**边界**是最强的线索:
  这次"只有两个 customView 按钮消失"直接锁定了原因

## L20 · 本地化:翻译只能写进上游的 .xcstrings,但可以做到"只增行"

**背景**(2026-07-21):把界面完全汉化,需要处理 436 条字符串。

**试过但失败的路**:"完全不碰上游文件,另起一份自己的中文表"。
Xcode 直接拒绝:`Localizable.xcstrings cannot co-exist with other .strings tables with the same name`。
**字符串目录和同名 .strings 不能并存,翻译只能写进上游那个目录。**

**关键手法 —— 复刻 Xcode 的 JSON 格式**:
Xcode 写 .xcstrings 用的格式很特殊(键与冒号间有空格、空对象带空行、**文件末尾没有换行**)。
用普通 `json.dump` 会把整个文件重排,`git diff` 变成"整份文件都改了",
`git pull upstream` 时冲突根本没法读。
`i18n/inject.py` 复刻了该格式,并在写入前自检"能否字节级还原原文件",不能就拒写。
结果:注入 436 条后 diff 只有新增行,逐 key 校验确认原有内容一字节未变。

**教训**:
- **要改一个由工具生成的文件时,先做"读出来再写回去,是否字节一致"的往返测试。**
  一致才动手。这个测试 10 分钟,能省掉后面无数次无法阅读的 merge 冲突。
- 末尾换行这种细节最容易被忽略 —— 往返测试会直接抓出来。
- 注入脚本要**双向报告**:哪些 key 没翻、翻译表里哪些 key 在目录里找不到。
  后者能立刻抓出弯引号/不间断空格抄错,不会静默失败。(实测 436 条零抄错)

**衍生发现**:
- Storyboard 分两类:在 `Base.lproj/` 里的可以纯新增 `<语言>.lproj/xxx.strings` 翻译(零上游改动);
  不在任何 `.lproj` 里的(本项目的 Settings/Account/Add/Inspector)必须先移动文件才能翻。
  判断方法:编译后看它落在 app 包根目录还是 `Base.lproj/` 里。
- 语言选择器的可选项要从 `Bundle.main.localizations` 动态读,**不要写死语言列表** ——
  以后加日语时选择器自动多一行,一行代码都不用改。
- 编译时 Xcode 会自动往 `project.pbxproj` 的 `knownRegions` 加一行新语言,这是必要的,不要还原。

完整操作手册见 `NOTES-i18n.md`。

## L21 · .xcstrings 的"设备变体":译文结构必须和英文一致

**现象**:界面上本该显示"On My iPhone"的地方,汉化后变成了字面的 `我的%@` —— 占位符没被替换。
**原因**:该字符串英文侧用的是 **`variations.device`** 结构
(iphone → "On My iPhone"、ipad → "On My iPad"、mac → "On My Mac"…),
**根本没有 %@ 这个占位符**。我按普通字符串翻成 `"我的 %@"`,自然没有东西可替换。
**教训**:翻译前**先看英文侧那一条是什么结构**。.xcstrings 里除了普通 `stringUnit`,
还有 `variations`(按设备 device / 按复数 plural)。译文必须用同样的结构,
否则不是显示错乱就是原样吐出占位符。
**应用**:`i18n/inject.py` 现在支持在翻译表里把值写成字典,
自动生成 `variations.device` 结构。检查是否还有其它变体条目:
`grep -l '"variations"' **/*.xcstrings`

## L22 · 改上游的界面前,先把「可调的数值」搬进自己的文件

**背景**(2026-07-21):用户要求改文章列表的样子。考古发现列表**不是 storyboard、
不是 Auto Layout**,而是逐帧手算坐标的 manual layout —— 字号、间距、颜色全是
`MainTimelineCellLayout.swift` / `MainTimelineCell.swift` 里的常量。这两个都是上游文件。

**现象(如果不管)**:每调一次字号就在上游文件里改一个数字。改十次就在上游文件里
留下十处散落的改动,`git pull upstream` 时每一处都是潜在冲突,而且冲突长得像
「上游把 12 改成 14,你把 12 改成 16」这种最难判断的样子。

**教训**:**改上游界面代码时,真正该做的第一件事不是改样子,是把「会反复变的数值」
搬到自己的文件里。** 上游文件的 diff 从此永远停在「一行换一行的引用」,
以后无论调多少次外观,上游文件都不再变。

**应用**:新建 `iOS/MainTimeline/TimelineStyle.swift`(本 fork 新增,上游不存在),
把常量按**原值**搬进去;上游文件里 `static let cellPadding = UIEdgeInsets(...)`
改成 `static let cellPadding = TimelineStyle.cellPadding`,每处带 `// [界面]` 标记。
规则已写进 CLAUDE.md 第 2 节「D 级 · 界面改造专用」。

**这一步的验收标准是「界面零变化」,而且必须真的去量**:
搬运时抄错一个数字,肉眼根本看不出来。做法是找一个**内容固定**的界面做对照 ——
本项目用的是「设置 → 文章列表布局」那两条预览(内容永远是 "At The Sign Of The
Prancing Pony"),改动前后各截一张图,去掉状态栏(时钟会变)后逐像素比。
**实测 280 万像素 0 处不同**,才敢说搬对了。
拿真实文章列表比是不行的 —— 已读状态、时间都在变,差异全是噪音。

## L23 · 正文页的样式可以「追加一层」,不必改上游的样式表

**背景**(2026-07-21):想改正文阅读页的排版。最直觉的做法是改
`Shared/Article Rendering/stylesheet.css` —— 但那是上游高频改动的文件。

**发现的通道**:`WebViewConfiguration.swift` 里有一个脚本清单
`let filenames = ["main", "main_ios", "newsfoot"]`,这些 .js 会在**每次页面加载前**
自动注入。往这个数组里加一个我们自己的脚本名(**上游改动 = 一行里加一个单词**),
脚本在页面加载完成后往 `<head>` 追加一个 `<style>`,就得到了一层我们完全控制的样式。
上游的 CSS 一个字都不用动,而且对 8 套主题同时生效。

**三个必须知道的细节(都是实测出来的,不是推的)**:

1. **样式必须插进 `<head>`,不能插进 `<body>`。**
   上游 `main.js` 的 `stripStyles()` 会把 `<body>` 里所有 `<style>` 全部删掉。
2. **同优先级时后加的赢,不需要 `!important`。**
   实测:我们的 `.articleTitle a:link { color: #cc0000 }` 盖过了上游同名同优先级的规则。
   这是整条通道的地基,所以专门用探针验证过,没有凭"CSS 应该是这样"下结论。
3. **标题颜色写在 `.articleTitle a:link` 上,不在 `.articleTitle h1` 上。**
   第一次探针写的是 `h1`,颜色没变 —— 因为标题文字被包在 `<a>` 里,
   子元素上的规则赢了父元素。**"没生效"不一定是通道坏了,也可能是选择器选错了对象。**

**方法本身值得复用(就是 L3 的探针法)**:先塞一条**故意难看**的样式
(正文底色刷成琥珀黄),截图确认真的变了,再清空。
如果一开始就写空 CSS,那么"界面没变化"既可能说明链路是通的,
也可能说明链路根本没接上 —— **两种情况长得一模一样,等于没验证。**

**衍生发现**:iOS 版**没有**正文字号滑块(那是 macOS 专属,
`ArticleRenderer.swift` 里 `text_size_class` 包在 `#if os(macOS)` 里)。
iOS 的正文字号来自系统的动态字体设置,由 `styleSubstitutions()` 注入成 CSS 的 `font-size`。
用户想调正文字号,要么改系统设置,要么由我们在这层 CSS 里覆盖。

## L24 · 换 app 图标:别覆盖上游的 PNG,改一行 xcconfig 指到自己的图标集

**背景**(2026-07-21):用户提供了一张新的 app 图标。

**最直觉但错误的做法**:直接覆盖
`iOS/Resources/Assets.xcassets/AppIcon.appiconset/` 里上游的三个 PNG。
后果:以后上游一换图标就是**二进制冲突** —— git 没法帮你合二进制文件,
只能手工挑一个,而且 `git diff` 完全看不出发生了什么。

**正确做法**:图标集的名字是一个**文本设置**,在
`xcconfig/common/NetNewsWire_ios_target_common.xcconfig` 里:
`ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon`。
所以另建一套 `AppIconCustom.appiconset`,把设置指过去就行,上游那套原封不动。

⚠️ **但不要改那个 common 文件** —— 小组件扩展和 Siri 扩展也 include 它,
它们各自的图标资源里没有 `AppIconCustom`。
应该改在**主 app 自己的** `xcconfig/NetNewsWire_iOSapp_target.xcconfig` 里追加一行:
因为它写在 `#include` 之后,会覆盖掉继承来的值,而且只影响主 app。

**核实手段**(不要凭"应该生效了"):
```bash
/usr/libexec/PlistBuddy -c "Print :CFBundleIcons" <app包>/Info.plist
# 应该看到 CFBundleIconName = AppIconCustom
xcrun assetutil --info <app包>/Assets.car    # 看实际编进去了哪几种外观
```

## L26 · app 图标素材必须是「满铺方图」,自带圆角会变成框里套框

**现象**(2026-07-21 用户指出):第一版图标素材本身就是一张**带圆角边框**的图。
直接拿去当 app 图标,iOS 又套了一层自己的圆角遮罩 —— 结果是**框里套框**,
整个图案往里缩了一圈,视觉上明显比旁边的系统图标"小一号",很难看。

**我当时漏掉了它,值得记下来为什么**:
我确实检查了,但我检查的问题是「**四角有没有毛刺**」——
我算了一下 iOS 的遮罩半径比原图自带的圆角切得更多,所以推断白色的角会被完全裁掉,
然后放大截图确认了角上确实干净,就下了"没问题"的结论。
**这个推理和这次观察都没错,错在问题问窄了。** 我核对的是"边缘有没有瑕疵",
而真正的毛病是"整个图案的占比不对" —— 一个只看角、不看整体的检查,永远发现不了它。

**教训**:
- **app 图标素材必须是内容满铺到边缘的正方形图**,不要自带圆角、不要留白边、
  不要自带投影。圆角由 iOS 统一加,加两次就是错。
- 更一般的:**验收视觉改动时,不要只盯着自己预判的那个风险点。**
  先整体看一眼"它和周围的东西放在一起协调吗",再去抠细节。
  我这次是先抠了细节,而整体那一眼从头到尾没看。

**顺带发现(第二版素材实测)**:满铺之后,靠近四角的内容会被 iOS 的圆角**真的切掉**。
第二版图右下角的 wifi 图形最外圈那道弧就被切断了。
所以"满铺"不等于"可以画到边":**重要元素要离边缘留出足够距离**
(iOS 的圆角半径约为边长的 22%,四角那一块基本都会没)。
验收方法:装到模拟器 → 主屏截图 → 把图标那一小块裁出来放大 6 倍看。
放大之前在正常尺寸下是看不出来的。

**实测出的经验数值(第三版通过)**:右下角的元素**离下边缘 8.2%、离右边缘 10.5%** 时
完整不被切;第二版是 5.1% / 9.0%,被切了。
所以角落元素的安全边距至少留 **8%** 以上。

⚠️ **这个问题只能在素材里解决,处理图片时避不开** ——
我们这边只是把 1254 等比缩到 1024,切图案的是 iOS 显示时套的圆角遮罩。

## L25 · iOS 26 的图标有三种外观,「色调」那种是系统按亮度生成的

**现象**:换完图标,浅色、深色主屏都好看;把主屏切到**「色调」**外观时,
别的图标都变成蓝色单色,**我们的还是白底黑字**,像块白板贴在一排蓝图标里。

**排查**:`xcrun assetutil --info Assets.car` 显示编进去的只有「默认」和「深色」两份,
**单色版根本不是我们提供的,是 iOS 自己生成的** ——
它把图转灰度,再把灰度值映射到用户选的色调上。

**原因**:我们这张是**浅底**图(白报纸底 + 黑 R),灰度后几乎全白,
映射后停在色阶最亮那端 —— 看起来就是"没上色"。
试过把 tinted 那条从 Contents.json 删掉让系统自己来,结果一模一样,
因为系统的输入还是这张浅底图。

**后续(2026-07-21 已解决)**:用户提供了一张**黑底白 R 的灰度图**作为 mono 版,
在 Contents.json 里显式登记成 `luminosity: tinted`。
「色调」外观下立刻变成深底浅字,和系统图标完全一致。**印证了下面这条教训。**

另有一个验证时的坑:改完深色素材后用 `xcrun simctl ui booted appearance dark`
切系统深色模式,图标**没变** —— 不是素材没生效,而是
**主屏图标外观是独立设置**,当时被钉在「默认」上。
测深色图标必须走 主屏长按 → 编辑 → 自定 → 深色。

**教训**:
- **单色图标的观感由原图的明暗分布决定,不由配置决定。**
  想在「色调」模式下好看,素材本身必须是**深底浅字**的高对比灰度图。
- 更一般的:凡是"系统会替你自动生成一个变体"的机制(单色图标、深色模式颜色、
  SF Symbol 的各种渲染模式),**生成质量取决于你喂进去的素材,不取决于你的设置**。
  设置对了但结果难看,先怀疑素材。
- 验收一个图标必须**三种外观都看**:长按主屏 → 编辑 → 自定 → 默认/深色/透明/色调。
  只看默认那种会漏掉这个问题。

## L27 · 对照物只有在"两种假设会给出不同结果"时才算证据

**这条是我犯了错之后才写对的,过程本身就是教训。**

**现象**(2026-07-21):用户问,真机上 app 图标会跟系统深浅色实时变,
模拟器里为什么不变 —— 是模拟器的问题还是代码没写好?

**我当时的推理**:
1. `assetutil` 确认三种外观都编进去了 ✅
2. 手动选「深色」「色调」,我们的素材都能正确显示 ✅
3. **"决定性证据":Apple 自己的「通讯录」「文件」在深色模式下也是浅色的** ——
   别人家也不跟随,所以不是我们的锅
4. 重启 SpringBoard 后仍不变(排除了"没刷新")

→ 结论:**模拟器不支持自动切换**。

**这个结论是错的。** 用户随后指出:主屏「自定」面板里选「深色」之后,
**下面还有一排子选项「始终 / 自动」**,要选「自动」才会跟随系统。
按用户说的一试,立刻正常 —— 我们的素材一点问题都没有。

**错在哪:第 3 条根本不是证据。**
那条我自以为"决定性"的对照,在两种假设下**预测完全相同**:

| | 假设 A:模拟器不支持 | 假设 B:全局开关没开 |
|---|---|---|
| Apple 的图标会不会跟随? | 不会 | **也不会** |

因为那个开关(当时停在「默认」)作用于**所有** app,Apple 自己的也不例外。
一条对两种假设给出同样预测的观察,**信息量是零**,
我却把它当成了压倒性证据,还因为它"感觉很硬"而停止了继续找原因。

**教训**:
- **对照物有没有用,取决于"两种假设会不会给出不同结果"。**
  动手前先问一句:如果是 A,我会看到什么?如果是 B 呢?
  **两个答案一样,这个实验就别做了,做了也白做。**
- 特别当心**全局性的设置/开关** —— 它天然会让"我的 app"和"别人的 app"
  表现一致,从而伪装成"环境的锅"。凡是"所有 app 都这样"的现象,
  在断定是平台问题之前,先找一遍有没有一个管着所有 app 的开关。
- **"我已经有决定性证据了"这个念头本身就是危险信号。**
  这次我正是因为觉得第 3 条够硬,就没再去翻那个面板的下一层。
- 仍然成立的一条:排查时要分清**"没生效"还是"生效了没刷新"**,
  两者表象一样、修法不同(这次用重启 SpringBoard 排除了后者,这步是对的)。

## L28 · RSS 文章没有现成的图片地址 —— `Article.imageURL` 是个陷阱

**背景**(2026-07-21):列表要显示正文首图的缩略图。

**看起来很顺**:`Article` 有个 `rawImageLink` 字段,注释还写着"用计算属性 imageURL"。
一眼看过去就是"数据层已经把图片地址准备好了"。

**真相**:对**普通 RSS/Atom 源,它永远是 nil**。
`Modules/RSParser/.../Feeds/XML/RSSItem.swift` 构造 ParsedItem 时那一行直接写着
`imageURL: nil`。只有 JSON Feed(`JSONFeedParser`)会填。Feedbin、ReaderAPI 也传 nil。
用户订阅的几十个源全是普通 RSS —— **这个字段一个都用不上**。

**教训**:
- **字段存在 ≠ 字段有值。** 看到一个正合心意的字段,别停在"有这个字段"就开始设计,
  要**回溯到它的写入端**看看谁真的会填。这次只要多看一眼解析器就能省下走弯路的时间。
- 数据来源的可用性是**可行性判断的核心**,要在写计划前查清,而不是写到一半才发现。

**最终做法**:从 `contentHTML` 里抽第一个 `<img>`。为此给 CLAUDE.md 第 5 节
「Swift 不解析 HTML」开了一个边界明确的只读口子(用户已确认)。
用上游自带的 `HTMLScanner`(它自己的 `HTMLLinkParser` 就是用它抽 `<a>` 的范例),
**没有写正则**。下载复用 `ImageDownloader.shared.image(for:)`,内存+磁盘缓存都是现成的。

**性能上做的两件事**(引用型博客正文可以有上百 KB):
① 只扫正文前 100 KB —— 首图几乎必在开头;
② 结果按 articleID 缓存,**包括"确认没有图"的负缓存** ——
没有负缓存的话,没图的长文每次滚动都会被重新完整扫一遍。

## L29 · 想替换上游的一个组件,先量它的「对外接口」有多大

**背景**(2026-07-21):上游的「阅读视图」依赖 Feedbin 付费服务 + 我们没有的密钥,
从来就是坏的。要换成本机跑 Readability.js。

**第一反应**是"这得改不少地方"——`articleExtractor` 在 `WebViewController` 里出现了 **20 次**。

**实际做法**:先把那 20 处逐个看一遍,**列出它们到底用到了什么**。结果只有 4 样:
`state` / `articleLink` / `process()` / `cancel()`。

于是把新类做成**同样的形状**,结果:
- 20 处引用里 **18 处一个字都不用改**
- 只改了「变量类型」和「构造那一行」两处
- 上游的 `ArticleExtractor.swift` **原封不动留在原地** —— 将来 pull 照常合并,
  想切回旧实现改回两行即可

**教训**:
- **"这个符号出现了 N 次"不等于"要改 N 处"。** 决定改动量的是**接口宽度**,不是出现次数。
  动手前花几分钟把用法列全,常常能把预估的工作量砍掉一个数量级。
- 替换上游组件时,**优先让新东西去适配旧接口**,而不是改调用方来适配新东西 ——
  前者让上游文件的 diff 停在最小,也留下了随时切回去的退路。
- 顺带一个具体收获:上游是用 `JSONDecoder` 解 `ExtractedArticle` 的,
  所以我让 JS 吐出**同样字段名**的 JSON,`ExtractedArticle.swift` 一个字都没动。
  **迁就已有的数据格式,比改数据结构划算得多。**

## L30 · 界面上多出来的东西,先问"是谁画的",别一上来就猜平台

**现象**(2026-07-21 用户提问):很多文章的图片下面多出两个按钮(一个灰色空壳、
一个蓝色放大箭头),问这是什么、能不能去掉。

**我走的弯路**:先去翻 WebKit 的 API,找到 `isElementFullscreenEnabled`,
觉得"多半是它";又猜是 `<video>` 的播放控件,还去数据库里数了 `<video>` 的篇数。
**猜了两轮都不对**,还差点基于错误前提去改 WebView 配置。

**一步到位的做法**:让用户说出**具体哪篇文章**,然后
`sqlite3 DB.sqlite3 "SELECT contentHTML FROM articles WHERE title LIKE '…'"`
把那篇的原始 HTML 拉出来看。**按钮就明明白白写在里面** ——
是 Substack 随 RSS 内容一起发过来的 `<button class="restack-image">`。

**教训**:
- **"这东西是谁画的"是可以直接查的事实,不是需要推理的问题。**
  网页里多出来的元素,查内容源是一步;猜平台行为是无限步。
- 遇到"很多文章都有"这种线索,**要一个具体样本**远比自己海选高效。
  我在没有样本的情况下试了两轮假设,拿到样本后一次就定案了。
- 顺带一个可复用的手法:内容类 bug 优先看**数据库里的原始内容**,
  而不是渲染后的样子 —— 渲染层会把来源信息糊掉。

**同一轮里的另一个发现**:功能被"抢"走时,表象是"功能不存在"。
app 明明有全屏图片查看器,但 Substack 把图片包在 `<a>` 里,点击被链接抢走去了浏览器,
**看起来就像这个功能根本没做**。
→ 用户报"没有某功能"时,先确认是**没有**,还是**有但被别的东西挡住了**。

## L31 · 筛选规则的每个分支都要定义结果,「不处理」不是默认安全的

**现象**(2026-07-21 用户验收发现):图片排版第一版只写了「宽 ≥400px 才参与顶边」,
小于门槛的图**什么都不做**。结果这些图退回上游默认的行内排版:竖图文字绕着图排、
小图靠左歪着,三个源三种样子。用户的原话是「好像没有一个清晰的规则」——
说得完全对,因为落选的那部分根本没有规则。

**教训**:「不满足条件」不等于「不用管」。落选者**也会被用户看到**,
它们的样子同样是规则的一部分。只定义"入选者怎样",等于把落选者交给了
不可控的默认行为,而默认行为可以任意难看。

**应用**:改成四桶穷尽:<100px 不动 / 竖图居中 / 够宽横图顶边 / 不够宽横图居中原大小。
任何输入必落一桶,没有「未定义」分支。写这类分类规则时先自问:
**每一种输入,我都说清楚它该长什么样了吗?**

## L64 · L32 那个坑我又踩了一次 —— 而且这次它静默毁掉了整层样式(附:防再犯的检查工具)

**现象**(2026-07-23 用户报告):播客文章里语音条和「在播客中打开」挤在同一行、文字被拦腰折断。
用户以为是布局没写好 —— 但那两条 CSS **本来就是对的**(`display: block; width: 100%`)。

**真因**:我在改顶栏那轮往 `nnw_appearance.js` 的样式模板字符串里加注释时,
用反引号包了两个代码名(``​`prefers-color-scheme`​`` / ``​`WebViewController.xxx()`​``)。
反引号**提前终结了模板字符串** → 整个脚本语法错误 → **该文件里所有样式全部失效**
(图片四桶规则、图注、正文排版、播客条……一个不剩)。

**为什么这么久才被发现**(这是最值得记的一点):
偏偏在同一轮,我把正文背景改成了**由 UIKit 画**(顶栏透明的前提)。
于是正文看起来还是暖纸色、字也还在,**肉眼完全看不出脚本已经死了** ——
新做的改动恰好盖住了它自己造成的破坏。直到几天后用户偶然打开一篇播客文章才暴露。

**教训**:
- **`.js` 不参与编译,`xcodebuild` 一个字都不会看。** 语法写错照样编译成功、装机成功,
  只在运行时安静地什么都不做。**"编译过"对这类文件毫无保证力**。
- **L32 已经写过一模一样的坑,我还是踩了。** 说明光靠"记着点"没用 ——
  于是这次补了工具:**`tools/check-js-syntax.swift`**,用 JavaScriptCore 的
  `new Function(源码)` 只解析不执行(所以引用 document/window 不会误报)。
  ```bash
  swift tools/check-js-syntax.swift        # 改完任何 .js 都跑一次
  ```
  **把它当成和 `xcodebuild` 同等的交付前必做项。** 工具本身也验证过:
  拿一个故意写坏的文件跑,它确实报错并退出码 1(L27:对照物要能区分两种假设)。
- **本项目里在 js/css 注释里提代码名,一律用「」,不要用反引号。**
- 更一般的:**当一轮改动同时改了 A 和 B,而 B 恰好能掩盖 A 的破坏时,风险最大。**
  这次是"脚本死了"被"背景改由 UIKit 画"盖住。改动越是相邻,越要分别验证,
  别让"整体看起来正常"当作两处都正常的证据。

## L32 · nnw_appearance.js 的模板字符串里,注释也不能带反引号

**现象**:在 STYLE 模板字符串**里面**的 CSS 注释里写了带反引号的 \`style="…"\`,
反引号被当成模板字符串的结束符,整个文件语法报错。
**应用**:那个文件的核心内容都在模板字符串里,里面的注释一律不用反引号。
每次改完先 `node --check` 再 xcodebuild —— 这次就是它抓的,快一个数量级。

## L33 · 对限流严格的服务,别为了「提前给点反馈」多打一枪

**现象**(2026-07-21,订阅发现 Phase A):Reddit 搜索能搜出来,**一点订阅就报
「The feed couldn't be found and can't be added.」**。版块名完全正确。

**定位**(值得复用):抓模拟器日志,一句话定案 ——
```bash
xcrun simctl spawn booted log show --last 20m --predicate 'process == "NetNewsWire"' --style compact | grep -iE "FeedFinder|429"
# → response_status=429
```
不是"找不到",是**被限流了**。上游 `FeedFinder` 只判断
`if !response.statusIsOK { throw feedNotFound }`,把 429 翻译成了「feed 不存在」。

**我的两个错**:
1. 搜索时我自己发了一次请求去「验证版块存在」,于是用户一次操作打 Reddit **两枪** ——
   而第二枪(真正要紧的订阅)挨了 429。
2. 那次验证走 `URLSession.shared`,**绕过了 app 自己的 429 记账**
   (`DownloadSession.requestShouldBeDroppedDueToActive429`,按域名退避)——
   只消耗配额、不参与退避,纯负收益。
3. 附带:我自己前面用 curl 连测十几次,模拟器和 Mac 共用出口 IP,
   **额度是我提前打光的**。验证外部 API 时要意识到这一点。

**教训**:
- **省下的请求要留给必须成功的那一步。** 「提前验证」听起来贴心,
  但它抢的是同一份配额;拼错名字反正订阅时也会暴露,而限流造成的**假错误**
  比拼错难查一百倍。
- **自己发的请求要和 app 走同一套下载器/记账**,否则会绕过退避、重试、
  条件 GET 这些既有机制,制造出"单独测都好、合起来就坏"的问题。
- 上游把多种失败压成一个错误时,**在自己这层把话说清楚**。拿不到具体状态码
  就**把两种可能都列出来**并各给一条行动建议 —— 比二选一猜错有用。

## L34 · 图文并排先看是不是表格,别急着怪样式

**现象**:Reddit 帖子正文里,缩略图和文字总是并排,「图片独占一行」的规则不生效。

**真相**(按 L30 的路子,直接拉 `contentHTML` 一眼就看见):
Reddit 把正文塞进了一个**一行两格的表格** ——
`<table><tr><td>缩略图</td><td>正文</td></tr></table>`。
图和文字并排**不是行内流的问题,它们本来就是两个并排的单元格**,
所以任何作用在 `img` 上的 `display: block` 都不可能有效。

**修法**:让这种排版表格的单元格堆叠。选择器**认结构不认来源**:
```css
.articleBody table:has(tr:only-child):has(td > a > img) :is(td, th) { display: block; ... }
```
「只有一行」+「格子里装着被链接包着的图」= 排版用的假表格。

**动手前先量杀伤范围**(这一步不能省):脚本扫全库 196 个表格 →
命中 26 个假表格、170 个真表格零命中,才敢发。
误伤真数据表这种事,**坏了不容易一眼看出来**,必须主动去量。

**顺带堵掉的一个坑**:表格外面套着 `overflow-x: auto` 容器,
所以额外加了一条「表格里的图片不参与顶到屏幕边缘」——
否则负边距会拖出一条横向滚动条。

## L35 · 数据被上游「解析了但没入库」时,绕路重取,别去改数据层

**背景**(2026-07-21,播客语音条):要拿每集的音频地址。

**看起来很顺**:上游解析器**确实解析了** `<enclosure>` ——
`RSSParser.swift` 里有 `article.attachments.insert(attachment)`。

**真相**:`Article` 数据模型**没有 attachments 字段**,
`ArticlesDatabase` 建库脚本里还有一句 `DROP TABLE if EXISTS attachments`。
**音频地址在入库之前就被丢掉了**,数据库里翻不到。

**两条路的取舍**:
- ❌ 在数据层加回字段 = 改 `Modules/Articles` + `Modules/ArticlesDatabase`(**C 级禁区**),
  而且改数据库 schema 是 merge 冲突的高发区,长期代价极大
- ✅ **要用的时候按 `Article.feedID` 重拉一次 feed**,用**上游自己的解析器**
  (`FeedParser.parse`)解出来,按 `uniqueID`(就是 RSS 的 guid)对上这一集。
  数据层一行不改

**代价必须主动控制**(这是这条教训的另一半):
- 按 feed 缓存,一个 feed 一次会话只拉一次
- **连「这个源根本不是播客」也要缓存**(负缓存)。没有负缓存的话,
  每打开一篇普通文章都会去拉一次它的 feed —— 那是灾难。
  和 L28 里缩略图那个负缓存是同一个道理。

**教训**:发现「上游解析了但没存」时,先算一笔账 ——
**重新取一次的成本**(加缓存后往往很低)通常远小于**改数据层的长期代价**。
顺带:这类判断的前提是查到写入端的真实代码,不能只看到有字段就以为有值(L28)。

## L36 · 同一个 API 的两个字段,可能一个能用一个是残次品

**现象**:iTunes 的单集接口同时返回 `episodeGuid`、`trackId`、`episodeUrl`。
`episodeUrl` 看着就是音频地址,直接用似乎能省掉「重拉 feed」那一整套。

**实测发现**:它返回的是 `..._preview.mp3?access_token=…` —— **试听片段**,
不是完整音频。付费/会员播客的完整音频只存在于用户自己订阅的那个 feed 里
(带用户自己的 token)。

**最终分工**:**音频走 feed(权威、且对私人 feed 也有效),跳转走 iTunes**。
`episodeGuid` 正好等于 RSS 的 guid,所以能精确跳到具体某一期,而不只是节目主页。

**教训**:
- **一个接口里的字段不是等价可信的。** 看到一个"正好我要的"字段,
  先验证它在**你的真实场景**下的值,而不是看字段名就用。
  这次只要多看一眼返回值里的 `_preview` 就能避免。
- 混合方案往往比纯方案好:**权威数据取自权威来源,附加信息取自方便来源**。

## L37 · 网页的"身份"被上游顺手设成了第三方域名 —— 嵌入的第三方内容会拒绝工作

**现象**(2026-07-21):在正文里嵌 YouTube 播放器,**所有视频**都报
「此视频不能观看 错误代码 152」。

**排查过程(这条的价值主要在过程)**:

先用**服务端可验证的手段**逐个排除,每一条都有证据、都不靠推测:

| 假设 | 怎么验的 | 结果 |
|---|---|---|
| 作者禁止嵌入 | oEmbed 接口测用户订阅里 8 个真实视频 | ❌ 8/8 都允许嵌入 |
| UA 被 YouTube 拒 | 分别用 Safari UA 和 app 的 NetNewsWire UA 请求 embed 页 | ❌ 两者都正常返回播放器 |
| 视频 CDN 被拦截规则挡了 | 把 ContentRules.json 全部 42 条打印出来看 | ❌ 没有 googlevideo/ytimg/youtube |
| Referer 不对 | 三种 Referer 各请求一次 | ❌ 都 200 |

到这里服务端手段用尽,剩下两个假设都在客户端。于是设计**一个对照实验**:

> 把**同一个视频**插进一篇非 YouTube 的文章里(The Conversation)。
> 同样的代码、同样的视频,**唯一的变量是文章的身份**。

结果:那边能正常播放 → **一次就定案**。

**真正的原因**:上游用 `loadHTMLString(html, baseURL:)` 渲染文章,
baseURL 取自文章链接。而 YouTube 文章的链接正好是
`https://www.youtube.com/watch?v=…` —— 于是**文章页自称是 youtube.com**。
嵌进去的播放器一校验"谁在嵌我",看到一个自称 YouTube 却没有 YouTube 会话的
页面,就拒绝播放。

**修法**:只对 YouTube 文章把 baseURL 换成中性身份。安全性是**量过的** ——
全库 15 篇 YouTube 文章里正文非空的有 **0 篇**,而 baseURL 的正经用途就是
解析正文里的相对链接,所以不可能解析错;另外 2293 篇走原分支不受影响。

**教训**:
- **`loadHTMLString(_:baseURL:)` 的 baseURL 不只是"解析相对链接的前缀",
  它决定了这个网页对外的身份(origin / Referer)。** 当 baseURL 恰好是某个
  第三方站点时,从该站点嵌入的内容会看到一个"冒名"的宿主页面而拒绝工作。
- **排查顺序:先穷尽服务端可验证的假设**(curl 能测的:状态码、UA、Referer、
  是否被拦),**再设计客户端对照实验**。前者便宜且证据硬,后者贵但能定案。
- 对照实验必须满足 L27 的标准:**两个假设要给出不同预测**。
  这次"同一个视频换一篇文章"正好只改变一个变量,所以一次就够。
- 改动这类全局代码路径时,**打击面要用数据量出来**,不能只说"逻辑上只影响 X"。

## L15 · 上游 CLAUDE.md 与我们的规则文件重名

**现象**:上游仓库自带英文版 `CLAUDE.md`,clone 后覆盖了用户准备的中文规则。
**应用**:上游版已改存为 `CLAUDE.upstream.md`(git 不跟踪)。将来 `git pull upstream` 如果 CLAUDE.md 冲突,**保留我们的中文版**,把上游新版本另存覆盖 `CLAUDE.upstream.md`。

## L38 · 自己加的控件必须显式设颜色 —— 继承来的 tintColor 可能让它隐形

**现象**(2026-07-21):往订阅列表的分组头加了「新建文件夹」按钮。用户报告三件事:
① 按钮**时有时无**(只有点了 `+` 弹出操作单之后才看得见);
② 看得见的时候**点击没反应**;
③ **看不见的时候,点它应该在的位置反而管用**。

**第 ③ 条是解开全部谜团的钥匙。** 它证明按钮一直都在、位置正确、点击链路也通 ——
**那就只剩"没画出来"这一种可能**。三个现象于是坍缩成两个原因:

- **没画出来**:按钮没显式设 `tintColor`,用的是继承来的值,在白底上等于隐形。
  弹出操作单会把背景压暗,这才显出一点灰色轮廓 —— 于是看着像"按了 + 才出现"。
- **点了没反应**:那一刻操作单正弹着,点击被模态层吃掉。**按钮本身从来没坏过。**

**教训**:
- **自己加的控件,颜色一定要显式指定**,不要依赖继承。继承来的值在某个界面上
  可能正好等于背景色,表现为"控件时有时无",极难往颜色上想。
- **用户描述的"三个 bug",可能只是一个原因的三种表象。**
  优先找那条**互相矛盾**的线索(这次是"看不见却能点"),它往往一句话就锁定真相。
- 顺带堵掉的隐患:在「视图即将显示」的回调里改约束,不调 `setNeedsLayout()`
  的话要等下一次外力触发才生效,同样表现为"时有时无"。

**另一个值得记的发现(布局)**:UILabel 的水平 hugging 优先级默认只有 251,
所以分组头里的标题会**自动拉伸**去占满剩余空间 —— 这才是"未读数贴着右边"的真正原因。
想让按钮紧跟在名字右边,必须先 `setContentHuggingPriority(.defaultHigh, for: .horizontal)`
让标题只占自己文字的宽度,再用一条 `>=` 约束把剩余空间让出去。

## L39 · 测试边界划错了地方:分派对了,活还是没干成

**现象**(2026-07-21):订阅发现页加了「全部」tab 做输入类型自动识别。
我为分派逻辑写了一批离线测试,**18/18 通过**,还因此抓到一个真 bug
(`youtube.com/channel/UCxxx` 这种不带协议头但带路径的网址被误判成关键词)。

然后交给用户,**第一个试的输入就失败了**:`youtube.com/@veritasium` 认不出。

**原因**:那个输入被正确分派给了 YouTube 解析器 —— 分派这一层完全没问题。
问题出在解析器**内部**:它判断「不以 http 开头 = 纯 handle」,
而 handle 的字符白名单里没有 `/` 和 `@`,于是一个完全正常的频道地址被拒了。

**我的测试只覆盖到「该交给谁」,没覆盖「交过去之后能不能干成」。**
测试边界划在了两个模块的接缝上,而 bug 正好在接缝的另一侧。

**教训**:
- **为一条链路写测试时,先问「这条链路的终点是什么」**,再看测试断言的是不是终点。
  这次终点是"拿到可订阅的地址",而我断言的是"分派给了哪个模块" —— 差了一层。
- 一批测试全绿会产生虚假的安全感,**尤其是当它们确实抓到过 bug 的时候**
  (我因为它抓到了一个真 bug,就更信任它了)。
- 补救方式:除了单元测试,**再跑一次端到端的真实验证**。
  这次修完后用 curl 把两种写法都跑通、确认能抓到 channelId,才敢交。

**同一轮里的一个正面例子(可复用)**:结果行的「订阅」状态原本只有
「未订阅 / 已订阅」两种,我主动补了**「正在订阅」**这一档。
因为订阅要联网验证,慢时好几秒界面毫无反馈,用户会以为没点上而反复点。
**凡是「点了要等」的操作,三种状态缺一不可:可点 / 进行中 / 完成,
而且失败路径必须把「进行中」收回去**,否则会永远转圈。

## L40 · 改名脚本做全文替换,会静默毁掉翻译

**背景**(2026-07-21):用户要把 app 改名成 Babel,并要求「做好基础设施,以后还会改」。
于是写了 `i18n/rebrand.py`。

**第一版的错**:对 `i18n/zh-Hans.json` 做了**全文字符串替换**。
但那个文件的**键是英文原文**,必须和上游 `.xcstrings` 里的英文一字不差,
`inject.py` 才能对上号。替换后键变成了
`"Babel 7 is dedicated to..."`,而上游那边仍是 `"NetNewsWire 7 is..."` ——
**6 条翻译会静默地注入不进去**,界面上悄悄变回英文,全程零报错。

**怎么发现的**:替换完**逐条看了 diff**,一眼看到键也被改了。
如果直接跑 `inject.py` 然后编译,界面上只是几行变英文,极难联想到改名脚本。

**教训**:
- **「键 / 值」结构的文件绝不能做全文替换。** 键是对外契约(要和别处对上),
  值才是内容。替换前先想清楚自己动的是哪一半。
- **脚本改完文件,必须看 diff,不能只看脚本报告的条数。**
  这次脚本理直气壮地说「已替换 17 处」,而其中 9 处是错的。
- 验证手段要落到**下游的可观测量**上:这次是「注入后上游 .xcstrings 只动了 8 行」——
  少一条都说明有键没对上。

**同一个脚本的第二个 bug(也值得记)**:
当 xcconfig 里已经是新名字、但文案还没改时(改到一半被打断),
脚本会提前 return 说「不用改」。
**这类「把状态从 A 推到 B」的脚本必须能反复跑,每次都把状态推到完整**,
而不是看一眼第一处就宣布无事可做。

## L41 · 「命令返回成功」不等于「事情做成了」—— 模拟器的假安装

**现象**(2026-07-21):修好一个 bug、编译、`xcrun simctl install`(返回成功)、
启动、让用户测 —— **用户测出来还是老样子**。于是开始查代码。

**排查过程走了三步弯路,每一步都值得记**:

1. 查地址本身:200、`application/rss+xml`、`<rss` 在第 150 字节 → 没问题
2. 把自己的 `fetch` + `isFeed` 抽出来独立跑:200、`isFeed=true` → **逻辑是对的**
3. 于是得出「逻辑对,但 app 运行时环境有问题」—— **一个没法收敛的结论**

**真相**:模拟器里堆了**两个 app 容器**。系统注册的是 40 分钟前那个旧的,
`simctl install` 把新版装进了另一个容器,**从来没被运行过**。
install 每次都返回成功,app 每次都能启动 —— 只是跑的是旧代码。

**决定性的一步**:
```bash
A=$(xcrun simctl get_app_container booted <bundle-id> app)   # 问出权威路径
cmp "$A/NetNewsWire" "<构建产物>/NetNewsWire"                  # 和刚编译的比一比
```

**中途还差点栽进第二个坑**:我用 `strings` 查构建产物,没找到新代码的日志串,
一度判定「装的是旧版」。**结论碰巧是对的,但方法是错的** ——
对照一下就发现:那条**确实打印过**的日志串同样找不到
(Swift 的 os_log 字符串不在普通字符串段)。
这个检查对两种假设给出相同预测,**信息量为零**(L27 那条规则)。
歪打正着比彻底错更危险,因为它会让你以为方法有效。

**教训**:
- **改了没生效时,第一件事是证明「跑的确实是新代码」**,而不是去查代码逻辑。
  前者一条命令,后者可以耗掉一小时。
- **凡是「装/部署/上传」这类动作,返回成功 ≠ 生效。** 要有一个**独立于该动作**
  的验证手段(比对二进制、查版本号、看日志里的新字符串)。
- 已固化成 `tools/install-to-simulator.sh`:装完必 `cmp`,不一致就 rsync
  覆盖到系统正在用的容器(非破坏性,不动数据容器)。
  实测这个卡死状态是**持续存在**的,该脚本每次都会报警并自动兜住。

**顺带一条**:`log show` **默认不保留 info/debug 级别**。
排查时看到「日志一片空白」,先加 `--info` 再下结论,否则会误判成「代码没执行」。

> ⚠️ **后记(2026-07-22,见 L49)**:本条说的"卡死持续存在、脚本每次报警"已查明真相 ——
> 不是 simctl 反复抽风,而是构建产物的 bundle id 被真机配置改成了 com.wenbopan,
> 脚本装的和比的根本是两个不同的 app。脚本已修(存在即覆盖、不再盲目 install)。

## L42 · 装机脚本只比主 stub、漏了 `.debug.dylib` —— 假安装的新变种

**现象**(2026-07-22):想用命令行自证「新代码进包了没」,先 `strings` 主二进制、
再按 UTF-8 字节 `grep` 主二进制,**连早就存在的中文串(如"翻译成中文")都找不到**。

**真相**:主可执行文件 `NetNewsWire` 只有 **~57KB**,是个 stub;
**Debug 版真正的代码在 `NetNewsWire.debug.dylib` 里**。在整个 .app 包里搜
(`grep -rla`)才发现新旧中文串都在那个 dylib 中。

**由此暴露的真隐患**:`tools/install-to-simulator.sh` 原来只 `cmp` 主 stub。
主 stub 很少变,而代码天天在 dylib 里变 —— 万一哪次**只有 dylib 变、stub 没变**,
`cmp` 会误判「已是新版」而**跳过 rsync 覆盖**,又回到 L41 那个「测的是旧代码」的坑。
这次是 stub 恰好也变了才触发整目录覆盖,侥幸没中招。

**修复**:脚本改为把 **stub 和 `.debug.dylib` 都比一遍**,任一不一致就整目录覆盖
(Release 版没有 .debug.dylib,就只比主文件)。

**教训**:
- **验证「装的是不是新代码」,要盯真正装代码的那个文件。** Debug 版是 `.debug.dylib`,
  不是主可执行文件。对着 stub 比,等于没比。
- 这也是 L27 的又一例:一个「对新旧都给同样结果」(主 stub 常常不变)的检查,
  信息量约等于零,却容易被当成「已确认」。
- 命令行搜字符串时:`strings` 只抓 ASCII、`grep` 主二进制可能搜错文件,
  中文/大型 app 要 `grep -rla` **整个包**按字节找,别对着一个文件下结论。

## L43 · 错误信息「只写不读」= 另一种静默失败

**现象**(2026-07-22 用户报告):没填 API Key 时点翻译键,按钮变成感叹号,
**但没有任何提示**,用户不知道为什么、也不知道该去哪儿。

**真相**:`TranslationController` 一直把失败原因存进 `lastErrorMessage`
(未配置、网络错、服务器 401 都写了),但**全项目只写、从没读过一次**,
更没弹给用户。等于每种失败都被压成一个哑巴感叹号。

**修复**:加一个 `presentError` 回调,界面层接成弹窗;未配置时在**点击入口**就直接弹
一句能照做的引导语(还不走 spinner→感叹号那一下),其它硬错误在 catch 里弹。
自动恢复等后台流程不弹(它们本就该静默)。

**教训**:
- L16 说「异步流水线里每一步的失败都必须有去向」。这条补一句:
  **「给用户看的那条路」也是一个去向。** 把错误 `存起来` 不等于 `告诉了用户`。
- 状态指示器(感叹号/⚠️)只说明「出事了」,说不清「为什么、怎么办」。
  凡是用户能动手解决的失败(没配 key、填错地址),必须给一句**能照做**的话。
- 区分「用户主动触发的失败」(要弹)和「后台流程的失败」(该静默),别把两者一锅端。

## L44 · 换列表底色要设 `config.backgroundColor`,不是 `collectionView.backgroundColor`

**现象**(2026-07-22,做订阅列表暖色背景):把 `collectionView.backgroundColor` 设成暖纸色,
**卡片变暖了(那是靠 cell),但卡片外的边距 / 组间空隙仍是系统灰 `#F2F2F7`**。
命令行逐点取样一眼定案:卡片内 `#F3F0EB`、左右边距 `#F2F2F7` —— 真有色差。

**原因**:`UICollectionLayoutListConfiguration`(`.insetGrouped` / `.plain` 这类系统列表)
**有它自己的一层 backgroundColor**(insetGrouped 默认是 `systemGroupedBackground` 系统灰),
它盖在 `collectionView.backgroundColor` 之上。所以改 `collectionView.backgroundColor` 看起来"没生效"。

**修法**:设 `config.backgroundColor = 暖纸色`(在用 config 建 layout **之前**设)。
边距 / 空隙 / 卡片外全部随之变暖,和 cell 一致,色差消失。

**顺带一条(分隔线)**:`config.showsSeparators = false` 想去分隔线,但**一旦设了
`config.itemSeparatorHandler`,它就覆盖 `showsSeparators`**,分隔线照画。
要真去掉,得在那个 handler 里把 `topSeparatorVisibility/bottomSeparatorVisibility = .hidden`。

**教训**:
- 系统列表(list config)是"三层背景":cell 背景 / 列表背景(config.backgroundColor)/
  collectionView.backgroundColor。看起来"设了没用",先想清楚自己动的是哪一层。
- **"有没有色差"这种事,肉眼靠不住,逐点取样一秒定案**(命令行 PNG 取色,不猜)。
- handler 一旦存在就压过对应的开关属性(showsSeparators),这类"处理器优先于属性"的坑,
  改属性没用时先找有没有 handler。

## L45 · 全局 `UINavigationBarAppearance.configureWithOpaqueBackground()` 会把大标题和 iOS 26 副标题一起冲掉

**现象**(2026-07-22):为了把导航栏那片也铺成暖色,我在启动时设了全局
`UINavigationBar.appearance()` 的 `standardAppearance/scrollEdgeAppearance`,
用 `configureWithOpaqueBackground()` + 暖底。结果**订阅列表顶部的「Feed」大标题
和「刚刚更新 / 正在更新」副标题(`navigationItem.subtitle`,iOS 26 特性)全没了**,
只剩一片空白 —— 用户圈出来问"这里的字怎么没了"。

**原因**:`configureWithOpaqueBackground()` 会重置 appearance 的文字属性,
在 iOS 26 上把大标题 + subtitle 的渲染一并搞没。全局 appearance 影响所有导航栏,
一处设错,处处标题消失。

**修法(也是正路)**:**别用全局导航栏外观去铺色。** 大标题态的导航栏系统默认本就是
**透明**的 —— 只要把下面列表的 `config.backgroundColor` 设成暖纸色(见 L44),
透明导航栏就会透出暖背景,标题 / 副标题原样保留。删掉那个全局改动,标题立刻回来,
顶部照样是暖的。

**教训**:
- **能靠"透出下层"解决的,就别去盖一层不透明的。** 想让导航栏区域变色,优先让它透明、
  露出已经变色的内容,而不是给导航栏本身刷一层(那会连带影响标题 / 副标题 / 分隔线)。
- 全局 appearance 代理是"大锤",一改影响全 app;宁可每个界面按需处理,也别图省事全局盖。
- 验收视觉改动要**连"该在的东西还在不在"一起看**(标题、副标题、角标),别只盯着"颜色对不对"。

## L46 · 全局强调色的真源在 colorset,不是代码调色板 —— 因为 storyboard 按名字直接引

**背景**(2026-07-22):把 app 强调色从蓝换成陶土红。本想收进代码里的 `AppAppearance` 调色板
(想"一处改全变")。

**为什么行不通**:强调色 `Assets.Colors.primaryAccent` 读的是命名色
`primaryAccentColor.colorset`。而**5 个 storyboard(Settings/Main/Add/Account/Inspector)
里的控件直接按名字 `primaryAccentColor` 引这个颜色** —— storyboard 读不了代码里定义的 UIColor。
所以若只改代码(把 Assets.Colors 重定向到 AppAppearance),storyboard 那些控件还是老色,不统一。

**结论 / 真源**:强调色的**唯一真源就是 colorset 本身**(`primaryAccentColor.colorset` +
`secondaryAccentColor.colorset`)—— 代码和 storyboard 都读它。改它的 RGB,一次全 app(含 storyboard)变。
所以"换强调色只改一个地方"这个目标达成了,只是那个"地方"是 colorset,不是 AppAppearance。

**教训**:
- **判断"颜色的真源在哪",要连 storyboard 一起看。** 若 storyboard 按名字引某命名色,那命名色
  (colorset)就是真源,代码层的重定向覆盖不到 storyboard。想全覆盖只能改 colorset 值。
- 反过来:纯代码画的东西(本项目的暖纸背景),真源放代码调色板(AppAppearance)最好;
  storyboard 也用到的颜色(强调色),真源只能放 colorset。**两类颜色的"真源"位置不同,别混。**
- 排查前先 `grep 颜色名 **/*.storyboard`,一眼看清有没有 storyboard 在按名引它。

## L47 · 派生出来的显示文本,必须有一个刷新时机 ——「值算对了」不等于「用户看到的是对的」

**现象**(2026-07-22 用户报告):设置页「翻译 API Key」右侧一直写着「已设置」。
用户把 key 删掉后,**不管点右上角的勾(保存)还是左上角的叉(取消),那行都还是「已设置」**。
用户的猜测是「大概只要 key 和服务地址填了一个就算已设置」。

**真相是两条,都不是用户猜的那条**(判断逻辑只看 key,跟服务地址无关):

**① 主因:那一行根本没被重新计算。**
「已设置 / 未设置」这几个字只在 `cellForRowAt` 里算一次。从子页 pop 回来时,
那个 cell 还在屏幕上、没被回收,**UIKit 不会重新问一遍**,于是显示的还是进子页之前的旧文字。
`SettingsViewController.viewWillAppear` 里挨个更新了主题、配色等一堆 label,
**唯独没有 `tableView.reloadData()`**——而恰恰只有这几行是我们后加的、走 `cellForRowAt` 出文字的。
所以「点勾」和「点叉」表现一样,不是因为两条路做了同样的事,而是**两条路都不刷新**。

**② 附带查出一个「勾保存」改造的漏网之鱼**:
`TranslationAPIKeyViewController` 的 `textFieldShouldReturn` 里还留着一句 `save()` ——
那是「离开就自动保存」时代的残留。用户清空输入框后按一下键盘的「完成」键,
**就已经写进钥匙串了**,之后再点「取消」根本取消不掉。
即「点叉不保存」这个承诺在改造后一直是**假的**,只是没人撞上。

**教训**:
- **凡是「从别处算出来的显示文本」(detail label、角标、状态字),
  都要回答一句:它什么时候重算?** 只保证「算的时候算得对」是不够的 ——
  用户看到的是上一次算的结果。这和 L43(错误信息只写不读)是同一族的病:
  **数据是对的,但没有走到用户眼前**。
- **分批改造(这次是「统一成右上勾保存」)收尾时,要把该语义的所有入口盘一遍。**
  这次漏的是键盘 return 键 —— 它不在「按钮」这个心智模型里,所以逐页检查按钮时看不见它。
  盘点手段就是一行 grep(`textFieldShouldReturn|viewWillDisappear|textFieldDidEndEditing`),
  十秒钟的事。
- **用户给的原因猜测要当线索,不要当结论。** 这次用户猜「两个填了一个就算」,
  照着这个猜测改(要求两个框都非空)会**改出新 bug** ——
  服务地址留空是合法且推荐的配置(留空 = 用默认 OpenRouter),
  那样会把最常见的正确配置误报成「未设置」。
  正确做法是接住用户的**意图**(「那行字要如实反映能不能用」),再自己去找真原因。

## L48 · `URL(string:)` 几乎什么都能解析成功 —— 拿它判断「地址填对没有」等于没判断

**背景**(2026-07-22,顺着 L47 往下查):想让设置页那行「已设置」如实反映
「配置到底能不能用」,于是复用了已有的 `configurationProblem`,
它里面用 `config.chatCompletionsURL != nil` 判断服务地址合不合法。

**实测**(写了个 7 行的 swift 脚本直接跑,没有靠推测):

| 输入的服务地址 | `URL(string:)` 结果 |
|---|---|
| `abc` | **非 nil**(`abc/chat/completions`) |
| `abc def` | **非 nil**(空格被转义成 `%20`) |
| `openrouter.ai/api/v1`(少了 `https://`) | **非 nil** |
| 空字符串 | **非 nil**(`/chat/completions`) |

它们全被当成合法的**相对地址**了。所以那句 `!= nil` **一个错误地址都拦不住**。

**影响不只是显示**:用户把服务地址写错时不会被拦下来,而是拿一个相对地址去发请求,
最后报一个看不懂的网络错误 —— 排查时会往网络、防火墙、服务商方向找,离真因很远。

**修法**:`chatCompletionsURL` 里额外要求 **scheme 是 http/https、且 host 非空**。
改完用 9 个用例回归:默认 OpenRouter、带尾斜杠、`http://localhost:1234/v1`(自建)、
别家兼容服务全部通过;上表那 4 种加上 `ftp://x.com`、`https://`(只有协议没域名)全部拒绝。
**「不误杀」和「拦得住」要一起量**,只测其中一半会漏掉另一半的回归。

**教训**:
- **`URL(string:)` 返回非 nil ≠ 这是个能用的网址。** 它的职责是解析,不是校验;
  相对地址也是合法 URL。要判断「用户填的是不是一个能发请求的地址」,
  必须自己检查 scheme 和 host。
- 更一般的:**校验函数要针对「你真正关心的那个性质」去写**,
  不要借用一个碰巧在手边、语义相近的判断。这次借用的是「能不能解析」,
  真正关心的是「能不能发出请求」,两者差得很远。
- 这类判断**必须用真实输入实测出一张表**再下结论。
  我如果只想「URL(string:) 解析失败会返回 nil,够用了」,这个洞会一直留着。

## L49 · 构建产物换了身份,装机脚本还在装旧身份 —— 模拟器长出「双胞胎 app」

**现象**(2026-07-22 用户报告):模拟器主屏出现**两个一模一样的 Babel**;
同时新功能"看不到效果"。用户问:是不是没编译/没加载?

**排查(值得复用的顺序)**:
1. `xcrun simctl listapps booted` → 确实装着两个:`com.ranchero.…` 和 `com.wenbopan.…`
2. 查构建产物的 Info.plist → bundle id 是 **com.wenbopan**(不是脚本里写的 com.ranchero)
3. 对两个容器分别 `grep -rqa 新代码符号` → **两个都含新代码**,"没编译/没装上"直接排除
4. 比数据容器:ranchero 303MB/87 源(用户真正在用的) vs wenbopan 8MB/11 源(意外产物)

**根因链条**:为了装真机(T6),仓库外的 `DeveloperSettings.xcconfig` 把
`ORGANIZATION_IDENTIFIER` 改成了 `com.wenbopan` → **模拟器构建产物的身份也跟着变了**。
而 `tools/install-to-simulator.sh` 写死 `BUNDLE_ID=com.ranchero…`,于是每次装机:
- `simctl install` 按产物自己的 id 装出/更新 **com.wenbopan 分身** ← 两个 app 的来源
- 脚本的 cmp 发现"系统注册的容器(ranchero)不是新代码" → rsync 覆盖 ← 真正生效的路径
- 结果:功能其实一直装上了;"看不到效果"另有其因(那次是 L44 的层级问题)

**顺带更正 L41**:当时说"simctl install 的假安装卡死**持续存在**,脚本每次都报警并兜住"。
现在真相大白:**每次报警不是 simctl 反复抽风,而是装的和比的根本是两个不同的 app**。
L41 那次的表象,极可能同样是这个原因(DeveloperSettings.xcconfig 正是那天配的)。
脚本已改:目标 app 已存在时**跳过 install 直接覆盖**(不再产生分身);全新模拟器才 install。

**教训**:
- **"写死的标识符"要和"由配置推导的标识符"定期对账。** bundle id 由 xcconfig 拼出来,
  改一处配置,所有写死它的脚本就全部悄悄失准 —— 而且不报错,只留下越来越怪的症状。
- **"每次都触发的兜底逻辑"本身就是 bug 的气味。** 兜底本该偶尔触发;
  它天天触发说明主路径从来没走通过,该查主路径而不是庆幸有兜底(L27 的变体:
  一个永远报警的检查,信息量趋近零)。
- 用户报"改了没生效"时,**先证明装上了没有,再查代码** —— 这次 grep 二进制十秒定案,
  避免了往"编译/加载"方向白查一轮。

## L50 · 本工程开着「表达式类型检查限时 1 秒 + 警告当错误」,CoreImage 会直接编译失败

**现象**(2026-07-22 写头部区氛围模糊时):`CIImage(image:)`、`CIFilter.affineClamp()`
等表达式全部报 `error: expression took >1000ms to type-check`,连换三种写法都过不去 ——
包括最朴素的 `let x: CIImage? = CIImage(image: input)`。

**原因**:工程的 OTHER_SWIFT_FLAGS 打开了类型检查限时警告,且
`SWIFT_TREAT_WARNINGS_AS_ERRORS = YES` 把它升级成错误。CIImage/CIFilter 的初始化器
重载极多,单个表达式的重载决议就能超过 1 秒 —— 这不是代码写得差,是这对组合的固有冲突。

**出路**:需要模糊时**别用 CoreImage**,用「缩到十几个像素再高质量放大」的经典技巧
(纯 CoreGraphics,效果上等价于重度高斯模糊,还更快):
见 `TimelineFeedHeader.softBlurred`。其它 CI 滤镜同理先想 CG 有没有等价物。

**教训**:遇到"表达式超时"类编译错误,**先怀疑框架 API 的重载规模,别急着怪自己的写法**;
拆函数、标类型只对边际情况有效,重载爆炸的 API 要换思路绕开。

## L51 · 上游的取图 API 会**悄悄退回一个按网址哈希染色的占位图标**

**现象**(2026-07-22,做单源页头图 v3):头图的底色应该取自订阅源图标的主色。
Lawfare 的图标明明是**墨绿色**(列表里每行都能看见),头图却是**紫色**。
Ars Technica 的鲜橙色则被算成灰褐色。

**真相**:代码里用的是 `IconImageCache.shared.imageForFeed(feed)`。它的取图顺序是
「feed 自带图标 → favicon → **SmallIconProvider**」,而最后那一档
(`Feed.smallIcon` → `FaviconGenerator.shared.favicon(feed)`)会
**合成一张地球仪占位图,颜色来自 `ColorHash(feed.url)` —— 网址的哈希值**。
真图标还没下载好的那一瞬间调用它,拿到的就是这张紫色地球仪。
而列表行是稍后渲染的,那时真图标已到,所以行里是墨绿的 —— 一个界面里两种颜色,
看起来像"颜色算错了",其实是"两次取图取到了不同的东西"。

**第二层问题(我自己写的)**:拿到占位图后我照常渲染并写了 `renderedKey`,
于是"图标就绪"通知来时,补装逻辑因为 `renderedKey != nil` 直接跳过 —— **永远卡在占位色**。

**修法**:
- 取图改成直接问真图标的下载器:`FeedIconDownloader.shared.icon(for:)` →
  `FaviconDownloader.shared.favicon(for:)`。它们没缓存时会**顺带发起下载**,
  到货后通知补装。**绕开 imageForFeed,就绕开了占位图那一档。**
- 拿不到真图标时**故意不设 `renderedKey`**,让通知能把它救回来。

**教训**:
- **"取图/取值"的 API 常常带一条你没注意的兜底分支**,而兜底值往往是"看起来正常、
  实际无意义"的东西(合成占位图、默认色、空对象)。用之前先读完它的取值顺序,
  尤其要问:**最后一档返回的是什么?**
- **同一份数据在界面上出现两次却长得不一样(这里是行图标 vs 头图),
  几乎必然是两条取值路径**,而不是"渲染错了"。先去比对两边调的是不是同一个 API。
- 「先用兜底值渲染,等真值到了再刷新」这个模式里,**必须让"用的是兜底值"这件事
  可被后续逻辑看见**(这里是不写 renderedKey),否则兜底值会永久固化。

## L52 · 我连续两次认定了错误的根因,而"把图导出来看一眼"一次就定案

**这条记的是排查方法本身,过程比结论值钱。**

上面 L51 那个"墨绿被显示成紫色"的问题,我在找到真凶之前,**先后给出过两个错误诊断**:

| # | 我的判断 | 依据 | 为什么错 |
|---|---|---|---|
| 1 | 半透明像素的**预乘 alpha** 把主色投票带偏了 | 机制上完全成立,而且确实会让颜色变暗变灰 | 用真实图标做离线对照后发现:两种 alpha 门槛算出的主色**完全一样**。机制成立 ≠ 它就是本次的原因 |
| 2 | `CGContext(data: &buffer, …)` 是**未定义行为**,读到了垃圾内存 | 这是真 bug(Swift 数组指针逃逸),而且"结果随机"完美解释"颜色莫名其妙" | 改成 `withUnsafeMutableBytes` 后重编重装,**主色一个字节都没变**,还是那个紫色 |

第三步我才做了本该第一步做的事:**把 app 实际拿到的那张素材图 `pngData()` 写进 Caches,
用命令行拷出来打开看** —— 一眼看见是一张**紫色地球仪**,当场定案。

**教训**:
- **L30 说过"这东西是谁画的,是可以直接查的事实,不是需要推理的问题"。这次我又忘了。**
  凡是"某个值不对"的排查,**先想办法把那个值本身导出来看**(存文件、打日志、写进数据库),
  再去推理它为什么不对。前者一步,后者可以无限步。
- **"这个机制确实会造成这种现象"不等于"这次就是它造成的"。** 两次误判都栽在这里:
  预乘 alpha 会让颜色变灰(真的)、野指针会让结果随机(真的),但它们都**没有发生**。
  这是 L27 的同一条规则换了个马甲 —— 判断一个假设有没有价值,要看**它能不能被证伪**,
  而不是看它听起来多合理。我当时该做的是:改完之后先验证颜色变了没有,再宣布修好。
- 具体到图像类问题:**把中间产物落盘**(素材图、遮罩、合成结果)是最快的排查手段,
  几行代码,比任何推理都硬。用完记得删掉那段临时代码。

## L53 · 异步到货的数据,不能用「问一次就算数」的方式去取

**现象**(2026-07-22,头图抓高清图):硅谷101、Marques Brownlee、Links TV 三个源
始终退回纯色渐变。而实测它们的大图明明存在:og:image 分别是
**1400×1400 的播客封面**和 **900×900 的 YouTube 频道头像**,
连元数据都**已经缓存在本地数据库里**。

**三层原因,全都源自同一个误解:我把「异步到货的数据」当成「同步就位的数据」在用。**

1. **候选来源里少了 og:image。** 这三个源的 `iconURL` / `faviconURL` **全是空的**
   (查数据库确认),只能靠网页元数据里的 og:image。补上后**零额外请求** ——
   元数据本来就是上游缓存好的。
2. **`HTMLMetadataDownloader.cachedMetadata(for:)` 是个内存缓存,app 启动时是空的。**
   它的语义是「有就同步给你;没有就**异步去取并返回 nil**,取到后发
   `.htmlMetadataAvailable` 通知」。所以**第一次抓图时,apple-touch-icon 和 og:image
   这两类候选根本还不存在**。我却按「问过了、没有」处理,记进负缓存,永不重试。
3. **修好重试之后仍然不灵 —— 一个竞态**:元数据到货的通知,**正好落在第一轮抓取
   还在进行的那零点几秒里**,被 `inFlight` 闸门挡掉;而这轮结束后不会再有通知,
   于是永远轮不到第二轮。
4. **还有一个「知足」陷阱**:硅谷101 第一轮能拿到一张 192px 的 apple-touch-icon,
   达到了最低门槛,于是被当成最终答案缓存 —— 那张 1400px 的封面就永远没机会了。

**修法(四条对应)**:①候选链补 og:image(放最后,够大的图标会早停,不打扰正常的源);
②负缓存改成**尝试计数**,允许新信息到货后再试;③抓取途中收到通知就**记一笔,
本轮结束后自动再抓**;④判据从「够门槛就收工」改成「**没到理想尺寸就继续升级**」,
新的一轮拿到更大的才替换。四条缺一不可 —— 我是一条条补上去的,每补一条都以为好了。

**教训**:
- **看到 `cachedXxx()` 这种同步取值 API,先去读它「没有的时候干什么」。**
  如果答案是「异步去取 + 发通知」,那么**第一次调用注定拿不到**,
  你的代码必须为「第二次」留好路 —— 而这条路上通常还埋着 in-flight 竞态。
- **「够用就收工」在数据会变好的场景里是错的。** 当更好的答案可能稍后才到,
  判据要写成「**离理想还差多少**」,而不是「有没有过线」。
- **一个功能失灵可能是好几个独立原因叠加的**。这次连修四处才通。
  每修一处都要**实际验证一次**(我靠日志逐轮确认候选数从 3 → 4、尺寸从 0 → 900px),
  否则很容易在只修了一半时误以为完工。

## L54 · 调"颜色的深浅"要在 HSB 里走明度轴,别在 RGB 里朝黑白混合 —— 那会把饱和度一起冲掉

**现象**(2026-07-22,头图标题染主题色):按主题色给标题上色,深色模式下用户说
「看着不太协调」。

**用户说得很准,而且原因是可量化的**:

| | 色相 | 饱和度 | 明度 |
|---|---|---|---|
| Acquired 的主题色 `#052C29` | 175° | **0.89** | 0.17 |
| 实际渲染出的标题色 `#B6C2C1` | 175° | **0.06** | 0.76 |

色相一点没变,**饱和度从 0.89 掉到 0.06** —— 浓墨绿被洗成了一块中性灰。
于是头图是浓浓的墨绿、标题却是灰的,两者看不出关系,这就是"不协调"的来源。

**根因**:第一版的写法是「在 RGB 空间把品牌色朝白色(深色模式)或黑色(浅色模式)混合,
直到对比度达标」。**朝白混合 = 三个通道一起往 255 靠 = 通道差值被压缩 = 饱和度必然下降**。
朝黑混合同理。也就是说,这个方法在"提高对比度"的同时**必然摧毁颜色身份**,
两个目标天然打架 —— 而我当时只盯着对比度这一个指标,还专门写了脚本证明"全部达标"。
**指标全绿,东西却难看**。

**修法**:改到 HSB 空间,**锁住色相与饱和度,只沿明度轴走**,一步一步走到对比度达标为止。
同一批源改完后:Acquired 深色模式下是 `#45918B`(饱和度 0.53),是真的墨绿了。

**教训**:
- **"变深 / 变浅"这个操作,语义上属于明度轴,就应该在有明度轴的色彩空间里做**(HSB/HSL/LCH)。
  在 RGB 里朝黑白混合是个看起来等价、实则会顺手改掉饱和度的近似做法。
- **一个只用单一指标(对比度)验证过的改动,可能在别的维度上已经坏掉了。**
  我上一轮跑了 7 个颜色 × 2 种模式的对比度表,全部达标,于是宣布完成 ——
  但没有任何一项检查在看"它还是不是原来那个颜色"。
  **补救办法很简单:把改动前后的 HSB 三个分量一起打出来对比**,一眼就能看见饱和度崩了。
- 用户说「不协调 / 怪」这类模糊反馈时,**先把可量化的属性列出来逐个对比**
  (色相、饱和度、明度、对比度),往往一眼定位,比反复猜"哪里怪"快得多。

## L55 · 「升级」逻辑只比了一个维度,于是用更大的坏东西换掉了更小的好东西

**现象**(2026-07-22 用户报告):进 Six Colors 时**头图一闪而过**,然后就没了。

**日志一路看下来,过程很清楚**:

```
① 144px 兜底 → 主色渐变
② 抓到 256px 的 sixcolors.png → 用大图,非白像素 39% ✓   ← 用户看到的"一闪"
③ 元数据到货 → 再抓一次
④ 抓到 1910px 的 og:image → **因为更大,替换掉了 256px**
⑤ 这张 og 图是白底的,非白像素仅 18% → 低于门槛 → 退回主色渐变
⑥ 而它的主色是 #E6E6E6(近白)→ 和纸色 #F3F0EB 几乎一样 → 头图等于消失
```

**根因**:我在 L53 里加的「不够好就继续升级」判据,**只比像素数**。
于是「更大」压过了「能用」——一张又大又白的 og 横幅顶掉了一张小而合格的 logo。
**而"能不能用"这个判据我明明已经写好了**(非白像素占比),只是没有用在选图那一步,
只用在了最后决定"这张图铺不铺得开"上。**同一个判据,两处口径不一致。**

**修法两条**:
1. 选图的分数改成 `像素 × 方图系数 × 可用系数`,可用系数取 **0.05**(近乎分类优先)。
   实测:256px 的 logo 得分 **256**,1910px 的白底 og 图得分 **29**,不会再被顶掉。
   替换判定也从「像素更多」改成「分数更高」。
2. 退回主色渐变时,保证那个颜色**在纸色上看得见**(`visibleGradientColor`,目标对比度 1.5)。
   否则近白的主色会让整片头图直接消失 —— 这是独立于上面那条的第二重保险。

**教训**:
- **凡是「用新的替换旧的」的逻辑,判据必须和「接受这个东西」的判据是同一套。**
  这次是同一个属性(非白像素占比)在两个地方各用各的,于是"选进来"和"用得上"打架。
  写升级/择优逻辑时先问一句:**我拿来排序的这个分数,包含了下游会拒绝它的那些理由吗?**
- **单维度择优必然踩这个坑**。"更大 / 更新 / 更快"都只是代理指标,
  真正要的是"更合用"。加权时宁可把否决项压到近乎分类优先,也别让它和主指标线性权衡。
- 这类 bug 的表现是**闪一下就没**(先对后错),很有辨识度:
  **说明正确结果曾经出现过,是被后续步骤覆盖掉的** —— 直接去查"谁在它之后写了这个值"。

## L56 · 给"会飞到导航栏的标题"做底,底必须和标题在同一层 —— 导航栏画在你上面

**现象**(2026-07-22 用户截图):标题往导航栏飞的过程中,**钻到一条不透明的纸色带子后面消失了**,
而那条带子还把头图和正文一起挡住。用户原话:
「顶栏没有变透明,把后面遮挡起来了,标题在往顶栏的遮挡后面跑」。

**根因:我给同一件事做了两层底,而其中一层在标题上面。**
- 一层是我自己浮层里的 `scrim`(和标题同层,标题在它上面)——这层是对的;
- 另一层是我顺手设的 `navigationItem.standardAppearance.backgroundColor = 纸色`
  —— **导航栏在控制器 view 的上面**,于是这层底盖住了标题。

我加第二层的动机是"停靠时标题要有干净的底",但**那件事第一层已经做了**;
第二层纯属重复,而且因为层级更高,直接把主角埋了。

**正确分工**:**导航栏自始至终透明,底只由浮层那条 scrim 提供**。
层级于是是「正文 → scrim → 标题 → 导航栏按钮」——标题稳稳停在自己的底上面,
两个圆钮仍然压在最上面。
(仍然要**显式**把 standardAppearance 设成透明,而不是不管:
iOS 默认的 standardAppearance 在内容滚上去后会自己画一层毛玻璃,那正是要避免的遮挡。)

**教训**:
- **在 UINavigationController 里,导航栏是控制器 view 的兄弟且在其之上。**
  任何"我要盖在导航栏区域上"的自绘内容,如果放在控制器 view 里,
  就**一定**在导航栏的背景之下。要么让导航栏透明、自己画全套,要么就用系统的标题。
  这两条路必须选一条,**不能一边让系统画底、一边自己画字**。
- **同一个视觉需求出现两个实现时,先问哪个能覆盖全部场景,然后删掉另一个。**
  这次两层底并存,不是"更保险",而是层级冲突的根源。
- 用户描述里「X 跑到 Y 后面去了」是**层级问题的明确信号**,
  比"看起来不对"精确得多 —— 直接去画层级图,不要先怀疑动画数值。

## L57 · iOS 26 的 `navigationBar.bounds` 把状态栏也算了进去 —— 拿它的中点对齐会偏高一大截

**现象**(2026-07-22,做标题飞向导航栏的动画):标题一路飞过头,**眼看要飞出画面**。

**定位手段(可复用):直接在用户的截图上量像素。**
- 两个圆钮(返回/筛选)的中心:**85pt**
- 标题被算到的位置:**57pt**
- 差了 28pt,而且 57 ≈ 114/2 —— 于是立刻能反推出:
  我用的 `navigationBar.bounds` 高约 **114pt**,即**从屏幕最顶端算起、把状态栏一起包进去了**,
  它的"正中"自然落在状态栏中间偏下,而不是圆钮那一条。

**修法**:改用两个可靠的量算停靠区 ——
`状态栏高度`(`windowScene.statusBarManager.statusBarFrame.height`)到 `safeAreaInsets.top` 之间那一条。
安全区顶 = 状态栏 + 导航栏,所以这一条正是导航栏本身,中点 = (62+106)/2 = **84pt**,与实测的 85pt 吻合。
另加一道保险:插值结果再怎么算,都不许越过停靠区上沿(`max(interpolatedY, minCenterY)`),
免得几何一变又飞出屏幕。

**教训**:
- **系统控件的 `frame`/`bounds` 未必等于"你眼睛看到的那块"。** 导航栏尤其如此:
  它的边界随系统版本、大标题模式、iOS 26 的浮动按钮样式而变。
  要对齐"看得见的东西",优先用**语义明确的量**(安全区、状态栏高度),
  少用控件自身的几何。
- **在用户给的截图上量像素,是最快的定位手段。** 这次量出 85 与 57,
  再看出 57≈114/2,一步就锁定了原因 —— 比读文档猜导航栏布局快得多。
- **凡是"按插值把东西移到某处"的动画,都要给终点加一道硬性夹取。**
  几何算错时,夹取能把"飞出屏幕"降级成"停在边界上",不至于完全失控。

## L58 · 脚本改代码出错,把递归悄悄种进了函数里 —— app 一启动就栈溢出崩溃

**现象**(2026-07-23 用户报告:app 打不开,附崩溃日志):
`Thread stack size exceeded due to excessive recursion`,栈里
`nnwInstallNavigationBarAppearanceUpdater` ↔ `nnwRefreshNavigationBarAppearance`
互相调用套了 **47431 层**。app 一进文章页(其实是 viewDidLoad)就崩。

**根因**:上一轮我用一个 python 脚本改这两个方法,脚本里有两处 `replace`:
- 第一处想把 `nnwRefreshNavigationBarAppearance` 的实现换成暖纸色 —— 它匹配的是"那五行 appearance 代码",
  但**那五行在 viewDidLoad 和这个方法里都出现过**,`str.replace` 默认替换所有匹配 → 把
  viewDidLoad 里也顺手插了一行"钩子",又把 refresh 末尾加了一行调回 install。
- 第二处 replace 因为锚点对不上直接抛了 AssertionError,**但第一处的破坏已经写进文件了**。
- 我看到 AssertionError 后只补了 install 里缺的那行,**没有回头检查第一处到底改了什么**,
  于是 refresh→install→refresh 的循环留在了文件里。而且编译能过(递归不是编译错),
  装机也不崩(时间线页不触发),直到用户点进文章页才爆。

**修法**:重写这两个方法,把分工写死 —— install 只"装一次+注册",refresh 只"重建外观",
两者绝不互相调用,并在注释里明写这条禁令。

**教训**:
- **`str.replace` 不带 count 参数时替换所有匹配。** 改代码的脚本里,要替换的片段但凡可能
  在文件里出现多次(样板代码、重复的几行),必须先确认唯一性,或改用带唯一上下文的锚点、
  或限制 `count=1`。这次那"五行 appearance"正是典型的会重复的样板。
- **脚本抛异常时,不能假设"什么都没改"。** Python 的 `open(w)` 是即时落盘的,
  异常前已经写入的部分留在了文件里。出异常后第一件事是 `git diff` 看**实际**改了什么,
  而不是只补上"看起来还差的那一步"。
- **编译通过 + 装机不崩 ≠ 没问题。** 无限递归、只在特定页面触发的崩溃,都能躲过这两关。
  改完动了行为的代码,至少要走一遍**真正会执行到那段**的路径(这次是"进文章页")。
  我改的是文章页导航栏,却没进文章页看一眼,是这次的直接疏漏。
- 这也是 L1 那类"改上游/样板代码"风险的又一面:**样板越像,脚本越容易误伤**。

## L59 · registerForTraitChanges 在 UIPageViewController 子树里不触发;与其"变了重建",不如让外观天然自适应

**现象**(2026-07-23,第二次报"深色模式文章页顶栏发浅"):明明上一轮"修好"了,又复发。

**日志一句话定案**(L52 的正确应用 —— 先暴露中间状态,别猜):
给 `nnwRefreshNavigationBarAppearance` 加一行日志打印 style,然后**停在文章页切深色**。
结果:切深色后**没有任何新的 refresh 日志**,只有进文章时那条 `style=light`。
→ 确认 `registerForTraitChanges([UITraitUserInterfaceStyle.self]){…}` 在这个场景**根本没触发**。
(文章页是 `UIPageViewController` 的子 VC,这类嵌套子树的 trait 变化传播不总是到达每个 VC。)

**为什么上一轮以为修好了**:我上一轮的验证方式是"**先切深色、再进文章页**" ——
那条路径 viewDidLoad→install→refresh 在深色下跑,自然是深色,看起来对。
真实会坏的场景"**停在文章页原地切深浅色**"我没测,于是假阳性过关。
(这和 L39 同源:测试路径没覆盖真正会出问题的那条。)

**真正的修法**:不要"变了重建",而是让外观**天然自适应**。
把 `navigationItem` 的三个 appearance 设成 **nil** → 回落到系统内置默认导航栏外观,
那个是永久深浅自适应的毛玻璃(订阅列表页就是它,因为压根没设过 appearance)。
设一次就够,不依赖任何触发。上游 viewDidLoad 里那句 `configureWithDefaultBackground()`
反而是"一次性固化、不自适应"的根因 —— 我们设 nil 把它覆盖掉。

**教训**:
- **`registerForTraitChanges` / `traitCollectionDidChange` 的触发不是无条件可靠的**,
  尤其在容器 VC(UIPageViewController/UISplitViewController)的子树里。
  依赖"某某变化时回调我"来维持外观一致,是脆的。
- **能让系统自适应的,就别自己缓存+重建。** 设 nil/用系统默认,往往比"监听变化手动刷新"
  更省、更稳 —— 少一条会漏触发的链路。手动重建只在系统确实不自适应时才做。
- **"修好了"必须用会真正复现 bug 的那条路径验证**,不能用一条恰好绕开 bug 的路径。
  这次两轮都栽在"先切深色再进页面"这条绕开路径上(L39 的又一次)。

## L60 · 透明导航栏会露出背后 WKWebView 的底色 —— web 深浅色不跟 app 同步时,浅色顶栏会变黑

**现象**(2026-07-23):想让文章内容页顶栏更"透",把 `scrollEdgeAppearance` 设成
`configureWithTransparentBackground()`(完全透明)。深色模式下看着挺透(用户说"非常透"),
**但切到浅色模式,顶栏变成一片纯黑**(用户截图),而正文是浅纸 —— 顶黑身浅,很割裂。

**原因**:导航栏透明后,它那一条区域**露出的是背后的 WKWebView**。而 web 正文的深浅色
走的是网页自己的 `prefers-color-scheme` / 注入的 CSS,**不总和 app 的 userInterfaceStyle 同步**
(尤其 app 切了深浅色但 web 没重新渲染时)。于是浅色 app + 深色残留的 web = 透明顶栏露出黑底。
"完全不透"和"变黑"其实是同一件事:透出来的是黑的。

**教训**:
- **透明导航栏 = 把顶栏那块的观感外包给了背后的视图。** 只有当背后是**你能控制、且一定
  和 app 同步**的内容时,透明才安全(比如订阅列表页背后是 UIKit 列表,深浅色系统统一管)。
  背后是 WKWebView(内容深浅色自成一套)时,透明会露出不受控的底,深浅色一错位就翻车。
- **"某方案在深色下好看"不等于"它对"** —— 这次深色下透出深 web、看着透,恰好掩盖了
  "透出的是 web 底"这个事实;换浅色立刻暴露。视觉方案必须深浅两色都验过才算数(又一次 L59)。
- 回退到「系统默认毛玻璃(全 nil)」是这一串尝试里唯一深浅都不出 bug 的:毛玻璃是**不透明**
  地盖住背后,所以不受 web 底色影响。代价是不如透明"透"——这是稳定性和通透感的取舍。

## L61 · 顶栏「不够透」的真凶:普通标题栏的 scrollEdgeAppearance 设 nil 会回落成毛玻璃

**现象**(2026-07-23):文章内容页顶栏怎么调都"偏实、不够透"。前面几轮换过材质、
改过颜色、试过完全透明,都没做出订阅列表页那种"顶部通透、滚动渐显毛玻璃"的两态。

**根因,两层,缺一层都做不成**:

**① 系统给的默认值本身就没有"透明态"。**
iOS 15 起,**普通标题栏**(不开大标题)的 `scrollEdgeAppearance` 若为 `nil`,
会**直接回落成 `standardAppearance`** —— 于是"内容在顶部"和"内容滚起来"用的是
**同一套毛玻璃**,永远不可能透。
而订阅列表页之所以零代码就有两态,是因为它 `prefersLargeTitles = true` ——
**大标题模式下系统给的 scrollEdge 默认值才是透明的**。
→ 所以「照抄订阅列表页 = 什么都不设」是错的,那是场景不同带来的假象。
文章页必须**显式**把 scrollEdge 设成 `configureWithTransparentBackground()`。

**② 系统不知道该盯哪个滚动视图,于是一直按"已经滚了"处理。**
两态切换的前提是导航栏能读到"内容滚到哪了"。订阅列表页背后就是普通列表,系统自己找得到;
文章页的 WKWebView 藏在 `UIPageViewController` 的子页面里,系统找不到。
→ 用系统正规接口 `setContentScrollView(_:)`(iOS 15+)把线接上。
调用时机要两处:**网页加载完**(本页第一次就绪)+ **翻页动画结束**
(翻到的那页可能早就加载完了,不会再触发前者)。

**教训**:
- **"某某属性设 nil = 用系统默认"这个直觉,在 UINavigationBarAppearance 上是错的。**
  nil 在这里的语义是"回落到另一个 appearance",不是"回落到系统内置外观"。
  同理:大标题模式和普通模式的默认值**不一样**,把一个页面的"零代码就好看"
  搬到另一个页面前,先确认两边的模式是否相同。
- **"抄那个好看的页面"要抄到机制层,不能只抄表面。** 这一串折腾里,
  「订阅列表页没写任何代码」这个观察是对的,但由它推出的「所以我们也不该写代码」是错的 ——
  同样的效果在不同模式下,一个来自默认值,另一个必须手写。
- 系统级两态(透明 ↔ 毛玻璃)一旦接通,**过渡动画、深浅色、材质全是系统的**,
  比自己拿 alpha 去插值一层毛玻璃更稳、更像原生(自绘那条路见 L56 的层级坑)。

## L62 · 毛玻璃要"薄一点",得调浓度(animator 的 fractionComplete),不是调 alpha

**现象**(2026-07-23):文章列表页停靠时那条毛玻璃,用户连着两轮说"不够透"。
中间换过材质(chrome → thin → ultraThin,已经是系统最透的一档),仍然嫌厚。

**先用一个能分辨真假的实验定性,别再猜**(L27 的应用):
当时有两个都说得通的假设 —— ①毛玻璃坏了(降级成半透明白板);②毛玻璃好的,但背后
那几行**已读**文章只有 45% 浓度(`readAlpha`),淡灰字被模糊+提亮冲没了。
两个假设会在同一个场景给出**不同结果**,所以让用户把**未读**文章(100% 浓度的深色粗体标题)
滚到顶栏后面截一张图:字透过来了 → 假设②。一张截图定案,没有多花任何一轮。

**真正的病根:拿 `alpha` 当"厚度"旋钮使。**
苹果明确说 `UIVisualEffectView` 的 alpha 小于 1 会让模糊失真。更要命的是**观感**:
alpha 调出来的"薄"= 清晰内容和模糊内容**叠加**的重影,不是真的薄。
所以以前只能在"alpha=1 的完整毛玻璃(太厚)"和"重影"之间二选一。

**正确做法**:把「从无到毛玻璃」这个变化交给 `UIViewPropertyAnimator`,
再用 `fractionComplete` 把它**停在任意档位**,alpha 全程保持 1。
那是系统插值出来的真毛玻璃 —— 模糊半径和着色一起按比例减弱。
于是"厚度"第一次成了一个可以随便调的数(`TimelineStyle.headerDockedScrimStrength`)。

**⚠️ 这个动画器有个会崩的坑,写的时候必须一起处理**:
`UIViewPropertyAnimator` 停在 `.active`(暂停中)状态时被释放会**直接崩溃**。
而本工程是 Swift 6 + 主线程隔离,**`deinit` 里够不着它**。收尾只能放在
`willMove(toWindow:)`(页面离开窗口时停掉)+ 回到顶部时顺手停掉 —— 让它活着的时间尽量短。
另外:`pausesOnCompletion = true` 不能少,否则进度推到头它就自动作废,之后再喂进度毫无反应。

**教训**:
- **"把某个视觉效果调淡"的第一反应是改 alpha,但对系统材质类视图(毛玻璃、振动效果)
  这条路是错的。** 它们有自己的强度概念,要用系统给的插值机制去调。
- **换材质换不出来的东西,别一直换材质。** 这次在三种 material 之间来回换了几轮都没解决,
  因为可调的那个维度压根不在材质列表里。**发现"选项试遍了都不对"时,
  要怀疑的是维度选错了,而不是继续在同一维度里挑。**

## L63 · 两个各自正确的功能,共用同一个状态就成了死循环 —— 滚动藏栏 × 顶栏跟踪滚动 = 栈溢出

**现象**(2026-07-23 用户实测):文章内容页往下滑,**卡死几秒后闪退**。
崩溃报告里 **28000 层递归**,`EXC_BAD_ACCESS` 撞在栈保护页上。

**回路**(从崩溃栈一层层读出来的,不是猜的):
```
滚动回调 → 藏栏 → 导航栏收起 → 安全区变化 → 系统调整滚动位置
        → 位置变了又发滚动回调 → 判断方向 → 现栏 → 安全区又变 → ……
```
两头都在改同一个东西(安全区/滚动位置),于是来回拉锯,永不收敛。

**为什么以前不崩、这次崩了**:「滚动藏栏」上线时这个回路就已经存在,只是闭合得松。
这次为做顶栏两态,把网页滚动视图交给导航栏跟踪(`setContentScrollView`),
导航栏于是会**主动**去更新那个滚动视图的安全区 ——
崩溃栈里 `_updateViewSafeAreaInsetsAndEagerlyUpdateContentScrollView` 这一帧就是铁证。
回路一下子闭合得又快又紧,几秒就爆。

**修法**:给藏/现栏加一道**防重入闸门**(`isTogglingBars`),切换期间所有滚动回调原地返回;
闸门到**下一轮 runloop** 才抬起(切换引发的布局是本轮同步跑完的,必须整段罩住);
抬闸时顺手 `primed = false` 重新取基准,免得把"系统的位置调整"当成用户滑动。
并把 `hideBars`/`showBars` 收进唯一入口 `nnwToggleBars(hide:)`,杜绝有人绕过闸门。

**离线复现 + 验证(这次真正学到的东西)**:
我把这段判断逻辑连同"切换→安全区变→同步回调"这条回路写成一个几十行的模拟,
扫 667 种滑动组合(安全区变化量 × 每帧滑动距离):
**旧代码 57 种爆栈**(爆栈参数正好是"安全区变 90~95pt、每帧滑 46pt",
即导航栏+状态栏的真实高度 + 快速滑动),**加闸门后 0 种,最深 2 层**。

**教训**:
- **两个功能各自都对,耦合到同一个状态上就可能成环。** 判断标准很简单:
  **A 改的东西会不会触发 B?B 改的东西会不会触发 A?** 只要两个"会",就必须有闸门。
  凡是"在滚动回调里改布局(藏栏/改 inset/改 safeArea)"的代码,**默认就该防重入**。
- **我又一次违反了 L58 的纪律**:编译通过、装机启动、日志无异常就交付了,
  但**滚动这条路径根本没被走到**。改的是"滚动时的行为",却没有任何一步真的滚动过。
  → 以后凡是我(AI)无法亲自操作的交互路径(滚动、拖拽、手势),
  **必须把逻辑抽出来做离线模拟**,而不是把风险丢给用户去撞。
  这次的模拟只花了几分钟,却精确复现了崩溃并证明了修复 —— 早写一轮就能省掉一次闪退。

## L65 · 拖放占位存在时改数据源 = 必崩;以及「订阅源顺序」在上游模型里根本不存在

**现象 A**(2026-07-23 用户实测):把源拖到收起的文件夹上、等它弹簧加载展开时闪退。
崩溃栈直指 `expandFolder` 里的 `dataSource.apply`,断在 UIKit 的
`_endItemAnimationsWithInvalidationContext:tentativelyForReordering:`(批量更新校验)。

**根因**:`.insertAtDestinationIndexPath` 这个落点意图会让 UICollectionView
**在列表里插入一个占位空隙**(就是那条"让开的缝",落点反馈全靠它)。
占位不在 diffable 的快照里,所以此时再 apply 新快照,UIKit 校验行数时对不上 → 断言失败。

**修法**:两种意图分场景用 ——
**悬停在文件夹行上用 `.insertIntoDestinationIndexPath`(不插占位,可安全展开);
其余用 `.insertAt`(要那条让位动画)**。
而弹簧加载**只对文件夹计时**,两处判断又都基于同一个"悬停对象",
于是天然满足不变量:**会触发展开的状态,一定不插占位**。(离线把这个不变量的 5 种状态跑了一遍。)

**教训 A**:
- **拖放进行中,UICollectionView 的实际行数可能比数据源多一个(占位)。**
  凡是"拖放途中改数据源"的功能(弹簧加载展开、自动新建分组…),
  都要先确认当前落点意图**不带占位**,否则必崩。
- 这类崩溃**编译期和静态阅读都看不出来**,而且只在特定手势路径上出现 ——
  又一次印证:交互路径必须让用户实测,或者把不变量抽出来离线验(L63)。

**📌 追记(2026-07-23 晚):这条约束最终把整个机制判了死刑,弹簧加载已被删除。**

用户提「悬停展开的文件夹,手指拖走时能不能自动合上」——**合上一样是改数据源**,
而"手指刚离开那个文件夹"之后的落点**绝大多数正是 `.insertAt`(有占位)**,
所以"一离开就合上"**根本没有安全的时机**。能用的只有两个:
展开另一个文件夹的那一刻、整个拖动结束之后 —— **恰恰都不是用户要的那一刻**。

于是这个机制的账算出来是这样的:**存在约一天,添了两次麻烦、崩了一次**
(挡住「A 和 B 之间」那条缝 → 用户说"几乎没机会触发";
把列表越撑越长 → 用户说"要悬停滑很长一段距离";外加本条这次闪退),
而它买到的只有"能把源直接放进收起的文件夹里的某个位置"这一个便利,
且该便利有平替(**先点开再拖**)。→ 按 L66 的原则**整个删掉**。

删掉之后:**拖动全程不再修改数据源**,本条记的这类崩溃在这个页面已无从发生。

**教训(比原来那条更值钱)**:
- **"能不能做"要连"反向操作能不能做"一起问。** 当初做弹簧加载时只验证了"展开是安全的"
  (悬停在文件夹上必然是不插占位的意图),**从没问过"那合上呢"** ——
  而一个只能开、不能关的状态,注定要靠别处打补丁去收拾。
  **凡是引入一个"自动改变界面状态"的机制,先把它的撤销路径找出来;找不到就别做。**
- **给一个机制记账:它带来的麻烦和它买到的便利,谁多?** 这次账很清楚,
  但如果不把三次事故摆在一起看,每一次都只会觉得"再补一个小 case 就好了"。
- 顺带:**"能不能加回来"的判断依据要写进代码里。** 现在
  `FolderManagerViewController` 顶部、`isDragInProgress` 附近、`DropZoneResolver`、
  以及离线脚本里都写了"别再加回来;若要加,必须把那条防崩不变量一并加回来" ——
  否则半年后有人只看到"这里少了个便利功能"。

---

**现象 B**(同一轮,用户要求"能不能拖动调整源的先后顺序"):**做不了,而且不该做。**

考古结论(证据确凿,别再重新评估一遍):
- `Folder.topLevelFeeds` 和 `Account.topLevelFeeds` 都是 **`Set<Feed>`** ——
  集合本身**无序**,模型层压根没有"顺序"这个概念可存。
- 上游列表的顺序是**显示时现算的**:`SidebarTreeControllerDelegate` 里
  `sortedAlphabeticallyWithFoldersAtEnd()`,**强制按字母排、文件夹排在后面**。
- 所以即使我们在管理页自己存一份顺序,**主列表(用户真正读文章的地方)照样按字母排** ——
  两个页面顺序对不上,反而更糊涂。OPML 导出/导入、同步账户也都不会带上这个顺序。

**上游的设计意图是「用名字控制顺序」** —— 用户自己的文件夹就是这么起名的
(`01 宏观`、`02 社会`、`03 严肃长文`…),源要排序照做即可。

**教训 B**:
- **接到"能不能拖动排序"这类需求,先查模型里到底存不存顺序。**
  存在 `Set` 里就是明确答案:不存。这时候硬做,等于在 UI 层伪造一个
  底层不承认、别处也不认账的状态。
- **给不出功能时要把替代路径一起给出来**(这里是"用名字前缀排序"),
  否则用户只听到"做不了"。

## L66 · 对有内部状态机的系统 API,**调用顺序本身就是语义** —— 一行位置放错,功能整个失效

**现象**(2026-07-23,拖动排序做完后用户复测):
「同一区域里拖动改变不了顺序了」「拖到文件夹第一行,却出现在第三行」。
用户原话:「我不太能完全总结出具体问题,就是拖动是有问题的,落点不准,或者拖动无效」。

**根因:一行代码放错了位置。**
上一轮为了修"预览缩回原点"的动画,我加了
`coordinator.drop(dragItem, to: previewTarget)`,并且**写在了所有数据计算之前**。
而这个调用会**当场改动 collectionView 的内部状态**;
紧接着算插入位置时要拿落点去问"那一行是谁"(`dataSource.itemIdentifier(for:)`),
问到的就成了错的行 ——
**算成原位就表现为"拖了没反应",算偏就表现为"落到第三行"**。
两个看起来无关的症状,其实是同一个根因。

**修法**:把那段挪到函数最后(用 `defer`,好覆盖所有 `return` 分支)。

**同一类坑当天又踩了一次**(2026-07-23,`reconfigureItems`):
为修「按编辑后勾选圈时有时无」加了 `collectionView.reconfigureItems(at: 可见的行)`。
它本身是对的,但**退出编辑模式有时是"刚搬完源之后自动触发"的**
(`performMove` 结束会退出编辑模式)—— 那一刻列表和数据源还没对齐,
`indexPathsForVisibleItems` 里混着已经不存在的行 → 撞断言崩溃。
修法两道保险:**延到下一轮 runloop** + **只挑数据源确实认得的行**。

**教训**:
- **拖放、动画、批量更新这类 API 背后都有状态机,"在哪一步调"和"调什么"同等重要。**
  判断标准很实用:**这个调用会不会改动我接下来还要读的东西?** 会,就必须往后放。
  这次要读的是"落点那一行是谁",而 `coordinator.drop` 正好会动它。
- **反过来也要问:我调用它的这一刻,数据稳定吗?**
  `reconfigureItems` / `performBatchUpdates` / `apply` 这些都要求
  "collectionView 看到的世界"和"数据源里的世界"是一致的。
  凡是可能在**数据刚变完**就被调到的路径(尤其是异步搬运的收尾回调里),
  都得延后一轮 + 校验有效性。
- **同一个机制崩到第二次,就别再加保险了 —— 把它拿掉。**
  上面那个 `reconfigureItems` 我先加了"延后一轮",又加了"过滤无效行",
  但它存在的理由其实很单薄:只为了让文件夹的展开三角跟着编辑模式换个样式。
  退一步看才发现**根本不用换** —— 勾选圈和展开三角各管各的点击,天然不冲突
  (点圈=勾选,点整行=展开)。**外观不再依赖编辑状态,那次刷新就整个不需要了。**
  一个需要不断打补丁才能不崩的机制,通常说明它本来就不该存在。

**⚠️ 收到崩溃报告时,先核对二进制指纹再动手改。**
这轮有一份报告其实来自**修复前的版本**(用户在新版装上之前又试了一次)。
崩溃报告里 `usedImages` 有 `NetNewsWire.debug.dylib` 的 UUID,
和当前装的一比就知道是不是同一份:
```bash
A=$(xcrun simctl get_app_container booted com.ranchero.NetNewsWire.iOS-DEBUG app)
dwarfdump --uuid "$A/NetNewsWire.debug.dylib"
```
不核对就照着旧崩溃改,很容易"修一个已经修好的问题",还可能因此引入新问题。
- **一个改动同时引出两个看似无关的症状时,先假设它们同源。**
  这次"排序无效"和"落点偏两行"看着是两个 bug,实际是同一行代码引起的 ——
  分别去修只会越修越乱。
- ⚠️ **这类时序问题离线模拟不出来**(依赖 UIKit 内部状态),
  L63 那套"抽出逻辑跑模拟"在这里帮不上忙 —— 只能靠用户实测。
  所以改动这类 API 时**更要控制单次改动量**:这次是在"拖动功能已经好用"之后
  为了美化动画又动了一次,结果把好用的部分弄坏了。
  **锦上添花的改动,风险不比修 bug 低。**

## L67 · 「代码里成立」不等于「用户到得了」—— 触发条件本身要被检验

**现象**(2026-07-23,用户看完上一版拖放方案后一句话点破):
>「A 展开、B 收起,把 A 的源拖到 A B 中间这个场景**几乎很难触发** ——
> 因为我想拖到中间,就**必然会先触发 B 展开**。
> **这是一个写在了代码里,但几乎没机会触发的场景。**」

**核查后完全属实。** 上一版我为这个场景写了明确的规则(手指往左退出缩进线 = 拿出文件夹)、
配了 9 个离线场景、全部通过 —— **逻辑没有一处是错的**。
但用户要到达那个规则,得把手指停在「A 的最后一个子行 + 靠左」那一小块;
而人的直觉是瞄两行之间的**分界线**,一往下就进了 B 的行 —— 那时不但判定变成"放进 B",
还会在 0.6 秒后**把 B 展开**,布局整个变掉。

**于是出现了一种很隐蔽的失败**:功能是好的,测试是过的,可用户**永远走不到那条分支**。

**根因不在那条规则,在判定的维度**:整套落点判断只有一个维度 —— 手指压着哪一行。
"行与行之间那条缝"表达不了"同层插队",只能靠"往上取最近的一行"猜。
**在一个维度上补丁打到第三轮,就该怀疑维度选少了**(这正是 L62 那句话的另一次应验:
"发现选项试遍了都不对时,要怀疑的是维度选错了")。
加上第二个维度(手指落在那一行的**上/中/下**哪一段)之后,规则表反而变简单了,
还白拿了一个原来做不到的能力(把源排到某个文件夹前面)。

**教训**:
- **交付一个交互方案时,除了"规则对不对",还必须回答"用户能不能稳定地到达触发条件"。**
  后者我这次完全没检验 —— 离线模拟只喂了我自己构造的坐标,
  而那些坐标恰好都落在规则成立的地方。**模拟能证明"给定输入时结果对",
  证明不了"用户真的会产生这个输入"。**
- **判断一个交互目标区好不好用,量它有多大、看它可不可见。**
  上一版那块区域大约只有半行高、没有任何视觉提示,紧挨着的还是一个会自动改变布局的区域。
  这三条里任何一条都是危险信号。
- **两个相邻的落点意图,若其中一个会改变布局(自动展开、自动滚动),
  它必须有明确的"安全边"** —— 否则用户瞄准另一个的过程本身就会触发它。
  现在的做法是让边缘带**天然不触发**展开,而不是靠"手别抖"。
- 顺带:**用户比任何测试都更早发现"够不着"这类问题**,因为他是唯一真正用手指去够的人。
  这类反馈要当成设计缺陷收下,别急着解释"代码里是支持的"。

## L68 · diffable 的「身份」里没有的东西,变了不会自己上屏

**现象**(2026-07-23 用户报,同一天里这类病的**第三次**露面):
文件夹那一行的副标题「4 个订阅源」,**把源拖进拖出之后数字不变**,
要等它滚出屏幕再滚回来才更新。

**病根**:diffable data source 判断"这一行要不要重画",看的是**行的身份**。
本页的身份是值类型标识(账户id / 文件夹id / 源id)——
这个设计本身是对的(上游随时重建对象,拿对象比 diff 会误判成全变了),
但它带来一个必然结果:**凡是"显示出来了、却不在身份里"的东西,变了都不会自动上屏**。
这一页至少有三样:文件夹里的源数量、文件夹/源的名字、异步到货的 favicon。

**同一个病之前已经露过两次**:
- 「按了编辑,勾选圈有的出现有的不出现」—— 进出编辑模式时数据没变 → 不重配
- 这次的「数字不更新」

**三条可选的修法,以及为什么选了第三条**:

| 办法 | 能不能用 |
|---|---|
| 把内容塞进身份(比如让文件夹标识带上源数量) | ❌ 身份一变,那一行就成了"另一行":展开状态、选中、层级父子关系全得跟着重来 |
| `NSDiffableDataSourceSectionSnapshot.reconfigureItems` | ❌ **section 快照没有这个方法**(只有普通快照有);而普通快照**装不下层级**(只含当前可见行),套回去会把展开结构压平 |
| `collectionView.reconfigureItems(at:)` | ❌ 直接对列表下命令,要求"列表看到的世界"和"数据源里的世界"当场一致 —— **为它崩过两次**(L66) |
| **给已存在的 cell 换一份 contentConfiguration** | ✅ 不增删行、不动层级,**完全不经过批量更新机制**,那条崩溃路径不存在 |

**做的时候踩到的两个坑(都值得单独记)**:
1. **基底必须取全新的 `defaultContentConfiguration()`**,不能拿 cell 现在那份来改 ——
   嵌套源的额外缩进是 `+=` 上去的,拿旧的当基底会**每刷一次就再往右挪 20pt**,
   几次之后整行缩出屏幕。(写的时候当场发现的,没交出去。)
2. **必须在 `apply` 的完成回调里刷**,不能紧跟着 apply 就刷 ——
   动画途中"第几行是谁"还没落定,可能把 A 的文字写进 B 那一行。

**教训**:
- **选"值类型标识"当 diffable 的身份时,就等于签下了一条义务:
  所有"显示了但不在身份里"的字段,都得有一个明确的刷新时机。**
  这不是可选的收尾工作,而是这个设计自带的成本。接手时先问一句:
  这一行上显示的东西,哪些不在身份里?它们靠什么更新?
- **同一个病第三次出现,就别再一处一处地补了** —— 前两次分别只修了勾选圈、只修了某一处,
  这次直接做成"每次刷新,可见行的文字统一重算一遍",把整类问题一次盖掉。
- 这也是 L47 的又一次应验:**"值算对了"不等于"用户看到的是对的",派生出来的显示文本必须有刷新时机。**

## L69 · 为了少改代码而留下的「同名派生属性」,会让语义变了的地方静默通过编译

**现象**(2026-07-23,给三个智能源加头图后用户实测):两个看起来毫不相干的 bug ——
①三个智能源**头图上没有标题**(图是好的);②**返回上一层再进来,头图整个不见了**,
只剩一个小标题停在导航栏。

**同一个根因,而且是一行**:

原来头图控制器里有个 `currentFeed: Feed?`。加智能源时我把它改成了**派生属性** ——
`if case .feed(let feed) = currentSubject { return feed }; return nil`。
这么做是为了"少改几处调用点"。而其中一处是滚动联动的守卫:

```swift
guard let collectionView, let host, currentFeed != nil else { return }
```

这行原本的意思是「**这个页面有没有头图**」,改完之后变成了「**是不是真订阅源**」——
于是智能源走到这里直接返回,`overlay.apply(...)` 从来没被调用过。后果正好两个:
- 标题从没被布局 → **头图上没有标题**
- `headerView.contentAlpha` 从没被重置 → 带着上一页滚动后的残留值(可能是 0)
  → **返回再进来,头图是透明的**

**编译器一个字都没说**,因为类型完全没变。

**定位靠日志,一次到位**:先看 `[头图]` 日志,里面清清楚楚写着
「已装:智能源「今天」用打包素材 1400px」—— **渲染每次都跑了**。
这一条就把「素材没进包」「渲染没跑」「资源名写错」全部排除,
剩下的只可能是"画出来之后被谁弄没了",直接指向滚动联动那一段。

**教训**:
- **把一个属性改成"从新状态派生"的同名属性,是最隐蔽的一种改动。**
  它让所有调用点**继续编译**,于是你不会去逐个看 —— 但其中凡是把它当作
  "更宽泛的那个意思"在用的地方,语义都悄悄变窄了。
  → 遇到这种重构,**别偷懒保留同名**:要么改名(强制每个调用点报错、逐个复核),
  要么就老老实实把每个调用点读一遍,问一句"它这里真正想问的是什么"。
- **一个改动引出两个看似无关的症状,先假设它们同源**(L66 已经写过一次,这次又中)。
  "没有标题"和"头图消失"看起来一个是文字、一个是图片,实际是同一行 guard。
- **日志的价值在于"能排除什么"**,不只是"能证明什么"。这次那条
  「已装……1400px」的日志把三种可能一次性排除掉,省下的是几轮猜测。
  → 所以每条渲染路径都该留一句带**关键数值**的日志(素材多大、算出的高度多少),
  光写"成功了"排除不掉任何东西。

## L70 · 「这个字体我见过」不等于「这个平台上有」—— 系统自带资源必须实测清单

**现象**(2026-07-23):我主动建议「中文标题改用宋体(STSongti-SC-Bold)」,
理由讲得头头是道(和 New York 同属衬线、报头气质一致)。写完装机,**一点变化都没有**。

**真相**:**宋体是 macOS 自带的,iOS 根本不带。**
`UIFont(name:)` 找不到就返回 nil,我的代码于是静默退回 New York,
New York 没有汉字,系统再退回苹方 —— 一路退到底,表现就是"完全没生效"。

**排查方式(值得复用)**:与其猜字体名对不对,不如**让 app 自己报一遍名单** ——
临时加一段代码遍历 `UIFont.familyNames`,用 `CTFontGetGlyphsForCharacters`
测每个字体认不认「全部未读已加星标」这几个简体字,打日志。一次就定案:
**iOS 26.5 上认识简体字的只有苹方的 4 个地区版 × 6 个字重,全是黑体,零衬线。**

⚠️ 中途我还差点被另一条错路带偏:先去翻模拟器运行时的字体文件目录,
发现有个日文明朝体 `HiraginoMincho.ttc`,而且**扫描那批文件时连苹方都没扫到** ——
说明**那个目录不是 iOS 加载中文字体的地方**,拿它下结论会错。
(后来直接用 CoreText 读那个 .ttc 测覆盖,确认它缺「读标观严肃长」等简体专用字,
用了会让一个标题里半衬线半黑体,比统一黑体更糟。)

**教训**:
- **平台自带什么资源(字体、图标、API),是个可以一秒查证的事实,别凭印象。**
  我这次的错误来源很典型:**在 macOS 上开发,把开发机上有的东西当成目标平台也有**。
  凡是"系统自带的 XXX",落地前先在**目标平台**上列一次清单。
- **静默回退链会把错误藏起来。** `UIFont(name:)` 返回 nil → 回退 → 再回退,
  全程不报错、不崩溃,表现只是"没效果",很容易被当成"参数没调对"继续瞎试。
  → 凡是写"找不到就用备选"的代码,**找不到时要留一句日志**,否则你永远不知道走了哪条路。
- **排查系统能力,首选让 app 自己报**,而不是去翻 SDK / 运行时的文件目录 ——
  文件目录里看到的不一定是运行时真正加载的那套(这次苹方就完全没出现在里面)。
- 顺带:**给用户的建议要标清把握程度。** 我当时说得像已经查过一样,
  用户据此拍了板,结果白做一轮。**没验证过的建议,要说"我需要先确认 iOS 有没有这个字体"。**

## L71 · 挂钩到别人的方法里,要问「我这行跑完之后,还有谁会改同一个东西」

**现象**(2026-07-23,给首页装头图):两个症状一起出现 ——
**①头图完全不显示**(日志说渲染成功了、素材也确认在包里);
**②系统大标题「Feed / 更新于 2 分钟前」照旧显示**,而我们明明设了 `.never`。

**两个都不是"没生效",而是"生效了又被盖掉"**:

**① 图被列表配置的底色盖住了。**
头图画在 `collectionView.backgroundView` 上,而首页的
`UICollectionLayoutListConfiguration.backgroundColor` 被我们(为了暖纸风)设成了
**不透明的纸色** —— 它盖在 backgroundView **之上**。
→ 改成 `.clear`,露出 `collectionView.backgroundColor`(也是纸色),观感一样,图能透出来。
⚠️ 这正是 **L44** 那条的续篇:当初为了改列表底色发现"必须设 config.backgroundColor",
现在为了让背景视图透出来,又必须**反过来把它清掉**。
**同一个属性,两个方向的坑,都踩过一遍了。**

**② 我们的 `largeTitleDisplayMode = .never` 被上游后面几行覆盖了。**
挂钩点选在 `viewWillAppear` 的中间,而上游在**紧随其后**的分支里写着
`self.navigationItem.largeTitleDisplayMode = .always`。
→ 把调用挪到方法**最后**。

**教训**:
- **往别人的方法里插一行时,不能只看"插在哪里能被调到",还要看"这一行跑完之后,
  剩下的代码还会不会改我刚设的东西"。** 判断很简单:把那个方法从头读到尾,
  搜一遍我设过的每个属性名。这次只要搜一下 `largeTitleDisplayMode` 就能发现。
- **"日志说成功了"只能证明这一层做完了,不能证明用户看得见。**
  这次渲染日志、素材进包检查全是绿的,东西却被另一层盖住。
  → 凡是"画上去"的功能,**验收必须是看到,不能是日志**;
  而在交给用户之前,起码要问一句:**它画在哪一层?那一层上面还有什么?**
- 顺带记下这一页的层级(以后再改别猜):
  `collectionView.backgroundColor`(纸)→ `backgroundView`(**头图**)→
  列表配置的 `config.backgroundColor`(必须 `.clear`)→ 单元格 → 我们的标题浮层。

## L72 · 素材换了、坐标没换 —— 以及「我全看过了」这句话我说得太满

**现象**(2026-07-23,用户验收头图):两张裁切明显不对 ——
「已加星标」浅色**根本没裁到主体**(用户:「乍一看不知道是什么意思」);
「全部未读」深色**读不出是夜里**。

**两个原因,都是同一类**:

**① 素材被换过,我却沿用了旧图的坐标。**
用户中途替换了「已加星标 浅色」——原来是古玩铺,换成了**一具铠甲**。
而我的裁切框是照着**古玩铺**定的(收到左半的青花瓷货架),
套到新图上正好裁在拉门和山水屏风上,主体一点没进画面。
**我当时说过一句「素材我全看过了」——其实那一轮只看了 6 张预览里的 3 张**,
恰好漏掉这张。文件大小其实早就变了(3270233 → 2877032),一比就能发现。

**② 我假设了浅深两版是同一场景,而实际只有一半是。**
Feed 页(NEW YORKER↔PLAYBOY)和今日未读(日出↔月出)确实是同场景昼夜版;
但全部未读的夜版是**另一幅画**(提灯夜行的信使),已加星标的夜版也是(一箱珍藏)。
我却让浅深共用一个裁切框,还在脚本注释里把"共用同一个框"写成了设计原则。
结果那个框在夜版上**把月亮和灯笼同时切掉了**,只剩中间一条街景 —— 当然读不出夜。

**教训**:
- **「素材没变」是个需要验证的假设,不是默认前提。** 用户随时会换图,而且
  **文件名往往不变**。→ 每次重跑加工前,先比一遍**文件大小 / 修改时间**,
  变了的必须重新看一眼原图再定裁切框。这一步只要几秒。
- **说"我全看过了"之前,先数一遍到底看了几张。** 这次是 3/6,却说成了全部。
  批量产物的检查要么全看,要么**明确说清抽查了哪几张、为什么是那几张**。
- **"同一个框适用于两版"这种便利假设,要用眼睛验证过才能写进注释当原则。**
  我把一个碰巧成立的巧合(另外两页确实是同场景)当成了整套素材的规律。
  → 抽象要从**看过的全部样本**里提,不能从看过的一半里提。
- 现在脚本支持 `darkCrop`(深色单独裁),并在注释里写清了
  **哪几页是真同场景、哪几页不是**。

## L73 · 我算的坐标和系统算的坐标不一致 —— 真机上炸出四个"看起来毫不相干"的现象

**现象**(2026-07-23,阅读栏在**真机**上验收。模拟器上一个都没出现):
① 新点进一个源、第一次点开文章 → **页面不在顶部**,正文前几行被阅读栏压住;
② 有时**两套元素同时画**(大标题和冻结小标题叠成一团);
③ 有时「拉到最上面还是冻结的样子」;
④ 源名那行**压在标题第三行上**。

**①②③ 是同一处漏掉:改了 `contentInset.top`,却没有把 `contentOffset` 挪同样的距离。**

阅读栏要占掉顶部约 250pt,所以给网页加了同样多的顶部内边距。
但**"滚到顶"这个基准位置也跟着变了**(从 0 变成 −250),而内容还停在 0 ——
于是**一进文章就等于"已经往下滚了 250pt"**。飞行进度按这个算:
- 完全没对上 → 进度直接是 1 → 一进来就是冻结态、正文被压住(现象①)
- 对上一半 → 进度停在中间 → 两套元素同时画(现象②)
- 换页时旧页的 inset 没还、新页又补不上 → 现象③

**修法**:改 inset 的同时 `contentOffset.y -= delta`。
⚠️ 而且要**先记下改之前的偏移再算**,不要赌"系统会不会自己调整" ——
那个行为随场景(是否在顶部、adjustmentBehavior、是否正在滚动)而变,不可靠。

**④ 是另一处同类问题:量高度用的 API 和画字用的 API 不是同一个。**
我用 `sizeThatFits` 量标题高度,而 UILabel 画字时用的是
`textRect(forBounds:limitedToNumberOfLines:)` —— **两者在多行 + 自定义字体时会给出不同结果**。
算矮了之后 UILabel **不会裁剪**,多出来的行直接溢出框外,压在下面那行上。
→ 改用 `textRect`,并且让下一个控件从**上一个控件实际的 frame 底边**往下排。

**教训**:
- **凡是"我自己算一套坐标、系统也算一套"的地方,先问:这两套什么时候会对不上?**
  这轮四个现象、两个根因,都是这一句话。
  具体到 UIScrollView:**`contentInset` 和 `contentOffset` 是一对**,
  改了前者而不动后者,等于悄悄把"顶部"挪了一段。
- **量尺寸要用"渲染时真正用的那个 API"。** `sizeThatFits` 和 `textRect` 看起来都能量,
  但只有后者是 UILabel 画字时自己调的。**不确定的时候,选那个"系统自己也会调"的。**
- **多个控件竖直排布时,让后一个从前一个的 `frame.maxY` 起算**,
  而不是各自用"我算出来的高度"拼。前者只可能对,后者会在测量出偏差时**叠在一起**。
- **模拟器一个都没暴露。** 这四个现象全都依赖真实的时序(网页加载慢、翻页快、
  第一次进入 vs 后续进入)。→ 涉及滚动、加载时序的功能,
  **模拟器验收通过不能算数,必须真机走一遍**。

## 顶栏「渐变透明」问题的现状小结(2026-07-23,未完全解决,交接给后续)

用户想要文章内容页 / 文章列表页的顶栏都做成订阅列表页那种"渐变透明毛玻璃"。折腾多轮未达满意,
**当前落在「不出 bug 但用户觉得不够透」的状态**。已试方案与各自的坑,后续别重复踩:

| 方案 | 结果 |
|---|---|
| 文章内容页 `configureWithDefaultBackground` + trait 监听重建 | 深色发浅 —— registerForTraitChanges 在 UIPageViewController 子树不触发(L59) |
| 文章内容页 全 nil(系统默认毛玻璃) | 深浅自适应、不出 bug,**但偏实,用户嫌不够透**。后查明:**全 nil 永远不会有透明态**(L61) |
| 文章内容页 scrollEdge 完全透明(**当时**) | 露出背后 WKWebView 底,浅色模式顶栏变黑(L60)—— 病根是**网页在自己画底** |
| 文章列表页停靠 scrim: chrome→thin→ultraThin material | 越换越透,但用户仍觉不够;疑似"停靠态顶栏背后是列表顶部空白,毛玻璃无内容可透" |

**2026-07-23 下午:文章内容页已按"纸色归 UIKit + 显式两态"重做,待用户验收。**
上面那张表里的两条死路,其实是**同一个死结的两半**,拆开就都通了:
- 「透明会变黑」的病根**不是透明**,是**网页在自己画底**(深浅色自成一套)。
  → 把纸色底的所有权收归 UIKit(网页 `background: transparent`,
  `WebViewController` 的 view 铺 `AppAppearance.paperBackground` 动态色),透明就安全了。
- 「毛玻璃太实」的病根**不是材质**,是**普通标题栏 scrollEdge=nil 会回落成毛玻璃**(L61),
  根本没有透明那一态可言。→ 显式设透明 scrollEdge + `setContentScrollView` 把线接上。

三件事(纸色归 UIKit / scrollEdge 显式透明 / setContentScrollView)**是一套,缺一不可**:
少第一件 → 复现 L60 的黑顶栏;少后两件之一 → 毛玻璃常驻,和以前一样实。

## L74 · 接管一个上游的显示值,要先数一遍它**一共有几个写入点**

**现象**(2026-07-23,做底部三档时):「全部」档规定**不显示任何数字**。
我改掉了两个 `configure(cell:)` 里写数字的地方,编译过、装机一看也对 ——
**过了十几秒,数字自己全回来了。**

**病根**:那个数字在上游一共有 **5 个写入点**,我只改了 2 个。
漏掉的三个里最要命的是 `unreadCountDidChange` 的通知回调 ——
后台同步一完成就把未读数**直接写回可见的 cell**(不经过 `configure`),
于是"刚切档时是对的、同步一结束就变回去"。另外两个是账户分组头(两处,创建时 + 通知回调)。

**教训**:
- **"接管一个显示值"和"改一处赋值"不是一回事。** 动手前先
  `grep "cell.unreadCount = "` 把**所有**写入点列出来,数量对上了再改。
  这次只要先 grep 一次就能一次做完,而不是装机等十几秒才发现漏了。
- **凡是通知驱动的直接刷新(`xxxDidChange` 里直接改可见 cell),
  都是绕过正常配置路径的旁路** —— 它们正是这类"改了又被改回去"的常客。
  接管显示值时,**旁路要和主路一起改**。
- 这是 **L68** 那一族的又一个变体:L68 是"变了不会自己上屏",这次是
  "上屏了又被别人写回去"。共同点是:**这一格上显示的东西,到底有几个人在写它?**
- 顺带:这次能抓到全靠**装机后隔十几秒又截了一张图**。
  只截一张"刚启动"的图会漏掉所有"后台任务跑完才发生"的问题。

## L75 · 用了 `UIButton.Configuration` 之后,`tintColor` 不再决定前景色

**现象**(2026-07-23):三档控件里给当前档设 `button.tintColor = 强调色`,
装机一看**图标和文字全是黑的**,颜色一点没生效。

**原因**:一旦给按钮设了 `configuration`,前景色走的是
`config.baseForegroundColor`;`tintColor` 只是它没被设置时的一个来源,
而工具栏这类容器会把自己的 tint 往下传,把它盖掉。**不报错、不警告,就是没颜色。**
→ 改成 `config.baseForegroundColor = 强调色` 当场就对。

**教训**:
- **新旧两套 API 混着用时,"老写法看起来还能编译"是最危险的情形** ——
  编译器不会说 `tintColor` 在这里已经不管事了。
  凡是给按钮设过 `configuration`,外观的每一项都要在 `configuration` 里设,
  别一半在 config、一半在按钮属性上。
- 又一次 L70 的形状:**静默回退/静默失效只表现为"没效果"**,
  很容易被当成"参数没调对"继续瞎试。这次是靠**装机截图 + 放大看颜色**一眼定案的,
  没有多花一轮猜。

## L76 · 通知的 userInfo 里放的是**枚举本身**,不是它的字符串值

**现象**(2026-07-23,做★档的星标索引时):想"只在星标变化时重数一遍",
于是写了 `note.userInfo[statusKey] as? String`,再和 `"starred"` 比。
**永远取不到** —— 判断整个失效,变成"每次已读状态变化都重数一遍全账户星标"。

**原因**:上游发通知时放进去的是 `ArticleStatus.Key` **枚举值**,不是 `String`。
虽然那个枚举的 raw 类型是 `String`,但 `as? String` 对枚举**不成立**。
`userInfo` 是 `[AnyHashable: Any]`,**编译器不会替你检查类型** —— 写错只会静默取到 nil。

**教训**:
- **从 `userInfo` 里取东西,类型要去发通知那一行确认,不能照着 key 的名字猜。**
  一次 `grep "post(name: .XXX"` 就能看到真正放进去的是什么。
- 这类错误的表现是"多做了事"而不是"报错",所以**不会有人发现** ——
  除非像这次一样正好在写注释时回头核对了一遍发通知的那行代码。
- 同一族的还有 L70(系统资源凭印象用)、L75(新 API 下老属性静默失效):
  **共同点都是"编译器不管、运行时不报、只是默默不对"**。
  凡是"跨过类型系统的边界"(userInfo、KVC、字符串 key、动态查找),
  都要去对面那一头看一眼真实写法。

## L77 · 「切换 A 会顺带重建界面」这种便车,只在 A 真的变了时才有

**现象**(2026-07-23,★档):切到★档之后,列表**一点变化都没有**。
过滤规则写了、编译过了、逻辑也对。

**原因**:换档之后我只做了一件事 ——
「让上游那个『隐藏没有未读的源』开关跟档位对齐」,
而重建整棵列表是**那个开关自己内部顺带做的**。
未读↔全部 时开关真的变了,于是白拿一次重建;
但 **全部→★ 时那个开关的目标值没变**(两档都是"不隐藏")→ 不切 → **也就没有重建**。

**教训**:
- **搭别人的便车之前,先问"他这趟车什么时候不开"。**
  「调用 X 会顺带做 Y」这种依赖,在 X 提前返回 / 判断出无需变更时**整条都会消失**,
  而且是静默消失(和 L19 那类"守卫直接 return"是同一个形状)。
- 修法不是去改上游那个开关,而是**把"我需要的那件事"显式写出来**:
  不该切时就自己调一次重建。多写三行,换来"不管走哪条分支都成立"。
- 排查这类问题的快捷判据:**功能在某些切换路径上生效、另一些不生效** ——
  那基本就是"某条路径少走了一步收尾",而不是核心逻辑错。

## L78 · 自定义视图放进工具栏,尺寸要**一次算死**,别让它跟着内容变

**现象**(2026-07-23 用户截图):底部三档控件里,当前档那颗药丸的**图标和文字叠在一起**。

**原因**:第一版让这条控件按内容自适应宽度(`intrinsicContentSize` 现算)。
换档时药丸从一格挪到另一格,需要的总宽变了,**而工具栏拿到的还是旧宽度** ——
于是展开的那颗药丸被压扁,图标和文字挤成一团。

`invalidateIntrinsicContentSize()` 也救不了:工具栏给 `UIBarButtonItem(customView:)`
排版的时机和我们改配置的时机不是同一拍,新宽度要等下一轮才可能被采纳,而那时已经画过一帧了。

**第一次修**(错的,记下来免得后人重走):开局用 `systemLayoutSizeFitting` 量一次尺寸钉死。
**还是会挤** —— 因为"量"用的是那个 API,而 UIKit 给按钮排版时用的是另一套,量出来偏小。
**这正是 L73 本身**:我算一套、系统算一套。用一个"我算的尺寸"去修"我算的尺寸和系统对不上",
等于在同一个坑里换个姿势再踩一次。

**第二次修(对的):不量了。**
三格各给一个**写死**的宽度(展开 84、收起 44),关键在于
**任何时刻都恰好是「一个展开 + 两个收起」→ 总宽是个常数**。
换档只是把宽度在三格之间挪一挪,**外层永远不需要重新问尺寸**。
于是三件事同时不可能发生:量歪、被压缩、整条控件忽宽忽窄地跳。
代价是宽度写死,前提是三个档都是两个汉字、字号也写死 —— 这个前提是稳的。

**教训**:
- 这是 **L73** 那句话的又一次应验:**"我算一套尺寸、系统也算一套"的地方,先问这两套什么时候会对不上。**
  放进系统容器(工具栏、导航栏)的自定义视图尤其明显 —— 容器只在它自己的节奏上问一次尺寸。
- **能定死的尺寸就定死。** "自适应"听起来更聪明,但它把一个静态问题变成了时序问题。
  这里的内容是**三个固定的短词**,压根没有自适应的必要。
- ⚠️ **最值钱的一条**:当问题是"我算的和系统算的对不上"时,
  **再算一次(哪怕算得更早、更准)都不是解** —— 解是让这件事**根本不需要算**。
  这次真正管用的不是"量得更对",而是"总宽恒定,谁都不用重新排版"。

## L79 · 上游某条路"一直都在",不等于它在**你新加的入口**下也成立

**现象**(2026-07-23):首页新加的「全局搜索」放大镜点下去,搜索框**整块上移了一个状态栏的高度**,
和时间叠在一起(用户截图)。而同一条代码路径,用外接键盘 ⌘F 走从来没出过问题。

**原因**:上游的 `showSearch()` 是 `推出文章列表页 → asyncAfter(.now()) → 激活搜索框`。
⌘F 和长按桌面图标那两个入口,页面往往**已经在屏幕上**了,那一个 runloop 够用。
而从首页点按钮是**一边推页面、一边打开搜索框** —— 激活时页面还没进窗口,
搜索框于是按"没有安全区"排版。**症状特征很明确:整块内容正好上移一个状态栏的高度。**

**第一次修**(错的):"每轮 runloop 看一眼是否就绪,最多看 30 轮就放弃"。
**30 轮 runloop 只有几毫秒,而推入动画要 0.35 秒** —— 于是根本没等到就放弃了,
表现成"第一次点没反应、再点一次才出来"。
⚠️ **别拿"轮数"当计时器:一轮有多长完全不由你定。**

**第二次修**(还是错的):改成跟着**转场协调器**走。可**第一次点进来时那个协调器拿不到**
(推入动画还没开始),于是走了"页面已就绪"的分支,搜索框没装上 ——
表现成"第一次点没反应、退出再点一次才出来"。

**第三次修**(对了一半):不猜时机了,挂到 `viewDidAppear`。
那是系统唯一保证"页面已经在屏幕上、导航栏也排好了"的时刻。
点放大镜时只**记一笔"待打开搜索"**,真正打开放在 `viewDidAppear` 里消费掉
(再加一个 0.6 秒的兜底,应付"这一页本来就在屏幕上、根本不会推入"的情形)。

**第四次修(真正对了):把"改搜索框摆法"和"激活搜索"分到两个排版回合。**
第三版仍然是「设 `preferredSearchBarPlacement = .stacked` → 紧接着激活」,
结果只生效一半:**范围切换条出来了,搜索框没装上**。

⚠️ **这一条的证据是用户给的,不是我推的**:上一版的表现是"第一次点空白、退出再点就正常"——
第二次之所以正常,恰恰因为**摆法在第一次点的时候就已经改成 `.stacked`**,到第二次激活时早已落定。
一个"第二次才对"的现象,直接指出了"前一件事需要一整个回合才生效"。

现在:**点按钮的那一刻改摆法**(顺手 `layoutIfNeeded()` 逼它当场生效)→
推入页面(整段动画)→ `viewDidAppear` 里才激活。中间隔着一整段时间。
同时把搜索激活期间的摆法换成经典的 `.stacked`(标题在上、搜索框在下、范围条再下面):
`.integratedButton` 是**给用户点按钮触发**设计的,由代码激活时排版一直不对。

**教训**:
- **给一条老路加新入口时,要问"这个入口和原来那些入口,时序上有什么不同"。**
  代码一行没改、行为却不同,差别全在"调用它的时候,世界处于什么状态"。
- **"整块内容正好偏移一个状态栏/导航栏的高度"是一个可以直接读出根因的症状**:
  基本都是"在进窗口之前就排了版"或"安全区还没算好"。见到这个偏移量就别猜别的了。
- ⚠️ **最值钱的一条(前后错了三版才想明白)**:
  **与其算"什么时候好了",不如挂到系统告诉你"好了"的那个回调上。**
  我先后用了"等一个 runloop""数 30 轮""盯转场协调器"—— 三个都是在**猜**,
  于是每一个都在某条路径上不成立。而 `viewDidAppear` 是系统的承诺,不用猜。
  凡是"要等页面/动画/布局就绪"的事,先去找那个官方回调,找不到再考虑轮询。
- **"第二次就好了"是一个信息量极大的现象**:它几乎总是在说
  "有件事需要额外一个回合才生效,而第一次的时候你没给它这个回合"。
  见到这种表现,别去查"第一次少做了什么",要去查**"第一次是不是把两件事挤在同一拍里做了"**。

## L80 ·(已回滚为背景,真正落地看 L82)一层"整页共享的浮层"做不出"跟着翻页一起滑"

> 本条记的是"翻页淡入淡出"那一版思路。用户看过后决定**不用淡入淡出、改用方案 C(每页一份)**,
> 淡入淡出代码已回滚。落地方案与完整教训见 **L82**;下面保留原分析当背景。

**背景**(2026-07-23):文章内容页的阅读栏(UIKit 画的大标题/源/头像)在左右翻页时**滞留**——
正文跟着页面滑走了,标题还钉在原地,过一下才刷成新的(用户截图 2/4)。
用户先想"让标题跟正文一起横向滑进滑出"。

**为什么"真同步滑动"这层做不到**:翻页那一瞬间,屏幕上**同时有两篇文章**
(旧的往外滑、新的往里滑)。要让标题也跟着滑,就得**同时存在两个标题**。
而这层浮层是**整个文章页共享的一份、只有一个标题标签** —— 画不出两个。
→ 想真同步,只有把标题变成"每一页自己的":要么放回网页(它本就是页面的一部分),
要么把这层 UIKit 做成"每个 WebViewController 各带一份"。前者丢飞行动画,后者是大改。

**这一版的折中(用户选定)**:不追求跟手指滑,改**淡出淡入** ——
翻页一开始(`willTransitionTo`)淡出,翻页结束(`didFinishAnimating`)换好内容再淡回。
挂的是**真回调**(不是猜时机),而且只动 `container.alpha`(不碰 inset/offset/逐帧布局,L73/L63)。
`didFinishAnimating` 的 completed / 未完成两路都要淡回,否则划一半又收回时栏会一直透明。

**教训**:
- **"一个共享实例能不能表现出 N 份同时存在的样子"是个该先问的问题。**
  转场、翻页、并排对比这类**同一时刻要展示多个状态**的场景,共享单例天然做不到,
  别在它身上堆补丁 —— 要么接受降级表现(这里的淡入淡出),要么把它拆成每单元一份。
- 淡入淡出是这里的**诚实降级**:承认做不到真同步,给一个稳的观感,而不是假装能同步却在真机上抖。

## L81 · `atDocumentStart` 注入的脚本,别把"要尽早生效的事"塞进 DOMContentLoaded 回调里

**现象**(2026-07-23 用户报):文章页**先闪出网页自带的老式表头**(源名/头像/标题/日期),
十几秒后才刷成阅读栏的新式样子。

**根因**:注入脚本虽然是 `atDocumentStart` 跑的,但它把"打标记类 + 装隐藏样式"
都写在了 `inject()` 里,而 `inject()` 被 `document.addEventListener("DOMContentLoaded", inject)`
**推迟到了 DOMContentLoaded**。一篇长文从 document start 到 DOMContentLoaded 可能好几秒 ——
那几秒里标记和隐藏规则都还没上,网页原生表头照常显示。
**"脚本在 document start 跑"不等于"脚本里每件事都在 document start 生效"** ——
只要那件事被挂进了某个晚触发的事件回调,就白瞎了 document start。

**修法**:把"打标记类"和"一小段只含隐藏规则的 `<style>`"提到脚本最顶层、当场执行。
`<html>`(documentElement)在 document start 一定存在,样式挂它身上即可,不必等 `<head>`。
需要 DOM 备好才能做的(读 body padding、扫图片尺寸)才留给 DOMContentLoaded 的 inject()。

**教训**:
- **`atDocumentStart` 的价值在"立刻",一旦把关键动作挂进 DOMContentLoaded/load 就丢了这个价值。**
  凡是"要在网页一出现就生效"的规则(尤其是**藏掉**某个原本会一闪而过的元素),
  当场同步装上,别等任何事件。
- 又一次 **L79 家族**:"我以为它早"和"它实际什么时候发生"是两回事,要顺着代码确认到底挂在哪一拍。

## L82 · "整页共享一层浮层"改成"每单元各带一份",是这类翻页错位的根治法(方案 C)

**背景**(2026-07-23):文章内容页的阅读栏在真机上翻页反复出问题 ——
标题滞留、老表头闪现,最后甚至标题和正文**叠成一团**(整块没被往下推)。
先试了"翻页淡入淡出"(L80),用户觉得治标不治本,拍板上方案 C,淡入淡出已回滚。

**根**:阅读栏是**一层浮层,盖在整个文章页上、所有页共享**。翻页时正文跟着页面横滑,
而这层钉在原地,只能靠"翻页结束后重新绑到新页、重算 inset、重藏表头"来追。
这种"事后重新同步"在真机上时序不可靠:新页的 webview/scrollView/加载状态每次都不同,
于是每翻一页都可能对不齐(inset 没生效→叠一起;表头没藏住→露出老表头)。

**方案 C(根治)**:把浮层从"整页共享一份"改成"**每个 WebViewController 各自带一份**",
贴在它自己那一页的 view 上、盯自己的 scrollView。于是:
- 横滑:每页连同它的栏一起滑 → 标题和正文**天然同步**,不再需要事后对齐。
- 竖滑:栏在自己那页里飞上去缩小 → **飞行动画一点没丢**。
- 藏表头 / 算 inset:各页管各页,**没有跨页重绑** → 那类竞态从根上消失。

`ArticleHeaderBarController` 这个类几乎没改(它只用 host.view,不碰 navigationItem),
只是把宿主从 ArticleViewController 换成 WebViewController、挂载时机改到每页自己的生命周期。

**⚠️ 改完必补的一环(差点又踩 L62)**:每页一份后,滑走一页 = 那页连同栏一起销毁。
毛玻璃动画器若正停在"活动中"被释放会**直接崩溃**。整页共享那版几乎不销毁所以没暴露,
每页一份后翻页频繁销毁必崩。→ 给容器加 `willMove(toWindow:)`,离开窗口先停掉动画器。

**教训**:
- **"一个共享实例扮演不出 N 份同时存在的样子"** —— 转场、翻页、并排对比这类
  同一时刻要展示多个状态的场景,共享单例天生做不到,靠"事后同步"补是脆的。
  正解常常是**拆成每单元一份**,让同步变成"天然的"而不是"算出来的"。
- **架构一换,原来"几乎不发生"的路径可能变成"频繁发生"**:共享单例极少销毁,
  改成每单元一份后销毁变频繁,于是"释放时的清理"(停动画器、注销观察)从可有可无变成必做。
  换归属前先问:**这东西的创建/销毁频率变了吗?原来偷懒没做的清理,现在还能偷懒吗?**
- 这次难得**在模拟器上验到了静止态**:用状态恢复直接开进那篇之前彻底叠一团的文章,
  现在排版完全正确。**能在模拟器复现的那一半一定要复现**,它把"改对没有"提前确认了一大半。

## L83 · 停掉一个会被回调重建的东西,只停一次是不够的 —— 要把"重建它的路"一起关掉

**现象**(2026-07-24 用户实测,方案 C 上线后):左右翻页**经常闪退**。
崩溃报告点名:`UIViewPropertyAnimator dealloc` 抛异常(活动中被释放,L62 那个),
上一帧是 `_smoothScrollWithUpdateTime`(惯性滚动的每帧回调)。

**根**:我按 L62 加了防护 —— 页面离开窗口时 `willMove(toWindow:)` 停掉毛玻璃动画器。
但**惯性滚动还在继续、KVO 的滚动回调还在触发**:页面已经不在窗口上了,
`layoutAndApply` 照样跑,跑到毛玻璃那段又把动画器**重新建了出来**,
然后页面被释放 → 新建的那个动画器正停在"活动中" → 崩。
**"停一次"防不住"停完又被建回来"。**

**修法**:在 `layoutAndApply` 开头加一道**窗口守卫** —— 不在窗口上就什么都不做。
于是离开窗口后动画器不可能被重建;页面被滑回来时 `layoutSubviews` 自然重新触发,一切恢复。

**教训**:
- **清理一个"由回调驱动重建"的资源,必须同时关掉重建它的那条回调路**,
  否则清理只是给重建让了个位。判断很简单:停掉它之后,**还有谁、在什么时机会再造一个?**
- 惯性滚动是个容易忘的"幽灵驱动":手指早就离开了、页面都快销毁了,
  它还在每帧发回调。凡是挂在滚动回调上的逻辑,都该问一句"页面不在屏幕上时它还该跑吗"。

**同一轮的另一个坑(翻页中途三层叠一起)**:把阅读栏的挂载提前到 renderPage(治"表头闪现")后,
**网页装载期间 WebKit 会自己重置滚动位置**,那一瞬的 contentOffset 不可信 ——
拿它算飞行进度就得出"半冻结"的鬼样子。修法:加一个 `contentSettled` 标记,
**没装载完一律按"停在顶部"画**,didFinish 之后才信真实偏移。
(又是 L73 家族:我读的偏移和 WebKit 正在写的偏移,不是同一个世界的。)

## L84 · 上游把"运行时状态"持久化在了"用户设置"同一个键上 —— 谁实时读它,谁就被临时值毒到

**现象**(2026-07-24 用户实测,方案 C 第二轮):同一篇文章 ——
下滑到底**顶栏整个消失**;继续滚动**内容自己乱跳**;滚回顶部出现**错乱的中间态**。

**根**(一条链,钥匙在上游一行代码里):
上游 `hideBars()` 的第一行是 `AppDefaults.shared.articleFullscreenEnabled = true` ——
它把"栏现在藏着"**持久化成了设置项**(上游语义:全屏开关 = 当前状态,重进恢复)。
而阅读栏模式的判断(`nnwReadingBarEnabled`)实时读的正是这个值:

```
下滑 → 藏栏(设计如此) → 标记被写成 true → 藏栏引发布局 → viewDidLayoutSubviews
    → nnwUpdateReadingBar 实时读标记 → 误判"用户开了沉浸模式" → detach()
    → 阅读栏连 contentInset 一起拆掉 → 顶栏消失、位置乱跳
```

**为什么共享版没炸**:它只在翻页/加载完时 update,从不在"藏栏引发的布局回调"里跑,
临时翻掉的标记从没被读到过。每页一份后 `viewDidLayoutSubviews` 正好撞在枪口上 ——
又一次 **L82 教训②** 的应验:换架构后,原来"几乎不走"的路径变成了"每次都走"。

**顺带发现的隐藏炸弹**:藏着栏时杀掉 app,标记**永久留在 true** ——
下次启动整个 app 变成沉浸模式、阅读栏全没了。这个炸弹和架构无关,一直埋着。

**修法(两道,互为保险)**:
1. `nnwToggleBars` 里:阅读栏模式下藏完栏**立刻把标记写回 false**(上游方法一行未动)。
   顺带拆掉了那颗持久化炸弹。
2. 本页的阅读栏模式**在开始装载时定格一次,中途不实时读**;换文章时重新定格。
   代价:在设置里切「全屏阅读」,对已打开的那篇要翻页才生效 —— 有意的取舍。

**教训**:
- **一个键同时承担"用户的选择"和"此刻的状态",迟早毒到某个读它的人。**
  见到 `设置项 = 运行时状态` 这种上游设计,自己的代码要么定格快照、要么另存一份,
  **绝不实时读**;还要问一句:这个状态**中途退出时会不会被永久留下**?
- 排查这类问题的入口是**"谁写这个值"**:`grep` 一遍所有写入点,
  发现"系统行为也在写它"的那一刻,谜底就出来了。

## L85 · 换掉一个容器的内容物,先问「原住民靠谁活着」—— weak IBOutlet 随数组替换而释放,上游一写就崩

**现象**(2026-07-25,文章页控件板上线后):app **打开秒崩**,崩在 `updateUI()` 的断言失败。
更糟的是这个版本被原样交付给了用户(见 L86)。

**根**:上游那 6 个工具栏按钮的 IBOutlet 全是 **weak**。它们能活着,唯一的原因是
一直待在 `toolbarItems` 数组里被数组强持有。控件板把整个数组换掉的一瞬间,
旧按钮没了持有者、全部释放,weak 引用变空;而 updateUI 里"往老按钮写状态"的上游代码
被我们**故意保留**(最小 diff)——保留了**写入**,却没保住**被写对象的命**。
启动即 `nextUnreadBarButtonItem.isEnabled = false` → 隐式解包空值 → 崩。

**修法**:板子加一个 `legacyItemsKeptAlive` 属性,替换数组**之前**把旧按钮原样收养进去,
永不使用、只管续命。上游的写入从此打在活对象上,恢复无害。

**教训**:
- **接管一个容器的内容时,要连问三件事**:①原来住在里面的对象,靠谁持有?
  ②我换掉之后它们还活着吗?③还有谁在写它们?——第③问是 L74(数写入点),
  第①②问是本条。L74 问"谁在写",L85 问"被写的还在不在"。这次恰恰是
  L74 做对了(保留上游写入不改)、L85 没想到(写入对象死了),两条要配套用。
- **weak IBOutlet 是"最后一根线断了才知道疼"的典型**:平时怎么折腾都没事,
  因为总有别的强引用兜着;哪天你动了那个"别的",它才崩给你看。
  凡是要移除/替换界面元素,先 ⌘F 它的声明看是不是 weak。

## L86 · 「装机+启动返回了 pid」≠「app 活着」;修崩溃要拿 UUID 证明「跑的是修过的那份」

**现象**(2026-07-25,和 L85 同一晚,两个交付纪律窟窿一起暴露):
① 控件板版本装机、launch 返回了 pid,我就当交付成功 —— 其实**那次启动已经崩了**,
崩溃报告的 pid 和我 launch 打印的一模一样,是用户打开 app 才发现秒崩。
② 第一轮修复我凭直觉断定凶手是 coordinator 空值(它确实是个真隐患,defer 让钩子
在"提前 return 的分支"也执行,coordinator 那时还没赋值),修完重装**还在崩**;
直到用崩溃日志里的 dylib **UUID** 和新编译产物一对,确认"崩的确实是修过的二进制",
才肯回头重读崩溃栈,找到真凶(L85 的 weak outlet)。

**教训**:
- **`simctl launch` 返回 pid 只说明"拉起过",不说明"活下来了"。**
  以后凡是装机交付,必须补一步:**等几秒 → `ps -p <pid>` 确认还在 →
  `ls -t ~/Library/Logs/DiagnosticReports | head` 确认没有新 .ips** —— 三条命令的事,
  这次省掉它的代价是把一个必崩版本交给了用户。
- **修崩溃的第一步不是猜原因,是核身份**:崩溃日志里带着二进制的 UUID
  (`dwarfdump --uuid` 可对),先确认"现在崩的是不是我改过的那份"。
  身份对上了还崩,才说明诊断错了,别再往同一个方向修。
- **一处崩溃栈可能藏着不止一个雷**:coordinator 空值和 weak outlet 都是真的,
  只是引爆顺序有先后。修掉一个不代表结案,重装复测才算数。

## L87 · 「悬停和放手共用同一个判定函数」≠ 一致 —— 两次调用之间,几何变了

**现象**(2026-07-25 用户实测):编辑订阅页拖源到文件夹**旁边**,悬停预告明明是
"排旁边"(行间开缝、无描边),**松手瞬间却被丢进了文件夹**。

**根**:悬停中 UIKit 为"插缝"把下方的行往下让了一行(缝是布局里的真实占位);
**松手瞬间缝合拢、行弹回原位**。放手执行时拿同一个手指坐标去重算判定,
对上的是"弹回后"的行框 —— 坐标正好落进跳上来的文件夹行中间,结论翻了。
代码里那句自信的注释「和悬停调用的是同一个函数,所以看着能放和真的能放必然一致」,
错在把"同一个函数"当成了"同一个结论":**函数一样,输入世界变了**(L73 家族:
我拿的坐标和系统此刻的行框,不在同一个时间点上)。

**修法**:放手不重算 —— `dropSessionDidUpdate` 把每次判定**缓存**下来,
`performDropWith` 直接用缓存(用户松手前亲眼看到的那份),重算只留作缓存为空的兜底;
didExit / didEnd 清缓存。所见即所得从机制上锁死,几何怎么弹都影响不到结论。

**教训**:
- **"共用同一个纯函数"只保证规则一致,不保证结论一致 —— 输入(几何/状态)在
  两次调用之间可能被系统改写。**要保证"预告=结果",正解是**只判定一次**:
  把预告那次的结论存下来,执行时直接用,而不是"再算一遍希望算出一样的"。
- 这和 L78(总宽恒定,谁都不用重新排版)是同一个哲学:当两次计算可能对不上时,
  **消灭第二次计算**,而不是让它算得更准。
- 排查这类"预告和结果不一致"的入口:先问**两次判定之间系统做了什么**
  (这里是插缝合拢),而不是先怀疑判定规则本身。

**L87 补记(同日,补丁切出的回归)**:悬停判定缓存上线后,「文件夹里的源往左拖拿出来」
失灵了 —— "拿出/留下"这条规则只取决于**手指自己的横向位置**,用户往左拉的最后一段路
被缓存冻在了"留在里面"。修:行锚(纵向,受行弹跳影响)用缓存;横向规则用**松手瞬间的
活 X** 重判 —— 这两种悬停在屏幕上本来就长一样(都是插缝),所见即所得对它们无约束,
松手位置才是用户的表达。**教训:缓存"用户看到的判定"时,先分清判定里哪些成分是
"屏幕反馈过的"(必须缓存)、哪些是"只有手指位置在表达的"(必须用活的),一刀全冻会切到后者。**

## L88 · 「卡死几十秒」但主线程是空闲的 —— 卡住的是系统放下动画在"追锚",冻结要盖住拖拽的两端

**现象**(2026-07-25 用户报):编辑订阅页长按悬停,app 卡死几十秒;期间"往左拖出文件夹"
也全部失灵。**采样快照显示主线程 90% 时间在空转等消息** —— 不是死循环,不是阻塞。

**破案过程(方法论,比结论值钱)**:模拟器点不了 → 后台挂 `sample` 看门狗(每 15 秒一张
全线程快照,常驻等用户复现)→ 用户复现后翻快照:主线程空闲,但
`_UIDragSetDownItemAnimation updateAnimationTargeting` 在**每帧重新瞄准目标** ——
卡住的是系统的"放下归位"动画,它追一个一直在动的锚,追了几十秒。
**"界面卡死"不等于"主线程忙/堵",还有第三种:输入被一个下不了台的系统动画扣住。**

**根**:本页收到模型通知就整棵重画(0.2s 防抖);已有的拖动冻结(isDragInProgress)
**两端各漏一段**:
- ① 冻结从 dragSessionWillBegin 才开始 —— **长按抬起动画期间**是空窗;
- ② didEnd 立刻解冻并补画 —— **放下动画还要飞 0.3~0.5 秒**,重画一来,
  动画的目标 cell 被换掉 → 每帧追锚 → 几十秒。
拖放会话烂掉后,松手的 drop 根本不被执行 —— "拿不出来"不是判定错,是执行被吞。

**修法**:冻结改为**从 itemsForBeginning 起、到 didEnd + 0.6 秒止**(盖住两端动画);
scheduleReload 的守卫从"直接丢弃、指望 didEnd 补画"改成**等待 + 保险丝**
(夭折的抬起不会来 didEnd,冻结超 15 秒且 UIKit 说无活跃拖拽就自行解冻 ——
否则列表从此永不刷新,L19 那族)。

**教训**:
- **给"和系统动画共舞"的界面做冻结,要按动画的完整生命周期划边界**:
  抬起动画、飞行、放下动画,一段都不能露在外面。"会话开始~会话结束"不是全程。
- **守卫的"直接 return"必须回答:谁负责善后?那条善后路一定会走到吗?**
  "拖动结束会补画"在夭折路径上是空头支票 —— 等待+保险丝才是自洽的。
- **主线程采样要看全部线程和系统动画栈**,只盯自己代码的符号会漏掉这类"系统在替你卡"。

## L89 · 拖动中「布局的行号」和「数据快照的行号」是两个世界 —— 拿行号跨界查询,查到的是错的行

**现象**(2026-07-25,"拿不出文件夹"三连修的最终根因):悬停诊断日志实锤 ——
手指挪 2pt,锚行从「文件夹行」跳成「文件夹里的 kottke」;手指在文件夹 4 **下方**,
查出来的锚行却是文件夹 3 的孩子;UIKit 自己的目的地行号也从 [0,15] 跳到 [0,13]。
锚行错了,后面判定、缓存、执行全错 —— 表现就是"拖到外面的行上,放下还在文件夹里"。

**根**:拖动中 UIKit 的**占位缝占着一个行号**。`indexPathForItem(at:)` 答的是
**布局世界**的行号(含占位),`dataSource.itemIdentifier(for:)` 答的是**快照世界**的
行号(不含占位)—— 从占位那行起,两个世界的行号全部错开一位;而且悬停意图在
「插缝 ↔ 进文件夹」之间切换时占位一插一撤,错位还在闪变。
拿一个世界的行号去另一个世界查身份,必然张冠李戴。落点高亮按行号找 cell,
描边也可能画在错的行上 —— 用户"看着预告也会错"的另一半原因。

**修法(消灭行号)**:cell 配置时把它代表的16 Item **盖钢印**(关联对象)在 cell 上;
锚行查找改为「翻 visibleCells、按 cell.frame(真实视觉位置)找、读钢印认人」,
落点高亮同款。占位怎么插撤、行号怎么错位,cell 的身份和位置都不经过行号,错不了。

**教训**:
- **UICollectionView 拖放期间,任何「坐标→行号→身份」的两段式查询都是雷**:
  两段各自属于不同世界(布局含占位/快照不含)。要么全程只用系统一手递来的目的地
  (它内部对过账),要么像这次一样**让身份直接长在 cell 上**,根本不过行号。
- L73→L87→L89 是同一条脊椎的三节:我算的 vs 系统算的;算一次 vs 算两次;
  这回是**同一个"行号"在两个坐标系里各有一套值**。通用药方始终是:
  找到那个"不需要换算的表示"(恒定总宽/缓存的判定/钢印),让换算彻底消失。
- 日志破案三板斧(这轮验证有效):放手点日志 → 悬停变化日志 + 系统目的地对账 →
  差异一眼定位。交互 bug 猜三轮不如埋一轮日志。

**L89 补记(同日,毒药的最后一处)**:锚行清了行号,**插入序号**那条路里还剩一个
`itemIdentifier(for: destinationIndexPath)` —— 往下拖时源行的格子被系统收起,
布局行号和快照行号又错开一位,"缝前面是谁"查错一行,表现为
「从下往上拖没问题,从上往下拖错位一个」(方向性指纹:往上拖目标在源行之前,不受收起影响)。
连"上半插前/下半插后"的判断一起收回自己做(锚行矩形 midY 判),destinationIndexPath
从整条拖放链路中彻底退场。**教训:清一种毒要 grep 全部用点 ——
`indexPathForItem / itemIdentifier(for:) / destinationIndexPath` 在拖放路径上一个都不能留;
清了三处漏一处,等于没清。**

## L90 · 「最常见的那种写法」不等于「在你的上下文里有效」—— 去色的混合模式一个字节都没改动

**现象**(2026-07-28,给订阅源图标做灰度化):第一版用的是网上最标准的 CoreGraphics 去色写法 ——
画原图 → `setBlendMode(.color)` → 盖一层纯灰。理论完全站得住(`.color` 的语义就是
「取上层的色相饱和度 + 下层的明暗」,盖一层饱和度为 0 的灰 = 去色)。
**编译过、装机过、日志明明白白打着「已加工」,截图上图标却还是彩的。**

**定案靠的是取样,不是眼睛**:对同一个像素点前后取色 ——
Quanta 的橙 `#FF8300` **饱和度 1.00 → 1.00**,一个字节没变。
如果只看截图,很容易得出「好像淡了一点点?」这种既不能证实也不能证伪的判断,
然后在同一个维度里继续换写法(换灰的深浅、换 alpha、换 format)。

**排查顺序里做对的一步**:先加日志区分两种假设 ——
①这条链路根本没被调用;②调用了但加工无效。
这两种假设的修法完全不同(前者去找写入点,后者去改算法),**必须先分开**(L27)。
日志打出「已加工 → 尺寸 144×144」,一句话排除了假设①,也顺带排除了
「图标没进包」「缓存返回了旧值」「cell 用的是别的类」这些猜测。

**修法**:不再在混合模式这个维度里试,换成**结果完全由自己决定**的路 ——
把像素读出来,按 Rec.709 权重自己算灰度,再写回去。一次成功。
图标最大 1024²、又有缓存,这点成本完全不值得为了「一行优雅的 API」去冒险。

**教训**:
- **「这是这件事最常见的写法」不是它在你的上下文里有效的证据。** 混合模式的行为受
  上下文的颜色空间、是否 opaque、是否 wide gamut 影响,而这些在 `UIGraphicsImageRenderer`
  里不是你直接控制的。**凡是「把效果交给系统某个开关」的做法,都要用可量化的方式验收。**
- **视觉改动的验收必须落到数字上**(L44 已经写过一次,这次又救了一轮):
  取样一个像素点、比对前后的 HSB,一秒定案。肉眼对「饱和度降了没有」极不可靠,
  尤其在深色模式的小图标上。
- **当一个效果「必须成立」时,优先选那条你能完全掌控结果的实现**,
  哪怕它看起来更笨(逐像素 vs 一行混合模式)。这和 L78 是同一个哲学:
  **能定死的就定死,别把静态问题变成依赖环境的问题。**

**同一轮里的一个副产品(记下来免得后人误判)**:排查途中在 `IconView.rectForImageView()`
里读到一句很可疑的注释 ——「**Aspect-fit, but never scale up**」(小图标按原尺寸居中,不放大)。
当时以为找到了「图标左边缘参差」的根因。**但日志证明它不是**:实际的 favicon 都是
144×144 或 1024×1024,远大于显示尺寸,一律会被缩小到铺满,那条分支根本走不到。
→ **读到一句可疑的代码,和证明它就是本次的原因,中间还差一步实测**(L52 家族)。

## L91 · 对标一个参考产品时,我犯的四种误判 —— 「它做了什么」不等于「它为什么能这么做」

**背景**(2026-07-28):用户给了 5 张 Reeder 截图,问「为什么它精美、我的粗糙」。
我做了量化对比,提出 10 条改进,分三个梯队。**最终只有 3 条落地**:
第一梯队 4 条里 1 条被用户否决,第二梯队 3 条用户看图后全否,
第三梯队 2 条**我自己考古后推翻**。命中率三成。

四条结论各自死于不同的误判,而且这四种都会再犯,所以逐条记下来:

**① 把「系统的默认样式」当成「我们的设计不一致」。**
我说界面上"三种形状语言并存:圆形描边 / 药丸 / 药丸容器"。查代码后发现
**没有一个是我们画的** —— 顶部就是 `rightBarButtonItems = [search, edit]`、
底部就是 storyboard 的标准 `toolbarItems`,零自绘。那些形状全是 **iOS 26 的
Liquid Glass 自动套的玻璃胶囊**。
→ 这是 **L30 换了个场景的重演**(「界面上多出来的东西,先问是谁画的」)。
L30 记的是网页里的元素,这次是 app 界面上的形状,**同一条规则我又忘了**。
**批评一处外观之前,先用 grep 确认那东西是我们画的还是系统画的。** 一次搜索的事。

**② 拿不对等的状态做对照。**
我说「Reeder 顶栏和内容同色无缝,我们有一条渐变带」——
但我拿的是 Reeder **未滚动**的截图(大标题还在屏幕中间)去比我们**滚动后**的状态。
我手上根本**没有 Reeder 滚动态的截图**,这个对照不成立。
而那条"渐变带"还正是 **L62 花了好几轮调出来的停靠态毛玻璃**,是有意设计。
→ **L27 的又一次应验**:对照物只有在"两种假设会给出不同结果"时才算证据。
这次更基础 —— **两边根本不在同一个状态上,连比都没比对。**

**③ 把跨语言的排版手法当成通用最佳实践。**
Reeder 设置页的精致感有一大半来自分区小标题 `BROWSER` / `ARTICLE LIST`:
**全大写 + 加宽字距 + 小字号**。这是**英文排版特有**的手法 ——
中文没有大小写,只能靠加字距模拟,而汉字加字距容易显得松散、像老式排版。
→ **参考物是英文界面时,先问一句:这个手法依赖它的文字系统吗?**
依赖的那些(全大写、首字母大写、连字、词间距)对中文界面一律不可迁移。

**④ 把「参考物提供了开关」当成「应该默认开」。**
Reeder 的外观设置里有 `Grayscale favicons`,我据此提议**并默认打开**了图标去色。
用户看了实物明确要求保留颜色。
→ **Reeder 把它做成开关而不是直接去色,这件事本身就说明那是口味问题,不是最佳实践。**
参考物提供了开关的地方,默认值该跟用户走,不该跟我的审美走。

**方法论上真正的收获(比上面四条更值钱)**:
第二、三梯队那 5 条,我都是**先出可交互的效果图、不动一行代码**,
让用户自己点开关看。结果他一句话就定了:「我感觉都不值得改」。
→ **对读不懂代码的用户,一张能自己操作的方案预览,抵得上几千字的论证**,
而且能让他**自己**得出"不值得"的结论,而不是被我说服。
凡是"要不要改外观"这类**没有对错、只有取舍**的决定,
**先做预览、不要先做实现** —— 这次省下的是三条改动的实现与回滚成本。

**一个反向的教训**:也正因为这样,前两个梯队我是**先实现再给用户看**的
(第一梯队直接改了代码装机),其中 favicon 去色那条做完又关掉。
虽然代价不大(留成了默认关闭的开关),但如果当时也先出效果图,
用户会在我动手之前就说"图标要有颜色"。**顺序错了,多做了一轮。**

## L92 · 一个机制修到第 3 版还不好,就该停下来问「能不能消掉它的前提」—— 我修到了第 7 版

**背景**(2026-07-28):用户报「点搜索按钮会先退回一个空白页」。
这个空白页来自我们自己的设计:全局搜索先 `selectSidebarItem(nil)` 清空选中、
再推出文章列表页 —— 那一页当然是空的。为了让用户退出搜索后不停在这个空页上,
配了一套"自动逃回首页"的机制。

**这套机制前后改了 7 版,全部失败**:

| # | 做法 | 否证 |
|---|---|---|
| 1 | 挂 `willDismissSearchController` | 某些收起路径不走这条回调 |
| 2 | 改挂 `didDismissSearchController` | 同上 |
| 3 | 轮询 `searchController.isActive` | **检测本身没问题**(日志确认),失败在后续动作 |
| 4 | `navigationController.popViewController` | 这一页**独占一个导航栈**(实测深度=1),没有上一页可回 |
| 5 | `viewController(for: .compact)` 拿栈再 pop | 拿不到,它不是 UINavigationController |
| 6 | 复用上游 `selectFeed(nil)`(内部会 show(.primary)) | **完成回调确实跑了**,界面纹丝不动 |
| 7 | 上面那条再延后 0.35 秒 | 一样 |

**最终的硬事实**:在 iPhone 的 collapsed 分栏里,
`rootSplitViewController.show(.primary)` 这个调用**就是无效的** ——
不管谁调、什么时机调、前面铺垫了什么。日志把它钉死了:调用执行、完成回调执行、界面不动。

**转折点在第 7 版之后**:不再修这套机制,改为**消掉它的前提** ——
进搜索前不清空选中,而是选中「全部未读」。背后那一页从此有内容,
**空白页不存在了,"从空白页逃出来"这件事自然也就不需要**,90 行机制整个删掉。

**教训**:
- **修一个机制到第 3 版还不好,就不该再修第 4 版,而要退一步问:
  这个机制是在补救什么?那个「什么」能不能不发生?**
  我这次一路修到第 7 版,每一版都"看起来只差一点点" ——
  正是这种"只差一点点"的感觉让人一直修下去。L66 早就写过这条
  (「一个需要不断打补丁才能不崩的机制,通常说明它本来就不该存在」),我又走了一遍。
- **判断"该不该继续修"的一个实用信号:每一版失败的原因都不同。**
  原因相同说明方向对、细节没调好;**原因每次都不一样,说明你在一片雷区里乱走**,
  换条路比继续走划算。
- **删代码比加代码可靠**:方案 B 净删 68 行,而且删掉的正是最脆的那部分。

---

**⚠️ 但这条教训有个尾巴,要一并记住(不然会误以为方案 B 是终点)**:

方案 B 之后用户提了新诉求 ——「退出搜索时,原来在哪就回哪」。
我按同样的思路做了方案 C(记下来处、退出时 `selectFeed(来处)`),
**结果空白页又回来了**:来处是首页时,`selectFeed(nil)` 真的把列表清空了,
而 `show(.primary)` 依然无效 —— **等于绕了一圈又踩回同一堵墙**。方案 C 已回滚。

所以这堵墙的真正形状是:**只要"回到首页"这件事本身依赖 `show(.primary)`,就走不通。**
真要满足"回到来处",得换成不依赖分栏导航的呈现方式(比如把搜索做成 modal),
那是重做,不是修补。详见 NOTES-todo T28。

**(2026-07-28 晚续)** 按 modal 方向重做完成(方案 D,`NNWGlobalSearchViewController`),
挂起机制那套也一并删干净了;`show(.primary)` 无效的静态尸检结论
(根因指向 UIKit 没实现 collapsed 下 show(.primary)→pop 的逆映射,
以及备用的 popToViewController 原语)记在 T28 里,别再对着这堵墙做实验。

**顺带记一个自己挖的坑**:这几轮我一直用
`xcodebuild ... | grep ... && ./tools/install-to-simulator.sh` 一条命令跑,
而 **`&&` 判断的是 `grep` 的退出码,不是 xcodebuild 的** ——
于是有一次编译失败了,脚本照样把**上一次的旧产物**装进了模拟器(L41 的翻版)。
以后编译和装机分两步跑,别串在一条命令里。

## L93 · definesPresentationContext 的页面里,`self.dismiss` 的第一目标是它自己弹出的东西

**现象**(2026-07-28,做 modal 全局搜索页,交付前的静态审查抓到,没上真机就修了):
modal 页面里挂了 UISearchController 且进页就激活(`definesPresentationContext = true`)。
「取消」按钮和"点结果关页"都写的 `self.dismiss(animated:)` —— 静态审查指出:
**这个 dismiss 关的不是 modal,是激活着的搜索控制器**。表现会是:
「取消」要点两次才关页;点结果时页面根本不关,文章跳转发生在它背后。

**原理**:UISearchController 激活是一次**真实的 presentation**,呈现上下文正是
设了 `definesPresentationContext` 的那个 VC —— 于是 `self.presentedViewController`
就是搜索控制器。而 UIKit 的 `dismiss` 规则:**自己名下有 presented VC 时,优先收它**。
我们的页面从 viewDidAppear 起搜索就常年激活,所以是必现路径。

**修法**(`NNWGlobalSearchViewController.closePage`):
关页统一走一个出口 —— 先 `searchController.isActive = false` 显式退掉搜索态
(顺便避免"搜索控制器在激活状态下被连根拆掉"的运行时警告),
再从**弹出者**发起 `presentingViewController?.dismiss(animated:completion:)`,一次收干净。
用户下拉关页不走这个出口,所以另挂 `presentationControllerWillDismiss` 补同一手。

**⚠️ 但第一版修复只对了一半 —— 用户实测又撞出第二层(同一天,更值钱的那半)**:

改成"从弹出者那头 dismiss"之后,用户测出来还是不对:**点取消后停在一张文章空白页上**
(截图是 ArticleViewController 的 "No selection" 空状态)。

病根在**一行之内的取值顺序**:

```swift
// ❌ 第一版:先退搜索态,再去"问"弹出者是谁
searchController.isActive = false
presentingViewController?.dismiss(animated: true)   // 这时问到的已经不对了

// ✅ 现在:先把弹出者捞在手里,再退搜索态,最后用手里那个发 dismiss
let presenter = presentingViewController
searchController.isActive = false
presenter?.dismiss(animated: true)
```

`isActive = false` 会**当场开始拆除搜索控制器的那层 presentation**,
而 `presentingViewController` 是顺着呈现关系现算出来的 —— 拆到一半时这条链正处在中间态,
算出来的就不是分栏控制器了,dismiss 于是发给了错的对象。

**定案靠的是日志,不是猜**(前面已经猜过一轮"是不是状态恢复""是不是有人 push 了文章页",
全是死路)。埋了三处:弹出时记导航栈、关页时记走的哪条路、关页后再记一次导航栈。
一轮测试就钉死了:两条路径关页后栈都稳定停在 `1 页:MainFeedCollectionViewController`,
点结果那条还额外确认了"目标文章在当前列表里=true"(没有静默失败)。

**教训**:
- **在设了 `definesPresentationContext` 的页面里写 `dismiss`,先问一句:
  此刻我自己名下有没有 presented VC?** 激活中的 UISearchController、alert、
  分享面板都算。有,`self.dismiss` 关的就是它,不是你以为的那层 modal。
  要关整摞,从 `presentingViewController` 那头发起。
- ⚠️ **最值钱的一条**:`presentingViewController` / `presentedViewController` /
  `navigationController` 这类**顺着关系链现算出来的属性,不是稳定的值,是"此刻的快照"**。
  只要中间夹了一个会改动这条链的调用(`isActive = false`、`dismiss`、`pop`),
  **就必须在改动之前把它取出来存到局部变量里**。
  判据和 **L66** 是同一条:「这个调用会不会改动我接下来还要读的东西?」——
  L66 是"读的是数据",这次是"读的是关系链",形状一模一样。
- **同一个坑连摔两次,说明第一次只修到了表层。**
  第一次修对了"该向谁发 dismiss",却没想到"向谁"这件事本身会被下一行改掉。
  → 修完一个时序 bug,再回头看一眼:**我这个修法自己有没有引入新的时序假设?**
- **"本仓库没有先例的 UIKit 组合,交付前值得单独跑一轮挑错。**
  这次的组合(modal 内挂 UISearchController)在仓库里是头一回,
  审查一轮就抓住了第一层必现 bug;第二层则是靠**埋日志**抓的 ——
  两种手段各抓一层,谁也替代不了谁。

## L94 · 「状态全对但东西不画」= 你调的那个开关根本不管这件事 —— 范围条四轮定案

**现象**(2026-07-28,全局搜索改 modal 之后的收尾):
文章列表页自己的搜索,范围条(该列表 / 全部文章)不显示。
这事当天已经修过一轮(commit `a92a012cc`,用户当时验收通过)——
但那轮只修好了**全局搜索**那条路,「进到某个源里点放大镜」这条路一直是坏的,没人发现。

**四轮走了两个完全不同的错误方向,值得都记下来**:

**方向一(错):以为是"没打开它"**,于是到处补 `showsScopeBar = true`。
翻 `UISearchController.h` 才发现方向从一开始就反了:

> 默认情况下 UISearchController **本来就会**在搜索激活时自动显示范围条
> (只要 `scopeButtonTitles` 至少两项),关闭时自动收起。
> **而只要你去设 `showsScopeBar`,就等于告诉系统"这事我自己管"**
> (`scopeBarActivation` 被改成 `.manual`)—— 从此系统撒手。

也就是说:**那句"打开范围条"的代码,正是范围条不出现的原因**。
我们一边把系统的自动档关掉,一边用一套比系统脆得多的手动逻辑去顶,换个摆法、重排一次就丢。
→ 改成显式声明 `.onSearchActivation`,之后一行都不碰 `showsScopeBar`。

**方向二(也错):以为是"有人把它抖掉了"**。改完还是不显示,于是怀疑
`didPresent` 里那句切 `.stacked` 摆法重排了搜索栏、把范围条抖没了,就把它撤了。**还是不显示。**

**定案靠日志**(埋了一行,把所有相关状态一次打全):

> `实际摆法=4(integratedButton),激活策略=3(正确),选项数=2(够),showsScopeBar=true`
> —— **状态全对,范围条就是不画。**

这一行同时否掉了前面两个方向:开关全拨对了,也没人抖它。
真正的原因是 **iOS 26 的 `.integratedButton` 摆法根本不渲染范围条** ——
范围条只在 `.stacked` 下才有安身之处(那条老结论其实一直是对的,是我中途怀疑错了)。

而摆法**四轮里一直是在 `didPresent`(搜索已经展开之后)才切的,那时搜索栏早排完版了,来不及**。
挪到 `willPresent`(排版之前)当场就好了。

**教训**:
- ⚠️ **最值钱的一条:"我要的状态全都设对了,东西还是不出现" —— 这不是没设对,
  是你调的那个开关跟这件事无关。** 别再加大力气去调同一组开关(我加了两轮),
  要去问:**这个东西的渲染,到底由谁决定?** 这次答案是"由摆法决定",
  而摆法这个变量在前三轮里根本不在我的怀疑名单上。
- **判据是"把所有相关状态一次打全",而不是逐个猜。**
  那一行日志同时否掉了两个方向、指出了第三个,一次顶三轮。
  埋日志时**别只打你怀疑的那一个值** —— 把这件事牵涉的状态全打出来,
  信息量最大的往往是"你以为不用看的那个"。
- **自己中途推翻的老结论,要留意是不是推翻错了。**
  「范围条只在 `.stacked` 下显示」是前人实测出来的,我因为"那是手动管理年代的结论"
  就把它降级成了可疑项 —— 结果它是对的,只是**当时没人发现它对时机也有要求**。
  → 推翻一条实测结论前,先分清它是**结论错了**还是**结论不完整**。
- **收尾时再复查一遍自己的改动**:这轮定案后我顺手把"设范围条"塞进了页面初始化那趟,
  而那个方法紧接着会把摆法改成 `.stacked` —— 等于页面平时就停在 `.stacked`,
  搜索框会常驻在标题下面,和头图打架。**是 L71 的形状,只不过这次"后面改我东西的人"是我自己。**
  → 一个方法既管 A 又管 B 时,别让只需要 A 的调用方去调它;拆出小方法。

## L95 · 导航栏会变高的那一刻,所有"按安全区算坐标"的自绘元素都要有人叫醒

**现象**(2026-07-28,用户**真机 iOS 27 beta 4**,模拟器 iOS 26 一个都没复现):
在文章列表页用搜索、退出后,顶栏乱掉 —— **搜索框和放大镜同时在**,
自绘的大标题跑到右上角、压在头图上(用户四张截图)。
用户原话:"感觉像是搜索界面没有完全退出来,或者动画到了某个点就卡住了,没有清理干净"。

**两个独立病灶,症状叠在一起看着像一个**:

**① 顶栏没拆干净 = 又一次"猜时机"。**
摆法(`.stacked` → `.integratedButton`)是在 `willDismissSearchController` 里
`DispatchQueue.main.async` 一拍之后切回去的 —— 那是在**赌**"一个 runloop 够转场跑完"。
模拟器上够,真机上不够,于是摆法在消失动画进行到一半时被改,导航栏留下中间态。
→ 改挂 `didDismissSearchController`(系统给的"已经收完了"的承诺),并给恢复方向补上
和进入方向同款的 `isActive` 守卫 —— 那两个方向**一直是不对称的**,进入有守卫、恢复没有。

**② 标题卡在"飞到一半" = 变量变了却没人通知。**
自绘标题的位置由一个 0→1 的进度值决定,而进度 =
`(contentOffset.y + adjustedContentInset.top) / 淡出距离`,
**只在 `contentOffset` 变化时才重算**(KVO)。
导航栏变高变矮改的是 `adjustedContentInset.top` —— **偏移量没动,KVO 不响**,
进度停在旧值上,标题就停在半空。列表的顶部内边距同理(只有 `layoutSubviews` 会重算)。
→ 给这两个自绘视图补 `safeAreaInsetsDidChange()`。
**仓库里本来就有对照组**:文章页的 `ArticleHeaderBar` 早就挂了这个方法,时间线这套一直缺着。

**教训**:
- ⚠️ **最值钱的一条:一个值是"算出来的",就要问它依赖的每个输入变化时,有没有人来叫它重算。**
  这次进度值依赖两个输入(偏移量、内边距),而重算只挂在**其中一个**上。
  平时另一个不会单独变,所以十几个版本都没事 —— 直到我们让导航栏会变高,它单独变了。
  → **判据**:把公式里的每个变量列出来,逐个问"它变的时候,谁会通知我?"没有答案的就是坑。
- **同一个仓库里,同类组件的做法不一致就是线索。** 文章页有 `safeAreaInsetsDidChange`、
  时间线页没有 —— 这种不对称本身就该被当成待查项,而不是"两边风格不同"。
  同理:进入方向有守卫、恢复方向没有,也是不对称。**成对的操作要成对地审。**
- ⚠️ **模拟器和真机不是同一个系统。** 这次用户真机是 **iOS 27 beta 4**,
  而我编译的 SDK 是 26.5、模拟器也是 26 —— **我这边根本测不到他那个系统**。
  L73 说过"涉及时序的必须真机走一遍",这次还要再加一句:
  **当用户的系统版本比你手上的新(尤其是 beta),你的"模拟器通过"连参考价值都要打折**,
  交付时必须说清"这一版我验不了,得你在真机上看"。
- **一个改动引出两个看着不相干的症状时,先假设它们同源** —— 这次确实同源:
  都是"导航栏高度会变了"这一件事的下游。(L66 早写过同一条。)

## L96 · 五轮之后终于把机制消掉了 —— 代价是我本可以在第二轮就消掉它

**背景**(2026-07-28,范围条与顶栏那场持久战的收尾):
文章列表页要显示搜索的「范围条」(该列表/全部文章),而它**只在 `.stacked` 摆法下渲染**。
于是我们在搜索进出时切摆法 —— 而**切摆法 = 让导航栏变高变矮**。
就这一件事,下游连着炸了三轮:

| 轮次 | 症状 | 当时的修法 |
|---|---|---|
| 1–2 | 范围条根本不出现 | 先怪"没打开它"(补 `showsScopeBar`),再怪"有人抖掉它"(撤 `.stacked`)—— 都错 |
| 3 | 定案:`.integratedButton` 不渲染范围条,摆法要在 `willPresent` 切 | ✅ 范围条出来了 |
| 4 | **真机**退出搜索后顶栏没拆干净、自绘标题卡在半空 | 挂 `didDismiss`、给自绘视图补 `safeAreaInsetsDidChange` |
| 5 | **真机仍然乱** | **不修了 —— 消掉前提** |

**最终方案**:这一页**不再挂系统的 UISearchController**。
搜索改由右上角我们自己的放大镜打开那个已经好用的 modal 搜索页,
范围切换在那一页里用一条**普通的 `UISegmentedControl`** 实现 ——
完全自己摆放、自己绘制,和 UIKit 的搜索栏排版机制彻底脱钩。
**导航栏高度从此恒定,那一整族问题连根消失。**

数据侧零成本:「该列表」这一档用的就是上游系统范围条「Here」档同一个东西
(`SearchTimelineFeedDelegate(searchString:articleIDs:)`),当前列表的文章 ID 协调器手上就有。

**净收益(反直觉但真实)**:上游文件 `MainTimelineModernViewController.swift`
**净减 14 行** —— 三个搜索代理回调全部还原成上游原样,只剩两处带标记的改动。
**比这场战役开始之前更接近上游。**

**教训**:
- ⚠️ **最值钱的一条:判断"该不该继续修"的信号,不是"还差多少",而是"失败在往哪个方向扩散"。**
  第 3 轮我明明已经成功了(范围条出来了),但那次成功**引入了一个新的自由度**
  ——"导航栏高度会变" —— 于是第 4、5 轮的失败出现在**完全不同的地方**(自绘标题、内边距、转场收尾)。
  **一个修法开始在下游长出新症状,就说明它引入的不是修复,是耦合。**
  L92 说的是"每一版失败原因都不同 = 你在雷区乱走",这次是它的升级版:
  **修好之后症状换了个地方冒出来,同样是雷区信号。**
- **"我能验证它吗"应当参与方案选择,而不只是事后补一句"我验不了"。**
  用户真机是 iOS 27 beta,而我的 SDK 和模拟器都是 26 —— 从第 4 轮起我其实已经
  **失去了验证能力**,却还在用"改一版 → 请用户测一轮"的方式推进,等于拿用户的时间做二分查找。
  → **当验证只能靠对方时,应该优先选"不需要验证也大概率对"的方案**
  (自己画的分段控件 vs 跟系统排版机制博弈),而不是继续选风险更高、只能靠试的那条。
- **"消掉前提"往往同时让 diff 变小。** 这次删掉整套摆法机制之后,
  上游文件反而比开工前更干净。**如果一个"简化"方案让上游 diff 变大,它多半不是真的简化。**

## L97 · 「激活着的搜索框」是一次真实的 present —— 今天它在三个地方咬了我

**同一条机制,一天之内换了三个马甲出现**(2026-07-28 / 07-29):

| 出现的地方 | 表面症状 |
|---|---|
| 新做的 modal 搜索页 | 点「取消」要按两下;点结果页面不关、跳转发生在它背后 |
| 发现页「订阅到」的文件夹选单 | 搜出结果之后点它**毫无反应** |
| 发现页「已经订阅过了」的提示 | 点订阅**毫无反应**(其实提示弹了,只是被吃掉) |

**共同的机制**:`UISearchController` 一激活,就是一次**货真价实的 presentation** ——
它成了所在页面的 `presentedViewController`。于是那一页:

- 自己调 `dismiss` → 关掉的是**搜索框**,不是你以为的那层 modal
- 自己再 `present` 别的东西 → UIKit 不允许对"已经在弹别人"的控制器再 present,**静默失败**

第三个尤其阴:发现页的错误提示走的是 fork 自绘的品牌卡片(`NNWMenu`),
而那个卡片开头有一句 `guard host.presentedViewController == nil else { return }`
(本意只是"防连点")—— 搜索一激活,这条守卫就把**所有**提示一起吃掉了。
用户看到的是"点了没反应",而日志里连一行错误都没有,因为代码根本没走到报错那步。

**教训**:
- ⚠️ **在可能有搜索框的页面里 `present` / `dismiss` 任何东西之前,先问一句:
  此刻这一层是不是已经在弹别的东西?** 激活中的 UISearchController、alert、
  分享面板都算。要弹,就顺着 presented 链走到**最顶上那一层**再弹;
  要关整摞,从 `presentingViewController` 那头发起。
- ⚠️ **"防连点"式的守卫别用 `presentedViewController == nil` 这么宽的条件。**
  它的本意是"别弹出两张一样的卡片",写成"名下有任何东西就不弹",
  等于顺手把搜索框、alert 全算了进去。**守卫要挡的是什么,就精确写什么**
  (改成 `if presented is NNWMenuViewController { return }`)。
- **静默的守卫是最贵的守卫。** 这三处全都是"直接 return,什么都不做" ——
  没有日志、没有提示,排查时连"代码走到哪了"都不知道。
  下次写 `guard ... else { return }` 时问一句:**它挡掉的时候,谁会知道?**

**用户的一句话顶我猜十轮**:我对着现象猜了两轮都没修好,直到用户说
「**再按一下右下角的叉号,就退回到了另一个层级,在那个层级里都正常**」——
那句话直接把"多一层"这件事指了出来。
→ **交互类 bug 里,用户对"我做了什么、然后看到什么"的描述往往直接指向机制,
  比我盯着代码猜有用得多。要主动问那种问题("有没有哪种操作顺序是好的?")。**

## L98 · 给界面定"定值"之前,先问内容会不会需要更多

**现象**(2026-07-29 用户报"设置页的字显示不完整"):
设置页有几行的文字被上下裁掉了一截。

**根因**:更早一轮"列表收紧节奏"把设置页的行高改成了
`heightForRowAt` 返回一个**算出来的定值**(字体行高 + 22)。
定值的毛病是:**只要某一行的内容实际需要更多空间,多出来的部分就被直接裁掉**。
而那几行的标签在**上游 storyboard 里本来就标着 `ambiguous="YES"`**(约束有歧义)、
又允许折行,中文一折就超。

**修法**:高度交还给内容自己算(`automaticDimension`),
"收紧"改用**压缩 cell 的上下内边距**达成 —— 压内边距等于压行高,
而内容需要多少仍然给多少。

**教训**:
- **"定值"和"下限"是两件事,而界面上你几乎总是想要后者。**
  写死一个高度/宽度之前问一句:**内容超了会怎样?** 答案若是"被裁掉",那就该给下限。
- 这已经是**同一形状的第三次**了(前两次是搜索栏摆法、范围条):
  写死一个值 → 内容需要更多时被截断。**把"能不能给下限"变成默认反射。**

## L99 · 模拟器继承 Mac 的系统代理 —— 代理拒绝连接时,症状长得像 app 的 bug

**现象**(2026-07-29 晚,试读 Phase B 验收时):用户报"试读页里点订阅,不管什么源都失败,
提示网络连接已中断",且**试读列表本身加载正常** —— 看起来就像新代码把订阅链路改坏了。

**真相(日志钉死的,不是猜的)**:Mac 开着本机代理(127.0.0.1:1082,HTTP/HTTPS 系统代理),
模拟器完全继承它。当晚代理对 app 新建的 CONNECT 隧道回 **503 拒绝** → CFNetwork 报
-1005「网络连接已中断」。且**不只订阅**:21:57 的整批订阅源刷新几十个请求同时 -1005 全灭。
用户把代理一关,一切正常。**代码零改动。**

**为什么"试读能加载、订阅失败"造成了误导**:两条通道用的是不同的 URLSession
(试读用 URLSession.shared;上游订阅/刷新走自己的 ephemeral 会话,RSWeb/Downloader.swift),
代理对两者新建连接的处置不一样 —— 于是"一半好一半坏",看起来像新代码的锅。

**教训**:
- **网络类报错先分流:是"这条代码路径坏了",还是"这台机器的网络环境变了"?**
  分流手段全是现成的:`log show` 里看有没有**别的模块也在同时报同样的错**
  (这次 21:57 那批全灭一眼定性);`scutil --proxy` 看代理配置;curl 分别直连/走代理各试一次。
- **模拟器不是干净环境:它带着 Mac 的系统代理、DNS、VPN 全套上网。**
  用户开关代理是常态(本项目用户在墙内),以后再遇到"突然全订不上/刷不出",
  第一嫌疑人是代理状态,不是昨天刚改的代码。
- **"一半功能好、一半功能坏"不一定是代码差异,也可能是环境对不同会话的差别待遇。**
  下结论前先确认两条路径**在同一时刻**的网络行为(这次靠的就是同一分钟内的日志对照)。

## L100 · 我有模拟器却没用 —— 四轮视觉改动都在拿用户的眼睛做二分查找

> 🔴 **2026-08-08 更正:本条的"解法"已作废,只留内核。**
> 内核(**改了界面不能靠猜、不能盲交**)继续成立;
> 但"**自己**用模拟器工具截图看"那半句**已被用户推翻** —— 那个工具同样太费 token。
> 现在的做法是:**交付时给用户一份能照着做的验收清单,由他来看**。
> 完整来龙去脉见 **L126**,规则正文见 CLAUDE.md 第 0 节第 7 条。

**现象**(2026-08-04,设计换血那一整轮):
连续四版视觉改动,每一版都是「我改 → 装机 → 用户看 → 用户说不行」。
用户的反馈依次是:「几乎没什么变化」「细节还是挺糙的」「改的非常差,几乎没什么效果」。
第四轮之后用户说了一句:**「你这里不是有自带的 iOS simulator 吗,你可以用这个自己看」**。

**我错在哪**:项目 CLAUDE.md 第 0 节第 7 条写着「别用操作电脑去点模拟器验收」,
那条规则的**理由是"太慢太费 token"**,针对的是通用 computer-use。
而本会话有专门的模拟器工具(截图 + 按坐标点按),**又快又便宜**。
我把一条有前提的规则当成了无条件禁令,于是明明能自己看,却四轮盲交。

**改用自己看之后,一轮就定位了三个真缺陷**:
渐变画成了矩形、冷灰色号糊在暖纸上、图标根本没换。这些**在截图里一眼就能看见**。

**教训**:
- ⚠️ **交付任何视觉改动之前,先问一句:「我能不能自己看到结果?」**
  能看就必须先看。让对方替你发现"图标没换"这种事,是在浪费对方的注意力,
  而且**你会失去对自己工作质量的判断力** —— 四轮里我每次都以为做完了。
- **规则要连着理由一起读。** 「别用 X 验收」的理由若是"太慢",
  那么出现一个快的 X 时,规则就该重新评估,而不是继续套用。
- 这条和 **L96** 是同一个母题的两面:L96 说"当验证只能靠对方时,要选不需要验证的方案";
  这一条更进一步 —— **先确认验证是不是真的只能靠对方**。

## L101 · 视觉还原不要目测,要采样 —— 四条规律我全猜反了

**现象**:照着用户给的三张参考图做「软面板」材质,凭目测画了三版,一版比一版不像。

**转折**:写了个 30 行的 Swift 取色工具(CoreGraphics 读 PNG、按坐标打印色号),
扫了参考图的边缘与内部。结果是**四条全反**:

| | 我目测的 | 实测 |
|---|---|---|
| 白边粗细 | 1.5pt、整圈均匀 | **0.55pt hairline**,只在上缘最亮、往下迅速衰减 |
| 层间色差 | 相差 10–15 级灰 | **只差 2–5 级**(面板 #E5 vs 画布 #EA) |
| 渐变方向 | 上亮下暗 | **由上向下变亮** |
| 阴影 | 短而重 | **极淡极宽**(贴边只暗 7 级、扩散约 5pt) |

其中最反直觉的一条:**面板和背景几乎同色**,层次全靠那道 hairline 撑起来 ——
色差一拉大立刻变回"贴上去的塑料板"。**越想做出层次越要克制色差。**

**教训**:
- **"看起来像"是设计判断,"是多少"是可测量的事实。** 后者别用前者代替。
- 取色工具的成本极低(半小时),而它一次就终结了三轮猜测。
  **凡是"照着某张图做"的任务,先写取色工具**。
- 顺带一个坑:CGContext 画进位图后**行序是否需要翻转要先验证**
  (我用图上一个已知位置的橙色块做了标定,才发现坐标是上下颠倒的)。

## L102 · 参考图给的是**相对关系**,不是那几个色号

**现象**:把参考图实测的冷灰 #E5/#E7 直接搬进 app,结果是"米色纸上糊了一块脏灰"
(用户原话:「改的非常差」)。因为本 app 的底色是**暖纸** #F3F0EB,不是参考图的中性灰 #EA。

**修法**:改成从 `AppAppearance.paperBackground` **现推** ——
面板 = 底色 −9/−6 级,胶囊 = 底色 +2/+6 级。底色是暖是冷都跟得上。

**教训**:
- **临摹一套视觉语言时,要提取的是"层与层之间的差",不是每层的绝对值。**
  绝对值只在原图那个环境里成立。
- 判据:把参考值搬进来之前问一句 —— **"这个数字依赖它原来的背景吗?"**
  依赖,就必须改写成相对量。

## L103 · 三个小而致命的实现坑(都是"看起来写对了"的那种)

同一轮里踩的三个,合并记一条:

1. **`CAGradientLayer` 的 `cornerRadius` 不开 `masksToBounds` 就不裁切** ——
   渐变会画成一块**矩形**压在圆角胶囊上(用户看到的"一坨多余的框内阴影")。
2. **读子视图的 frame 之前,要先让容器把版排完**(`stack.layoutIfNeeded()`)——
   `super.layoutSubviews()` 只排到 stack 这一层,stack 内部给按钮定位是**之后**才发生的,
   这时读到的是**上一档的旧宽度**,遮罩就画错位了(用户报"控件的遮罩错位了")。
   这是 L95「算出来的值要有人叫它重算」的变体:**读的东西还没被算出来**。
3. **`Shared/` 目录会被扩展目标(Widget/Share/Intents)编译,那里看不到 `iOS/` 的类型** ——
   同一轮踩了两次(想把强调色、星标图标转发到 iOS 侧的类型)。
   判据:**往 `Shared/` 里写代码时,先问"扩展目标也能编译过吗?"**

## L104 · 采样只扫了一条边,就把结论当成了全部 —— "玻璃感不强"的病根

**现象**(2026-08-04 二轮):L101 记着「白边只在上缘最亮、往下迅速衰减」,
于是材质把下半圈的亮边画到了 3% 透明度(等于没有)。
用户的反馈是:**「你新画的几个控件,玻璃的感觉都不强」**。

**重扫参考图 IMG_2440 那条轨道的四条边**(逐像素,不是隔 12 像素跳着采):

| 边 | 峰值 | 宽度 |
|---|---|---|
| 上缘 y=1142 | **#FFFFFF** | 4–6px |
| 下缘 y=1418 | **#FFFFFF** | 4–6px |
| 左缘 x=664–667 | #F6–#F9(246–249) | 4px |
| 右缘 x=1665–1670 | **#FFFFFF** | 6px |

**四条边都亮**。第一轮之所以得出"往下衰减",是因为**只扫了上缘那一条竖线**,
把"从上缘往面板内部走"的衰减(那是填充渐变)误当成了"亮边沿周长衰减"。

**这一圈完整的亮边就是"玻璃厚度"的全部来源。** 只画上缘 = 一张贴纸。

**教训**:
- **采样要覆盖到结论所声称的整个范围。** 结论说的是"整圈",就必须四条边都扫;
  只扫一条边只能支撑"这条边上是什么样"。**证据的覆盖面 < 结论的覆盖面 = 猜。**
- L101 说"别目测,要采样"是对的,但**采样本身也会撒谎** —— 采样点选错、步长太粗
  (第一轮 step=12,而亮边只有 4–6px 宽,**峰值直接被跳过去了**)都会给出假事实。
  判据:**量一个几像素宽的特征时,步长必须小于该特征的宽度。**
- 顺带:这条推翻了 L101 表格里"白边粗细"那一行。**旧结论没删,是被更正的** ——
  见 NNWSoftMaterial.swift 文件头里并列的两版。

## L105 · 三个"看起来写对了"的实现坑(软面板二轮)

1. **画得对 ≠ 点得到**:`UIBarButtonItem(customView:)` 里的自定义视图,
   **光有 `intrinsicContentSize` 不够,必须显式钉死宽高约束**。
   不钉的话导航栏不给它算尺寸,`bounds` 是零 —— 子视图照样画得出来(没开 clipsToBounds),
   但**命中测试按 bounds 走,所有触摸都落空**。屏幕上一切正常,按钮就是点不动。
   这是 L73「我算一套坐标、系统算一套」的又一次。
   ⚠️ 判据:**自定义视图进任何"由系统摆位"的容器(bar/toolbar),都要自带尺寸约束。**

2. **`UIImageAsset` 登记深浅两张,`UIBarButtonItem.image` 不认**。
   想让烘焙进图片的面板跟随深浅色,试了标准做法(asset 登记 light/dark 两张、
   让 UIKit 显示时自己挑)——**实测无效**:深色下仍是浅色那张,重启 app 也一样。
   改成「按传进来的 traits 现画 + `registerForTraitChanges` 时重画」才对。
   判据:**"UIKit 会自己处理"这类假设,和别的假设一样要截图验证。**

3. **`UITraitCollection.current` 在视图层级之外拿到的 `displayScale` 是 1**。
   用它去取缩放,3 倍图会被**按 1 倍解释** —— 54pt 的图变成 162pt,
   四个按钮全部胀成三倍大,导航栏挤到把搜索折进了「…」溢出菜单。
   判据:**画位图时的 scale 要从宿主视图的 `traitCollection` 拿,别用 `.current`。**

## L106 · 换掉系统组件时,别照抄它的内容 —— 把它的**产物**接过来

**处境**(2026-08-04):用户要求长按菜单也长成参考图那套质感。
但**系统长按菜单由系统进程渲染,没有公开接口能改样式**,只能整个换成自绘。

**差点走错的那条路**:照着上游每个菜单抄一份 `NNWMenu.Item`。
上游有 6 处菜单、每处 5–10 个 action、分散在 4 个文件里 ——
抄一份是几百行重复逻辑,**而且从抄完那天起就开始和上游漂移**
(上游加一项我们不会知道,而这个 fork 的最高优先级是保持可 merge)。

**走通的那条路**:上游每个菜单的 `actionProvider` 结尾都是
`return UIMenu(title: "", children: menuElements)` —— **把那一行换成
「拿这棵 UIMenu 去弹我们自己的卡片,然后返回 nil」**。
于是上游内容一行没抄,以后上游加菜单项我们自动跟上,
而且**够不到它那些 private 的 action 构造函数也无所谓** —— 我们要的是产物,不是配方。
改动落在 CLAUDE.md 允许的「一行换一行」通道里,每处两三行。

**卡在哪儿、怎么破的**:`UIAction.handler` 是私有的,`UIAction` 也没有公开的 `perform()`。
解法是 **`UIControl.addAction(_:for:)` + `sendActions(for:)`** ——
造一个不入界面的 `UIButton`,把 action 挂上去再自己触发它。两个都是公开 API
(`UIButton(primaryAction:)` 本来就是这么工作的)。
**已实测**:长按订阅源 → 点「拷贝 Feed 网址」→ `xcrun simctl pbpaste booted`
读回 `https://feeds.feedblitz.com/marginalrevolution`。

**教训**:
- **要替换一个系统组件的外观时,先找它的"产物"在哪里被交出来。**
  拦在产物那一层,通常比重做一遍内容便宜一个数量级,而且不会漂移。
- 这条和 L96 是一家:**耦合越少的改法,越不需要被持续维护。**
- 顺带一条设计判断:**"顶部那排只有图标的快捷键"该放哪几项,机器判断不了。**
  试过"自动取第一组" —— 订阅源菜单第一组只有一项,提不出一排;
  时间线菜单第一组有四项,里头「标记以上为已读 / 标记以下为已读」**摘掉文字就认不出来**。
  图标行只装得下"一眼认得出"的动作,所以改成**由调用点点名放几个**。
  判据:**能不能只用图标表达,是语义问题,不是数量问题。**

## L107 · 比例尺没有锚点,后面每一个数字都是错的

**现象**(2026-08-05):用户看了自绘选单后说「玻璃质感完全没有、边缘也没有反光、
黑色不够深不够通透」。三条全部**量得出来**,而病根是同一个:
前两轮把参考图换算成 pt 用的 **6.4 px/pt 是猜的,没有任何依据**。

**这次怎么把比例尺钉死的**:找一个"全世界都一样"的东西当锚点 ——
菜单正文字号。实测 IMG_2442 里「Hide」的升部高 63px;拉丁字母升部 ≈ 0.75em,
所以 em ≈ 84px;而菜单正文就是 iOS 标准的 **17pt**。→ **84 ÷ 17 = 4.94 px/pt**。
**交叉验证**:卡片宽 1255px ÷ 4.94 = **254pt** —— 正好是 iOS 系统菜单的标准宽度。
两条独立线索对上,比例尺才敢用。

**用对比例尺之后,一次性暴露出四个错**:

| | 参考图实测 | 我们(错的) | 错因 |
|---|---|---|---|
| 亮边 | 6px = **1.2pt** | 0.8pt | 比例尺按 6.4 算,细了 1/3 |
| 文字 | **#0A0A0A**(亮度 10) | #2C2823(亮度 **40**) | 直接用了 app 的暖墨,没量 |
| 圆角 | ≈167px = **34pt** | 26pt | 目测"约为宽度 1/10",实际是 1/7.5 |
| 分隔线 | **1.2pt**、暗 21 级 | 1 像素发丝线、极淡 | 想当然照搬了系统菜单的做法 |

**教训**:
- **换算比例尺必须有锚点,而且锚点要能交叉验证。** 没锚点的 px→pt 换算等于占卜,
  它会把后面每一个尺寸都按同一个比例带偏 —— 而且带偏得很一致,所以"看着还行",
  正是最难自查的那种错。
- 找锚点的窍门:**找那个"全世界都一样"的量**(系统正文字号、标准控件宽度、
  安全区高度……),别找设计师自由发挥的量。
- 这条是 L101/L104 的第三级:L101 说别目测要采样;L104 说采样要覆盖全;
  **L107 说采样出来的数,还得有正确的单位才有意义。**

## L108 · 在纸上打磨不出玻璃 —— 三轮细节调整都没碰到真正的变量

**现象**(2026-08-05):用户连着三轮说「玻璃质感不够」。
我每轮都在调细节(亮边粗细、圆角、字色、分隔线),每轮都更接近一点、又都差一层。

**量出来的真因**:参考图三张图里,**app 自身的表面是 #EAEAEA —— 精确中性 R=G=B=234**。
而本 app 的底是 #F3F0EB(243,偏暖 8 级)。由此:

| | 参考图 | 暖纸 |
|---|---|---|
| 画布 | 234 中性 | 243 暖 |
| 亮边 vs 画布 | 255−234 = **Δ21** | 253−243 = **Δ10** |
| 面板 vs 画布 | 232−234 = **Δ−2** | 235−243 = **Δ−8** |

**白色是亮度的天花板。** 画布越亮,那道纯白亮边越发不出光 —— 我们只有参考图**一半**的
边缘对比度。而"玻璃"这个观感,恰恰全靠那道边;参考图的面板和背景几乎同色(Δ2),
色差根本不参与撑层次。我们反而把面板压暗 8 级去补层次,方向是反的。

**换成中性画布后实测**:画布 #EAEAEA(与参考图逐像素相同)、面板 232(相同)、
亮边对比度 Δ10 → **Δ18**。观感一步到位。

**教训**:
- **"质感"往往不是对象本身的属性,是它和环境的**关系**。**
  三轮都在改"那块面板",而决定它像不像玻璃的是**它周围那片东西**。
  判据:**调了三次细节还差一层,就该怀疑自己在调错的变量。**
- **两种材质互斥时,细节打磨只会让你无限逼近但永远到不了。**
  暖纸 = 有色、亮;玻璃 = 中性、稍暗。想要玻璃就得放弃纸,没有中间态。
- ⚠️ 这一条推翻了 L102 的适用范围。L102 说"参考图给的是相对关系,不要搬绝对色号",
  那是**在"底色暖"这个前提固定时**成立的。当用户要的正是那个底色本身时,
  绝对值就是答案。**判据:先确认"什么是固定的",再决定搬相对还是搬绝对。**

## L109 · 量圆的直径要横扫取最宽处 —— 竖扫量到的是弦

**现象**(2026-08-05):用户报"底栏三个控件大小不一样"。我竖扫测量,
得到左圆钮 21.7pt、三档 34pt,于是**按 21.7 去改**(把三档降到 28 迁就它)。
结果用户下一句是「中间那个又上下太矮了」—— 因为**我的测量是错的**。

**错在哪**:圆形在**非圆心**的位置竖扫,量到的是**弦**,不是直径。
我固定在 x=128 扫,而圆心在 x≈155 —— 偏了 27px,量出来自然偏小。
改用**横扫找最宽处**之后,真直径是 **34.3pt**,和三档的 34pt 本来就基本一致。

**代价**:白白往错误方向改了一轮(把三档从 34 降到 28,又降到 31),来回三次。

**教训**:
- **测量方法要匹配被测对象的形状。** 量矩形任意一条线都行;
  量圆**必须取最宽处**,否则得到的是弦。
  判据:**下手前问一句"我这条扫描线,一定经过它最宽的地方吗?"**
- 这条和 L104 是一家:L104 是**采样范围**不够(只扫了一条边),
  这条是**采样位置**不对(没扫在圆心)。
  **两者都会给出看起来很精确、其实是错的数字** —— 比目测更危险,因为它带着小数点。
- ⚠️ 顺带:**发现测量错了要主动说**。这一轮我先报了"21.7pt",
  下一轮才发现是弦 —— 如果不说,用户会以为控件真的变小过。

## L110 · 重绘一整套之前,先查这套里有没有已经单独定过案的

**现象**(2026-08-05):用户说 dock 上「分享 / 长图 / 翻译」三个图标太丑,
问能不能用回上一版。查证后发现:**长图和翻译那两个,用户之前已经分别验收过四版和三版**——
- 长图:2026-07-25 四版,定在"虚线选取框 + 相机"(截图工具的通用语法)
- 翻译:2026-07-30 三轮选型(单气泡→自绘双气泡(用户嫌丑)→**系统 translate 符号**),
  连 14.5pt 这个字号都是三次调出来的

而 2026-08-04 那轮「dock 7 个图标全部手绘」把它们一起换掉了。
**那不是改进,是把用户签过字的东西推翻重来。**

**教训**:
- **"统一整套"这种动作天然会碾过个案。** 动手前先扫一遍:
  这套里有没有哪个是**单独定过案**的?那些要留,而且要在注释里写明为什么留。
  判据:**看到"全部/统一/整套"这类词,先去翻这些东西各自的历史。**
- 这个项目的注释里其实**写着**选型过程(NNWIcons.longImageShare 的头注释、T38),
  是我没去读就动手 —— 记录写了没人读,等于没写。
- 还原成本极低(旧代码都还在,一行一个),但**用户要多花一轮来发现和指出** ——
  这才是真正的代价。

## L111 · "以后都保持一致"要用**共享常量**保证,不能靠记性

**处境**(2026-08-05):用户要求导航栏两颗圆钮和底栏圆形控件一样大,
并且**「以后都一直保持一样(如果又要修改任何一方)」**。

这一轮之前,同一个尺寸散在**三处**各自写死:
`NNWSoftGlassButton.discSize` / `roundButtonImage` 的 diameter 默认值 /
`NNWReadingModeBar.barHeight`。当天已经因此改错过三次(34→28→31→34)。

**做法**:立一个 `NNWSoftMaterial.controlDiameter`,三处全部改成**读它**。
改一个数,三处一起变;想只改一处,得先在代码里把 var 拆回去 —— 有摩擦,而摩擦正是目的。

**教训**:
- **用户说"以后要保持一致"时,他要的是一个约束,不是一次对齐。**
  把数字对齐是当下生效;把数字合并成一个真源才是**以后**生效。
  判据:**听到"以后 / 一直 / 每次",就去找那个能让它自动成立的结构。**
- 反过来:**同一个语义的数字散在多处,就是在等着它们跑偏。**
  这次是尺寸,之前是色号(散在 NNWSoftMaterial 和两个 colorset,
  于是换强调色时有些地方跟不上 —— 同一个病)。

## L112 · 深色模式不是"另一个配色",是**另一半的验收面**

**现象**(2026-08-05):用户在深色下一次指出三处丑:文件夹带一块方板、
底栏按钮又白又不统一、文章页 dock 是浅灰塑料板。

三处**全是我自己改出来的**,而且**每一处的浅色版都是当天验过、用户认可的**:

| 症状 | 根因 | 什么时候种下的 |
|---|---|---|
| dock / 三档变浅灰塑料板 | 为修"不够白不够亮"压了一层 **52% 的白,却没分深浅色** | 同一天 |
| 文章列表页圆钮还是浅色那张 | 那一页**漏了深浅色观察者**(首页有,这页忘了加) | 同一天 |
| 文件夹图标带一块方板 | 造 `IconImage` 时没设 `isBackgroundSuppressed`,宿主自己垫了底 | 同一天 |

**我在这一整轮里提醒了用户四次"深色还没验过",但自己一次也没去验。**
提醒不是验收 —— 把风险说出来只是把它记下来,不会让它消失。

**教训**:
- **深色不是"配色的另一档",是验收面积的另一半。** 改任何视觉的东西,
  "做完"的定义里必须包含深色截一张,否则就是只做了一半。
- **凡是给浅色打的补丁,先问一句"这个补丁在深色下是不是反的"。**
  那层 52% 的白在浅色下是"提亮",在深色下是"刷白" —— **同一个操作,相反的效果**。
  判据:**补丁里出现常量时,看它是不是应该随 traits 分叉。**
- **同一类机制要成套检查。** 首页有深浅色观察者、文章列表页没有 ——
  因为它们是分两次加的。判据:**加完一处这类"注册/观察"的代码,
  立刻 grep 一遍还有谁用了同一个机制。**

## L113 · 就地改写自己读的那个字段 —— 第二遍就烂了

**现象**(2026-08-05):用户换了强调色后,底栏两颗圆钮**变成两块纯色圆饼**,图标没了。

**根因**:`nnwRestyleTimelineToolbarIcons` 里写的是
`item.image = roundButtonImage(icon: item.image …)` ——
**读的和写的是同一个字段**。第一遍没事(那时 `item.image` 还是上游的原图);
但我当天给这一页补了深浅色 / 换色观察者,于是它会被**再次触发** ——
第二遍就把"已经合成好的圆钮"当成图标,再合成进一个新的圆钮里,
反复几次就糊成一块纯色。

**为什么之前没暴露**:那个函数原来只在 `viewDidLoad → configureToolbar` 里跑**一次**。
**是我加观察者的那一刻,把一个"只跑一次"的函数变成了"会跑很多次"的函数**,
而它并不是幂等的。

**教训**:
- **任何"就地改写自己读的那个字段"的函数,都要问一句「跑第二遍会怎样」。**
  这类函数在只跑一次的年代是对的,一旦有人给它加了触发源就立刻烂掉 ——
  而加触发源的人(我)当时完全没意识到自己改变了它的调用次数。
- 修法不是"加个标记防止重跑",而是**让它从不变的源头现算**
  (这里改成每次都从 `Assets.Images` 的原图合成)。
  **幂等比防重入更可靠** —— 防重入要维护状态,幂等不用。
- 判据:**给一个已有函数加观察者 / 通知 / 定时器之前,先读它一遍,
  确认它跑第二遍、第十遍的结果和第一遍一样。**

## L114 · 手搓的系统效果,在下一个系统版本上是负债

**现象**(2026-08-05):用户在 **iOS 27 beta 真机**上报「底栏变成了遮罩,不是毛玻璃」。
我的 SDK 与模拟器都是 26,**复现不了** —— 和 L94/L95 那次(搜索栏摆法)是同一个处境。

**我们当时的实现**:`UIBlurEffect(.systemUltraThinMaterial)` **+ 自己压一层 52% 的白**。
那层白是为了修用户"不够白不够亮"加的。在 26 上底下的模糊还透得出来,所以看着是玻璃;
**一旦模糊在新系统上渲染得弱一点,占主导的就变成我们那块白板** —— 也就是"遮罩"。
换句话说:**那层白本来就是一块遮罩,只是在 26 上被模糊盖住了。**

**改法**:不去"修"手搓的那套,而是换成系统自己的 **`UIGlassEffect`**(iOS 26+ 原生液态玻璃),
并把白层从 52% 降到 10%(原生玻璃本身就够亮,再压白就又成遮罩)。

**教训**:
- **凡是"手工拼出系统观感"的实现,都是跨版本负债。** 系统材质由系统负责在各版本上渲染,
  我们拼的组合只在写它的那个版本上被验证过。
  判据:**做某个效果前先查一遍系统有没有原生 API** —— 有就用,别拼。
- **当一个效果由「系统层 + 我们的补偿层」叠成时,补偿层就是故障时的裸露面。**
  系统层一弱,用户看到的就是我们的补偿层。所以补偿层要尽量薄。
- ⚠️ **测不到的系统上,别声称修好了。** 我只能说"26 上不坏、原理上更稳",
  27 必须由用户验 —— 这一条和 L96 一致:**验证只能靠对方时,要选不需要验证的方案**
 (换成系统机制,正是"不需要我验证"的那一类)。

## L115 · 复现不了的 bug,不等于只能猜 —— 把测量仪器送到出问题的那台机器上

**处境**(2026-08-05):用户在 **iOS 27 beta 真机**上报「底栏没有那道柔和的渐隐,是硬切断」,
开发机 SDK 与模拟器都是 26,**复现不了**。前两轮就是在这个处境下盲改的,两版都没修好。

**这一轮做对的三件事,顺序很重要:**

1. **先在能复现的地方否掉旧因果。** 在 26 上做隔离实验(一个二进制 + 环境变量切条件,
   避免"不同构建/不同滚动位置"混进来),量出**拍平不拍平逐像素相同**、
   上一轮那个"修法"**反过来强制关掉也逐像素不变** —— 它是个空操作。
   ⚠️ 这一步的关键是**反向实验**:只做"关掉它看有没有变化"不够,
   还要做"强制它做相反的事",并**打日志回读确认真的写进去了** ——
   否则 A≡C 可能只是"废话相等"(那行代码压根没跑)。
2. **把仪器送到出问题的机器上。** `xcrun devicectl` 能装 app、**用环境变量启动**、
   读控制台、**把文件从 app 容器拉回电脑**。于是 26 上那套隔离实验**原样搬到 27 上跑**了。
   再让 **app 自己给自己截图、就地采样、把数字打进日志**,并把原图写进文档目录拉回来 ——
   "屏幕上有没有渐隐"就变成了可读的数字和可看的原图,不再依赖用户描述。
3. **仪器本身要先校准**(L104 的延伸:采样自己也会撒谎)。
   `drawHierarchy` **未必捕捉得到系统画的效果** —— 如果捕捉不到,它会给出"没有渐隐"的假象,
   而那恰好是我们想找的结论,**最容易骗过自己**。所以先在 26 上跑,和 Mac 上直接截图量的
   130 个采样点对比:平均差 0.12 级、最大 6 级,对上了才敢拿去 27。

**结果**:一轮二分就命中真凶 —— 不是「拍平工具栏」,是 `hidesSharedBackground`;
而且**病根根本不是"渐隐丢了",是内容压根没滚到栏底下去**(栏底下没东西,自然没得渐隐)。

**教训**:
- ⚠️ **"我复现不了"是一个关于工具的判断,不是关于问题的判断。** 下手猜之前先问一遍:
  **我能不能把测量搬到那台机器上?** 有线连着的真机能装、能带环境变量启动、能读日志、
  能拉文件 —— 能做的比想象中多得多。
- **让程序自己测量自己**,是把"用户的眼睛"换成"我能读的数字"的通用办法;
  代价只是一个临时文件,查完删掉。
- **但仪器必须先在已知答案的地方校准。** 没校准的仪器给出的"没有信号",
  和"信号真的不存在"长得一模一样。
- 这条是 L100 的下一级:L100 说"先确认验证是不是真的只能靠对方";
  **L115 说:就算那台机器不在手边,也先问"能不能把尺子递过去",再考虑猜。**

## L116 · 两个系统版本要的**相反**时,只能按版本分道 —— 但要先把"相反"量出来

**现象**(2026-08-05):`UIBarButtonItem.hidesSharedBackground = true` 这一句,
在 **iOS 26 上是必须的**(不设就是两层胶囊套娃,中间一道灰缝 —— 已在 26 模拟器上复现并截图),
在 **iOS 27 上是有害的**(一设,内容就不再从栏底下穿过去,变成硬切断 + 一条不透明黑带)。

**同一句话,两个系统要的正好相反。** 所以没有"一个写法两边都对"的解,只能按版本分道。

**但分道之前必须把两边都量出来**,否则就是拿一边的观感去赌另一边:
- 27 上不设 → 交界处每行平均亮度 214→**195→225**(内容一路穿过去)✅
- 26 上不设 → 截图里清清楚楚两层胶囊 ❌
- 26 上照旧设 → 改前改后**逐像素零差异**(171 行全部相同)✅ 确认没有退化

**顺带排除掉的两条**(都有日志确认执行了,画面逐行不变,**别再走**):
`UIScrollView.bottomEdgeEffect` 对 WKWebView 无效;`UIScrollEdgeElementContainerInteraction` 无效。

**教训**:
- **版本分道是最后手段,不是第一手段** —— 但当两个版本的需求真的相反时,
  再漂亮的"统一写法"都是自欺。判据:**能不能找到一个两边都成立的写法?
  找不到,就老老实实分道,并在注释里写清楚"为什么两边相反"。**
- **分道的两边都要有实测证据。** 只验新系统 = 拿旧系统赌;
  只验旧系统 = 什么都没修。这次两边都截了图、都逐像素比了。
- ⚠️ 这条是 **L114 的第二次**:L114 说"手搓的系统效果在下一个系统版本上是负债",
  这次的负债不是"手搓",而是**"拆掉系统的一层外壳"** ——
  外壳顺带提供的东西(内容能从栏底下穿过去),下一个版本换了绑定关系。
  **判据不变:"让某个系统外壳消失"时,先想清楚那层外壳还顺带提供了什么。**

## L117 · 同一个 bug 换个说法报第二次,就没人往同一个原因上想

**现象**(2026-08-05):用户在 iOS 27 真机上先后报了两次:

| 时间 | 用户的说法 | 我们当时的诊断 | 实际 |
|---|---|---|---|
| 8-05 白天 | 「底栏**变成了遮罩**,不是毛玻璃」 | 我们自己压的那层 52% 白太厚 → 换成 `UIGlassEffect`(L114) | ❌ 误诊 |
| 8-05 晚 | 「底栏**没有那道渐隐**,是硬切断」 | 拍平工具栏把渐隐一起拍掉了 → 改了两版 | ❌ 误诊 |

**两次是同一个 bug**:`hidesSharedBackground = true` 在 iOS 27 上让内容不再从栏底下穿过去,
于是栏那块是一条**不透明黑带**(= 用户说的"遮罩"),内容在栏上沿**硬切断**(= "没有渐隐")。
一个原因,两个症状;**因为用户换了个说法,我们就当成两件事各查了一轮。**

**为什么会这样**:症状描述是**现象**,而现象和原因是多对一的。
我们两次都从"用户这句话描述的是什么效果"出发去找那个效果的实现
(遮罩→找我们画的白层;渐隐→找 `UIScrollEdgeEffect`),
**从来没问过"这两句话会不会是同一件事"**。

**教训**:
- ⚠️ **同一个地方、同一个系统版本上反复出问题,先假设是同一个原因**,
  哪怕用户的两次描述听起来毫不相干。判据:**在动手查第二次之前,
  把第一次的记录调出来读一遍,问一句"这两个现象能不能由同一个东西解释"。**
- **别从"用户这句话描述的是什么效果"出发**,那会直接把你带到那个效果的实现上去 ——
  而真凶往往在别处。要从"这一块屏幕上到底发生了什么变化"出发,**逐像素量**。
- 这条也解释了为什么 L114 那次的修法(换 `UIGlassEffect`)"看起来有效" ——
  它其实什么都没修,是用户的注意力从"遮罩"转到了"渐隐"。
  **判据:修完之后要让用户确认"原来那个症状消失了",而不是等他报下一个症状。**

## L118 · 借别人的回调去读一个状态 —— 那个状态可能"还没变"或"根本不会让回调响"

**现象**(2026-08-05):文章页 dock 搬出工具栏后,做了个钩子让它跟着栏一起藏/现 ——
挂在 `viewSafeAreaInsetsDidChange` 里,读 `navigationController.isToolbarHidden`。
上一轮只看静态截图就交了。这一轮一操作:**下滑藏栏,dock 纹丝不动。**

**埋探针量出来是两个坑叠在一起**,而且**两个都会单独让它失效**:

| 坑 | 实测 |
|---|---|
| ① 工具栏藏不藏,底部安全区**完全不动** | 藏栏前后都是 96.0,一个字节没变 → 回调根本不为它而响 |
| ② 回调恰好插在上游那两句中间 | 上游是「先导航栏、后工具栏」,回调在第一句里就同步发生 → 读到的工具栏值**一定是旧的** |

坑 ① 的来路值得单独说:**是我们自己把工具栏抹成透明底 + 清空 `toolbarItems`,
它才不再撑安全区的。** 换句话说,**是我们亲手掐断了自己想借的那根线**。

⚠️ **我第一版只修了坑一**(判据改成读导航栏)——结果**藏得下去、回不来**:
上滑那次读到工具栏的旧值 true。**修一半 = 换一个方向坏。**

**最后的修法**:推迟一轮 runloop 再读,等两个状态都落定;判据取「导航栏藏 **或** 工具栏藏」。

**教训**:
- ⚠️ **「借 X 的回调去读 Y 的状态」是一种隐藏的耦合,下手前要量两件事:**
  **(1) Y 变化时,X 一定会响吗?(2) X 响的那一刻,Y 落定了吗?**
  这两条各自都能让钩子静默失效,而且**症状一模一样**(什么都没发生),分不出是哪一条。
- **这条是 L114/L116 那一族的第三次**:那两条说的是"让某个系统外壳消失时,
  先想清楚那层外壳还顺带提供了什么"。这次消失的外壳是**工具栏的底**,
  顺带没了的是**"它的显隐会改变安全区"** —— 而那正是我挂钩子的地方。
  **判据不变,只是这次被咬的是我自己写的钩子,不是画面。**
- **"读一个刚被 set 的属性"要问一句:set 它的那段代码跑完了吗?**
  上游一个方法里连着 set 两样东西,而第一样就会同步引发回调 ——
  在那个回调里,第二样永远是旧的。**推迟一轮是最省事的解,不需要理解上游的顺序。**
- 还有一条方法上的:**修完一个方向要立刻验反方向。** 藏和现是两条独立的路径,
  这次就是"藏对了、现错了",而只测藏的话看起来完全正常。

## L119 · 烘焙成 `CGColor` 的颜色是**隐性状态** —— 它只在"下一次排版"时才会纠正

**现象**(2026-08-05,用户在 iOS 27 真机上报):从别的 app 切回来时,
底栏三档控件**中间那颗选中胶囊变成了深色模式的样子**(黑底),而整个 app 是浅色的。

**机制(读代码 + 探针确认的那一半)**:
`NNWSoftPanel.layout(in:)` 把语义色 `resolvedColor(with: traits)` 之后写进
`CAGradientLayer.colors` —— 那是 **`CGColor`,不跟随深浅色**。
它是一次性烘焙的结果,**只有下一次 `layoutSubviews` 才会重新解析**。
于是只要**曾经有一帧是用深色排的版**,而之后没人再叫它排一次,这块面板就永久停在深色。
(iOS 退到后台时会为 app 切换器**额外用另一种深浅色渲染一遍**,是最可能造出那一帧的地方。)

**⚠️ 我复现不出来 —— 而且我把这件事本身当成了信息。**
在 iOS 26 模拟器上试了四条路径:冷启动、切后台再回来、切系统深浅色、
**系统深色 + app 配色设成"浅色"**(最贴近用户当时的处境)—— **四条全部正常**。
探针还确认了:在 26 上退到后台时**根本没有发生任何一次烘焙**,
这正好解释了为什么 26 上复现不出来 —— 那一帧在 26 上不存在。

**所以修法不是"堵住那条路",是"让它自愈"**:
`NNWSoftPanel.install(in:)` 里订阅 `didBecomeActive`,回前台时叫宿主 `setNeedsLayout()`。
不管那一帧是怎么来的,回前台必重画一次。
挂在 `NNWSoftPanel` 这一层而不是某个宿主上 —— 所有软面板共用同一个烘焙机制,
病是共用的,**要修就在共用的那层修一次**(L112 的"同一类机制要成套检查")。

**验的是什么(重要:我验的不是"bug 没了")**:
复现不了就没法验"好了"。所以改为验**链路是活的** —— 埋探针,从主屏点图标回到 app,
日志里每一块面板(含那颗胶囊)都收到 `becomeActive → setNeedsLayout`,
紧接着就是 `bake style=light`。**"回前台会重新解析颜色"这句话是量出来的,不是写出来的。**

**教训**:
- ⚠️ **"把动态色解析成 CGColor"= 给自己造了一份缓存,而缓存需要失效策略。**
  UIColor 会跟着 traits 走,CGColor 不会。凡是 `resolvedColor(with:)` 之后
  存进图层的地方,都要回答一句:**"谁负责在它过期时叫它重来?"**
  这个项目已经在同一个坑里栽过两次了(L105 第 2 条的烘焙图、L112 的深色补丁)。
- **复现不出来时,可以退一步验"修法的链路",而不是硬凑复现。**
  两者不等价,交付时必须说清楚差别 —— 但"按机制修 + 验链路"远好过"猜一个改动就交"。
- ⚠️ **复现失败本身是证据。** "26 上退到后台根本没发生烘焙"这条否定结果,
  反过来支持了"那一帧只在 27 上出现"的判断 —— 别把复现失败只当成"没查出来"。
- 这条和 L114 同一句结尾:**测不到的系统上,别声称修好了。** 要用户确认。

## L120 · z-order 赢了,触摸还是没到 —— 私有容器的命中测试不按子视图顺序走

**处境**(2026-08-05,把首页三档控件搬出工具栏做浮层):控件画得出来、位置逐像素对齐,
但**点不动**。三轮探针,三个结论,一轮比一轮深:

| 挂在哪 | z-order | `hitTest` 被调用? |
|---|---|---|
| 本页的 view 里 | 页面之内最前 | **0 次** |
| 导航控制器的 view 里、置顶 | 日志确认全场最前([3],比装工具栏的 `FloatingBarContainerView` 还靠前) | **0 次** |
| **split view controller 的 view 里** | 最前 | ✅ 到了,一切正常 |

第二行是关键证据:**我们排在最前,触摸却依然绕过我们** ——
唯一的解释是 `UILayoutContainerView`(导航控制器的 view,私有类)在 iOS 26 上
**自己重写了命中测试**:底部栏那片区域的触摸直接路由给栏容器,
根本不按 z-order 遍历子视图。在它肚子里怎么置顶都没用,得跳出它这一层。

**顺带一个测量教训**:iOS 26 上 `navigationController.toolbar.frame` 实测是
**{0,0,402,874} 整屏**、它的 `safeAreaInsets.bottom` 是 0 ——
**这条"工具栏"不是一条栏的几何,不能当尺子用**(第一版据此算位置,偏高 54pt)。
可靠的尺子是**窗口自己的安全区**(`window.safeAreaInsets.bottom` = Home 条那 34pt)。

**教训**:
- ⚠️ **"排在最前"不等于"先拿到触摸"。** z-order 决定的是**默认**的命中测试顺序,
  而任何一层祖先都可以重写 `hitTest` 把这个默认掀翻 —— 尤其是系统私有容器。
  判据:**排查"点不动"时,第一问不是"我在不在最前",而是"我的 `hitTest` 被调用了没有"**
  (一行日志就能分清是"路由没走到我"还是"走到了被我自己挡了" —— 两种病,修法完全不同)。
- **在私有容器里打不赢,就往上一层挂。** 系统的定制行为通常只住在某一层
  (这次是导航控制器的 view);它的父级(split view 的 view)还是标准行为。
- 这条是 L105「画得对 ≠ 点得到」的下一级:L105 是**自己的尺寸**没立好,
  这条是**别人的路由**不走寻常路。症状一模一样,都是"看得见点不动"。
- 顺带:**系统对象的 frame 不一定长成它名字的样子**(整屏的"工具栏")。
  拿系统视图当几何参照物之前,先打一行它的 frame 看看。

## L121 · 要么全归系统,要么全归我们 —— **半分最贵**

**用户 2026-08-05 问了一个很准的问题**:「为什么创世版本也是在 iOS 27 上,
但没有今天这么多模拟器和真机的不一致?是不是我们的代码哪里出了问题?」

**答案:是我们造成的,但不是"写错了",是我们主动把系统的活接了过来。**

创世版的底栏、按钮、胶囊**全是系统画的**。系统组件的职责就是
"在每一个 iOS 版本上都渲染正确" —— 苹果替我们扛了跨版本一致性。
这一轮的视觉换血,本质是**把系统外壳换成自己手搓的材质**:
每换掉一处,就多一处"只在写它的那个版本上被验证过"的实现(L114 说的就是这件事)。

**但真正贵的不是"换",是"换一半留一半"。**
2026-08-05 晚用户报的两个新问题,是同一个病的两个症状(L117 的判据又中一次):

| 用户看到的 | 谁画的 |
|---|---|
| 右上角两颗变成连体药丸 | 搜索/编辑在 27 上**不画**自己的圆盘 → 只剩系统给两项合起来的那一颗 |
| 底部中间比两边窄、不对称 | 三档是**我们**画的(直径恒定),齿轮/加号是**系统**画的(尺寸系统定) |

两个画家在同一条线上并排,尺寸和风格**必然**对不齐。
iOS 26 上看不到,是因为 26 上我们把系统那层整个拆了(`hidesSharedBackground`),
全部归我们所以自洽;27 上那个拆法有副作用(内容不再从栏底下穿过去,见 L116),
只好把系统那层留着 —— **于是就半分了**。

**教训**:
- ⚠️ **判据:一条栏 / 一组控件,要么全归系统,要么全归我们。**
  混着来的那一版,在**当前**系统上可能碰巧协调,但它对不齐的风险是**结构性**的 ——
  系统那半边随时会在下一个版本换尺寸,而我们这半边不会跟着变。
- **"接管系统外观"是一笔要长期还的债,债主是每一个新 iOS 版本。**
  接之前先问:这一处**值不值得**自己画?(dock / 三档值得 —— 它们是设计的主角、
  而且陀螺仪反光要求边归我们;齿轮/加号本来不值得,是被"半分"逼着一起搬的。)
- 反过来也成立:**如果决定不自己画,就要接受系统的样子**,别去"凑"一个补偿值
  (那要按版本猜系统的内边距,是 L114 那类负债的又一种写法)。

## L122 · 一条"只在某个上下文里成立"的结论,会固化成代码里的版本分叉 —— 上下文没了也没人回去重评

**现象**(2026-08-05 晚,装上 iOS 27 模拟器之后的第一个发现):
用户在 iOS 27 真机上报两处丑(右上角两颗变连体药丸、底栏中间比两边矮),
病根都是 `NNWSoftMaterial.systemDrawsBarCapsule` 这处**按系统版本分道**:
27 上让系统画栏里的胶囊、我们不画,于是**系统按 48pt 画、我们按 40pt 画**,必然对不齐(L121)。

**那处分叉是怎么来的**:T42/L116 量到「iOS 27 上设 `hidesSharedBackground` 会让内容
不再从栏底下穿过去(硬切断 + 黑带)」,所以只好在 27 上保留系统那层。
**那次测量是在文章页的 `WKWebView` 上做的** —— 而结论被写成了"iOS 27 上不能拆",
一条**关于某个滚动视图**的事实,固化成了一条**关于系统版本**的规则。

**装上 iOS 27 模拟器后第一件事就是复验它,两条都被推翻/失效**:

| 复验对象 | 结果 |
|---|---|
| 首页(普通 `UICollectionView`)拆掉系统胶囊 | 栏那一带逐行亮度 **203–235 随内容起伏**,一路到 870pt → **内容照旧穿过去** |
| 文章列表页 | 212–235 随内容起伏 → 同上 |
| 文章页(当初出问题的那一页) | **早就不适用了** —— dock 已搬出工具栏,`toolbarItems = []`,**没有任何 bar item 会去设那个属性** |

第二行才是最扎人的:**那处分叉的前提,在几小时前 dock 浮层化的时候就已经消失了**,
而我们不但没去掉它,还在它造成的两个症状上花了一整轮(用户报 → 我猜 → 用户截图 → 我再猜)。

**修法**:删掉分叉(`systemDrawsBarCapsule` 恒 `false`、`nnwHideSystemGlassCapsule` 去掉版本早退)。
实测结果:iOS 26 改动前后逐项不变(三档 40.0 / 圆钮 40.4,中心 820 / 822);
**iOS 27 现在和 26 逐项一致** —— 用户报的两处一起消失,而且**没有搬那四颗控件**(省掉一大笔风险)。

**教训**:
- ⚠️ **记一条结论时,把"我是在什么条件下量的"和结论本身一样醒目地写下来。**
  L116 写了"在文章页量的",但**结论句写成了"iOS 27 上不能拆"** ——
  后来读的人(我)只记住了结论句。判据:**结论句里就要带上适用范围**,
  别指望它下面那行小字。
- ⚠️ **改了架构之后,回头查一遍"哪些旧决定的前提被我改掉了"。**
  dock 搬出工具栏是个架构改动,它让一处版本分叉失去了存在理由 ——
  这种"顺带作废"不会自己冒出来提醒你。判据:**做完一个结构性改动,
  grep 一遍那个结构曾经的理由里点名过的开关/分叉。**
- **"我复现不了"会让错误的结论活得特别久。** 拿不到 iOS 27 的那几轮,
  L116 是唯一的证据,谁也没法质疑它。**拿到能复现的工具之后,
  第一件事不是写新代码,是复验那些"当初只能靠一次测量定下来的结论"** ——
  这一条今晚立刻回本了(装完模拟器 20 分钟就推翻了它,并顺手治好两个症状)。
- 这条和 L100 是同一个母题的两级:L100 说"规则要连着理由一起读";
  **L122 说:结论要连着适用范围一起记,而且范围变了要有人回去重评。**

## L123 · 两次"量错了" —— 仪器对准了错的东西,数字照样精确

2026-08-05 深夜,用户报「中间控件比两边高 / 左右两个可能不一样大」。
**他三条观察全部成立**,而我在同一段排查里连着栽了两次,**两次都是"数字很精确,但量的不是那个东西"**。

### 第一次:装到了另一台模拟器上

`tools/install-to-simulator.sh` 全程用 `xcrun simctl ... booted`。
那天**同时开着 iOS 26 和 iOS 27 两台**,而 **`booted` 在多台启动时是有歧义的** ——
它挑一台,而且不告诉你挑了哪台。脚本把新版装进了 27 那台,
于是我"在 iOS 26 上验证没退化"量到的是**几十分钟前的旧二进制**,
据此得出"iOS 26 上那个 2pt 修正没生效"——**完全是假的**。
用 UDID 精确装之后,26 上一次就对了。
**已在脚本里加闸门**:多于一台就停,并打印精确命令(`SIM_UDID=<UDID> ./tools/...`)。

### 第二次:阈值只覆盖了特征的一部分,于是"宽度变了"被当成"问题解决了"

齿轮左边有一块残留的圆角矩形。我加了 iOS 27 新属性 `paddingRemoved` 之后重量,
外接框宽度从 **43.67 → 39.67**(和加号完全一致),我就宣布"修好了"。
**放大截图一看,那块东西还在** —— `paddingRemoved` 只是把整项往外挪了 4pt,
残留物被圆盘盖住更多、落到了我那个阈值(≥246)扫不到的位置。
**宽度这个指标,只能回答"亮边最外沿在哪",回答不了"那块东西还在不在"。**

**教训**:
- ⚠️ **报告"修好了"之前,先问:我这个指标,是不是**直接**测量了那个症状?**
  用户说的是"看见一块多余的东西",而我量的是"外接框宽度" —— 那是**代理指标**。
  代理指标可以在症状还在的时候变好。**判据:症状是视觉的,最终确认就得回到眼睛
  (放大截图),数字只用来定位和防止目测偏差。**
- ⚠️ **量之前先确认"我量的这台机器 / 这个进程,装的是不是我刚编的那份"。**
  这个项目为此写过整个脚本(L41/L42 的 cmp 校验),但那套校验**只保证"某台设备上的版本对"**,
  不保证"是我想要的那台设备"。判据:**多设备环境里,一切 `booted` / 默认目标都要换成显式 ID。**
- 这条和 L104(采样范围不够)、L109(扫描线没过圆心)、L115(仪器没校准)是一家,
  但角度更钝:**前几条是仪器不准,这两次是仪器很准、只是没对准要测的东西。**
  合起来一句:**"我量到了" ≠ "我量的是它"。**

## L124 · 我加的钩子挂在一个**从来不成立的分支**里 —— 而"看起来是好的"骗过了我

**现象**(2026-08-08,做用户第 10 件「齿轮压住标题」时):
把「夹住标题宽度」的调用加进了上游 `updateNavigationBarTitle(_:)`:

```swift
if let label = navigationItem.titleView as? UILabel {
    label.sizeToFit()
    nnwClampNavigationTitleWidth()	// ← 我加的
}
```

装机一看,标题确实不再被齿轮压住了 —— **我差点就据此收工**。
后来为了别的事埋了一行日志,才发现 `nnwClampNavigationTitleWidth()` **一次都没执行过**。

**为什么不成立**:本 fork 的头图上线之后,`TimelineFeedHeaderController`
会把 `navigationItem.titleView` **换成一个空视图**(titleView 非 nil 时系统就不画
title/subtitle 了,那是它压住系统标题的手段)。于是 `titleView as? UILabel` 恒为 nil,
而**有头图的页面正好是全部单源页** —— 也就是这个功能唯一要管的那些页面。

**那"看起来好了"是怎么回事**:同一轮里我还把右上角两颗圆钮换成了一颗更窄的双图标胶囊
(96pt → 68pt)。省下的 28pt 恰好够那个源名不再被压住 —— **另一处改动替它把症状盖住了。**

**真正的标题在哪**:在头图那套里(`TimelineFeedHeaderOverlay.titleLabel`),
它从头图飞到导航栏那一条,宽度上限是 `屏宽 − 2×sideMargin`,和右上角控件毫无关系。
夹宽度必须夹**它**,而且要**跟着飞行进度收紧**(起点不动,终点收到控件之间的净空)。

**教训**:
- ⚠️ **给上游的方法加钩子时,先确认那一行所在的分支在目标页面上成立。**
  最省事的确认方式不是读代码,是**在钩子里放一行日志、跑一次、回读**(L115 的"回读确认"
  同一个动作,这次要确认的是"到底有没有被调用")。
- ⚠️ **同一轮里改了两处、症状消失了,不等于你想改的那处生效了。**
  这是 L123「我量到了 ≠ 我量的是它」的变体:这次连"量"都没有,只是看症状没了。
  判据:**一轮里如果有两处改动都可能解释同一个好结果,必须把它们分开验一次。**
- **本 fork 已经把上游的标题机制整个换掉了**(头图 + 会飞的标题)。
  以后凡是"和标题有关"的活,**第一站是 `TimelineFeedHeader.swift`,不是 `navigationItem`**。

## L125 · 「点顶栏第一次回顶、第二次回原位」为什么没做成 —— 三条路的实测结果

用户 2026-08-08 第 3 件里有一句「点顶栏第一次回顶+记原位,第二次回原位」。
**第一半做成了,第二半三条路全部失败**,记在这里免得下一轮重跑一遍:

| 试过什么 | 实测 |
|---|---|
| 靠 `scrollViewShouldScrollToTop` 收第二次 | ❌ 列表**已经在最顶上时系统压根不问这个方法**(日志里那一串点击只出现了一次调用) |
| 给 `navigationController.navigationBar` 加点击手势 | ❌ 一次都没触发 |
| 给列表加点击手势 + 只收顶栏那一带 | ❌ 一次都没触发(`shouldReceive` 只收到正文区的触摸,顶栏那一带的一次都没有) |

后两条同一个病根:本 fork 的**头图浮层**(`TimelineFeedHeaderController` 的 overlay)
`frame = host.view.bounds` 铺满整页、盖在列表之上,顶栏那一带的触摸先落到它身上就没了。

**教训**:
- ⚠️ **系统"顺手给"的回调不一定两次都给。** `scrollViewShouldScrollToTop` 的语义是
  "要不要滚到顶",**已经在顶上时系统认为无事可做,连问都不问**。
  想用它做"来回切换"的开关,前提根本不成立 —— 这是 L118「借别人的回调」的又一例,
  而且这次量出来的是最彻底的那种失败:**回调压根不响**。
- ⚠️ **自己铺过整页浮层的项目,任何"某一带的点击"都要先问:那一带现在归谁。**
  我们为了让标题会飞而铺的那层 overlay,顺带把顶栏那一带的触摸全接走了 ——
  又一次 L114/L116「让某个东西存在/消失时,先想清楚它还顺带管了什么」。
- **做不成就写清楚做不成,别留半截代码。** 三条死路的代码已全部删掉,
  只留 `scrollViewShouldScrollToTop` 里那句"记原位"(它是活的,将来那一半接上就能用)
  和这段说明。

## L126 · 我用「有专门的工具」绕过了一条规则,而那条规则的**理由**根本没变

**经过**(2026-08-08,用户第二次提出同一件事):

| 时间 | 发生了什么 |
|---|---|
| 2026-07-21 | 用户:别用 computer-use 点模拟器验收,**太慢太费 token** |
| 2026-08-04 | 我发现本环境有**专门的 iOS 模拟器 MCP 工具**,以"又快又便宜"为由绕过那条规则,还写进了 **L100** |
| 2026-08-08 | 用户当场推翻:**那个工具同样太费**,让我以后都交给他验收,并且要求写进项目大原则 |

**错在哪**:那条规则的理由是**成本**,不是"computer-use 这个工具不好用"。
换一个工具并不改变成本 —— 截图是图片,每一张都要整个进上下文,
"专门的工具"只是让**我**省事,不让**账单**省钱。
我当时那句"又快又便宜"是**没有量过就下的结论**,而且它恰好指向我想要的方向
(自己动手比写验收清单省心)—— 这种"结论刚好合我意"的时候最该停下来量一量。

**教训**:
- ⚠️ **绕过一条规则之前,先把它的"理由"和"当时的手段"分开。**
  规则写的是"别用 X",理由是"太贵"。你找到了 Y,要问的是
  **"Y 便宜吗"**,而不是"Y 不是 X 吧"。判据:**理由没变,规则就没变,换工具不算解法。**
- ⚠️ **L100 那条本身没全错,错的是它的解法。** 内核("改了界面不能靠猜")继续成立,
  只是**验收的执行者应该是用户**。修正一条旧教训时,要把"内核"和"当时选的做法"拆开写,
  别整条推翻、也别整条留着 —— 这次就是只作废了后半句。
- **省心和省钱不是一回事。** 自己点模拟器对我最省心(不用把验收步骤讲清楚),
  但那正是成本所在。**写一份用户能照着做的验收清单,是这条规则真正要求的产出。**
- 这条和 L122 是一家:**结论要连着适用范围一起记**。L100 当时若写成
  「在**成本可接受**的前提下自己截图」,后来这次就不会撞车。

## L127 · 一条老约束我照单全收了三轮 —— 它的**理由**其实根本没盖住我要做的那件事

**经过**(2026-08-09,用户报「文章列表页上下的控件质感不一致」):

底部那几颗圆钮一直是**画进一张图片**的不透明圆盘,而同一屏的顶部胶囊、三档轨道是**真玻璃**。
浅色下两者亮度接近(实心 232、纸底 234)混得过去,**深色下当场分家** ——
玻璃只补 3–6% 的白、亮度跟着背后滚动的文章浮动,而图片是恒定的 #272727。

为什么一直是图片?因为代码里写着一条约束,而且写了三处、我经过三轮都没质疑:

> 「工具栏那两颗**只能换 image**。加号是 storyboard 的 IBOutlet、上游还往它身上挂
>  `menu` 和 `isEnabled`,**换成 customView 会把长按菜单整个弄丢**。」

这次终于去拆它,发现**理由和结论之间少了一步**:

| | |
|---|---|
| 结论(措辞) | 只能走 `image` 这条通道 |
| 理由(实际防的) | **换掉整个 `UIBarButtonItem` 对象**会打断上游那些写入点 |
| 我要做的 | **对象不换**,只给它设 `customView`,再把 `menu`/`isEnabled` 转发过去 |

对象没换 → IBOutlet 还是原来那个 → 上游的写入点一个都没断;
`toolbarItems` 数组一个字节没动 → `expectedItemCount == 3` 那条守卫照旧满足。
**理由的射程根本没覆盖到这个做法**,而我把结论当成了铁律。

**教训**:

- ⚠️ **这是 L126 的镜像,两条要一起读。**
  L126 是"规则的理由没变,我却以为换个工具就绕过去了"(**不该绕的绕了**);
  L127 是"规则的理由本来就没盖住这个做法,我却以为盖住了"(**该走的没走**)。
  合成一条可操作的判据:**碰到一条老约束,先把它的"理由"单独复述一遍,
  再问「我现在这个做法,是不是真的落在这个理由的射程里」** ——
  两次都栽在"只读结论、不复述理由"。
- ⚠️ **自己写下的约束最危险**,因为读到它时会自动信任。
  这三处措辞是我自己写的,还写得很有底气("都是硬的")。
  判据:**约束的注释里要把"理由"和"当时的做法"分两行写**,
  别写成一句"只能 XX" —— 那句话会把后来的人(包括我自己)挡在门外。

**顺带一条量法上的收获**:

- 四颗圆钮从"图片"变成"自绘视图"之后,**验证对齐的办法整个换掉了**。
  以前 bar item 的圆盘是画在系统内部视图里的,我们拿不到,只能让 app 自己截图、
  扫亮边**反推**外接框(T50 那一轮就是这么量的,也正是 L123「量的不是它」的温床)。
  现在四颗都是我们自己的视图,**直接读 `frame` 就行**,一行日志,精确到小数点。
  实测:圆盘 上802.00 / 下842.00 / 高40.00 / 中心822.00,
  和首页那条三档浮层**四项完全相同**(旧的截图反推值是 39.67 / 821.83)。
  判据:**「我量的是不是它」这个问题,最好的答案往往不是把仪器调准,
  而是换一个不需要反推的量法** —— 有时候换实现顺带就把量法换好了。

## L128 · 遮一条**会动**的边,遮罩就得跟着它动 —— 我差点把它钉死在一个固定位置

**现象**(2026-08-09,修用户报的「头图上那两条硬边界」):

首页头图是 `collectionView.backgroundView`,**固定不动**,列表内容从它上面滚过去。
而内容里每样东西都带着**不透明的暖纸色底**(分组标题栏是矩形、分组卡片是圆角矩形),
压在纸色背景上隐形,**一压到图上就现出一条硬边**。

修法是在内容的上边缘盖一条「纸色 → 透明」的渐变带,把那条边埋进纸色里。
**我的第一版打算把带子钉在"头图底边"** —— 那是个固定位置,算起来最省事。

**为什么不成立**:那样只在"内容刚探头"的那一瞬有效。再往上滚,
**内容的上边缘越过了带子**,带子还留在原地,那条硬边照样露出来 ——
而用户报的恰恰是"滚到这个部分的时候很丑",也就是**滚动过程中**。
带子必须挂在内容的上边缘上,跟着它一路往上走。

**顺带的第二个坑**:算那个位置时手边正好有个现成的 `progress`(0…1 的滚动进度),
拿它反推最顺手。**但 `progress` 是夹过的** —— 滚过 140pt 之后它恒为 1、不再变化,
而内容的边还在继续往上走。用夹过的量去定位一个没被夹的东西,过了那个点就全错。
所以另外算了一个**未夹**的 `scrolled`。

**教训**:

- ⚠️ **遮罩/装饰要盖住的如果是一条会动的边,那么"盖在哪"必须由那条边的当前位置算出来,
  不能由一个固定地标算出来。** 判据很好用:**问一句"用户一直滚下去,它还成立吗"** ——
  这一版第一眼看着对,是因为我只在脑子里过了"刚开始滚"那一帧。
- ⚠️ **别顺手复用一个"夹过"的量。** `progress`、`alpha`、`fractionComplete` 这类
  0…1 的值天生带截断,**它们回答的是"动画演到哪了",不是"东西在哪"**。
  要位置就老老实实用位置。这和 L123「我量到了 ≠ 我量的是它」是一家:
  这次是**拿对了仪器、读的却是另一个量程**。
- 📌 记一条能直接用的:本 fork 的头图是 `backgroundView`(固定),内容从它**上面**滚过去。
  所以任何"内容和头图打架"的问题,遮罩都只能加在**浮层**里(内容之上),
  而且都要跟着内容的边走。加在头图那一层是没用的 —— 那一层在内容底下。

## L129 · 用一块遮罩去藏一条边,**遮罩自己的每一条边界也得是化开的** —— 否则只是把边挪了个地方

**现象**(2026-08-09,L128 那件事的**同一天、下一个来回**):

按 L128 把渐隐带做成"跟着内容上边缘走"之后,我把它画成**两段**:
上端纸色不透明 → 下端透明,整块放在交界线**上方**(也就是盖在头图上)。
装机给用户看,**当场打回:「现在的效果变成这样,更丑了」**。

**错在哪**:那块带子的**顶端是一块不透明的纸色,而它上面什么都没有** ——
于是图上凭空多出一条**新的硬边**;而真正要治的那条边(在带子下方、内容的上沿)
因为那里 alpha 已经衰减到 0,**根本没被盖住**。
一句话:**我把遮罩画反了,既造了新边,又没盖住旧边。**

**正确的形状是三段**,骑在交界线上:
上端透明(图干净)→ **交界线处不透明**(把内容那条硬边埋掉)→ 下端又透明(内容干净)。
图化进纸色、纸色化进内容,上下都没有边。

**教训**:

- ⚠️ **一块用来"藏边"的遮罩,它自己的每一条边界都必须是渐变到 0 的。**
  只要有一条边界是"从不透明直接到没有",那里就会长出一条新的边 ——
  **遮罩没有消灭边,只是把边搬了个家**。
  自查方法:把遮罩的四条边界一条条问过去「这一条是化开的吗」,一条都不能漏。
- ⚠️ **"要盖住的那条线"必须落在遮罩最不透明的地方,不是落在边上。**
  我当时想的是"带子铺在内容上方,把上面的东西挡住",
  却没问"**alpha 最高的那一点,是不是正压在要藏的那条线上**"。它当时压在 40pt 之外。
- 📌 和 L128 连起来读,这两条是同一件事的两半:
  **L128 说遮罩要跟着那条边走(位置对不对),L129 说遮罩自己不能带边(形状对不对)。**
  两个都对了才有用 —— 我第一次两个都错,第二次只改对了位置。
- ⚠️ **这也是"没法自己验"的代价的一次实证。** 界面的事我看不到(CLAUDE.md 第 0 节第 7 条),
  所以每交一版都相当于赌一次。**赌注是用户的一个来回**,那就更该在交之前
  把"这块东西的每条边长什么样"在脑子里画一遍 —— 这一次我只画了一半。

## L130 · 我连着两版都在"糊边",而边的成因根本不是我以为的那个 —— 用户放大截图才点破

**经过**(2026-08-09,同一件事的第三个来回):

用户报首页头图上有"直角"和"圆角"两条边界。我的诊断是:
**内容带着不透明的纸色底,压到图上就现出硬边**。
据此连做两版遮罩,**两版都失败**:

| 版本 | 做法 | 结果 |
|---|---|---|
| v1 | 内容上边缘画一条「纸色 → 透明」的带子 | 方向反了,在图上**多画一条新硬边**。用户:「更丑了」 |
| v2 | 改成骑在交界线上的三段晕(透明→纸→透明) | 最上面那条治好了,**下面每块卡片的边还在** |

用户**放大截图**指着「智能 Feed 和今天中间」说那两条还在,我才去把成因查到底:

**这一页的列表底色是 `.clear`** —— 那是 2026-07-23 为了让头图能透出来才特意设的。
于是**卡片之间的缝、卡片左右的留白,露出来的全是头图**。
也就是说:边**不是**"内容的上边缘压在图上"造成的一条,而是
**每一块不透明的纸色元素都被头图从四面描了一圈**。
我一直在治"最上面那一条",而它只是这一圈里最显眼的一段。

**正解是垫,不是盖**:在**头图之上、列表内容之下**铺一层纸色底(上沿羽化),
让内容底下从头到尾都是纸 —— 卡片和缝隙同色,**边就不存在了**。

**教训**:

- ⚠️ **"边"是两种颜色相邻才有的东西。与其一条条去糊,不如让相邻的两边同色。**
  盖遮罩是在事后补救(还会把文字一起洗糊,v2 就把「智能 Feed」洗花了);
  垫一层底是**消掉产生边的前提**(L92 那条老规矩的又一例)。
- ⚠️ **修了一版没好、症状"少了一点"时,第一反应不该是"再调调参数",
  而是"我对成因的判断是不是只对了一部分"。**
  v2 确实治好了一条边 —— 正是那个"部分有效"让我以为方向对了、只是量不够。
  判据:**部分有效最容易骗人,因为它同时兼容"方向对但不够"和"只碰巧治了一个特例"。**
- ⚠️ **一个"为了 A 而设的旧决定",往往就是 B 的病根。**
  `config.backgroundColor = .clear` 那一行注释写得清清楚楚("给纸色的话头图就看不见了"),
  我读过它、还在 T52 里引用过,**却没把它和"缝里露出图"联系起来**。
  这是 L122 的又一次:**改架构时要回头查哪些旧决定的前提被作废了** ——
  这次是反过来,**查一个新症状时要回头看哪些旧决定正在制造它**。
- 📌 **用户放大的那张截图,比我三次推理都管用。** 界面的事我看不到,
  所以当用户说"还在"的时候,**要的不是我再猜一版,而是问他"具体是哪两块之间"** ——
  这次是他主动放大才结束的,下次我该先问。

## L131 · 一天之内四次「量错了」—— 而且每一次都不是仪器不准,是**对准了别的东西**

2026-08-09 把控件尺寸统一这件事,前后量了四轮才拿到能用的数字。四次的错法各不相同,
但合起来是同一句话:**"我量到了" ≠ "我量的是它"(L123)** —— 这次是它的四个新变种,值得逐条记。

### ① 我一直在往**用户没在看的那台**模拟器上装

两台模拟器都开着,我一路 `SIM_UDID=$SIM26` 装、`SIM26` 读日志,
而**用户全程在 iOS 27 那台(SIM27)上看**。表现是:用户说"点了、停在那儿了",
而我这边日志**一行没有**,还去怀疑探针写错了。
📌 **L123 的第一条(装到了另一台)原样重演了一次**,当时的补救是"脚本认 UDID",
但那只保证"我知道我装的是哪台",**没保证"那台就是用户在看的那台"**。
✅ **用户当场定规矩:以后一律以 SIM27 为主。** 已写进 NOTES-progress 与 NEXT-PROMPT。

### ② 量到的 52.8 / 48 是**转场动画中间**的值

第一次拿到底部三个控件的数字是 `圆钮 52.80 / 三档 48.00`,我差点据此下结论"两者不同高"。
但那一条日志的时间戳距离"头图已装"只有 **0.1 秒** —— 正是**页面推入的动画当中**,
iOS 26/27 的推入转场会**缩放视图**。等页面停稳再量,是干干净净的 `44 / 40`,中心都是 822。
📌 判据:**量一个"稳定值"之前,先问"这一刻它稳了吗"** ——
时间戳离页面切换太近的采样一律不能用。

### ③ 探针自己**挂在了不成立的分支上**,而且是静默的

`nnwLogNavBarGeometry` 第一版写着 `guard let window = bar.window else { return }`。
而它是被 `updateNavigationBarTitle` 带起来的 —— 那一刻(启动恢复中)导航栏**还没上屏**,
于是**静默返回**,一行都不打。我看到"没有日志",第一反应是"代码没跑"。
📌 这就是 **L124 长在探针身上**:钩子挂在不成立的分支里,而"没有输出"骗过了我。
判据:**探针量不到时必须把"我量不到"也打出来,绝不静默 return;
拿不准时机的,就写成重试。**

### ④ 找导航栏,找到的是**里面那条隐藏的空栏**

改成"从视图树里找 `UINavigationBar`"之后仍然量了个空:
本页住在**分栏控制器自己那一列的导航控制器**里,**里外两条栏都在树里**,
深度优先先撞上的是里面那条(隐藏、没有 item)。
最后改成**直接扫窗口顶部那一带**,不问它是什么类 —— 才拿到东西。
📌 判据:**按"类型"去找一个东西,前提是那个类型在树里唯一。
不唯一的时候,按"位置"找比按"类型"找可靠。**

### 这四条之外,真正最贵的那条

**`navigationController` 不等于"那个真正在推页面的栈"。**
文章列表页的 `navigationController.viewControllers.count == 1`、
`navigationController.parent` 才是真正的栈 —— 我 2026-08-09 上午写的返回键守卫
(`count > 1`)因此**恒不成立,返回键一次都没装上**,而系统那颗顶着,看起来像做好了。
⚠️ **文章页早在 2026-08-08 就把这个事实逐字写在
`NNWArticlePaging.nnwPopTargetNavigationController` 上了,我没去看。**
判据:**动导航栈之前,先在本仓里搜一遍"谁已经和这个栈打过交道" ——
这个工程的坑通常已经有人替你踩过,而且就在隔壁文件里。**

## L132 · 同一个错误在一天里犯了**三次** —— 而 L124 早就写好了防它的做法,我三次都没照做

**2026-08-09 一整天,「钩子挂在一个从来不成立的分支上」这件事发生了三次:**

| # | 哪里 | 那个恒不成立的条件 | 表现 |
|---|---|---|---|
| 1 | 返回键 | `navigationController.viewControllers.count > 1` | 返回键**一次都没装上**,而系统那颗顶着位置,看起来像做好了 |
| 2 | T24 让路(第一版) | `host as? ArticleViewController` | 让路的口子**从来没打开**,抖动一次都没被治到 |
| 3 |(2026-08-08 的旧账)标题夹宽度 | `navigationItem.titleView as? UILabel` | 同上,靠另一处改动把症状盖住了 |

**三次的形状完全一样**:我加了一个带条件的钩子,**没有验证它是否被走到**,然后当成"做好了"交付出去。
其中第 2 次尤其贵 —— 用户拿着一份刷了 150 行同一句日志的真机记录回来,
而那句日志本身就在喊"我还在触发",我却以为已经修好了。

**L124 早就把做法写死了**:
> 「给上游的方法加钩子时,先确认那一行所在的分支在目标页面上成立。
>  最省事的确认方式不是读代码,是**在钩子里放一行日志、跑一次、回读**。」

我三次都跳过了这一步。

**教训**:

- ⚠️ **凡是新加的"带条件的钩子",交付前必须有一次"它被走到了"的证据。**
  不是读代码推理,是**日志、断点、或者一个能观察到的行为变化**。
  这条已经是 L124,再犯就不是"忘了",是**没把它当成流程**。
  ✅ 从现在起当硬规矩:**加了 `if`/`guard`/`as?` 才生效的钩子 → 同一轮内埋一行日志 → 读一次 → 再谈交付。**
- ⚠️ **写 `as?` 之前,去调用方确认那个参数到底是什么类型。**
  第 2 次那个 `host` 是 `WebViewController`(`WebViewController+ReadingBar.swift:113` 传的 `self`),
  而我看到属性名叫 `host`、想当然以为是"这一页的宿主控制器"。
  判据:**参数名描述的是"角色",不是"类型"。角色相同的东西在不同层级上有不同的类。**
- ⚠️ **同一个错误反复出现时,要怀疑的不是记性,是流程。**
  三次都不是"不知道该验证",是"知道但没做"。这种情况下再写一条"要小心"没有用 ——
  得把它变成**动作**(埋日志),而不是**意识**。

---

## L133 · 不点界面也能把 app 送进文章页 —— 用命令行深链(这条能长期省钱)

**背景**:CLAUDE.md 第 0 节第 7 条把"在界面上点"划给用户。可是**探针要被走到才有数据**,
而这条阅读栏的探针只在"文章页打开"时才装 —— 于是出现一个死结:
我不能点,用户又还没跑,交付时就只能说"应该会打印"。**那正是 L124/L132 栽过三次的形状。**

**解法(2026-08-09 第十三轮实测可用)**:

```bash
xcrun simctl openurl <UDID> "netnewswire://x?id=<文章ID>#nnw://showunread"
```

文章 ID 从数据库拿:

```bash
DB=$(xcrun simctl get_app_container <UDID> com.wenbopan.NetNewsWire.iOS-DEBUG data)/Documents/Accounts/OnMyMac/DB.sqlite3
sqlite3 "$DB" "select a.articleID, substr(a.title,1,30) from articles a
  join statuses s on s.articleID=a.articleID where s.read=0 limit 5;"
```

**为什么要写成这个别扭的样子**(直接用 `nnw://showunread?id=…` 会报 LS error 115):

- 小组件那套深链用的 `nnw` scheme **根本没注册**在 Info.plist 里 ——
  WidgetKit 的 `widgetURL` 是系统直接投递给宿主 app 的,不走 scheme 注册,所以上游没注册它。
  于是 `simctl openurl` 找不到接管的 app,直接失败。
- app **真正注册**的 scheme 是 `netnewswire`(和 `feed` / `feeds`)。
- 而 `SceneDelegate.scene(_:openURLContexts:)` 判断用的是
  **`urlString.contains("nnw://showunread")`** —— 是**子串匹配**,不是 scheme 匹配。
- 所以:用注册过的 `netnewswire` 当外壳,把那个标记字符串塞进 **fragment**(`#` 后面),
  `id` 照常放 query。两个条件同时满足 → 上游那段代码原样接住,把文章打开。
- ⚠️ **塞进 path 不行**(`netnewswire://open/nnw://showunread?id=…` 实测无效),
  多半是被规范化时改了形状。**fragment 才稳。**

**这条能用来做什么 / 不能做什么**:

| 能 | 不能 |
|---|---|
| 打开指定文章 → 阅读栏装上 → 验证探针通电、量"平时"的基线 | **不能**代替"拽过头翻篇"的手势 —— 那条路只有手指能走 |
| 连着切几篇 → 量换文章时内边距怎么变 | 不能验收观感(那还是用户的事) |

**教训**:**"我不能点界面"不等于"我只能干等"。**
在放弃自验之前,先找一遍**这个 app 自己对外开的口子**(URL scheme、深链、通知、数据库)——
上游为别的目的(小组件)留下的入口,常常正好能当测试夹具用。
判据:**先问"有没有不经过界面的入口",再问"要不要请用户帮忙点"。**

---

## L134 · 我盯着一个"在剧烈摆动"的量查了两轮 —— 而那个量根本不是我以为的那个量

**现象**(2026-08-09,第十二轮 → 第十四轮):

第十二轮的探针打出这一行,我据此把真凶锁定为 `ArticleHeaderBar.syncInset()`:

```
排版=45 次 | inset上 207.33→323.33 | offset -323.33→0 | contentH 0→5533
```

推理链看起来很硬:`inset上` 每次翻页摆动**整整 116pt**,而全项目只有 `syncInset()` / `releaseInset()`
会同时写 `contentInset.top` 和 `contentOffset.y` —— 那不就是"正文在动"的直接原因么。

**第十四轮的探针把它整个推翻了。** 一次翻页里我们**只写了一次** inset(delta=175.33),
写完再没动过;KVO(谁写都拦得到)抓到的调用栈**就是我们自己那一行**,没有第三方。

**病灶在探针的名字里**:那个 `inset上` 读的是 **`adjustedContentInset.top`** ——
系统**夹过**的值 = 我们写的 `contentInset.top` **+ 安全区**。
新探针把两个值分开打,答案一目了然:

| | 原始(我们写的) | 夹后(探针打的那个) |
|---|---|---|
| 翻页期间 | 175.33,**一动没动** | 175.33 → 291.33 |

**差值恒等于 116,正好是一个安全区。** 摆动的从头到尾是**安全区分量在翻页中途被加进来**。
我追了两轮的"剧烈摆动"里,**我们的那一份贡献是 0**。

**教训**:

- ⚠️ **UIScrollView 的"内边距"有两个,差一个安全区,而且它们的名字长得几乎一样。**
  `contentInset` 是**你写进去的**,`adjustedContentInset` 是**系统夹过给你看的**。
  探针打后者、结论却指向"谁在写前者",中间隔着一个你没量的加数。
  ✅ **凡是"某某在变"的探针,把"我们写的那一份"和"系统加的那一份"分开打。**
- ⚠️ **一个数字"摆动的幅度恰好等于某个已知常量"(这里是安全区 116),
  那多半不是巧合,而是它在指认自己的成分。** 我当时看见 116 只想到"好大一个摆动",
  没想到"116 是什么"。✅ **看到整数级的巧合,先把它当线索,不要当噪声。**
- ⚠️ 这是 **L123(「我量到了」≠「我量的是它」)的第三遍**,但比前两遍更隐蔽 ——
  前两遍是仪器对准了别的对象,这一遍**对准的对象是对的,只是读了它的另一个属性**。
  ✅ 判据升级:**不只问"我量的是不是那个东西",还要问"我读的是不是那个东西的那个字段"。**
- ✅ 做对的一件事(值得保留):这一轮**没有直接改 `syncInset`**,而是先埋探针跑一轮。
  第十二轮立的那条硬规矩("假设型的修改,探针先行一轮")**当场省下了一次错误的手术** ——
  按老结论动刀的话,我会去改一个从头到尾没做错事的函数。

---

## L135 · "每次都抖"这句话,一句顶十轮探针 —— 而我差点又去修一个只占 1/4 的现象

**现象**(2026-08-09,第十五 → 第十六轮):

第十五轮我从探针里挑出一个很显眼的数:**9 次翻页里有 3 次 `安全区 62→116`**,
而 116 − 62 = 54 正好是导航栏高度,`头区容器高` 跟着摆同样的 54pt。
证据链看起来很漂亮,我已经准备下一轮照它动刀了。

**然后用户说了一句话**:

> 「成功滑到上一篇就肯定会抖;不抖,那就是没滑成。」

**这是一个双向的全称命题。** 它立刻把那个候选判了死刑 ——
那个现象在约 20 次翻页里只出现 **5 次**,而抖动是**每一次成功翻页都有**。
**一个 1/4 频率的原因,解释不了一个 4/4 频率的症状。**

顺着"必须每次都发生"这个筛子回头看同一批数据,不变量当场就浮出来了:
每次写内边距时 `夹后 == 原始`(滚动视图安全区还是 0),翻完变成 `原始 + 116`。
**这个每次都成立**,而且它只在"真的新建了一页"时发生 —— 和"没翻成就不抖"严丝合缝。

**教训**:

- ⚠️ **先问症状的频率,再挑原因的频率 —— 两边对不上就直接出局。**
  我连着几轮都在"从数据里找最显眼的异常",却没先确定**症状本身有多频繁**。
  ✅ 动手排查前先问一句:**"这个症状是每次都有,还是偶尔?"**
  这一句话能在看数据之前就砍掉一半候选。
- ⚠️ **显眼 ≠ 相关。** 54pt 的摆动很扎眼,但它是个**旁观者**。
  探针会把所有会动的量都端上来,而"会动"和"是病因"之间隔着频率这道匹配。
- ✅ **用户的一句定性描述,信息量可以远高于一屏数字。**
  他给的不是"哪里坏了",而是**一个约束条件**(抖 ⟺ 翻成)——
  约束条件能筛掉假设,而数字只能提出假设。
  📌 判据:**多问用户"什么时候会/不会发生",别只问"是不是还有问题"。**

---

## L136 · `offset` 变没变,和"内容看起来动没动",是两个问题

**现象**(2026-08-09,第十六 → 第十七轮):

我埋探针时写了这样一张判读表:

> - 安全区 `首0→末116` 而 offset **没有**跟着走 → 正文真的跳了,病根坐实
> - 安全区涨 116 而 offset **同步走了 −116** → 系统替我们补偿了,视觉不跳,得继续找

真机数据回来是第二种:`滚动视图安全区 首0→末116 | offset 首-175→末-291`。
按我那张表,应该判"没事,继续找"。

**但那张表是错的。** 内容原点在屏幕上的位置 = **`−contentOffset.y`**。
offset 从 −175 走到 −291,内容原点就从"离顶 175"变成"离顶 291" ——
**正文实实在在往下掉了 116pt。**

offset 跟着安全区走,只说明"滚动位置**仍然是顶**";
可**"顶"这个位置本身被移动了**。位置不变的是**滚动量**,不是**画面**。

**教训**:

- ⚠️ **写判读表的时候,把"我要判断的东西"翻译成"屏幕上的量",再决定看哪个变量。**
  我要判断的是"正文看起来动没动",对应的量是 `−offset`(以及 `adjustedContentInset`),
  而我却去看"offset 有没有变化"。**差一个负号和一层语义,结论正好相反。**
- ⚠️ **"系统补偿了"这种说法要当心。** UIKit 在安全区变化时确实会动 offset,
  但它维持的是**滚动位置的语义**(还在顶部),不是**像素位置**。
  ✅ 判据:**滚动视图里,"位置不变"至少有两种意思 —— 滚动量不变、画面不动。
  说之前先讲清楚是哪一种。**
- ✅ 好在这次判读表是**写在代码注释里、连同数据一起回读**的,所以错误当场被数字纠正。
  📌 **把判读表写下来的价值,不只是提醒自己,更是让它可以被数据证伪。**

---

## L137 · 我加了个"守卫"就以为安全了 —— 而守卫的前提被我自己新加的依赖破坏了

**现象**(2026-08-09,第十七 → 第十八轮):

诊断做对了(真机 11/12 次翻页,正文在动画中途整体掉 116pt),修法把 app 弄成了**白屏假死**。

改动是两条:
1. 把 `syncInset` 从「绑定文章时调一次」改成「**每次排版都调一次**」;
2. 目标值从 `measuredHeight` 改成 `measuredHeight + max(expectedSafeTop − scrollView.safeAreaInsets.top, 0)`。

我在注释里给自己打了包票:

> 「不必担心 L63 那个『每帧改 inset → 递归重排』的老坑:
>  内部有「变化不足 0.5pt 就不落笔」的守卫,真正写入很少。」

**那句话有一个没说出口的前提:目标值是稳定的。**
而第 2 条改动恰恰让目标值**依赖 `safeAreaInsets`** ——
一个**会被我们自己的写入反过来影响**的量。目标值来回跳,守卫就永远拦不住,
每帧都落笔,主线程转死。

**更要命的是**:`ArticleHeaderBar.swift` **文件开头就写着**这条禁令(L63 的账):

> 「本文件的飞行动画只允许改 `transform` 和 `alpha`,**绝不在滚动回调里碰
>  `contentInset` / `safeArea`**。内容下移那一下只在绑定文章时做一次,不在每帧做。」

**我改的正是它明令禁止的那件事,而这段字就在我反复读了七八轮的那个文件的头部。**

**教训**:

- ⚠️ **"我加了守卫所以安全"是一个需要证明的命题,不是一句免责声明。**
  守卫拦的是"变化足够小",它成立的前提是**被比较的目标值本身收敛**。
  ✅ 判据:**加幂等守卫时,先问一句「目标值会不会被我这次写入改变?」
  会 → 这不是幂等,是振荡。**
- ⚠️ **依赖一个"会被自己影响的量"来计算写入值,就是在造反馈环。**
  `safeAreaInsets`、`adjustedContentInset`、`contentSize` 全是这一类。
  ✅ 要补偿,就用**不参与那个环**的手段 —— 本文件早给了答案:`transform`。
- 🔴 **文件头的硬约束,在动手改这个文件的核心函数前要重读一遍。**
  我读了那个文件七八轮,却在最后动刀时没有回头看那 60 行的"使用须知"。
  ✅ 判据:**约束不是读过就内化了;改到被约束的那一行时,回去把约束原文再读一次。**
- 🔴 **而且我把一个会卡死主线程的版本直接交给了用户。**
  「平时」路径我自验了(命令行深链,正常),但**卡死发生在真机的另一条时序上**,
  自验没覆盖到。✅ **改了每帧路径这种全局性的东西,自验要覆盖"反复进出文章页"
  而不只是"打开一篇"。**

---

## L138 · 我指认了四个"病根",最后一个**实测存在、频率完全吻合、修掉之后观感零变化**

**经过**(2026-08-09,第十二 → 第二十轮):

| # | 指认的病根 | 怎么倒的 |
|---|---|---|
| 1 | `syncInset` 反复改内边距 | 探针:一次翻页只写一次,且没有第三方 |
| 2 | 解冻那一帧标题突变 | 探针:12 次 `跳变=0` |
| 3 | 安全区 62→116 摆动 54pt | **频率对不上**:20 次里只有 5 次,而症状是每次都有 |
| 4 | 滚动视图安全区 0→116 晚到 | **实测存在、11/12 次、频率完全吻合** → 修掉 → **用户:一点没变** |

第 4 个最值得记:它**不是猜的**。探针直接量到 `滚动视图安全区 首0→末116 | offset 首-175→末-291`,
正文确实在动画中途整体掉 116pt;它只在"真的新建了一页"时发生,
和用户那句「翻成了就一定抖,不抖就是没翻成」严丝合缝。
补偿做完、机制跑通、无振荡无残留 —— **观感一点没变。**

**错在哪:我把 L135 那个频率筛当成了定位工具。**

「必须每次都发生」是**必要条件,不是充分条件**。而一次成功翻页里"每次都发生"的东西多得很:
新建 WebView、装载网页、`setViewControllers` 的整页动画、我们写内边距、安全区到货、
协调器换选中……**它们全都通过这个筛子。**
我却把**第一个通过筛子、又能讲通机制**的量当成了病因 —— 而"能讲通机制"只是让我更相信,
它并没有增加任何证据。

**教训**:

- ⚠️ **频率筛只能把候选缩到一个"类",不能定位到"个"。**
  从类到个,唯一的一步是**"改掉它 → 症状变没变"**。
  ✅ 判据:**在拿到"改了之后症状变了"之前,不要说"病根找到了",
  哪怕数据再吻合、机制再讲得通。**
- ⚠️ **"实测存在"和"是病因"是两回事。** 那 116pt 的位移是真的、可复现的、
  和症状同频的 —— 它只是**不是用户看见的那个东西**。
  一个系统里可以同时有好几个"真实存在但无关"的运动。
- 🔴 **最贵的一条是流程,不是技术:这类"只有人眼能判"的问题,
  应该小步改、频繁让用户看一眼,而不是攒够一个完整假设再做一次大的验证。**
  今天用户跑了 8 次真机,大半花在了错误方向上 ——
  如果第一次就用一个"最小可感知改动"去探(哪怕很糙),会更早发现方向不对。
  ✅ **判据:验证成本落在用户身上时,优化的目标不是"每次假设更完备",
  而是"每次让用户判定的间隔更短"。**

---

## L139 · 分组时"跳过"的东西,不代表"不存在"——`translation.js` 图文顺序错乱的根子

**现象**(2026-08-11):用户报 MacRumors 翻译后文字总是堆到图片前面,图片堆到后面。

`splitBody` 把正文切成若干"可译单元"数组(`translatable`),再按字符数把连续的单元
分进同一组;真正落地时 `applyGroup` 是"在这组第一个节点的位置插入译文、删掉这组
**所有**旧节点"。分组这一步只看 `translatable` 这个抽象序列,完全不管**物理上**、
在 DOM 里,两个被分进同一组的单元之间是不是还站着别的东西(比如一张 `<img>`)——
图片没有文字,`collect()` 里 `text.length === 0` 就直接跳过了,组装数组时**跳过**等于
**从没出现过**。于是两段被图片隔开的文字被分进同一组,`applyGroup` 一提交,
图片纹丝不动,两段文字却被合并搬到了图片原来的位置之前。

**教训**:

- ⚠️ **"数组里挨着"和"文档里挨着"是两回事**——只要中间可能被跳过东西,
  前者就不能替后者做决定。这里的"跳过"特指 `collect()` 因为
  `text.length === 0` 而 `continue` 掉的元素,不是递归下钻或别的分支。
  ✅ 判据:**凡是"先过滤出一个子序列,再对子序列做相邻判断"的写法,
  过滤掉的东西如果在原始结构里还占着位置,就必须让子序列的元素带上
  "我和上一个之间隔着别的东西"这个信息,不能假装它们是原始意义上的相邻。**
- ⚠️ **修复要挑"会引发同一类错误"的东西下手,不是挑"最显眼的那个源"。**
  MacRumors 只是最容易撞上(整篇 `<br>` 断句、没有 `<p>`,可译单元又多又碎),
  但根子在 `splitBody` 的分组算法本身,任何"图片直接夹在两段可译文字之间"的源
  理论上都可能中招,只是撞上的概率随可译单元的大小和数量变化。
  ✅ **判据:排查一个"某个具体源出问题"的 bug 时,先问"这段代码的假设,
  是不是对所有源都可能不成立",而不是问"这个源有什么特殊之处"——
  后者容易导向"专门为这个源打补丁",前者才会修到根上。**
- ⚠️ **不是所有"被跳过的东西"都值得当边界**——同样被跳过的 `<br>` 就不该强制断组:
  它不承载任何看得见的内容,合并跨过它不会造成任何东西错位,
  而 MacRumors 恰恰全篇都是 `<br>`,不分青红皂白地断组会把请求数炸开。
  ✅ **判据:决定"跳过的东西算不算边界",要问它是不是"占着一个看得见的位置"**,
  不是简单地"只要被跳过就当边界"。

---

## L140 · 交互式转场"不跟手":百分比和动画挪动的像素数,必须是同一个分母

**现象**(2026-08-11):左滑开原文改成 `UIPercentDrivenInteractiveTransition` 交互式呈现
`SFSafariViewController` 之后,用户反馈"页面跑得比手指滑动快"。

病根:`nnwUpdateBrowserInteraction` 里把"手指拉多远算 100%"写成了一个拍脑袋的常数
(`nnwBrowserTravelDistance = 200`),而 `NNWBrowserSlideTransition` 里浏览器页
**实际**滑动的距离是 `transitionContext.containerView.bounds.width`(容器整个宽度,
iPhone 上普遍 390~430pt)。手指拉 200pt,`interactor.update()` 算出来的百分比已经是
100%,可动画要挪动的其实是全宽 —— **两个"100%"背后对应的像素数根本不是同一个数**,
浏览器自然比手指快了一倍还多。

**教训**:

- ⚠️ **交互式转场的百分比,必须用"动画本身实际挪动了多少像素"来换算,
  不能凭手感另挑一个"看着差不多"的距离常数。** `percent = 位移 / 动画实际总距离`
  才是唯一保证"手指走多远、画面就走多远"的公式 —— 这也是系统自己的侧滑返回手势的
  标准算法(`percent = translation.x / view.bounds.width`),不是需要发明的新东西。
  ✅ 判据:**写 `UIPercentDrivenInteractiveTransition` 之前,先去动画代码里确认
  它实际会把视图挪动多少 pt,拿那个数字(或者能推出那个数字的量,比如容器宽度)
  当分母,不要另设一个常数。**
- ⚠️ **这类"分母不对"的 bug,症状是"方向对、体感不对"**——不跟手、比手指快/慢,
  不是完全不动或者反向动。诊断时不要被"基本能用"迷惑,反而更容易被当成"手感问题"
  含糊过去,实际上是一个可以精确定位、精确修复的数字错误。
- ✅ 修法确认:分母改成呈现那一刻真实量出来的容器宽度(`top.view.bounds.width`,
  和转场动画里 `containerView.bounds` 同源),整个手势期间存住复用,不再是常数。

---

## L141 · "卡死"不一定是死循环——一张看不见的卡片挡在那儿,效果一模一样

**现象**(2026-08-12):用户报"点开一个失效的订阅源就卡死",附了真机日志。

日志里"试读加载失败"后面紧跟一行 `NNWMenu` 弹出的记号,再跟着一串
`Unable to simultaneously satisfy constraints`(量到 0 宽),然后日志**直接断了**。
病根:`presentError`(自绘的 `NNWMenu.show`,不是系统 `UIAlertController`)在网络回调
里被调用时,承载错误卡片的那个页面**还没真的挂进窗口**(push 转场没放完——DNS
失败这类错误常常几十毫秒就回来,比 350ms 的转场动画还快)。卡片用
`view.safeAreaLayoutGuide` 算位置、量尺寸,在这个时机量出来的数字不可靠,
真机实测卡片那层量到 0 宽——**卡片本身量不出来、可能干脆不可见,
但压暗的全屏蒙层照样铺在屏幕上、照样吃掉所有触摸**。用户看到的
"点哪儿都没反应"其实是一张看不见的卡片挡在那儿,和真死循环观感上没有区别。

**教训**:

- ⚠️ **"卡死"这个症状本身不指向"死循环",先看日志是不是在某一行之后彻底安静。**
  死循环通常会留下**海量重复日志**(比如同一行 warning 刷屏);
  而"看不见的东西挡住了输入"是**日志戛然而止**——主线程没有循环,
  只是没有任何东西再触发新的工作。这两种"卡"的日志指纹完全不同,
  分清楚了才知道该往哪个方向查。
- ⚠️ **弹出式浮层(菜单/卡片/弹窗)从异步回调里触发时,先确认宿主视图已经在窗口里。**
  `present()`/自定义浮层的位置和尺寸计算大多依赖 `safeAreaLayoutGuide`、
  `systemLayoutSizeFitting` 这类"必须已经挂进窗口才可靠"的量,
  而**网络回调完成的时机和页面转场完成的时机是两条独立的时间线**,
  谁先谁后没有保证——错误提示这类"随时可能从后台冒出来"的 UI 尤其容易撞上这条。
  ✅ 判据:**共用的浮层组件(本项目是 `NNWMenu.show`)应该自己在入口挡一道
  "宿主还没挂进窗口就先别弹、等一拍再试"**,不要指望每个调用点自己记得判断——
  调用点是网络回调、用户点按、通知回调等等到处都是,总有一个会踩中这个时机。
- 📌 这次没能现场复现(试读这条路是"当场抓 feed",没有命令行深链能切进去),
  修法完全是**读日志推出来的**——但逻辑链条(0 宽的证据 + 紧接着安静的证据)
  和机制(转场用时 vs 网络失败用时谁更快,是真实存在的竞态)都立得住,
  不是纯粹的猜测。**这类"读日志定位、没法现场复验"的修复,交付时要如实说清楚
  "我没能复现,这是按证据链推出来的"**,别把"讲得通"包装成"已验证"。

---

## L142 · 上一条(L141)的守卫**一次都没触发过** —— 我守错了量

**现象**(2026-08-12,同一天晚些时候):按 L141 的结论,在 `NNWMenu.show` 入口
加了 `guard host.view.window != nil, host.view.bounds.width > 0`,不满足就每 50ms 重试。
用户重新真机编译、连测 **3 轮**,日志签名和修之前**一模一样**:
「试读加载失败」→「NNW选单 · 从第0层弹出」→ 一串 0 宽约束冲突 → 静默。
那条新加的「host 迟迟没挂进窗口」**一次都没打出来过**。

用户当时的第一反应是"是不是你根本没把改动落到文件里"。查过了:改动在文件里,
也在装进真机的那份二进制里(`Debug-iphoneos/NetNewsWire.app/NetNewsWire.debug.dylib`
里 grep 得到新守卫的中文串)。**代码进了包,是守卫本身无效。**

**病根**:`push` 转场**一开始**,新页的 `view` 就已经被塞进转场容器,
而**转场容器本身就在 window 里**。所以从转场第一帧起,
`view.window != nil` 和 `bounds.width > 0` **就都是真的**。
这条守卫在物理上不可能拦下任何一次 —— 它守的是"有没有挂进窗口",
而真正要守的是"**转场放完了没有**"。这两件事在 push 期间是分离的。

真正的机制:在转场中途 `present` 模态,UIKit 的出场/入场回调会错乱,
被弹出的浮层**可能收不到 `viewDidAppear`** → 永远停在 `alpha = 0` 的隐形态;
可它的**根视图 alpha 仍是 1、仍然铺满全屏**,照样吃掉所有触摸
(隐形的**子**视图不参与命中测试,根视图自己却参与)。
于是:页面看着好好的,连下拉刷新都拽不动;点「好」只有按钮自己的高亮,
`dismiss` 也一并失灵 —— 一张关不掉的透明墙。

**教训**:

- ⚠️ **加完守卫要先证明它「会触发」,再谈它「有没有用」。**
  一条从未打印过日志的守卫,和没加是等价的。这一轮的三次真机日志里
  **本来就有证据**(新日志一次没出现),但我把注意力放在"为什么还卡"上,
  没去问"我加的那句到底跑没跑"。
  ✅ 判据:守卫的**否定分支必须留一行日志**(这次留了,做对了),
  然后**下一轮第一件事是查这行日志在不在**,不在就说明假设从根上错了,
  别急着在错的假设上继续加码。
- ⚠️ **"在窗口里" ≠ "已经安定下来"。** UIKit 的转场是把新视图**先**挂进窗口、
  **再**放动画。想等"页面安定",要问 `transitionCoordinator`
  (自己的 / `navigationController` 的 / `tabBarController` 的),
  挂到它的完成回调上,而不是去测 `window` / `bounds` 这类**转场一开始就已经成立**的量。
- ⚠️ **全屏浮层要有"打不开就自动让路"的兜底,不能只依赖某个回调一定会来。**
  一个 `viewDidAppear` 没来就永远隐形、却照样吃触摸的浮层,
  等于把整个 App 锁死,而且用户**没有任何逃生出口**。
  ✅ 本项目的三道兜底:①露面之前 `isUserInteractionEnabled = false`(触摸直接穿过去);
  ②0.8s 没等到入场回调就自己补显或自行退场;③`dismiss` 后 0.3s 还在窗口上就强拆浮层。
  这三道都各留一行 error 日志 —— 下次再出事,日志会直接说是哪一段兜的。
- 📌 **手摆 frame 的视图不要用 `translatesAutoresizingMaskIntoConstraints = false` 去"消"约束警告。**
  `card` / `shadowHost` 是 `placeCard()` 里手动赋 frame 的,它们**没有**任何描述
  自己位置的约束;关掉隐式约束等于让它们彻底失去尺寸,那才会真做出一张看不见的卡片。
  日志里那串 0 宽冲突的正确修法是**给它们一个像样的初始 frame**,
  让第一轮求解就发生在真实宽度上(`placeCard` 要等 `viewDidLayoutSubviews`,
  在那之前 UIKit 已经先解过一轮了)。

---

## L143 · 给「静态表」加**一行**能拦住,加**一区**拦不住 —— 这是两件事

**现象**(2026-08-12):给「源信息与设置」页(storyboard 静态表)末尾加了一个
「取消订阅」区。一打开这页就崩:

    NSRangeException: index 3 beyond bounds [0 .. 2]

**病根**:静态表的数据源住在 UIKit 内部,它手里那个数组**只有 storyboard 里那 3 个区**。
我让 `numberOfSections` 多报一个,UIKit 就会拿区号 3 去问**一整套**方法。
我按着同文件里 +NNWTitleTranslation 的先例拦了 9 个(numberOfRows / heightForHeader /
titleForHeader / viewForHeader / heightForRow / indentationLevel / cellForRow /
didSelectRow / numberOfSections)——**还是漏了**(footer 那几个、各种 estimated…)。

**教训**:

- ⚠️ **"照着旁边那个先例做"之前,先问这两件事要答的问题是不是同一个集合。**
  +NNWTitleTranslation 加的是**行**:静态表对行只会问三四个问题,是**闭集合**,拦得住。
  加**区**要答的是**开集合**(footer 标题/高度/视图、各种 estimated…,而且随 iOS 版本增加)——
  拦不全就必崩。表面上都是"在静态表里补东西",实际风险量级完全不同。
  ✅ 判据:**要拦的方法数量说不清是几个,就说明这条路本身选错了**,换一条不经过那套机制的路。
- ✅ 换的那条路:`tableView.tableFooterView`。表尾视图**完全不经过数据源**,
  UIKit 只是把它贴在最后一区下面 —— 零个数据源方法被改动,结构上不可能再越界。
  代价只是它不长得像一个 cell(红字按钮居中),而这正好也是 iOS 常见的样子。
  顺带的好处:上游文件的净改动从 **9 处拦截 + 去掉一个 private** 缩到 **1 行调用**。
- 📌 这次是**我自己交付的东西当场崩了用户的 App**。编译通过 ≠ 跑得起来:
  静态表这类"数据源在框架内部"的东西,编译器一个字都帮不上忙。
  这类改动**必须在脑子里把"框架会来问哪些问题"列全**,列不全就别用这个机制。

---

## L144 · `SFSafariViewController` 是跨进程的:手势装在它 view 上收不到触摸

**现象**(2026-08-12):给内置浏览器加"从左边缘右滑返回文章",
第一版直接 `safari.view.addGestureRecognizer(UIScreenEdgePanGestureRecognizer)`。
编译通过,真机实测**完全没反应**,一条日志都没有。

**病根**:`SFSafariViewController` 的网页内容是**跨进程**渲染的(remote view)。
Apple 的设计目标之一就是"宿主 App 不能观察用户在网页上做了什么"——
落在网页区域的触摸根本不会交给宿主进程的手势识别器。**这不是写错了,是设计如此**,
所以也永远等不到一条日志。

**修法**:在 `safari.view` 上盖一条 20pt 宽、通到底的**宿主进程的**透明视图,
手势装在那条上。这条视图归我们所有,触摸一定收得到,和跨进程那件事彻底无关。

**教训**:

- ⚠️ **凡是系统给的"内容不归你"的控件(SFSafariViewController / 各种 picker /
  AuthenticationServices 这类 view service),都要先问一句:它的内容是不是另一个进程画的?**
  是的话,**任何"往它内部伸手"的做法都不成立** —— 不只是手势,还包括读 DOM、
  截图、遍历 subview、KVO 它的内部状态。判据很简单:
  **这个控件存在的理由如果包含"保护用户不被宿主 App 看到",那就一定是跨进程的。**
  ✅ 能做的只有"在它**外面**加东西":盖一层自己的视图、用它自己开放的配置项
  (比如 `SFSafariViewController.Configuration.activityButton`)。
- 📌 我在交付时**已经如实标注了这条风险**("宿主这边加的手势通常仍能收到触摸,
  但我没法在不跑起来的情况下证明"),用户一轮就验掉了。
  **这一条做对了**:没法自验的假设要当场说清楚,别包装成"应该没问题"——
  否则用户会拿它当已验证的结论去用。

---

## L145 · 我加的探针报了「高 0」,而我上一轮把它读成了「Apple 不让我们收触摸」

**现象**(2026-08-12,紧接 L144):把手势从 `safari.view` 换成"自己盖一条 20pt 透明感应条"。
真机还是没反应,但这次日志有一行:

    [链接] 右滑返回:感应条已装(高 0.000000)

**病根**:`install` 是紧跟在 `present` 后面调的,**那一刻 host 还没布局**,
`safari.view.bounds.height` 是 0。我用它算了 frame,又配上 `.flexibleHeight` ——
autoresizing 是**按比例**缩放的,**0 的比例永远是 0**。
于是这条感应区始终是 20×0,一个像素的可点区域都没有。**和跨进程一点关系都没有。**

**教训**:

- ⚠️ **`present` / `addSubview` 之后立刻读 `bounds` 拿到的是布局前的值。**
  这条在本项目里已经是第三次以不同面目出现了(L103 第 2 条"读尺寸之前要先让它被算出来"、
  NNWMenu 的 `card` 初始 frame、这次)。
  ✅ 判据:**一个视图的位置/尺寸只要依赖父视图,就用约束钉,别自己算 frame** ——
  约束是"等布局跑起来再解",天然不受"我在哪个时机装的"影响。
  非要用 frame 的(比如 `tableFooterView`、手摆位置的浮层),就得自己保证读到的是布局后的值。
- ⚠️ **上一轮 L144 的结论下早了。**
  (⚠️ 2026-08-12 再更正一次:我一度以为"命中测试能命中我们的视图"就证伪了 L144 ——
  **那个推论本身是错的**。`hitTest` 只是**宿主进程内的一次几何查询**,
  **不等于真实触摸的投递**:系统完全可以按 `SFSafariViewController` 的屏幕区域
  把触摸直接路由给它那个进程,宿主这边的 hitTest 照样返回我们的覆盖层,
  而 `touchesBegan` 一次都不会来。第四轮探针把 z 序、尺寸、容器归属全部证成之后
  手势**依然**不触发 —— L144 重新成为最佳解释。见 L146 末尾。) 第一版(手势直接装 `safari.view`)确实没反应,
  我把它归给"SFSafariViewController 跨进程,触摸不给宿主"——这个机制是真的、
  但**它是不是这次的病因,并没有被证明**。我紧接着写的第二版又因为一个完全无关的
  低级错误(高 0)失败,等于连着两轮都没验到那个假设。
  ✅ 判据:**"我换了做法之后还是不行"不等于"我原来的归因是对的"** ——
  新做法可能有它自己的新 bug。每一轮都要先看探针说了什么,再谈假设。
  (这次探针救了场:如果没有那行"高 0.000000",我大概率会继续沿着跨进程那条路
  去做 WKWebView 自建浏览器 —— 那是几百行的改动,而真正的问题是一行 frame 计算。)

---

## L146 · `SFSafariViewController` 会**持续重排**自己的子视图 —— 别把东西塞进它里面

**现象**(2026-08-12,右滑返回的第三、四轮):在 `safari.view` 上盖一条透明感应条,
尺寸正确、`bringSubviewToFront` 也调了,真机上还是一次触摸都收不到。探针拍到:

    (布局后) 提前之前排第 0/2 | 命中的是感应条=false
    (转场后) 提前之前排第 0/2 | 命中的是感应条=true

**两趟都是第 0 位** —— 也就是说第一趟我把它提到最前之后,`SFSafariViewController`
**又把自己的内容视图压回了我们上面**。它会持续重排自己的子视图,
任何**一次性**的 `bringSubviewToFront` 都只是暂时的,等用户真正去滑的时候早被压回底下。

**修法**:挂到 `safari.view.superview` —— 也就是 UIKit 为这次模态呈现建的**呈现容器**。
那一层是宿主自己的,`SFSafariViewController` 不管它,z 序没人来抢。

**教训**:

- ⚠️ **系统控件的 view 内部是它自己的地盘,你塞进去的东西随时会被重排/移除。**
  和 L143(静态表加区)是同一族的错:**"往系统控件内部伸手"这个动作本身就是脆的**,
  不管伸的是数据源方法、子视图还是手势。
  ✅ 判据:**要盖在系统控件上,就盖在它的"外面"** —— 它的父容器、呈现容器、或者自己的窗口层。
  那些层归你,系统不会来动。
- ⚠️ **`bringSubviewToFront` 修不了"别人会重排"的问题**,只能修"我加早了"。
  区别在于:前者要在**每次重排后**都再提一次(等于要监听一个你监听不到的事件),
  后者提一次就够。**分不清是哪一种,就先加个探针把 z 序打出来** ——
  这次正是"两趟都是第 0 位"这个数字把两者分开的,不是靠猜。
- 📌 **连着三轮都没修好,但每一轮的失败原因都不同**(未验证的归因 → frame 算错 → z 序被抢)。
  能收敛靠的是**每轮都留下能证伪的探针**,而不是"再试一个想法"。
  没有探针的那一轮(第一轮)是唯一一轮完全白跑的。

**⚠️ 2026-08-12 追加(第四轮之后)**:把感应条挪到呈现容器(`UITransitionView`)之后,
探针显示**每一项都对**:`感应条 20×874 | 容器里排第 1/2(最后一个) | 容器=UITransitionView |
命中的是感应条=true`。**手势依然一次都不触发。**

于是本条 L146 的"z 序被抢"只是**沿途修掉的一个真实缺陷**,并不是病因。
到这一步为止,能同时解释全部证据的只剩一条:
**触摸在比宿主 hitTest 更低的层级就被路由给了 `SFSafariViewController` 那个进程**,
也就是 L144 说的那件事 —— 那么**盖在它上面的任何覆盖层都拿不到触摸**,这条路是死的。

📌 **额外的方法论教训**:`hitTest` 返回 true **不能**用来证明"触摸会来"。
想验证"触摸到底来没来",探针要打在 `touchesBegan` / `gestureRecognizerShouldBegin` 上,
而不是打在一次几何查询上 —— 我这四轮里有一轮就是被这个假阳性带偏的。

**⚠️ 2026-08-12 终局追记(第五轮,铺满整页的诊断)**:把感应层放大到**铺满整页**
(探针实测 402×874、hitTest 命中),加了比手势更底层的 `touchesBegan` 探针 ——
**一次都没响**,同时网页照常能滚能点。三件事放在一起,唯一成立的解释:
**SFSafariViewController(iOS 26 真机)的内容住在它自己的 window 里,叠在 App 窗口之上**,
触摸从**窗口一级**就被路由过去,根本不进 App 的窗口。

最终教训(压过前面几条的细节):
- **"盖在 SFSafariViewController 上"这个思路从第一步就不成立** —— 不是 z 序、不是尺寸、
  不是时机,是**它压根不在你的窗口里**。宿主侧的一切指标(hitTest / 层级 / 约束)都可能
  全绿,但那只是在检查自己窗口内部,触摸路由发生在更上游。
- **验证"触摸能不能来"的唯一可靠探针是 `touchesBegan`(或手势回调)真的响**;
  hitTest、subview 顺序、bounds 全是间接证据,这次它们集体给出了假阳性。
- 用户提供的对照(Reeder 用 SFSafariViewController 且能右滑关闭)指向正路:
  **系统对"普通 present 的 SFSafariViewController"自带左缘滑动关闭**(由 Safari 自己的
  进程/窗口实现,所以人家不需要碰触摸路由)。我们的左滑跟手动画用了
  `modalPresentationStyle = .custom` + 自定义转场,**很可能正是它把系统这套白给的手势
  顶掉了**。下一步是做减法验证,不是继续加东西。

---

## L147 · 有方向的入场动画,作用面积一大就变成"页面级转场"

**现象**(2026-08-12):给每组译文加了 `clip-path: inset(0 100% 0 0) → inset(0)`
的从左往右擦入。单段落落地时很好看 —— 正是"文字填进色条"那个感觉。
但**全篇翻完的那一刻**,用户报"整页有一把刷子从左到右扫过"。

**病根**:翻译分组是**逐组翻倍**的(1000 → 2000 → 4000 字符封顶),
所以**最后一组几乎是整篇**。它一落地,`applyGroup` 给那一组里**每个**元素都加上了
同一条擦入动画 —— 几十个元素**同时、同方向**擦入,单个看是"填充",
叠在一起就是一次页面级的横扫。

**教训**:

- ⚠️ **判断一个入场动画好不好看,不能只看它作用在一个元素上的样子。**
  有方向的动画(擦入 / 滑入 / 位移)**作用面积越大越像"转场"**,
  而转场是有语义的 —— 它在说"你换页了",可这里根本没换页,所以观感是错的。
  ✅ 判据:**这个动画会不会同时作用在很多元素上?** 会的话就只用**无方向**的
  (淡入、缩放),或者给每个元素错开起始时间把"同时"打散。
  面积可变的场景(分组大小不固定、列表长度不固定)尤其要按**最大**的那次来想象。
- 📌 这次的修法是**换动画类型**,不是调参数。擦入再慢再快都还是横扫 ——
  方向性是它的固有属性,不是可以调出来的东西。
